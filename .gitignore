node_modules
dist
.env
.env.*
!.env.example
!.env.*.example
*.log
*.tsbuildinfo
.mcp.json

# TLS 憑證與私鑰（絕不入版控）
certs/
*.key.pem
*.key
*.pem

# 個人工作文件 / 進度報告（不入版控）
進度報告*.md
*-Peter-v*.md

# Claude Code 排程器執行期 lock（暫存檔，不入版控）
.claude/scheduled_tasks.lock

# Legacy 參考用大型二進位（ground truth，留本機不入版控）
reference/*.xlsx

# F064 分派結果匯出產物（輸出檔，含真實客戶分派資料，留本機不入版控）
/assignment_result_*.xlsx
/assignment_result_*.csv
