node_modules
dist
.env
.env.test
.env.local
*.log
*.tsbuildinfo
.mcp.json

# 個人工作文件 / 進度報告（不入版控）
進度報告*.md
*-Peter-v*.md

# Claude Code 排程器執行期 lock（暫存檔，不入版控）
.claude/scheduled_tasks.lock

# Legacy 參考用大型二進位（ground truth，留本機不入版控）
reference/*.xlsx
