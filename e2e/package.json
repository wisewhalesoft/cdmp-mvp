{
  "name": "@cdmp/e2e",
  "version": "1.0.0",
  "description": "CDMP acceptance/fidelity ring — Playwright tests against the live integrated stack (web + api). Authored by test-generator, blind to implementation, from prototypes/ + docs/specs. See README.md.",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:list": "playwright test --list",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:f117": "playwright test tests/fidelity-f117-dept-ratio.spec.ts --project=admin"
  },
  "license": "UNLICENSED",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
