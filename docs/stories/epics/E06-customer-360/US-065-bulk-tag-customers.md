# US-065：批次標記客戶

> **Story ID**：US-065
> **Epic**：[E06 — Customer 360](epic-brief.md)
> **優先級**：Should Have
> **階段**：Phase 2
> **預估點數**：8

---

## User Story

**As a** 已登入的使用者（Admin 或 User）
**I want** 在客戶清單中選取多位客戶，一次為他們批次新增或移除標籤
**So that** 我能高效率地對大量客戶進行分類標記，節省逐一操作的時間

---

## 驗收標準

### AC-1：進入多選模式
- **Given** 使用者在客戶清單頁面（US-060）
- **When** 使用者點擊清單左上角的「多選」按鈕，或勾選任一客戶列的 Checkbox
- **Then** 清單每列顯示 Checkbox，頂部操作工具列出現，顯示「已選取 N 位客戶」

### AC-2：全選當頁
- **Given** 多選模式已開啟
- **When** 使用者點擊清單標頭的「全選」Checkbox
- **Then** 當前頁面所有客戶列被選中，工具列更新「已選取 N 位客戶」

### AC-3：批次新增標籤
- **Given** 使用者已選取至少 1 位客戶
- **When** 使用者點擊工具列的「新增標籤」按鈕，在標籤選擇 Dialog 中選擇一個或多個標籤後點擊「確認」
- **Then** 系統為所有選取的客戶批次建立 customer_tag_assignment 記錄（跳過已有該標籤的客戶），顯示成功通知「已為 N 位客戶新增標籤（M 位客戶已有此標籤，略過）」

### AC-4：批次移除標籤
- **Given** 使用者已選取至少 1 位客戶
- **When** 使用者點擊工具列的「移除標籤」按鈕，選擇目標標籤後點擊「確認」
- **Then** 系統為所有選取客戶中有該標籤指派的客戶批次刪除對應的 customer_tag_assignment 記錄，顯示成功通知「已從 N 位客戶移除標籤（M 位客戶無此標籤，略過）」

### AC-5：批次上限
- **Given** 使用者嘗試選取超過 200 位客戶進行批次標記
- **When** 使用者勾選超過 200 筆
- **Then** 系統顯示提示訊息「批次操作最多支援 200 位客戶，請縮小選取範圍」，超過 200 筆後無法繼續勾選

### AC-6：批次操作進度回饋
- **Given** 使用者確認批次標記操作（選取 100 位客戶）
- **When** 系統正在執行批次寫入
- **Then** 顯示進度指示（如載入 Spinner 或進度條），操作完成後顯示結果摘要通知

### AC-7：退出多選模式
- **Given** 多選模式已開啟
- **When** 使用者點擊「取消」按鈕或再次點擊「多選」按鈕
- **Then** 清單回到一般瀏覽模式，所有 Checkbox 取消勾選，工具列隱藏

---

## Technical Notes

- 批次上限：Phase 2 設定為 200 筆，後端亦需驗證此限制
- 批次操作採用單一 API 呼叫（非逐筆呼叫），傳入 customerIds 陣列與 tagIds 陣列
- 重複指派跳過（冪等操作）：已有標籤的客戶不報錯，只記錄略過數量
- 批次操作在單一 DB Transaction 中執行，確保原子性（全部成功或全部回滾）

### API 端點

**批次新增標籤**

- 端點：`POST /api/v1/c360/customers/bulk-tag`
- Request Body：
```json
{
  "customerIds": ["uuid1", "uuid2", "..."],
  "tagIds": ["uuid-tag1"],
  "action": "add"
}
```
- Response（200 OK）：
```json
{
  "action": "add",
  "processedCount": 100,
  "skippedCount": 5,
  "failedCount": 0
}
```
- 錯誤（400 Bad Request）：customerIds 超過 200 筆

**批次移除標籤**

- 與批次新增共用端點，`action` 改為 `"remove"`

---

## 測試案例

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| 1 | 勾選 3 位客戶，點擊「新增標籤」，選擇「VIP」 | 3 位客戶均成功指派「VIP」標籤 |
| 2 | 其中 1 位客戶已有「VIP」標籤，批次新增 | 成功通知顯示「1 位已有此標籤，略過」 |
| 3 | 批次移除 5 位客戶的「VIP」標籤，其中 2 位無此標籤 | 成功移除 3 位，通知「2 位無此標籤，略過」 |
| 4 | 嘗試選取 201 位客戶 | 系統提示批次上限 200，無法繼續勾選 |
| 5 | 點擊全選 Checkbox | 當頁 20 位客戶全部被選取 |
| 6 | 批次操作中途點擊取消 | 操作已完成的部分保留（非 undo），退出多選模式 |
| 7 | 退出多選模式 | 清單回到一般瀏覽，Checkbox 消失 |

---

## 依賴關係

- **Blocked By**：US-060（客戶清單必須存在）、US-064（批次標記複用單筆指派的核心邏輯）
- **Blocks**：無

---

## Definition of Done

- [ ] 批次標記 API（POST /api/v1/c360/customers/bulk-tag）實作完成
- [ ] 批次上限驗證（200 筆，DB 層 + API 層）
- [ ] 重複指派跳過邏輯實作完成（冪等操作）
- [ ] 單一 Transaction 原子性確保
- [ ] 前端多選模式實作（Checkbox、全選、工具列）
- [ ] 批次操作進度指示與結果摘要通知
- [ ] 單元測試覆蓋率達標（> 80%）

---

## 相關文件

- **Epic Brief**：[E06 Epic Brief](epic-brief.md)
- **相關 Story**：US-060（客戶清單）、US-064（單筆標記）、US-066（依標籤篩選）
