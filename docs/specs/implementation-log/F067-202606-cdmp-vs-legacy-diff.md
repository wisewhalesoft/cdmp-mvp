# F067 差異報告：CDMP vs Legacy（202606）

> 日期：2026-06-18 ｜ CDMP run：`573352ce`（已匯入 legacy 部門/個別比例後重跑）｜ legacy 來源：`reference/202606 分派名單.xlsx` 工作表1

## 1. 範圍與方法（重要前提）

**兩邊案件集完全不同 → 這是「分佈比對」，不是逐案 diff。**

| | CDMP | Legacy |
|---|---|---|
| 案號格式 | `A1.../E1...` | `F3...` |
| 案件重疊 | ≈ 0（不同 ID 體系 / 不同抽取資料） | — |
| 名單數 | 3（OB202606001/002/003 ready） | 12（001-004,006-013） |
| 總案件 | 55,863 | 109,685 |
| Tier 體系 | 統一 T1–T5（migration 162） | 變體（T1HM/T32/T51/THC…，已映射回 T1–T5 比對） |

→ 絕對數量、CR 總量、逐案分派**不可比**；可比的是**每名單的分佈形狀（%）**，且僅限同名單同設定。

## 2. 部門分佈比對（✅ 吻合 — 驗證比例套用正確）

每名單 4 課佔比（L=legacy / C=CDMP）：

| 名單 | n (L/C) | XVE1 | XVE2 | XVE3 | XVE4 |
|---|---|---|---|---|---|
| 001 | 29,173 / 25,420 | 33.1 / 32.7 | 34.6 / 34.2 | 15.2 / 15.3 | 17.0 / 17.8 |
| 002 | 21,017 / 17,528 | **32.3 / 32.3** | **34.3 / 34.3** | **15.4 / 15.4** | **18.0 / 18.0** |
| 003 | 16,682 / 12,915 | 32.3 / 32.4 | 34.3 / 34.3 | 15.4 / 15.3 | 18.0 / 17.9 |

- **002（CR=0）逐課完全相同**；003 差 ≤0.1%；001 差 ≤0.8%（因 CR 預指派佔比略不同：legacy 6.91% vs CDMP 6.19%）。
- **結論：給定相同部門/個別比例，CDMP 分派分佈與 legacy 一致 → 分派演算法（Stage 3/4 比例 + CR 預指派）忠實。** 員編層亦吻合（XVE1 課內個別佔比對上 ob_empl_set ration）。

## 3. Tier 分佈比對（⚠️ 不吻合 — 評分卡/card 模型未對齊）

> **⚠️ 2026-06-25 更新：本節（2026-06-18 快照）診斷已被 §6 修正並結案。** 真因**非** config 未匯入（逐值比對證實 config 早已 1:1 對齊 legacy），而是 raw score 系統性偏低（引擎欄位讀法 + SALES_STS config 漂移 + PROJECT_TP 簡化）。修復後 tier 已逐名單對齊 legacy，詳見 §6。

| 名單 | Legacy（映射後） | CDMP |
|---|---|---|
| 001 | T1=69.3% T2=22.8% T3=7.8% | **T3=100%** |
| 002 | T1=8.2% T2=62.2% T3=29.6% | **T3=100%** |
| 003 | T3=83.8% T4=16.2% | **T4=100%** |

**根因（已查證，非比例問題）**：
- CDMP score **有真實變化**（001: 81~152、003: 83~155）→ 計分引擎（F098/F099/F100）正常運作。
- 但 score 全落入**單一 card_level**（001/002 全 D、003 為 C/D）→ 單一 tier。
- 對照 legacy `OBLEVELCARD_LEVEL` 區間（type-S：D=0~152、C=153~168、B=169~190、A=191+）：score 81~152 落 D 是「依該卡設定正確」。
- legacy 之所以有 T1/T2/T3 spread，是因為**案件分屬不同 card_type（S/H/E…）→ 不同分數區間 → 不同 card_level → 不同 tier**；CDMP 對這批名單的 **card_type 指派 + 評分卡設定（OBLEVELCARD_*/OBTIER）未對齊 legacy**，導致 card_level/tier 退化為單一值。

→ **要讓 tier 維度可比，需比照比例匯入：載入 legacy `OBLEVELCARD_COLUNM/LEVEL/SCORE/VERSION` + `OBTIER`，並對齊 card_type 指派邏輯**（CSV 已 dump 在 `reference/DumpData/`，本次未匯入）。

## 4. CR 比對（ℹ️ per-list 接近）

| 名單 | Legacy CR% | CDMP CR% |
|---|---|---|
| 001 | 6.91% | 6.19% |
| 002 | 0% | 0% |
| 003 | 0.20% | 0.22% |

CR 是 pool 案件屬性，案件集不同故略有差；per-list 量級一致，CR 優先分派邏輯（F102）方向正確（cr_id→emplid、is_cr=Y）。

## 5. 結論

| 維度 | 狀態 | 說明 |
|---|---|---|
| 部門分佈 | ✅ 吻合 | 匯入 legacy 比例後逐名單對齊（002 完全相同）→ 演算法正確 |
| 員編分佈 | ✅ 吻合 | 課內個別佔比對上 ob_empl_set |
| CR | ✅ 方向正確 | per-list 量級一致 |
| **Tier** | ✅ **吻合**（2026-06-25，見 §6）| F104 引擎欄位修正 + m302 啟用 H SALES_STS + F105 PROJECT_TP composite → 三名單逐格對齊 legacy（run `64555220`）|
| 逐案分派 | N/A | 案件集完全不同，本質不可逐案比對 |

**業務簽核前置**：四維度（部門 / 員編 / CR / tier）**全部驗證通過 → 均可簽核**（tier 於 2026-06-25 對齊，見 §6）。

---

## 6. 2026-06-25 更新：Tier 維度已對齊（✅ RESOLVED）

§3 原診斷「評分卡 config 未匯入」**經查證為誤**——逐值比對證實 CDMP 的 `OBLEVELCARD_LEVEL`(22/22)、`OBLEVELCARD_SCORE`(370/370 逐列)、`OBTIER` **早已 1:1 對齊 legacy**。tier 不吻合的真因是 **raw score 系統性偏低**，來自三個獨立缺陷，已逐一修復：

1. **計分引擎欄位讀法錯誤（F104）**：AD-E07-10-L 12 欄語意偏差（gender / 電話有無 / 郵遞號 / '專案'關鍵字 / '經銷商'…）→ 修對後 card_level 從單一 D 散成 A/B/C/D。
2. **H 卡 SALES_STS config 漂移（m302）**：dev DB 把 H/SALES_STS 標 `inactive`（seed/legacy 皆 active；根因＝seed loader「非空就 SKIP」無法自癒），少計一維（COMPOSITE，最多 +36）→ 啟用後 001 avg +27。
3. **PROJECT_TP 計分被簡化（F105）**：F104 把 composite 當 category，16 個 spec_tp `level1=NULL` row 變死碼、73.9% 案件 PROJECT_TP 拿 0 → 復原 COMPOSITE 真語意後 +21/案。

**重跑驗收（run `64555220`，2026-06-25）三名單逐格對齊 legacy：**

| 名單 | 卡 | Legacy | CDMP 修復前（`573352ce`）| **CDMP 修復後（`64555220`）** |
|---|---|---|---|---|
| 001 | H | T1=69.3 T2=22.8 T3=7.8 | T3=100 | **T1=67.9 T2=23.2 T3=8.9** ✅ |
| 002 | H | T1=8.2 T2=62.2 T3=29.6 | T3=100 | **T1=9.0 T2=59.1 T3=32.0** ✅ |
| 003 | S | T3=83.8 T4=16.2 | T4=100 | **T3=85.2 T4=14.8** ✅ |

整體 tier 從「T3=80% 無 T1」→ **T1=33.7 / T2=29.1 / T3=33.8 / T4=3.4**。

**結論更新**：tier 維度 ⚠️不吻合 → **✅ 吻合**（逐名單分佈形狀對齊 legacy）。§3 提的「card_type 指派對齊」**非必要**——這批名單 card_type 已正確，tier 對齊靠修計分引擎 + config 漂移即達成。四維度全數可簽核。

> 衍生邏輯透明度註記：本次對齊靠「引擎 EQ 對齊 legacy SP」保證，衍生規則（如 PROJECT_TP `level1='A'`=借新還舊）寫死於引擎、業務不可見。供簽核確認之衍生對照見 **AD-E07-10-S「計分衍生碼 Decode Dictionary」**。
