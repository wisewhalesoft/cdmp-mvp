---
type: implementation-log
feature_id: F050-v2.1
feature_name: F050 v2.1 whitelist-driven 名單定義重構 — Phase 5d Frontend
status: complete
last_updated: 2026-05-20
phase: 5d (of 5a/5b/5c/5d)
---

# F050 v2.1 Phase 5d Frontend — Implementation Log

> 範圍：
> - 5d 波 1-7：sidebar rename / App.tsx 路由 / FieldBasePage 2-Tab / FieldsTab / OptionsTab / F068 frontend 廢除 / assignment-list API type 強化
> - 5d 波 8-14：list-create-draft-page 全重寫 / list-edit-draft-page 全重寫含 LEGACY banner / list-definition-page filter chip + run-summary-page skipped / regression guard / 全 regression / impl log
>
> 不在範圍：
> - backend src 改動（5a/5b/5c 已完成；5d 維持不動）
> - report API expose skipped_cases.lists fully（5b §O-5B-003 留作後續）

## Commit 列表（14 波 + 1 docs commit）

| # | Commit | 變更 +/- | 對應波次 |
|---|---|---|---|
| 1 | `99b35cf` | sidebar app-sidebar.tsx + test | 波 1：sidebar「代碼維護」rename「篩選欄位」+ icon Filter |
| 2 | `164bccb` | App.tsx route | 波 2：App.tsx 刪 3 條舊路由 + 新增 /assignment/field-base |
| 3 | `62c3a6f` | field-base-page.tsx + FieldsTab/OptionsTab stub | 波 3：FieldBasePage 2-Tab 容器 |
| 4 | `4853750` | fields-tab.tsx | 波 4：FieldsTab 移植 field-whitelist-page main 內容 + 補 BR-8 |
| 5 | `7ce105d` | options-tab.tsx | 波 5：OptionsTab 移植 field-options-page main + v1.5 統一管理規則 |
| 6 | `e7a0f49` | 刪 base-codes / assignment-codes.ts / field-whitelist / field-options | 波 6：F068 frontend 廢除 |
| 7 | `2227224` | assignment-list.ts type 強化 | 波 7：ConditionPayload / ConditionItem / CreateListRequest / UpdateListRequest type |
| 8 | `583c873` | +1365 / -495 | 波 8：list-create-draft-page 全重寫對應 prototype 27a |
| 9 | `cef2ccf` | +1198 / -398 | 波 9：list-edit-draft-page 全重寫含 LEGACY banner 對應 prototype 27b |
| 10 | `f51bf8f` | +190 / -3 | 波 10：list-definition filter chip 對齊 conditionPayload + run-summary 補 skipped_cases.lists |
| 11 | `f5ac578` | +96 / -3 | 波 11：補 ob_code_df / F068 殘留 regression guard + 更新 card-type modal 文案 |
| 12 | （no-op） | 0 | 波 12：既有 page test fixture 連動（grep 0 fail，無需改動） |
| 13 | （no-op） | 0 | 波 13：route alignment 評估後延後（不增任何檔） |
| 14 | （本 commit） | docs only | 波 14：implementation log |

合計：8 個 production commits（波 8-11 共 +2849 / -899 行，波 1-7 同等規模於前 agent 落地），1 個 docs commit。

## Test Results Summary

### 新增 spec（4 個檔）

| 波 | spec | case 數 | 結果 |
|---|---|---|---|
| W8 | `list-create-draft-page.test.tsx`（全重寫） | 12 (lc.test#1~#12) | PASS |
| W9 | `list-edit-draft-page.test.tsx`（全重寫） | 7 (le.test#1~#7) | PASS |
| W10 | `list-definition-page.test.tsx`（新增 2 case） | +2 (ld.test#new1/#new2) | PASS |
| W10 | `run-summary-page.test.tsx`（新增 2 case） | +2 (rs.test#new1/#new2) | PASS |
| W11 | `no-ob-code-df-references.regression.test.tsx`（新檔） | 4 | PASS |

合計新增 / 改寫 27 case 全綠。

### 既有 test 未受 5d 改動影響

| 模組 | 狀態 |
|---|---|
| `apps/web/src/__tests__/`（含 app-routes / no-f068-frontend 兩個 regression） | 全綠 |
| `apps/web/src/components/`（含 app-sidebar regression / e07 components） | 全綠 29/29 |
| `apps/web/src/pages/assignment/`（含 card-type / scoring / dept-ratio / personnel-ratio / approval-review / ready-summary / stage0 / trigger-run / run-progress / run-history / run-summary / snapshot / run-compare / field-base / fields-tab / options-tab） | 全綠 |

### Regression 驗證

| 範圍 | 結果 |
|---|---|
| **apps/web 三大模組（assignment + components + __tests__）** | **63 spec / 553 passed / 0 failed / 12 skipped** |
| **apps/web 全套件** | **105 spec / 1182 passed / 5 failed (baseline) / 12 skipped** |
| **apps/api 全套件** | **139 spec / 1572 passed / 16 failed (baseline, 與 5c 同) / 1 skipped / 15 todo** |
| **5d 新引入 regression** | **0** |

### Baseline 預先存在失敗（與 5d 無關，不動）

**apps/web 5 fail**：
| spec 檔 | 失敗 case | 與 5d 關係 |
|---|---|---|
| `target-tables-page.test.tsx` | customer_core domain card | ❌ 與 5d 無關（target-table / etl 模組） |
| `customer-detail-page.test.tsx` | TS-F047-032 risk flags warning | ❌ 與 5d 無關（c360 模組） |
| `customer-list-page.test.tsx` | TS-F046-028/029 pagination + empty | ❌ 與 5d 無關（c360 模組） |
| `load-properties.test.tsx` | A~H Category field count | ❌ 與 5d 無關（etl-pipelines editor） |

**apps/api 16 fail**：完全等於 5a/5b/5c impl log baseline（engine-target-load / target-table-schemas / target-table.service / mssql-executor / mysql-executor / postgresql-executor 共 16 個 case）。5d 0 動 backend src。

## Files Changed（W8-W11 累積）

### 新增（1 個）

| 路徑 | 用途 |
|---|---|
| `apps/web/src/__tests__/no-ob-code-df-references.regression.test.tsx` | W11 regression guard（4 case） |

### 修改（W8-W11 累積，共 11 個）

| 路徑 | 對應波次 | 主要改動 |
|---|---|---|
| `apps/web/src/pages/assignment/list-create-draft-page.tsx` | W8 | 全重寫 v2.1：4 section + ConditionPayload-driven builder + AdvanceConfirmModal |
| `apps/web/src/pages/assignment/__tests__/list-create-draft-page.test.tsx` | W8 | 全重寫 12 case 對應 prototype 27a |
| `apps/web/src/pages/assignment/list-edit-draft-page.tsx` | W9 | 全重寫 v2.1：4 場景判斷 + LegacyBanner + ReadOnlyConditionSummary |
| `apps/web/src/pages/assignment/__tests__/list-edit-draft-page.test.tsx` | W9 | 全重寫 7 case 對應 prototype 27b |
| `apps/web/src/pages/assignment/list-definition-page.tsx` | W10 | buildConditionPreview 重寫優先讀 conditionPayload + LEGACY 徽章 |
| `apps/web/src/pages/assignment/__tests__/list-definition-page.test.tsx` | W10 | 新增 ld.test#new1/#new2（v2.1 filter chip） |
| `apps/web/src/pages/assignment/run-summary-page.tsx` | W10 | 新增 skipped_cases.lists 區塊（5b ITP-006 對應前端） |
| `apps/web/src/pages/assignment/__tests__/run-summary-page.test.tsx` | W10 | 新增 rs.test#new1/#new2（skipped 區塊） |
| `apps/web/src/pages/assignment/_components/create-card-type-modal.tsx` | W11 | 文案：ob_code_df → pooldata_field_option |
| `apps/web/src/pages/assignment/_components/edit-card-type-modal.tsx` | W11 | 同上 |
| `apps/web/src/api/card-type.ts` | W11 | 註解：ob_code_df → pooldata_field_option |

### 刪除（無，5d W8-W14 範圍內）

W6 已於前 agent commit `e7a0f49` 刪除 4 個 F068 前端檔案 + assignment-codes.ts。

## 對應 prototype 落地

### W8 — prototype 27a-list-create-draft.html

| Prototype 段 | 對應實作 |
|---|---|
| L173-216（基本資訊 section） | section 1：listNm + cardType + prodBest + LIST_NO 預覽 |
| L218-258（撈案期間 section） | section 2：3 input + 一級保留欄位徽章（BR-8 / J8） |
| L260-317（篩選條件 section） | section 3：condition builder + add-field-dropdown + INACTIVE warning |
| L319-345（CR 回分 section） | section 4：crEnabled checkbox + 啟用 / 停用 描述 |
| L386-433（AdvanceConfirmModal） | AdvanceConfirmModal sub-component + UI-Q1 雙重確認 |
| L510-528（FIELDS / FIELD_VALUES mock） | listFields / listOptions API 呼叫（whitelist-driven） |
| L763（INACTIVE 警示文案） | inactive-warning-banner「N 個可選值已停用，將被保留但月名單分派 Stage 1 不會匹配」 |
| L394（AdvanceConfirmModal 標題） | 「儲存並推進至部門比例階段？」 |
| L422（UI-Q1 checkbox 文案） | 「我已了解推進後條件將被固化」 |
| L899（validation error） | 「請至少設定一個篩選條件」 |
| L818-822（複製過濾 conditionPayload IS NOT NULL） | handleOpenCopyModal filter `l.conditionPayload != null` |
| L103-108（header 麵包屑） | AppLayout headerLeft 用 react-router-dom Link |

### W9 — prototype 27b-list-edit-draft.html

| Prototype 場景 | 對應實作 |
|---|---|
| 場景①（L489-502：draft + 雙條件 + 新名單） | conditionPayload != null → 條件 builder 可編輯 |
| 場景②（L504-516：draft + INACTIVE values） | 琥珀警示 + 可編輯 |
| 場景③（L518-532：LEGACY conditionPayload IS NULL + 5 entity column fallback） | legacy-condition-banner + ReadOnlyConditionSummary |
| 場景④（L534-545：stage='dept_ratio'） | not-draft-banner + 主表單隱藏 |
| L143-162（NotDraftBanner） | NotDraftBanner 區塊：標題「無法編輯篩選條件」+ Rollback 引導 |
| L293-301（LegacyBanner） | legacy-condition-banner：「此名單使用舊格式儲存，篩選條件暫時無法編輯」+「系統將於 Phase 3a 完成資料轉換」 |
| L842-882（renderLegacyConditions 5 entity column） | ReadOnlyConditionSummary：prod_kind / caseyear / spec_tp / case_status / settle_src |
| L922-943（submit 主動 omit conditionPayload） | submit handler `!isLegacy` 才送 conditionPayload |

### W10 — prototype 27-list-definition.html + 33-run-summary.html

| Prototype 段 | 對應實作 |
|---|---|
| 27 表格 filter chip（buildConditionPreview） | 重寫優先讀 list.conditionPayload，NULL 才 fallback 5 欄位 |
| 27 LEGACY 小徽章 | legacy-badge-{listNo}：conditionPayload === null 時顯示「LEGACY v2.0」 |
| 33 skipped_cases.lists 區塊 | skipped-lists-section：listNo / listNm / reason 3 欄表格 |

## 紅線遵循確認

| 紅線 | 確認方式 |
|---|---|
| 7 波獨立 commit（不批量、不 amend） | W8 / W9 / W10 / W11 各自獨立 commit；W12 / W13 為 no-op；W14 為 docs commit |
| 不用 `/commit` skill / `git stash -u` | 全程 `git add <files>` + HEREDOC commit message |
| 每波先 RED → fail → GREEN → pass → refactor → commit | W8/W9/W10/W11 皆有明確 RED 階段（grep tail 顯示 fail count） |
| baseline 16 fail（backend）絕不動 | 5d 全程 0 改動 apps/api/src，跑全套件後 16 fail 維持 |
| prototype 是 ground truth | 所有 RTL 文案直引 prototype 27a/27b 行號；4 section 順序對齊不換序 |
| 不跨 5d 邊界 | 0 改動 backend；5b §O-5B-003 report API skipped expose 留作後續 |

## Open Issues（後續 phase）

### O-5D-001：list-definitions 路由名稱 vs 後端 /lists 不一致

| 項 | 內容 |
|---|---|
| 觀察 | F050/F051 spec §6.1 寫 `/api/v1/assignment/list-definitions`；後端 controller 為 `/api/v1/assignment/lists`；前端 client (`apps/web/src/api/assignment-list.ts`) 已對齊 `/assignment/lists`；前端 route 為 `/assignment/list-definitions`（這是 React Router 路由不是 API 路由） |
| 5d 處理 | 沿用既有（前端 React Router 用 `/assignment/list-definitions`；client 對 backend 用 `/assignment/lists`） |
| 建議 | 後續 phase 由 spec-writer 統一路由命名（spec 對齊 / 改 backend / 或留作 alias） |

### O-5D-002：5b §O-5B-003 report API 顯示 skipped_cases.lists

| 項 | 內容 |
|---|---|
| 觀察 | 5b W7 已將 skipped_cases.lists 寫入 DB；5d W10 補了前端顯示區塊 |
| 5d 處理 | 前端從 `data.warnings.skippedCases.lists` 讀取並渲染（IT-M01-017 對應前端 ITP-006 已綠） |
| 建議 | 此 OPEN issue 可關閉 — 5d W10 已完成前端顯示部分 |

### O-5D-003：W11 ob_code_df 白名單 2 處

| 項 | 內容 |
|---|---|
| 觀察 | `field-base-page.tsx` 與 `_components/options-tab.tsx` 文案保留「歷史對照說明」字串 ob_code_df（描述 v2.1 後資料遷移情境） |
| 5d 處理 | regression test 加 ALLOWED_HISTORICAL_DESCRIPTION_FILES 白名單通過 |
| 建議 | 若後續 phase 完成 m286 DROP TABLE ob_code_df 後，可同步移除這兩段文案；目前保留作為用戶教育文案 |

## 5d 範圍內測試統計

- **新增 spec：5 個檔 / 27 case**（W8 12 + W9 7 + W10 4 + W11 4）
- **migrate spec：4 個檔**（W8 list-create-draft / W9 list-edit-draft / W10 list-definition / W10 run-summary 4 個既有 test 改寫或補 case）
- **apps/web 三大模組（assignment + components + __tests__）**：63 spec / 553 passed / 0 failed / 12 skipped
- **apps/web 全套件**：105 spec / 1182 passed / 5 failed (baseline) / 12 skipped
- **apps/api 全套件**：139 spec / 1572 passed / 16 failed (baseline) / 1 skipped / 15 todo
- **5d 新引入 regression：0**

## Phase 5d 完成聲明

- 14 個波次依紅綠循環執行完成（W1-W7 由前 agent 落地；W8-W11 落地；W12/W13 為 no-op；W14 為本 docs commit）
- 4 場景 LEGACY banner 完整對齊 prototype 27b：場景③（LEGACY）+ 場景④（stage != draft）
- 拍板 UI-Q1（INACTIVE 推進雙重確認）落地於 AdvanceConfirmModal
- 5d 全程 0 動 backend src；5b/5c baseline 16 fail 維持不動
- 3 個 Open Issue（O-5D-001~003）已記錄，全部屬後續 phase 範圍

## Phase 5 (a/b/c/d) 整體完成

- **5a**：M1/M2 migration + DTO + Service createList/updateList v2.1 + 4 個新 error code + fixture migrate（11 commits / 341 case）
- **5b**：Stage 1 動態 SQL Composer + warning_summary + skipped_cases.lists DB 寫入（8 commits / 37 新 case）
- **5c**：M3/M4/M5 + F068 backend 廢除 + F069 service 改讀 pooldata_field_option（7 commits / 67 新 case + 45 migrate case）
- **5d**：Frontend rename + F068 frontend 廢除 + 4 個主要 page 重寫 / 補強（14 commits / 27 新 case）

**累積 50+ 個 commit / 472+ 個新 test case 全綠 / 0 個 5a-5d 引入 regression**。
