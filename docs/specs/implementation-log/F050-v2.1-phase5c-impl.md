---
type: implementation-log
feature_id: F050-v2.1
feature_name: F050 v2.1 whitelist-driven 名單定義重構 — Phase 5c M3-M5 + F069 改寫 + F068 廢除
status: complete
last_updated: 2026-05-20
phase: 5c (of 5a/5b/5c/5d)
---

# F050 v2.1 Phase 5c — M3-M5 migration + F069 service 遷移 + F068 模組廢除

> 範圍：
> - M3 migration (spec_tp 32 筆 UPSERT)
> - M4 migration (case_status whitelist + 4 options)
> - M5 migration (DELETE ob_code_df 三組 tbl_id) + F069 service 改讀 pooldata_field_option（同 commit deployment gate）
> - F068 模組廢除 8 step（§18.7 Step 順序微調 1→2→4→5→6→3→7→8）
> - error-codes.ts 3 個 F068 code 移除
>
> 不在範圍：
> - React frontend（5d 處理 — base-codes-page.tsx / assignment-codes.ts / RTL）
> - assignment-list / Stage 1 composer（5b 已綠）
> - sidebar 前端入口（5d 處理 — TS-F068-DEP-009 frontend 部分）

## Commit 列表（7 個 commit + 1 docs commit）

| # | Commit | 變更 +/- | 對應波次 |
|---|---|---|---|
| 0 | `6d3a9c6` | docs（C1+C2 補修） | 前置（spec-writer + test-designer） |
| 1 | `273fc0b` | +487 -0 | 波 1+2：M3 migration（spec_tp 32 筆 UPSERT + idempotency + down） |
| 2 | `cf20953` | +371 -0 | 波 3：M4 migration（case_status whitelist + 4 options + idempotency + down） |
| 3 | `2b81b2b` | +920 -127 | 波 6 🔒 **deployment gate**（M5 + F069 改 5 處 + helper + module + 5 fixture migrate） |
| 4 | `85b46f1` | +44 -2 | 波 10：app.module.ts 移 AssignmentCodeModule |
| 5 | `60754e5` | +74 -1498 | 波 11：刪 assignment-code 9 個檔（含 e2e）+ 新 404 e2e |
| 6 | `e181063` | +94 -15 | 波 12：刪 error-codes 3 個 code + 清空目錄 + W9 spec GREEN + feature-flag-coverage 同步 |

合計：**+1990 / -1642 行**，**淨增 348 行**（淨刪 F068 模組約 1500 行 + 新增 M3/M4/M5 / regression / e2e 約 1850 行）。

## Test Results Summary

### 新增 spec — 6 個檔 / 共 48+ case

| 波 | spec | case | 結果 |
|---|---|---|---|
| W1 | `m283-upsert-spec-tp-options-32.spec.ts` | 15 | PASS |
| W3 | `m284-seed-case-status-whitelist-and-options.spec.ts` | 11 | PASS |
| W4 | `card-type-source-pooldata-option.spec.ts` | 7 | PASS |
| W5 | `prod-kind.helper.spec.ts` | 4 | PASS |
| W6 | `m285-delete-ob-code-df-redundant-tbl-ids.spec.ts` | 13 | PASS |
| W9 | `error-codes-no-f068.regression.spec.ts` | 9 | PASS |
| W10 | `no-assignment-code-module.regression.spec.ts` | 2 | PASS |
| W11 | `test/no-assignment-codes-route.e2e-spec.ts` | 6 | PASS |

### 既有 spec migrate — 5 個檔 fixture 從 ObCodeDf 改 PooldataFieldOption

| spec | 既有 case 數 | 結果 |
|---|---|---|
| `card-type-list.service.spec.ts` | 12 | PASS |
| `card-type-create.service.spec.ts` | 10 | PASS |
| `card-type-update.service.spec.ts` | 9 | PASS |
| `card-type-stats.service.spec.ts` | 4 | PASS |
| `card-type-delete.service.spec.ts` | 10 | PASS |

合計 migrate 45 case 全綠。

### M01 Migration Test 對照表

| test ID | 5c 狀態 | 涵蓋方式 |
|---|---|---|
| MT-M3-001 | PASS | `m283 spec` it「DELETE column_name=spec_tp → INSERT 32 筆」+ functional 31 筆 count |
| MT-M3-002 | PASS | `m283 spec` it「ON CONFLICT DO UPDATE 冪等」+ functional 第二次跑 count 不增 |
| MT-M3-003 | PASS | `m283 spec` it「down DELETE spec_tp」+ functional 還原 m24 placeholder 3 筆 |
| MT-M4-001 | PASS | `m284 spec` it「INSERT 4 筆 01/02/03/04」+ functional option_value=['01','02','03','04'] |
| MT-M4-002 | PASS | `m284 spec` it「whitelist INSERT case_status categorical 案件結清期別」 |
| MT-M4-003 | PASS | `m284 spec` it「DO NOTHING 冪等」+ functional 重複跑不增加 |
| MT-M4-004 | PASS | `m284 spec` it「down 先 options 後 whitelist」+ functional count=0 |
| MT-M5-001 | PASS | `card-type-source-pooldata-option.spec.ts` MT-M5-001 case + W9 regression guard（C5 拍板：靜態掃描非 CI 外部腳本） |
| MT-M5-002 | PASS | `m285 spec` C2 紅線 + functional SQLite 跑 DELETE PROD_KIND/SPEC_TP/CASE_STATUS |
| MT-M5-003 | PASS | 同上（一 SQL 一次刪三 tbl_id） |
| MT-M5-004 | PASS | `m285 spec` functional 驗 BEST_CASE / LIST_TYPE 未被刪 |
| MT-M5-005 | PASS | `m285 spec` down() partial restore 反 INSERT 至 ob_code_df |
| MT-M5-006 | PASS | `card-type-source-pooldata-option.spec.ts` 驗 listCardTypes 從 option 取 prodKindName |

### F068 廢除驗證對照表

| TS-F068-DEP ID | 5c 狀態 | 涵蓋方式 |
|---|---|---|
| TS-F068-DEP-001 | PASS | `no-assignment-codes-route.e2e-spec.ts` GET /api/v1/assignment-codes → 404 |
| TS-F068-DEP-002 | PASS | 同上 GET /:tblId → 404 |
| TS-F068-DEP-003 | PASS | 同上 POST → 404；含 PUT / disable variants |
| TS-F068-DEP-004 | PASS | `error-codes-no-f068.regression.spec.ts` 9 case：ERROR_CODES/ERROR_MESSAGES 不含 3 個 key + fs 全 src 掃描無殘留引用 |
| TS-F068-DEP-005 | PASS | `no-assignment-code-module.regression.spec.ts` 2 case：app.module.ts 不含 AssignmentCodeModule + 目錄不存在 |
| TS-F068-DEP-006 | PASS | `card-type-source-pooldata-option.spec.ts` 7 case：F069 listCardTypes / createCardType / updateCardType 全從 pooldata_field_option 取 prodKindName |
| TS-F068-DEP-007 | PASS | `m285 spec` functional 跑 m285 後 ob_code_df 三組 tbl_id count=0 |
| TS-F068-DEP-008 | PASS | M5 migration pre-condition check（拍板 C5：採 regression-guard 風格而非 CI 外部腳本） |
| TS-F068-DEP-009 | **SKIP**（5d） | frontend sidebar 入口檢查屬 5d 範圍 — 留 Open Issue O-5C-001 |

### F076 v1.5 對照表

| TS-F076 ID | 5c 狀態 | 涵蓋方式 |
|---|---|---|
| TS-F076-001 | PASS | `m284 spec` functional case_status 4 筆 seed |
| TS-F076-002 | **COVERED**（既有 m22）| caseyear 8 筆 0~6+99 由 m22 seed；不在 5c 範圍 |
| TS-F076-003 | PASS | `m283 spec` functional spec_tp count=32（C1 補修對齊：來自 CSV TBL_ID='02'） |
| TS-F076-004 | PASS | `m283 spec` 第二次 up() count 仍 32（UPSERT 冪等） |
| TS-F076-005 | PASS | `m284 spec` 第二次 up() count 仍 4（DO NOTHING 冪等） |
| TS-F076-006 | **SKIP**（5d） | frontend RTL（caseyear 99 停用後顯示 7 筆）屬 5d 範圍 |
| TS-F076-007 | **COVERED**（5b composer）| 停用不回溯 — composer Path A 不過濾 is_active=false 之 values |
| TS-F076-008 | **COVERED**（既有 spec）| F076 處長 RBAC 403 由既有 pooldata-field-option controller spec 覆蓋 |

### IT-M01 整合測試對照表

| IT-M01 ID | 5c 狀態 | 涵蓋方式 |
|---|---|---|
| IT-M01-011 | **重跑 PASS**（5b 已綠 + W2/W3 後 spec_tp/case_status seed 完整對齊）| 隨 W2/W3 完成後 spec_tp options 32 筆 + case_status whitelist 1+4 筆完整存在 |
| IT-M01-012 | **重跑 PASS** | 同上 |
| IT-M01-022 / IT-M01-023 | **COVERED**（5b composer）| SQL Injection allowlist 防禦由 5b UCQ-025~029 覆蓋 |

## Files Changed（7 commit 累積）

### 新增（9 個檔）

| 路徑 | 用途 |
|---|---|
| `apps/api/src/database/migrations/1711360000283-UpsertSpecTpOptions32.ts` | M3 migration |
| `apps/api/src/database/migrations/__tests__/m283-upsert-spec-tp-options-32.spec.ts` | M3 spec |
| `apps/api/src/database/migrations/1711360000284-SeedCaseStatusWhitelistAndOptions.ts` | M4 migration |
| `apps/api/src/database/migrations/__tests__/m284-seed-case-status-whitelist-and-options.spec.ts` | M4 spec |
| `apps/api/src/database/migrations/1711360000285-DeleteObCodeDfRedundantTblIds.ts` | M5 migration |
| `apps/api/src/database/migrations/__tests__/m285-delete-ob-code-df-redundant-tbl-ids.spec.ts` | M5 spec |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-source-pooldata-option.spec.ts` | F069 service 來源遷移 spec（W4/W6 新增） |
| `apps/api/src/modules/assignment-scoring/helpers/__tests__/prod-kind.helper.spec.ts` | helper unit spec（W5/W6 新增） |
| `apps/api/src/common/errors/__tests__/error-codes-no-f068.regression.spec.ts` | error code 移除 regression guard（W9/W12） |
| `apps/api/src/__tests__/no-assignment-code-module.regression.spec.ts` | app.module / 目錄 regression guard（W10/W12） |
| `apps/api/test/no-assignment-codes-route.e2e-spec.ts` | F068 路由 404 e2e（W11） |

### 修改（10 個檔）

| 路徑 | 主要改動 |
|---|---|
| `apps/api/src/modules/assignment-scoring/services/card-type.service.ts` | 5 處 `codeDfRepo` → `optionRepo` (PooldataFieldOption)；is_active 布林取代 stadt/enddt 期間判定 |
| `apps/api/src/modules/assignment-scoring/helpers/prod-kind.helper.ts` | `assertProdKindActive` 簽名改吃 `Repository<PooldataFieldOption>`；其餘函式（todayYyyymmdd / sdateToday）保留 |
| `apps/api/src/modules/assignment-scoring/assignment-scoring.module.ts` | TypeOrmModule.forFeature 移除 ObCodeDf，加入 PooldataFieldOption |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-list.service.spec.ts` | fixture mock codeDfRepo → optionRepo（含 is_active 取代 stadt/enddt） |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-create.service.spec.ts` | 同上 |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-update.service.spec.ts` | 同上 |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-stats.service.spec.ts` | DI token 改 PooldataFieldOption |
| `apps/api/src/modules/assignment-scoring/__tests__/card-type-delete.service.spec.ts` | 同上 |
| `apps/api/src/app.module.ts` | 移除 AssignmentCodeModule import + imports[] |
| `apps/api/src/common/errors/error-codes.ts` | 移除 CODE_IN_USE / CODE_TYPE_INVALID / CODE_NOT_FOUND（ERROR_CODES + ERROR_MESSAGES 共 6 行） |
| `apps/api/src/common/feature-flags/__tests__/feature-flag-coverage.regression.spec.ts` | 移除 assignment-code controller 條目；count check 10 → 9 |

### 刪除（10 個檔 + 3 個目錄）

| 路徑 | 原因 |
|---|---|
| `apps/api/src/modules/assignment-code/__tests__/assignment-code.controller.spec.ts` | F068 Step 2 |
| `apps/api/src/modules/assignment-code/__tests__/assignment-code.service.spec.ts` | F068 Step 2 |
| `apps/api/src/modules/assignment-code/assignment-code.controller.ts` | F068 Step 4 |
| `apps/api/src/modules/assignment-code/assignment-code.service.ts` | F068 Step 5 |
| `apps/api/src/modules/assignment-code/assignment-code.module.ts` | F068 Step 6 |
| `apps/api/src/modules/assignment-code/dto/create-code.dto.ts` | F068 Step 3（順序微調至 6 之後） |
| `apps/api/src/modules/assignment-code/dto/update-code.dto.ts` | 同上 |
| `apps/api/src/modules/assignment-code/dto/list-codes-query.dto.ts` | 同上 |
| `apps/api/test/assignment-code.e2e-spec.ts` | F068 e2e |
| `apps/api/src/modules/assignment-code/__tests__/`（目錄）| F068 Step 7 |
| `apps/api/src/modules/assignment-code/dto/`（目錄）| F068 Step 7 |
| `apps/api/src/modules/assignment-code/`（整個目錄）| F068 Step 7 |

## 架構決策落地

| AD-E07-18 章節 | 落地位置 |
|---|---|
| §18.3 M3（spec_tp 32 筆 UPSERT） | `1711360000283-UpsertSpecTpOptions32.ts` + spec 15 case |
| §18.3 M4（case_status whitelist + 4 options） | `1711360000284-SeedCaseStatusWhitelistAndOptions.ts` + spec 11 case |
| §18.3 M5（DELETE ob_code_df 三組 tbl_id） | `1711360000285-DeleteObCodeDfRedundantTblIds.ts` + spec 13 case |
| §18.2.7 deployment gate（M5 + F069 同 commit） | commit `2b81b2b` 同時包含 m285 + card-type.service 5 處改 + helper 改 + module forFeature + 5 fixture migrate |
| §18.7 Step 1（app.module.ts 移除） | commit `85b46f1` |
| §18.7 Step 2~6（刪檔順序，微調為 2→4→5→6→3） | commit `60754e5`（順序原因：dto 被 controller import，若先刪 dto 會 tsc 失敗） |
| §18.7 Step 7（刪空目錄） | commit `e181063` |
| §18.7 Step 8（error-codes.ts 3 個 code） | commit `e181063` + W9 regression spec（前置 fs 全 src grep 確認零外部引用） |
| §18.10 R7（C1+C2 spec 補修對齊） | C0 commit `6d3a9c6`（spec-writer + test-designer）+ 全 5c 落地以英文常數 tbl_id 與 CSV TBL_ID='02' 為準 |

## 拍板採用之計劃 4 項

| # | 拍板 | 落地處 |
|---|---|---|
| C3 | F069 同 PR 改 5 處（含 assertProdKindActive helper） | commit `2b81b2b` 同 commit 包含 card-type.service 5 處 + helper.ts 完整改寫 |
| C4 | deployment gate 採 regression-guard 靜態掃描 | W9 / W10 regression spec 採 fs 掃描，不另設 CI 外部腳本 |
| §18.7 Step 3 順序微調 | 採 1→2→4→5→6→3→7→8 安全順序 | commit `60754e5` 已執行 dto 最後刪，commit message 註明微調理由 |
| R-5C-08 frontend 延 5d | base-codes-page.tsx 等不動 | 列為 Open Issue O-5C-001 |

## Regression 驗證

| 範圍 | 結果 |
|---|---|
| **W6 deployment gate commit 後**：assignment 三模組 + migration | 825 passed / 0 failed |
| **W11 後**：assignment 三模組 + migration + regression | 826 passed / 1 RED（W10 目錄條件留 W12） |
| **W12 後（全綠）**：三模組 + migration + regression | 883 passed / 1 skipped / 11 todo / 0 failed |
| **W13 全 apps/api**：134 spec | 1572 passed / **16 failed (baseline 與 5a/5b 同)** / 1 skipped / 15 todo |
| **5c 新引入 regression** | **0** |

### Baseline 16 fail 確認與 5b 同（不動）

| spec 檔 | 失敗 case | 與 5c 關係 |
|---|---|---|
| `engine-target-load.spec.ts` | TS-F044-018/019 | 紅線指定不動 |
| `target-table-schemas.spec.ts` | A~H category coverage 8 個 | 與 5c 無關 |
| `target-table.service.spec.ts` | columnCount 2 個 | 與 5c 無關 |
| `mssql-executor.spec.ts` | OFFSET/FETCH NEXT + keyset 2 個 | 與 5c 無關 |
| `mysql-executor.spec.ts` | LIMIT + keyset 2 個 | 與 5c 無關 |
| `postgresql-executor.spec.ts` | LIMIT $N readBatch 1 個 | 與 5c 無關 |

確認方式：5c commit 範圍觸及 `assignment-scoring/`（card-type service/helper/module/fixture）+ `assignment-code/`（已刪）+ `database/migrations/`（m283/m284/m285）+ `common/errors/`（error-codes）+ `app.module.ts` + `__tests__/`（root regression）+ `test/`（e2e），完全未觸及 `etl/` / `extraction-task/` 目錄。

## Deployment Gate 部署順序提示

**同 PR 必含**：本 7 個 commit（不含 `6d3a9c6` 之 docs commit，docs commit 可早一輪合入）

**Staging / CI 執行順序**：
1. 跑 m281 / m282（5a，若尚未跑）→ m283（M3）→ m284（M4）
2. 部署 W6 commit `2b81b2b` 之 service 改動（card-type.service / prod-kind.helper / module forFeature）
3. 跑 m285（M5）— 此時 pre-condition check 會驗證 pooldata_field_option 三欄完整
4. （後續 commit 不依賴 migration 執行）：W10 / W11 / W12 純 code 變動，部署完成即可

**Production rollback**：M5 down() 為 partial restore（從 pooldata_field_option 反 INSERT 回 ob_code_df），不還原 stadt/enddt 精確值。**Production rollback 須從 DB snapshot 還原**。

## Open Issues（後續 phase）

### O-5C-001：Frontend F068 廢除（5d 範圍）

| 項 | 內容 |
|---|---|
| 觀察 | TS-F068-DEP-009（前端 Sidebar 不含「指派代碼」入口）未在 5c 階段執行；apps/web/src/pages/assignment/base-codes-page.tsx + assignment-codes.ts API client + 對應 RTL test 仍存在 |
| 5c 處理 | 全部 frontend 範疇延至 5d；implementation log 標 SKIP |
| 建議 | 5d 派 frontend agent：(1) 刪 base-codes-page.tsx + assignment-codes.ts；(2) 移除 sidebar 「代碼維護」入口（依 J4 拍板 rename「篩選欄位」）；(3) 補 RTL spec 驗證 sidebar 不含 F068 入口；(4) 更新 React Router 移除對應 route |

### O-5C-002：apps/web 內 ob_code_df 殘留引用評估（5d 範圍）

| 項 | 內容 |
|---|---|
| 觀察 | 5c 未掃 apps/web 是否有對 /api/v1/assignment/codes/* 的引用；若 frontend 有殘留 client 仍呼叫此 API，部署後會跑 404 |
| 5c 處理 | 不掃（屬 frontend 範疇） |
| 建議 | 5d 派 frontend agent 執行 `grep -r "assignment-codes\|assignment/codes\|AssignmentCode" apps/web/src` 確認；發現殘留則同步刪除或改用 F075/F076 API |

### O-5C-003：ob_code_df entity / table 是否最終移除（OPEN）

| 項 | 內容 |
|---|---|
| 觀察 | J2 拍板保留 ob_code_df entity 與 table（僅刪三組 tbl_id 紀錄）；目前 AppModule 仍 register ObCodeDf entity；assignment-scoring 已不依賴 |
| 5c 處理 | entity 與 AppModule registration 保留；不影響運作 |
| 建議 | 5d / 後續 phase 評估 ob_code_df 是否還有其他 module 使用（grep 已確認剩 m150 / migrations / entity 自身）；若確定 100% 無依賴可發 m286+ migration 全表 DROP，但屬獨立決策不在 F050 v2.1 範圍 |

### O-5C-004：feature-flag ENABLE_E07_REFACTOR_PHASE3 範圍縮小

| 項 | 內容 |
|---|---|
| 觀察 | feature-flag-coverage.regression.spec.ts 之 E07_WRITE_CONTROLLERS 從 10 降至 9；F068 已不需要 feature flag |
| 5c 處理 | spec 已同步調整 count |
| 建議 | 5d 評估是否需要重新整理 E07 feature flag 範圍文件，或在 ENABLE_E07_REFACTOR_PHASE3 完全啟用後移除整個 feature flag |

## 5c 範圍內測試統計

- **新增 spec：8 個檔 / 67 case**（M3 15 + M4 11 + M5 13 + service 7 + helper 4 + error-codes regression 9 + module regression 2 + e2e 6）
- **migrate spec：5 個檔 / 45 case**（card-type-list/create/update/stats/delete fixture）
- **assignment-scoring 模組**：27 spec 全綠 / 257 passed / 0 failed / 11 todo
- **三模組 + migration + regression**：83 spec 全綠 / 883 passed / 1 skipped / 11 todo / 0 failed
- **全 apps/api 套件**：134 spec / 1572 passed / 16 failed（baseline，與 5a/5b 同）/ 1 skipped / 15 todo
- **5c 新引入 regression：0**

## Downstream agents 引用

- **5d（Frontend F068 廢除）**：應讀 O-5C-001 / O-5C-002 + commit `60754e5` / `e181063` 之 backend 對應變更
- **5d（report API skipped 顯示）**：應讀 5b impl log §O-5B-003（與 5c 無關）
- **Code review**：所有變動 +/- 統計、commit hash 序列、deployment gate commit 標記、AD-E07-18 落地對照表均見本文件

## Phase 5c 完成聲明

- 13 個波次依紅綠循環執行完成（W1/W2 合併、W4/W5 同 commit gate、W7/W8 由 W6 涵蓋、W9 由 W12 GREEN）
- W6 deployment gate 嚴格落地於單一 commit `2b81b2b`，commit message 明示 §18.2.7 / §18.7 與部署順序
- C1+C2 spec 矛盾於 commit `6d3a9c6`（5c 前置）已由 spec-writer + test-designer 補修，5c 落地全程以英文常數 tbl_id + CSV TBL_ID='02' 為準
- §18.7 Step 順序微調（1→2→4→5→6→3→7→8）於 commit `60754e5` message 註明理由
- baseline 16 fail 維持不動，5c 0 新增 regression
- 4 個 Open Issue（O-5C-001~004）已記錄，全部屬 5d 或後續 phase 範圍
