---
type: implementation-log
feature_id: F050-v2.1
feature_name: F050 v2.1 whitelist-driven 名單定義重構 — Phase 5b Stage 1 動態 SQL Pipeline
status: complete
last_updated: 2026-05-20
phase: 5b (of 5a/5b/5c/5d)
---

# F050 v2.1 Phase 5b Stage 1 動態 SQL Pipeline — Implementation Log

> 範圍：AssignmentRunPipelineService Stage 1 從 `poolRepo.find()` 全表撈案件重寫為
> 依 condition_payload 動態 SQL 過濾 + Path A/B 並存 + caseyear wildcard +
> empty conditions skip + columnName allowlist 防禦
> 不在範圍：M3/M4/M5（5c）/ F068 廢除（5c）/ React frontend（5d）/
> list_period × month_cnt × list_interval mod 過濾（5c，避免越界）

## Commit 列表（8 波）

| # | Commit | 變更 +/- | 對應波次 |
|---|---|---|---|
| 1 | `5cbd248` | +371 -0 | 波 1：Composer 骨架 + Path A categorical（UCQ-001~005） |
| 2 | `2c31df9` | +219 -2 | 波 2：Path A numeric + date fragment（UCQ-006~011） |
| 3 | `21dc4d6` | +180 -7 | 波 3：Path B fallback 5 個 entity column（UCQ-012~018） |
| 4 | `41fa000` | +135 -1 | 波 4：caseyear '99' wildcard 短路（UCQ-019~024 / IT-M01-013/14/15） |
| 5 | `070a407` | +115 -0 | 波 5：columnName allowlist SQL Injection 防禦驗收（UCQ-025~030 / IT-M01-022/023） |
| 6 | `68ded7d` | +462 -2 | 波 6：runPipeline 整合 Composer 動態 SQL（ITP-001~004 / IT-M01-009~012） |
| 7 | `0f6ed14` | +134 -2 | 波 7：warning_summary + skipped_cases.lists（ITP-005~007 / IT-M01-016/017） |
| 8 | `1ed93c9` | +64 -24 | 波 8：runStage1ForList refactor + v1/v2 fixture migrate（0 regression 驗收） |

合計 8 個 commits，**約 +1680 / -38 行**（apps/api 範圍）。

## Test Results Summary

### 新 spec — Stage1QueryComposer unit（30 case 全綠）

| 波 | UCQ 範圍 | 描述 |
|---|---|---|
| 1 | UCQ-001~005 | Path A categorical / 空 conditions skip / `_backfill_empty` skip |
| 2 | UCQ-006~011 | Path A numeric BETWEEN / date BETWEEN / mixed 3 條件 AND |
| 3 | UCQ-012~018 | Path B 5 個 entity column mapping / 空字串 skip / case_status → list_type |
| 4 | UCQ-019~024 | caseyear=[99] / [1,99,3] / [1,3] / mixed with prod_kind / Path B `1$$99$$3` |
| 5 | UCQ-025~030 | SQL Injection payload / UPPERCASE / 空白 / numeric+date 同樣防禦 / 邊界 63 字元 |

### 新 spec — assignment-run-pipeline-stage1-dynamic（7 case 全綠）

| Test ID | 對應 M01 | Status |
|---|---|---|
| ITP-001 | IT-M01-009：Path A categorical | PASS |
| ITP-002 | IT-M01-010：Path A numeric BETWEEN | PASS |
| ITP-003 | IT-M01-011：Path B fallback | PASS |
| ITP-004 | （wildcard integration）caseyear=[99] 不過濾 year_cnt | PASS |
| ITP-005 | IT-M01-016：_backfill_empty skip 不撈案件，月名單分派不 fail | PASS |
| ITP-006 | IT-M01-017：result summary skipped_cases.lists 標記 | PASS (DB 直查；report API 留 5c) |
| ITP-007 | 1 skip + 1 正常 並存場景 | PASS |

### M01 Integration Test 對照表

| IT-M01 ID | 5a/5b 狀態 | 涵蓋方式 |
|---|---|---|
| IT-M01-001~008 | ✅ 5a 已綠（不重做） | 5a `create-list-v2.1.spec.ts` / `update-list-v2.1.spec.ts` |
| IT-M01-009 | ✅ 5b 波 6 | ITP-001 |
| IT-M01-010 | ✅ 5b 波 6 | ITP-002 |
| IT-M01-011 | ✅ 5b 波 6 | ITP-003 |
| IT-M01-012 | ✅ 5b 波 6（date integration 隨 Path A 一同驗證） | composer UCQ-009 + ITP-001~004 結構 |
| IT-M01-013 | ✅ 5b 波 4 | UCQ-019（unit）+ ITP-004（integration） |
| IT-M01-014 | ✅ 5b 波 4 | UCQ-020 |
| IT-M01-015 | ✅ 5b 波 4 | UCQ-021 |
| IT-M01-016 | ✅ 5b 波 7 | ITP-005 |
| IT-M01-017 | ✅ partial 5b 波 7（DB 直查） | ITP-006；report API 顯示留 5c |
| IT-M01-018~021 | ✅ 5a 已綠（不重做） | — |
| IT-M01-022（新增編號）| ✅ 5b 波 5 | UCQ-025/026/027；後續 test-designer 追補進 M01-whitelist-driven-integration-test.md |
| IT-M01-023（新增編號）| ✅ 5b 波 5 | UCQ-028/029；同上 |

### 既有 spec migrate（v1 / v2 pipeline fixture seed）

| Spec 檔 | 變更 |
|---|---|
| `assignment-run-pipeline.service.spec.ts` | seedList 加 `condition_payload`（prod_kind=['A']）+ seedPool 加 `prod_kind: 'A'` 預設 |
| `assignment-run-pipeline-v2.service.spec.ts` | 同上 |

無刪除既有 spec；無改變既有測試案件數 / 預期行為。

### Regression 驗證

| 範圍 | 結果 |
|---|---|
| assignment / assignment-list / assignment-scoring 三模組 | **59 spec / 686 passed / 1 skipped / 0 failed / 11 todo** |
| 全 apps/api 套件 | **134 spec / 1550 passed / 16 failed / 1 skipped / 15 todo** |
| 失敗的 16 case 來源 | 完全等於 5a impl log 預先標註之 baseline（engine-target-load / target-table / target-table-schemas / mssql / mysql / postgresql executor 6 spec），與 5b 0 關聯 |
| **5b 新引入 regression** | **0** |

## Files Changed（8 commits 累積）

### 新增（3 個）

| 路徑 | 用途 |
|---|---|
| `apps/api/src/modules/assignment/stage1/stage1-query-composer.ts` | Pure function `buildStage1WhereConditions` + Path A/B + caseyear wildcard + allowlist 防禦 |
| `apps/api/src/modules/assignment/stage1/__tests__/stage1-query-composer.spec.ts` | UCQ-001~030 unit spec（30 case） |
| `apps/api/src/modules/assignment/services/__tests__/assignment-run-pipeline-stage1-dynamic.spec.ts` | ITP-001~007 integration spec（7 case） |

### 修改（3 個）

| 路徑 | 主要改動 |
|---|---|
| `apps/api/src/modules/assignment/services/assignment-run-pipeline.service.ts` | (a) import buildStage1WhereConditions；(b) Stage 1 段改為 composer + queryBuilder；(c) 新增 stage1SkippedLists 收集；(d) warning_summary 新增 `EMPTY_CONDITIONS_SKIPPED` 碼；(e) skipped_cases JSON 新增 `lists` key；(f) 抽出 `runStage1ForList` private method |
| `apps/api/src/modules/assignment/services/__tests__/assignment-run-pipeline.service.spec.ts` | seedList + seedPool 補 condition_payload / prod_kind 預設值 |
| `apps/api/src/modules/assignment/services/__tests__/assignment-run-pipeline-v2.service.spec.ts` | 同上 |

### 刪除（無）

5b 不刪任何檔。

## 架構決策落地

| AD-E07-18 章節 | 落地位置 |
|---|---|
| §18.5 路徑 A（categorical / numeric / date fragment patterns） | `stage1-query-composer.ts` buildPathA + buildCategorical/Numeric/DateFragment |
| §18.5 路徑 B（5 個 entity column mapping → ob_pool_data 欄位） | `stage1-query-composer.ts` buildPathB + PATH_B_MAPPING 表 |
| §18.5 columnName allowlist 防禦（`/^[a-z][a-z0-9_]{0,63}$/`） | `stage1-query-composer.ts` SAFE_COLUMN_NAME_RE |
| §18.5.1 caseyear '99' wildcard | Path A categorical 分支 + Path B numericInt 分支共用 `CASEYEAR_WILDCARD` 短路 |
| §18.5.2 空 conditions 名單 skip | composer 回 `skipReason='EMPTY_CONDITIONS'` + runPipeline `runStage1ForList` + `stage1SkippedLists` 收集 + warning_summary 第三類碼 |
| §18.6 衍生規則 BR-10 / J6（Path B 對應） | PATH_B_MAPPING + caseyear 整數 parseInt |
| §18.10 R3（OQ-TEST-002 拍板後處置） | Logger.warn 訊息格式 + skipped_cases.lists 標記 |
| §18.10 R6（caseyear 99 wildcard 衍生風險） | composer skip 整 fragment + 留 fn_calc_tier_level 評估屬下游（spec 已標 OPEN） |

## 拍板採用之計劃 4 項

| # | 拍板 | 落地處 |
|---|---|---|
| GAP-5B-02 | 自行加 IT-M01-022 / 023 編號，後續 test-designer 追補 | composer UCQ-025~029 已標註對應；implementation log 對照表已寫 |
| GAP-5B-03 | `list_period × month_cnt × list_interval mod` 5b 不做 | composer 故意未實作；架構決策 §18.5 Mermaid 雖有提，但無 test case 對應；留 5c |
| R-5B-08 | total_lists 維持 `validLists.length` 不扣 skip | `runPipeline` `completeRun` 第 4 參數仍傳 `validLists.length`；ITP-007 已驗 |
| R-5B-09 | Composer 為 pure function | `stage1-query-composer.ts` 純 export function；無 NestJS DI；assignment.module.ts 未動 |

## Open Issues（後續 phase）

### O-5B-001：M01 spec IT-M01-022 / IT-M01-023 編號待 test-designer 追補

| 項 | 內容 |
|---|---|
| 觀察 | 計劃 GAP-5B-02 拍板：5b 自行加 IT-M01-022 / 023 為 SQL Injection 防禦測試；M01-whitelist-driven-integration-test.md 尚未列入此編號 |
| 5b 處理 | composer unit spec UCQ-025~029 已驗證 + 註明對應編號 |
| 建議 | 5c 派 test-designer 將 IT-M01-022 / 023 正式追補進 `docs/test-specs/integration/M01-whitelist-driven-integration-test.md` 之 §五 / §附錄 GAP 對照表 |

### O-5B-002：list_period × month_cnt × list_interval mod 過濾 5b 不啟用

| 項 | 內容 |
|---|---|
| 觀察 | AD-E07-18 §18.5 Mermaid 與正文「list_period_* 比對」段提到 month_cnt BETWEEN + interval mod 比對；但 M01 test spec 未列任何 test case |
| 5b 處理 | composer 故意不實作；list_period_start/end/interval 仍為 ob_list_definition entity 一級欄位、不入 condition_payload、Stage 1 不啟用此過濾 |
| 風險 | Stage 1 撈案件範圍可能廣於業務期望（不依 month_cnt 過濾），但有 condition_payload 其他欄位過濾仍可控 |
| 建議 | 5c 派 test-designer 補 IT-M01-024 ~ IT-M01-026（list_period 過濾、interval mod 過濾），spec-writer 補 F050 §5.1 規則明文，TDD 補實作 |

### O-5B-003：result summary report API 顯示需 5c 補

| 項 | 內容 |
|---|---|
| 觀察 | IT-M01-017 spec 寫「GET assignment_run report API」應顯示 skipped 名單；本 5b 只實作 DB 寫入 `run.skipped_cases.lists` |
| 5b 處理 | ITP-006 改驗 DB 直查；report API 顯示行為留 5c |
| 建議 | 5c 派 TDD 更新 `AssignmentRunReportService` 讀取 `skipped_cases.lists` 並 expose API |

### O-5B-004：fn_calc_tier_level 與 caseyear=99 wildcard 衍生風險

| 項 | 內容 |
|---|---|
| 觀察 | AD-E07-18 §18.10 R6 標 OPEN：Stage 1 wildcard 跑全年數案件，但 Stage 2 `fn_calc_tier_level` 若有對 year_cnt 計分維度，可能與業務預期不符 |
| 5b 處理 | composer 正確實作 wildcard skip；Stage 2 邏輯未改 |
| 建議 | 5c 派 system-architect 查閱 `fn_calc_tier_level.sql` 確認是否有 year_cnt 計分維度，再決定是否需特殊處理；屬下游風險不阻塞 5b 收尾 |

## Baseline 預先存在失敗（與 5b 無關，不動）

跑全 apps/api 測試套件共 **16 個 failure**（與 5a impl log 預先標註完全一致）：

| Spec 檔 | 失敗 case | 與 5b 關係 |
|---|---|---|
| `engine-target-load.spec.ts` | TS-F044-018/019 | ⛔ 紅線指定不動 |
| `target-table-schemas.spec.ts` | A~H category coverage 8 個 | ❌ 與 5b 無關（customer_core schema） |
| `target-table.service.spec.ts` | 2 個 | ❌ 與 5b 無關（columnCount） |
| `mssql-executor.spec.ts` | OFFSET/FETCH NEXT + keyset 2 個 | ❌ 與 5b 無關（v1.4.7 改動後遺留） |
| `mysql-executor.spec.ts` | LIMIT + keyset 2 個 | ❌ 與 5b 無關 |
| `postgresql-executor.spec.ts` | LIMIT $N readBatch 1 個 | ❌ 與 5b 無關 |

確認方式：5b commit 範圍 `git diff cb45d3e..HEAD --stat apps/api/src` 僅觸及
`modules/assignment/` 目錄（含 `stage1/` 新目錄），完全未觸及 `etl/` 與
`extraction-task/` 目錄。

## 5b 範圍內測試統計

- **新增 spec：2 個檔 / 37 case**（composer 30 + dynamic integration 7）
- **migrate spec：2 個檔**（v1 + v2 pipeline 共 21 case fixture 補 condition_payload）
- **assignment 三模組合計**：59 spec / 686 case 全綠
- **全 apps/api 套件**：134 spec / 1550 passed / 16 failed（baseline）/ 1 skipped / 15 todo
- **5b 新引入 regression：0**

## Downstream agents 引用

- **5c（M3-5 spec_tp / case_status backfill + F068 廢除）**：可直接讀
  `stage1-query-composer.ts` 的 PATH_B_MAPPING 表確認 Path B fallback 5 個
  entity column 仍生效；不需動 composer
- **5c（list_period × month_cnt × list_interval mod 補實作）**：應在 composer
  Path A 補 fragment（建議在 `buildPathA` 主迴圈外加總體 fragment，因 list_period_*
  非 condition_payload 內欄位而是 entity 一級欄位）
- **5c（report API 顯示 skipped）**：讀 `run.skipped_cases.lists` JSON 即可
- **5d（Frontend）**：無 5b 直接相依；Stage 1 屬月名單分派後端行為，前端不直接呼叫
- **Code review**：所有變動 +/- 統計、UCQ-ID / ITP-ID / IT-M01-ID 對照表、5a/5b 範圍切分均見本文件
