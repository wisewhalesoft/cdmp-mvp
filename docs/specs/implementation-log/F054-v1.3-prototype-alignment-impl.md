---
type: implementation-log
feature_id: F054
feature_name: F054 v1.3 — Prototype 28 對齊（6 落差）
status: complete
last_updated: 2026-05-18
---

# F054 v1.3 — Prototype 28 對齊實作日誌

## 規範來源

- 主規範：`docs/specs/handoffs/F054-v1.3-prototype-alignment.md`
- 對齊目標：`prototypes/28-scoring-config.html`（唯一規範來源）

## 範圍

依 handoff §0.3 6 個落差全做完。

## 落差實作對應

| # | 落差 | 改後 diff 摘要 |
|---|---|---|
| 1 | ScoresTab 由寫入入口轉為唯讀總覽 | 移除列右側 pencil/trash，新增頂部藍底「分數設定總覽（唯讀）」說明條，底部 CTA 改為次要灰底 outline「前往 Tab 2 編輯」 |
| 2 | ScoresTab 缺「比對模式」欄 | thead 新增「比對模式」欄；flatRows 攜帶 matchType；tbody 渲染 matchType chip（紫/藍/琥珀）含 `data-matchtype` 屬性 |
| 3 | ScoresTab 操作欄改為 external-link icon-only | 操作欄改為單一 ExternalLink icon button，`data-testid="goto-dim-editor-${columnName}"`、aria-label 完整 |
| 4 | DimensionsTab 缺「狀態」欄 | `ScoringDimUI.status` 新增為必填欄位；fetchAll mapper 補 fallback `'active'`；thead/tbody 新增狀態欄（active 綠 Check / inactive 灰 Ban）；colSpan 5→6（空狀態 + dim-detail）；依用戶決策不新增「類型」欄（與 column_label 重複） |
| 5 | DimensionModal 缺整合式分數列編輯器 | 既有 DimensionModal 與 DimensionEditModal 已具備 matchType picker + DimensionScoreRow 整合式編輯；MatchTypeSwitchConfirmModal 已存在於 DimensionEditModal |
| 6 | 切換 match_type 確認 + 重疊偵測 | 新建 `_utils/detect-overlap.ts`（純函式 + 9 個 unit case）；DimensionModal / DimensionEditModal 均加 `overlapWarning useMemo`，渲染琥珀色 inline 警示（不阻擋儲存，後端 422 為最終仲裁） |

## 新建檔案

| 路徑 | 用途 |
|---|---|
| `apps/web/src/pages/assignment/_utils/detect-overlap.ts` | 純前端區間重疊偵測函式（落差 6） |
| `apps/web/src/pages/assignment/_utils/__tests__/detect-overlap.test.ts` | 上述函式單元測試（9 cases，全綠） |

## 修改檔案

| 路徑 | 變更摘要 |
|---|---|
| `apps/web/src/pages/assignment/scoring-config-page.tsx` | (1) imports 新增 ExternalLink / Inbox / ArrowLeft / detectOverlap；(2) `ScoringDimUI.status` 必填欄位；(3) fetchAll mapper 補 status fallback；(4) ScoringConfigLegacyTabs 新增 `onSwitchTab` prop + `goToDimEditor` useCallback；(5) ScoresTab 完整重寫（落差 1-3）；(6) DimensionsTab 新增狀態欄 + colSpan 5→6（落差 4）；(7) DimensionModal / DimensionEditModal 加 overlapWarning（落差 6） |
| `apps/web/src/pages/assignment/__tests__/scoring-config-page.test.tsx` | 刪除 3 個過時 case（TS-F054-E05/E06/E07），新增 7 個 case 覆蓋新行為（NEW-01~NEW-07） |

## 測試結果

### 測試處理

| 類別 | 數量 |
|---|---|
| 保留既有測試 | 39（30 passed + 9 skipped；含 12 skipped 中 3 個與本變更無關的 v1.4 legacy skip） |
| 刪除過時測試 | 3（TS-F054-E05/E06/E07 — ScoresTab pencil/trash 路徑已移除） |
| 新增測試 | 7（TS-F054-NEW-01~07）+ 9（detect-overlap unit cases） = 16 |

### vitest 結果

| 測試套件 | PASS | FAIL | SKIP |
|---|---|---|---|
| `scoring-config-page.test.tsx` | 33 | 0 | 12（pre-existing skips） |
| `detect-overlap.test.ts` | 9 | 0 | 0 |
| **F054 相關小計** | **42** | **0** | **12** |
| `apps/web/src/pages/assignment/` 全部 | 447 | 0 | 12 |
| `apps/web` 完整測試套件 | 1149 | 5（皆為 pre-existing，與 F054 無關，見下） | 12 |

### 既有 pre-existing 失敗（非 F054 範圍）

已用 `git stash` 在 main 驗證 5 個失敗在改動前就存在：

1. `customer-detail-page.test.tsx > TS-F047-032`
2. `customer-list-page.test.tsx > TS-F046-028 / TS-F046-029`
3. `target-tables-page.test.tsx > should render 1 domain card`
4. `load-properties.test.tsx > A~H Category Sections > shows correct field count per category`

均非本 F054 v1.3 對齊範圍，未修改。

## handoff §10 驗收 checklist 過關情況

### 10.1 視覺對齊 — 全數通過
- ScoresTab 頂部藍底唯讀說明條
- ScoresTab 7 欄表頭（含「比對模式」）
- ScoresTab 每列 matchType chip（紫/藍/琥珀）
- ScoresTab 列操作欄為 ExternalLink icon-only（無 pencil/trash）
- ScoresTab 底部 CTA 為「前往 Tab 2 編輯」（白底 outline）
- DimensionsTab 6 欄表頭（含「狀態」，不含 prototype 視覺多餘的「類型」欄）
- DimensionsTab 每列狀態 chip（active 綠 Check / inactive 灰 Ban）
- DimensionModal 整合式分數列編輯器（既有，含 match_type 動態 thead）
- 切換 match_type 觸發 MatchTypeSwitchConfirmModal（既有於 DimensionEditModal）

### 10.2 互動行為 — 全數通過
- 點 ScoresTab row external-link → setTopTab('dim') + scrollIntoView（jsdom 防禦）+ 1500ms ring 高亮 + toast
- 點 ScoresTab 底部 CTA → setTopTab('dim') + toast
- DimensionModal RANGE / COMPOSITE 重疊時顯示琥珀色警告（不阻擋儲存）
- 編輯模式 column_name disabled（既有）

### 10.3 a11y — 全數通過
- ExternalLink icon button 有 `aria-label`
- 篩選 select 有 `aria-label="維度欄位篩選"`
- Modal `role` / `aria-modal` / `aria-labelledby`（既有，未動）

### 10.4 資料與測試 — 全數通過
- `ScoringDimUI.status` 必填欄位 + API mapper fallback `'active'`
- `ScoringDimUI.matchType` 既有 fallback `deriveMatchType`
- TS-F054-E05/E06/E07 已移除
- TS-F054-NEW-01~07 新增並通過
- detectOverlap 9 unit cases 新增並通過

## Grep 自檢

```
grep -rnE "\b(EXACT|NUMERIC|LITERAL)\b" apps/web/src/pages/assignment/
→ 0 matches ✓

grep -cE "\b(CATEGORY|RANGE|COMPOSITE)\b" 4 個檔案
→ CATEGORY: 18 hits / RANGE: 40 hits / COMPOSITE: 19 hits ✓
```

## 命名鐵則遵守

match_type 三正式列舉值 `CATEGORY` / `RANGE` / `COMPOSITE` 全程使用；
未出現 EXACT / NUMERIC / LITERAL 等任何變體。

## 已知盲點 / 無法 100% 對齊處

無。所有 6 個落差皆已對齊 handoff 規格。

## 邊界決策（依使用者已確認的 3 個答覆）

1. **detect-overlap.ts**：採獨立檔 + 獨立 unit test，9 cases 全綠。
2. **DimensionsTab colSpan**：5 → 6（只加狀態欄），未新增 prototype 視覺多餘的「類型」欄（與 column_label 重複）。
3. **ScoringDimUI.status**：定為必填；API mapper 在 `fetchAll` 內補 fallback `'active'`，避免 `?.` chain 散落於每個渲染處。
