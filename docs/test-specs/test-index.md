---
type: test-design-index
version: "2.61"
status: draft
last_updated: 2026-08-18
covers: [F001, F002, F002SM, F003, F004, F005, F006, F007, F008, F009, F010, F011, F012, F013, F014, F015, F016, F017, F018, F019, F020, F021, F022, F023, F024, F025, F026, F027, F028, F029, F030, F031, F032, F033, F034, F035, F036, F037, F038, F039, F040, F041, F042, F043, F044, F045, F046, F047, F048, F049, F050, F051, F052, F053, F054, F055, F056, F061, F064, F068, F073, F074, F075, F076, F077, F081, F085, F089, F090, F091, F092, F094, F095, F096, F097, F098, F099, F100, F101, F102, F103, F104, F108, F109, F110, F111, F112, F113, F117, F118, F119]
---

# CDMP MVP — 測試設計索引

> **專案**：CDMP（Customer Data Management Platform）v1.0 MVP
> **測試文件總數**：110 份（4 策略文件 + 72 Feature 測試文件 + F039 策略文件 1 份 + F040/F041 測試文件 2 份 + 整合測試 2 份 + Migration 測試 1 份 + Regression Guard 1 份 + F109 test 1 份 + **Infrastructure test 23 份** + **F117 test 1 份** + **F118 test 1 份** + **F119 test 1 份**）
> **總測試場景數**：3861 個（前 3746 + **F119 類別型篩選欄位新增文字比對運算子（包含 / 不包含 / 完全等於）（US-183，AD-E07-50）+115**：`features/F119-test.md` 後端 stage1 SQL 語意層 49（`f119-categorical-operator-fragment.spec.ts` 28：純函式契約 + ★核心 BR-6 NULL 八格矩陣真實 SQL 執行 + AC-9 未跳脫必紅案例；`f119-customer-core-categorical-operator.spec.ts` 8 + `f119-customer-financial-categorical-operator.spec.ts` 6：customer_core/financial 文字運算子 + I-CATOP-NULL-MATRIX-01 靜態掃描）+ 後端 assignment-list 黑箱/驗證層 30（`f119-preview-hit-count-text-operators.spec.ts` 7：`previewHitCount` 黑箱 contains/not_contains/equals/escape/零可選值/caseyear；`f119-condition-validation.spec.ts` 13：`createList` 對 AC-1/AC-6/AC-8/BR-2/BR-3/caseyear 驗證黑箱；`f119-signature-backcompat-duplicate.spec.ts` 10：★核心 T-11/T-12 向後相容簽章 + AC-16 重複判定 T-13~T-16）+ 前端 `_utils` 純函式 13（`f119-labels.test.ts` 6 + `f119-condition-summary.test.ts` 7，後者採 AC-15 例句而非 AD §3.6 樣板，見 F119-test.md C-Q1 說明）+ 前端 Page 整合 23（`list-create-draft-page.test.tsx` 追加 12：AC-1/AC-5（含 C-6 文字↔文字不清除）/AC-8/AC-11/AC-12/caseyear/AC-17；`list-edit-draft-page.test.tsx` 追加 4：AC-18（含 2026-08-18 dispute 裁決後強化之確認 modal 案例）；`list-kanban-page.test.tsx` 追加 3：AC-15/AC-17 Kanban chip + Detail Drawer；`stage0-estimate-page.test.tsx` 追加 4：AC-13/BR-13），合計 3861（含 7 個真實 MSSQL 案例，見下）。**本輪依 team lead 指示排除** Playwright e2e fidelity / Stryker mutation / dependency-cruiser metric gate，僅產出 vitest/jest 單元 + 整合測試，故本節新增場景數僅計入上述 115 個（後端新增 f119-categorical-collation.mssql.spec.ts 7 個，真實 MSSQL 執行，BR-8 大小寫/全半形敏感度），未如 F117/F118 含 E2E Fidelity 場景）；**2026-08-18 R-F119-04 缺口補強 +12**（AC-14 五路徑一致性，team lead 指派：`f119-ac14-cross-path-parity.mssql.spec.ts` 4 個，真實 MSSQL 執行，路徑1 MSSQL 下推 vs 路徑3 JS filter chain 對 167 萬列真實 `ob_pool_data` 逐 PK 相等；`f119-ac14-stage0-delegation.spec.ts` 4 個，SQLite，spy 驗證路徑4 委派路徑3 時 operator/keyword 不失真；`f119-ac14-pg-semantic-equivalence.spec.ts` 4 個，路徑2 PG 下推之 SQL 字串/語意等價，非真實 PG 執行，已誠實標記驗證層級較低），本輪合計 3873 個
>
> ⚠️ **已知既有落差（非本輪引入，記錄供未來清理）**：本索引「測試文件總數/總測試場景數」敘事行（上兩行）與下方 Coverage Summary Table 之逐列加總，於 AD-E07-42-P3d 併入時起即存在未追平的落差（P3d 當時只更新了敘事行數字，未同步新增 P3d 於 Coverage Table 之列，table 端 Infrastructure 小計因而少計 1 檔/58 案例）。本次 P5b 更新已將 Coverage Table 之 Infrastructure 小計/總合計依「表格自身既有列加總」邏輯正確累加（17 files/1050 → 加 P5b 後之結果，見下表），但**未**回頭補上遺漏的 P3d 列，故本頁敘事行與 Coverage Table 實際列加總（見下方「總合計」列）之間，落差仍為 1 檔/58 案例（P3d 列缺失，本次 P5g 更新未一併修復，維持原狀留待未來一次性稽核）。建議未來一次性稽核：於 Coverage Table 補上 `AD-E07-42-P3d` 列（58 案例，參照 `infrastructure/AD-E07-42-P3d-test.md`）即可追平。
>
> **v2.54 F110 `code_decode` 節點（泛用單趟多欄位代碼解碼）測試設計新增（2026-07-09，US-173／AD-E05-7，spec-first /tdd 流程，spec-writer 已完成 F110 spec，本輪 test-designer）**：新增 `features/F110-test.md`（**28 個場景**，dialect-neutral 設定驗證＋語意契約）+ `infrastructure/AD-E07-41-P4-codedecode-test.md`（**85 個場景**，比照 AD-E07-41-P4a~e 慣例之 SQL 生成正確性＋真實 MSSQL/PG 驗證）。`code_decode` 為既有 `LookupExecutor`（F043 §4.8）之泛用化：對同一張字典表以任意數量、任意 filter 之多組「代碼→描述」mapping 一次完成解碼，取代 `customer_core` 現行 31 個各自「就地全表 UPDATE」之 `lookup` 節點（收斂為 9 個節點），效能由 45 分鐘以上降至約 3 分鐘以內（US-173 AC-3）。**兩檔分工**（F110-test.md「測試策略」章節詳述）：F110-test.md 僅涵蓋設定驗證（AC-10：`mappings`≥1／字典來源必填／`outputAlias` 跨 mapping 唯一 I-CODEDECODE-COLLISION-01／錯誤訊息表）與 Mock queryRunner 語意契約（LEFT JOIN／NULL／欄位順序決定性），**不**斷言 SQL 文字或執行真實比對；SQL 生成正確性與 US-173 AC-2 硬性安全網（逐格等價）之**實際證據**全數在姊妹文件。**核心設計洞察（test-designer 本輪查證）**：US-173 AC-2 安全網（I-CODEDECODE-EQ-01，舊 `lookup` 鏈 vs 新 `code_decode`）之兩側**皆在同一顆 MSSQL 引擎上執行**，不像既有 P4d EQ-PG 群組需要 PG 才能比對，故此群組**不受** P4a `RESOLVE-002`（5433 不可達）先例影響、不得設計為 degradable，列為 MUST-FIX；AC-9/BR-11（PG/MSSQL byte-identical）是**另一個獨立需求**（新機制跨兩引擎比較，非新舊機制比較），比照 P4d §0.5 既有政策設計為 degradable。姊妹文件分層：**GATE 5**（fixture 沿用 P4b 9 張 lookup 來源表決策 + 5 張字典表 `_cdmp_id` 存在性逐一核對 + EQ 代表性群組選擇 + migration 事實來源確認 + Harness 沿用確認）+ **SQLGEN 18**（兩個 dialect 各自驗證 I-CODEDECODE-JOIN-FILTER-01／DEDUP-TIEBREAK-01／NORMALIZE-01／COLLISION-01 之 SQL 文字結構 + `SELECT INTO` 非就地 `ALTER`+`UPDATE` + MSSQL `OPTION (HASH JOIN)` / PG 無 hint 之負向確認）+ **DISPATCH 4**（第 10 對 handler 註冊，`lookup`/`code_decode` 並存不覆蓋）+ **🔴🔴EQ-MSSQL 20**（DoD 核心安全網：3 個代表性字典群組──單一等式 `raw_e5a2345c`／複合條件 MLMC／無 filter 郵遞區號──涵蓋 happy-path／查無對應 NULL／TRIM 空白邊界／大小寫 collation 契約／重複 key 取首筆定義一致性／9-mapping 旗艦綜合／防笛卡兒積列數不變／中文 round-trip／跨群組互不干擾／冪等重跑）+ **EQ-PG-BYTEIDENTICAL 7**（degradable，5433 可達才執行）+ **PERF-NFR 4**（live-verified 非 CI 斷言，US-173 AC-3 門檻定義與量測方法，不預寫具體秒數）+ **MIGRATION 14**（`etl-pipelines.json` 收斂後定義逐欄核對 F110 §7.2 對應 + `step_count`=34／`version`=14 + `up()`/`down()` 冪等與回滾 + I-CODEDECODE-MIGRATION-01 PG/MSSQL 共用模組 byte-identical + migration glob 範圍排除 + mojibake description 替換）+ **REG-LOOKUP 5**（既有 F043/P4b lookup 套件不回歸，AC-8）+ **STATIC 4 + REG 5**。全部 5 項不變式（I-CODEDECODE-JOIN-FILTER-01/DEDUP-TIEBREAK-01/NORMALIZE-01/COLLISION-01/EQ-01，另加 I-CODEDECODE-MIGRATION-01 共 6 項）逐一有對應測試群組。不需額外 spec-writer 產出（F110 spec 已完成，本輪僅 test-designer）。總場景數 3270→**3383**；總文件數 102→**104**。
>
> **v2.55 F111 分派總覽儀表板測試設計新增（2026-07-12，US-177／AD-E07-46，spec-first /tdd 流程，spec-writer + system-architect 已完成，本輪 test-designer）**：新增 `features/F111-test.md`（**104 個場景**：後端 62 + 前端 42）。對應 US-177（17 AC / 14 TC）+ F111 spec（單一薄型唯讀聚合端點 `GET /assignment/overview?ym=`，4 區塊判別聯集 DTO）+ AD-E07-46（新建 `AssignmentOverviewModule`，`AssignmentOverviewService` 零 Repository 注入、僅組合既有 5 個服務）。**測試邊界**（AD §10 明文）：聚合層零 SQL，`assignment-overview.{service,controller,util}.spec.ts` 全數可 mock 進行、不需真實 DB 連線，CI 常駐。**核心紅線**（對應 7 個 I-OVW-* 不變式）：(1) 區塊獨立失敗（ISO 組 8 案）—任一或多區塊拋例外，HTTP 恆 200，僅該區塊 `{error:true,errorCode}`；(2) Guard 例外不受區塊包裝影響（CTRL-010，AD §9 特別點名之易誤植陷阱）—`role='user'` 必須是真正 403，不可誤實作為 200+四區塊皆 error；(3) empty≠zero≠error 三態（EMPTY 組 9 案，含 EMPTY-006/009 兩處陷阱守門）—`hasActiveLists=false⇒total=null`（非 0）、`hasCompletedRun=false` 為合法空值（`error=false`）不可與拋例外之 `error=true` 混淆；(4) `computeDeptEstimate` 去重（DEDUP 組 4 案，含 `selectedYm===targetWorkYm` 對稱情境）；(5) scope 透傳不重新實作（SCOPE/UNSCOPED 組 7 案）；(6) AD §11 殘留議題邊界（RISK 組 2 案，處長+區塊三失敗時 `scope.deptCode` fallback null）。前端 42 案含角色渲染差異（部長/處長/admin/user）、跨區塊三態獨立顯示、各區塊互動細節（KPI 卡導向、空狀態文案差異化、超門檻紅色警示）、唯讀守門（無寫入 API 呼叫）、Sidebar 排序迴歸（分派總覽 #1／篩選欄位 #2）。總場景數 3383→**3487**；總文件數 104→**105**。
>
> **v2.57 F113 員工編號作為登入識別碼測試設計新增（2026-07-13，US-179／AD-E02-5，spec-first /tdd 流程，spec-writer + system-architect 已完成，本輪 test-designer）**：新增 `features/F113-test.md`（**105 個場景**：後端 85 + 前端 20）。對應 US-179（16 AC / 20 TC）+ F113 spec（`users` 新增選填 nullable `employee_no`，登入頁 Email/員工編號擇一識別）+ AD-E02-5（entity plain column 不宣告 unique + 新 MSSQL migration `1751884800004` filtered unique index 兩軌唯一性、登入分支不 trim、大小寫敏感比對）。**測試邊界**：與 F109/F112 之關鍵差異——絕大多數案例（78/85 後端）全數以 SQLite/mock 驗證，僅 SCHEMA 群組之 live 結構存在性斷言與 P1B2 companion 群組需要 `.mssql.spec.ts`；兩軌唯一性之真實併發防護本身不在本輪範圍（僅驗證 filtered index 結構存在），已記入殘留風險。**核心紅線**：(1) 登入分支絕不 trim（I-EMPNO-LOGIN-EXACT-NO-TRIM-01）——AD 明文修正任務指示原建議之 `trim()`，前後含空白之員工編號登入必須比對不到；(2) 登入大小寫敏感 vs 清單搜尋大小寫不敏感刻意不同，兩者不可混用；(3) **⚠️⚠️ test-designer 本輪逐行查證發現既有 `auth.service.spec.ts` 之 `TS-F001-007` SQL Injection 測試（`sqlInjectionEmail` 不含 `'@'`）將因本次變更被誤路由至 employee_no 分支，現行 `findOne` 呼叫參數斷言必然失敗，非任務指示籠統提及之「有既有測試會壞」，已精確定位行號並給出修法**（REGX-001）；(4) **⚠️⚠️ `mssql-p1b2.mssql.spec.ts` 五項既有測試（`BASELINE-003`/`STATIC-004`/`FILTER-001`/`PARITY-004`/`PARITY-005`）為 CI 阻斷風險**，本文件已查證現行 `EXCLUDED_TABLES`（`mssql-p1b2.mssql.spec.ts:231`）為整表排除機制，明訂修法須改為「索引層級白名單」（`AND NOT (t.name='users' AND i.name='uq_users_employee_no')`），並設計 P1B2-006 regression guard 防止 tdd-implementation 圖方便誤用整表排除、連帶損失 `users` 既有 PK/email unique index 之 parity 覆蓋。分層：**LOGIN 14**（含大小寫/trim/停用順序/不洩漏三項核心紅線）+ **LOGINDTO 4** + **FORGOT 3**（AC-13 之 AD 對 spec 字面修正：400 非 200）+ **DTOVAL 15**（格式三段優先序 + 正規化邊界）+ **CREATE 7** + **UPDATE 8**（含排除自身 + 跨帳號大小寫並存）+ **LIST 8**（含大小寫敏感/不敏感對照組）+ **SCHEMA 6**（靜態 4 + live 2）+ **GUARD 4** + **STATIC 4** + **REGX 5**（既有測試必要修改）+ **P1B2 7**（⚠️⚠️ CI 阻斷風險companion）+ **FE-LOGIN 6 + FE-CREATE 6 + FE-EDIT 5 + FE-LIST 3**。8 項不變式（I-EMPNO-*）逐一有對應測試群組。總場景數 3568→**3673**；總文件數 106→**107**。
>
> **v2.56 F112 類別型篩選欄位可選值自動建議測試設計新增（2026-07-12，US-178／AD-E07-47，spec-first /tdd 流程，spec-writer + system-architect 已完成，本輪 test-designer）**：新增 `features/F112-test.md`（**81 個場景**：後端 59 + 前端 22）。對應 US-178（18 AC / 14 TC）+ F112 spec（`GET :columnName/distinct-values` 查來源表 distinct 值 + `POST :columnName/options/bulk` 批次新增可選值，兩進入點：F075 新增欄位 Modal / F076 可選值管理頁）+ AD-E07-47（裁定精確 DISTINCT 不抽樣、獨立 app-level `Promise.race` 逾時不依賴已調至 1 小時的全域 driver timeout、`DISTINCT_VALUES_QUERY_TIMEOUT` 改判 500 非 spec 建議之 504、稽核彙總單筆寫入既有 `after_value`）。**測試邊界**：與 F109 之關鍵差異——本 Feature **全數可用 mocked** `dataSource.query`/`fieldRepo`/`optionRepo`/`manager.transaction` 驗證（沿用既有 `getAvailableColumns` mock 慣例），**不需要**真實 MSSQL 連線，MSSQL 專屬 SQL 形狀以斷言傳入字串驗證。**核心紅線**（對應 8 個 I-DVAL-* 不變式）：(1) 就緒優先於存在性（I-DVAL-READY-BEFORE-EXIST-01，ORDER 組）—來源表不存在時必須回 503，不可被欄位存在性檢查搶先誤判為 404；(2) 逾時 HTTP 碼修正（TIMEOUT-002）—`DISTINCT_VALUES_QUERY_TIMEOUT` 必為 500，明確斷言非 spec 原文建議之 504；(3) RBAC 拒絕碼精確查證（GUARD 組）—test-designer 本輪逐行查證 `director.guard.ts`/`director-or-section-chief.guard.ts` 原始碼，處長拒絕碼為 `E07_REQUIRES_DIRECTOR`、無角色使用者為 `E07_ROLE_NOT_ASSIGNED`，兩者不同且皆非 spec/AD 敘述性文字籠統提及的 `AUTH_FORBIDDEN`；(4) 稽核彙總單筆（AUDIT-001）—N 筆新增僅寫入 `assignment_audit_log` 1 筆；(5) bulk 冪等略過三態（BULK-003/004/005）—既有 active／既有 inactive／批次內重複皆須略過且不得回 409。**額外查證發現**：`options-tab.tsx` 既有「新增可選值」按鈕採 `disabled={!canWrite}`（DOM 存在僅 disabled），但 spec §7.3 對新按鈕明文要求處長「不渲染」（DOM 不存在），兩者模式不同，已於 FE2-003 明確標註斷言方式（`queryByTestId===null`）以攔截 tdd-implementation 誤沿用既有模式之陷阱。總場景數 3487→**3568**；總文件數 105→**106**。
>
> **v2.52 MSSQL 全面遷移 P4 收尾 tech debt — raw-data.service 完整性測試設計（2026-07-09，P4/P5 期間刻意 scope-out 之收尾切片，跳過 spec-writer）**：新增 `infrastructure/AD-E07-41-P4-followup-rawdata-test.md`（**52 個場景**）。對應 memory `project_mssql_full_migration`「剩餘 Phase4」清單 + P4e impl log §七「範圍外家族清單」+ `risks-and-gaps.md` R-MSSQL-P4E-01/02：(1) `createRawTable()` 字串來源 PK 映射 `NVARCHAR(MAX)` 於 MSSQL 建表拋錯（CREATETABLE-FINDING）之修法驗證；(2) `getColumnMetadata`/`getIndexedColumns`/`getRawData` 分頁（`LIMIT ? OFFSET ?`）/`insertBatch` 非 full-mode 路徑，四者仍以二元 `this.isPostgres` 判定、`DB_TYPE=mssql` 時誤入 SQLite 分支之修法驗證。**★★ 本文件之獨特方法論（使用者本輪明確指示）：設計階段直接對本機 dev MSSQL 2022 容器 `cdmp-mssql`/`CDMP_TEST` 實測全部關鍵假設，取代理論推導後留待 tdd-implementation 驗證的慣例**，以拋棄式 `probe_*` 表逐一驗證 13 項事實（V1-V13，測後全數清理、`INFORMATION_SCHEMA.TABLES` 複查零殘留）。**核心紅線 1（🔴🔴 真庫實測推翻任務書隱含假設）**：任務書「PK 字串欄改有界 `NVARCHAR(≤450)`」隱含「450 是 DDL 成功門檻」之假設經 V2 實測推翻——**任意 `n≤4000`（非僅 450）之 `NVARCHAR(n)` 皆能讓 `CREATE TABLE` 成功**，唯一決定 DDL 成敗的是「是否為 `MAX`」型別（V1：`NVARCHAR(MAX)` 立即拋 `#1919`）；真正的 900-byte 限制（V3 實測：450 字元 OK / 451 字元拋 `#1946`）是在 **INSERT 時**依實際寫入值 byte 長度判定，與宣告欄位長度無關——即使宣告 `NVARCHAR(4000)`，只要實際客戶編號值 >450 字元仍會在 INSERT 階段失敗。**核心紅線 2（🔴 真庫實測發現任務書未列出之第三候選）**：V4 實測 `NONCLUSTERED PRIMARY KEY` 可將限制放寬至 1700 bytes（850 字元），揭露候選 (a)/(b) 之外的第三方案「來源 PK 宣告 `NONCLUSTERED` + `_cdmp_id IDENTITY` 作 `CLUSTERED` 排序鍵」，已納入 §一 `GATE-001` 與任務書兩候選並列供決策（三者皆已真庫驗證技術可行，純屬業務選型，test-designer 依角色分工不代為選擇）。**核心紅線 3（🔴🔴 複合 PK 900-byte 加總限制）**：V5 實測證實 900-byte 限制為複合鍵**全部欄位加總**而非逐欄獨立（225+225 字元合計 900 bytes OK；400+400 字元合計 1600 bytes 拋 `#1946`），任務書 `≤450` 公式僅對單一字串鍵欄位成立，已設計 §一 `PKFINDING-005` MUST-FIX 守門。**核心紅線 4（🔴🔴 MSSQL 參數上限確切邊界，非官方文件字面值）**：V7 實測確切邊界為 **2098 個參數成功、2099 個起拋 `#8003`**——官方錯誤訊息文字雖寫「maximum of 2100」，但本專案 `mssql`（tedious）套件堆疊之實際安全上限為 2098（driver 層 off-by-2），已據此設計 §五 `INSERTBATCH-GATE-002`（建議切片門檻取明顯低於 2098 之保守值，如 2000）。**核心紅線 5（🔴 `getRawData()` 呼叫鏈依賴順序）**：test-designer 查證 `getRawData()` 步驟 3 呼叫 `getColumnMetadata()` 先於步驟 7 分頁查詢，V13 已實測現行 mssql 落 `PRAGMA table_info` 分支對真實 MSSQL 拋 `#102`——即使任務書僅點名分頁語法症狀，`getColumnMetadata`/`getIndexedColumns`/分頁片段三者必須一起修，否則 `getRawData()` 於 mssql 下 100% 卡在第 3 步。**核心紅線 6（🔴🔴 postgres/sqlite 亦零既有覆蓋，本文件範圍擴大）**：逐一 grep 確認待修 4 方法（`getRawData`/`getColumnMetadata`/`getIndexedColumns`/`insertBatch`）於既有 `raw-data.service.spec.ts`（僅涵蓋 `formatCopyValue`/`supportsCopy`/`openCopyWriter`）與任何 e2e spec 皆零覆蓋，任何 driver（含 postgres/sqlite）皆無回歸保護網——§七 REG 因此重新定調為「新建基準」而非「確認既有不變」（REG-003/004）。**核心紅線 7（🔴 查證修正 P4e `DISPATCH-005` 文字精確度）**：`insertBatch` 除 P4e 原文字提及之 incremental 模式外，`extraction-execution.service.ts` §175-244 顯示 full-mode 但來源 executor 不支援 streaming 時同樣落入 `insertBatch()` 迴圈，已設計 §六 `E2EAPI-004` 補齊此真實觸發場景。**其餘 V6/V8-V13 真庫實測事實**（無靜默截斷、`OFFSET...FETCH` 無 `ORDER BY` 拋 `#153`、分頁邊界正確、`INFORMATION_SCHEMA.COLUMNS`/`sys.indexes` 查詢形狀確認、`information_schema.columns` 小寫物件名稱拋 `#208` 強化 `I-MSSQL-CATALOG-CASE-01` 適用範圍至物件名稱層級、現行陷阱程式碼確切錯誤代碼）詳見文件內「★★ 真庫已驗證事實」表。分層：**PKFINDING 10**（🔴🔴 三候選決策關卡 + V1-V6 真庫驗證 MUST-FIX 群組）+ **GETCOLMETA 5**（V10/V12/V13 驗證）+ **GETIDXCOLS 5**（V11/V13 驗證，含 Observability >100,000 列非阻擋）+ **GETRAWDATA-PAGE 9**（🔴🔴 V8/V9 驗證，`_cdmp_id` 非保證存在之澄清）+ **INSERTBATCH 9**（🔴🔴 V6/V7/V13 驗證，援用既有 Pattern B `escapeQueryWithParameters` 慣例）+ **E2E-API 6**（公開入口全鏈路整合，含 DISPATCH-005 文字精確度補齊）+ **REG 5**（🔴🔴 postgres/sqlite 新建基準）+ **STATIC 3**（決策記錄）。不需 spec-writer，比照 P1~P5 系列先例；優先度定為 P2-TechDebt（非阻擋 cutover）。總場景數 3178→**3230**；總文件數 100→**101**。
>
> **v2.51 MSSQL 全面遷移 P5g ETL target_load ATOMIC 資料完整性修法（交易包裝）測試設計（2026-07-08，使用者裁定「切換前必做」，接續 P5b/P5c）**：新增 `infrastructure/AD-E07-43-P5g-test.md`（**53 個場景**）。對應 AD-E07-43 v1.1 §7（ATOMIC 資料完整性風險：現象/修法評估 a/b/c/架構師推薦 (a) 交易包裝/範圍歸屬裁定）+ 新增不變式 I-ETL-ATOMIC-LOAD-01。**本文件性質為「修法驗證」而非「探測」**：P5b §六 ATOMIC 群組（6 案例）已探測證實分支 A（資料遺失）成立、`target-load-handler(.ts/-mssql.ts)`/`pipeline-runner.ts` 於該輪列為凍結清單 report-not-fix；本文件是這些檔案的正式解凍，驗收依據為修法後之分支翻轉（資料保留、失敗正確回報）。**核心紅線 1（🔴🔴 test-designer 新查證：I-ETL-ATOMIC-LOAD-01 文字範圍缺口）**：不變式文字僅列 fullMode/partition_replace，但 `target-load-handler-mssql.ts` 之 customer_core UPSERT 路徑為兩段式獨立陳述式（`UPDATE...FROM` + `INSERT...WHERE NOT EXISTS`），同樣無交易保護，會產生「既有列已更新但新列缺失」之不一致中間態；PG UPSERT 為單句天生原子不受影響。已獨立立 §四 UPSERT-ATOMIC 群組並設計決策關卡（UPSERTATOMIC-003）建議 architect 後續擴大不變式文字。**核心紅線 2（🔴🔴 交易範圍黑盒操作化）**：任務指示要求「交易應包 target-load 的 clear+insert，非整個 pipeline」，test-designer 將此操作化為 TypeORM 既有公開屬性 `queryRunner.isTransactionActive`（已查證 `node_modules/typeorm/query-runner/QueryRunner.d.ts` 確有此 API）之黑盒斷言（§五 SCOPE-001：任一 target_load 節點執行完畢後必為 false），不需臆測交易確切起訖語句位置即可驗證範圍正確性；7.8M 列規模下是否需分批（SCOPE-005）明確設計為不預設答案之決策關卡，若分批則與不變式文字「INSERT 失敗必須完整回滾」存在潛在張力，交 architect 確認。**核心紅線 3（🔴🔴 test-designer 新查證：PG 交易中止污染連線風險）**：PostgreSQL 交易內任一陳述式失敗後連線進入 aborted 狀態、僅接受 ROLLBACK，若 handler 未先呼叫 `rollbackTransaction()` 即 re-throw，`pipeline-runner.ts` 外層 `outputStore.cleanupAll(queryRunner)` 會連帶失敗——已設計 §九 CLEANUPTXN-001 為黑盒 MUST-FIX 訊號（清理階段可觀察錯誤，非白盒 spy）；MSSQL 側對稱行為（無 `SET XACT_ABORT ON`）不可假設，列為 CLEANUPTXN-002 探測。**核心紅線 4（🔴 架構師 §7.2 聲明之真實環境驗證）**：架構師論證「標準 Read Committed 下讀者不會看到中間態」，test-designer 查證 PG 為 MVCC（非阻塞）、MSSQL 預設 Read Committed 無 RCSI（`docker/mssql-init.sql` 未見任何 SNAPSHOT 設定）為鎖based（可能阻塞等待）——兩者皆不會讀到空表，但使用者體感不同，已設計 §六 ISOLATION 群組（雙連線手動編排 harness，繞開 handler 直接驗證資料庫引擎本身之隔離語意）真實探測，不預設答案。**其餘查證發現**：影響面盤點確認 `target-load-handler(.ts/-mssql.ts)` 為 6 條 pipeline 共用（P5b 5 條 + P4d customer_core 1 條，§八 IMPACT-001）；P5b 既有 ATOMIC-001~006 案例將與修法後行為直接矛盾，已設計 IMPACT-003 為 MUST-FIX 防誤判回歸守門，要求 impl log 記錄同步更新方式。Harness 完全沿用既有基礎設施不新建：MSSQL 側沿用 P5b `CDMP_P5B`（`connectMssqlP5b`/`insertTargetRows`/`countTarget`/`FIX_*` 系列直接複用 P5b 壞值注入手法）、customer_core UPSERT 側沿用 P4d `CDMP_TEST`（前綴隔離+精準 DELETE）、PG 側沿用 P5b `P5B_PG_DB`+`pgPortReachable()` degradable 政策。分層：**GATE 4**（🔴🔴 交易管理位置決策關卡 + isolation level 決策 + Harness 沿用確認 + 暫存表建立時機決策）+ **🔴🔴TXNCORE 7**（DoD 核心：MSSQL 側 4 種壞值形狀修法後回歸沿用 P5b 壞值注入手法 + 可觀察性回歸 + service 層狀態回寫 + 🔴可恢復性）+ **PGTXN 6**（degradable：4 種壞值形狀 PG 對稱版 + 兩引擎對稱結論鎖定旗艦 + skip 自我驗證）+ **UPSERTATOMIC 3**（🔴🔴 本文件新查證發現：MSSQL 兩段式部分套用風險 MUST-FIX + PG 回歸 + 不變式範圍決策關卡）+ **🔴🔴SCOPE 6**（DoD 核心旗艦：`isTransactionActive` 黑盒不變式 + 上游節點不受影響 + 前置唯讀查詢不受影響 + 暫存表建立時機收斂 + 🔴7.8M 列分批決策關卡 + `##` 表相容性）+ **🔴🔴ISOLATION 4**（架構師聲明真實驗證：PG MVCC 確認 + MSSQL 阻塞/非阻塞不預設答案探測 + 業務體感記錄性決策關卡 + 規模無關性說明）+ **REGRESSION-SUCCESS 6**（DoD 核心：4 條 fullMode + partition_replace + customer_core UPSERT 乾淨路徑不破壞 + dedup 決定性 + 冪等 + 觀察性效能）+ **IMPACT 4**（🔴 pipeline 全集鎖定 + 異動點界定 + 🔴🔴P5b 既有案例矛盾防誤判 MUST-FIX + node-dispatcher 不受影響）+ **🔴🔴CLEANUPTXN 4**（本文件新查證：PG aborted transaction MUST-FIX + MSSQL 對照探測 + MSSQL finally 回歸 + PG 既有缺口記錄性）+ **STATIC 4**（交易呼叫靜態掃描 MUST-FIX + pipeline-runner 未改回歸 + 呼叫站點對稱 + 其餘 8 handler 未改）+ **REG 5**（tsc + P5b 既有套件同步更新非單純不回歸 + PG/sqlite 路徑不變 + 共用表隔離慣例 + 月名單分派套件不受影響）。**明確排除**：P5b 探測工作本身（已完成，本文件僅沿用其壞值注入手法）；P5c MONTHRUN-DIFF／P5d datetime2／P5e F067 簽核（不同子切片，互不阻擋）；7.8M 列規模真實效能數字（SCOPE-005 決策關卡留待 tdd-implementation 依真實量測另行記錄，不臆測）。總場景數 3125→**3178**；總文件數 99→**100**。
>
> **v2.50 MSSQL 全面遷移 P5c MONTHRUN-DIFF 真實完整月名單分派跨引擎逐列比對測試設計（2026-07-08，P5 全量 CI + 業務簽核第三片，接續 P5b）**：新增 `infrastructure/AD-E07-43-P5c-test.md`（**39 個場景**）。對應 AD-E07-43 §3.1（MONTHRUN-DIFF 執行方式，manual/script）+ §5 P5c DoD（`ob_monthly_run_result` 全部案件 9 個關鍵欄位逐列比對）。**核心紅線 1（🔴🔴 環境約束驅動之三層方法論裁定）**：`postgres-test`（5433）本機不可達、`dev PG`（5432）依 P4a 既有先例視為唯讀不可注入測試，故任務指示之 PG 端來源三選（(a) PG 直接同資料重跑／(b) JS oracle 黃金基準／(c) 既有 PG 快照）裁定以 **(b) JS oracle 為 Tier 1（MUST-FIX，唯一始終可行路徑）**，(a)/(c) 列為 **Tier 2/3（degradable，`pgPortReachable()` 探測 5433 可達才啟用，不偽綠）**。**核心紅線 2（🔴🔴 test-designer 逐碼查證確認 Tier 1 可直接複用既有 golden 程式碼，非重新實作）**：`executeV2`（`assignment-run-pipeline.service.ts:730` 起）之 code comment 自陳「此 JS 路徑為 golden oracle，與 PG SQL 下推逐列確定性等價（AC-15 DoD）」，已完整組合 Stage 2 計分＋F102 `applyCrPriority`＋F101 `distributeStage3to4`；`executeStage2to3PushdownMssql`（lines 1128-1218）現行確認已完整四步下推（計分→清除→CR前置→比例分派），對稱 PG 版 `executeStage2to4Pushdown`，AD §0「MSSQL 月名單分派全鏈現已全部有值」經直接讀碼確認屬實。Tier 1 機制：兩側皆連同一 MSSQL DataSource（P5b 已產出之真實資料），MSSQL 側走真實 `runPipeline()` 頂層入口，JS oracle 側以 `Object.create(prototype)`+bracket-access（沿用 F104 pg spec 既有手法）直接呼叫私有方法，寫入同一表之不同 `run_id`，逐列 diff。**核心紅線 3（🔴🔴 已知限制之設計因應，非新發現但本文件首次需在全鏈組合層級處理）**：`executeStage1Chain` 內部 customer_core 片段為 PG-only SQL（P3a 已查證），對 MSSQL 連線執行含 customer_core 條件之名單會拋錯；已設計 §0.4「Stage 1 單次執行、雙寫兩個 run_id」策略繞開，聚焦本文件於 Stage 2-4/CR 組合驗證、不誤將已由 P3a 窮盡驗證之 Stage 1 customer_core 正確性重複拉入判定範圍。**核心紅線 4（🔴 AD 內部欄位計數落差）**：AD §3.1 列 10 欄（含 `cr_nm`）、§5 DoD 文字稱「9 個關鍵欄位」但漏列 `cr_nm`，裁定採聯集 10 欄，記錄非阻擋。**核心紅線 5（🔴🔴 方法論本質差異提醒，避免與 F067 混淆）**：F067（CDMP vs legacy）案件集完全不同、僅能比分佈；本文件兩側讀同一份來源資料、案件集應完全相同，核心判定為**逐列 0 差異**（非分佈相近），§五 DIST 分佈比對僅為業務可讀輔助摘要非核心判定。**核心紅線 6（🔴🔴 決策關卡，交 architect/業務裁定非本文件自行裁定）**：I-MSSQL-SIGNOFF-GATE-01 條文字面要求「PG/MSSQL 結果一致」，Tier 1 本質是「JS oracle vs MSSQL」，是否已足以滿足字面要求列為 §一 GATE-002 決策關卡，要求 §六 REPORT-004 於報告最顯著位置聲明實際涵蓋 Tier 範圍，供 P5e 業務簽核判斷佐證力道。**其餘查證發現**：本文件正式取代並擴大 P3b §二十一 `MONTHRUN-DIFF-001/002` stub（原僅 score/card_level/tier_level 三欄、無方法論細節）；§四 DATECAST-BOUNDARY（3 案例）承 P3d `DATECAST-003` 於完整鏈路層級延伸揭露 `appl_date` 非午夜時間分量邊界，不預設答案、交 P5d 業務裁示；建議執行方式為 TypeScript 獨立腳本（非常駐 vitest），比照 F101/F102/F104 既有真實月重跑慣例。分層：**GATE 5**（🔴🔴 Tier 1 佐證力道決策關卡 + 欄位計數裁定 + customer_core 繞開機制確認 + Harness 資料庫策略決策）+ **HARNESS 5**（fixture 矩陣 + Stage 1 雙寫機制 + 資料公平性前提守門）+ **🔴🔴CHAIN-EQ 10**（DoD 核心旗艦：全量逐列 EQ + 分名單 + CR 專項×2 + ration=0 + BR-F102-08 + 🔴組合缺口專項 CHAINEQ-007 + customer_core 名單 + 冪等 + 🔴🔴差異根因分析框架決策關卡）+ **DATECAST-BOUNDARY 3**（Probe，不預設答案）+ **DIST 4**（輔助性分佈摘要，比照 F067 格式）+ **🔴REPORT 5**（DoD 核心：報告結構 + 案件級記錄 + 🔴🔴Tier 範圍聲明決策關卡）+ **PG-ENHANCE 3**（degradable Tier 2/3）+ **STATIC 2 + REG 2**。**明確排除**：P5b 5 條 pipeline 端對端驗證本身（已完成）、P5d datetime2 業務裁示（僅設計揭露案例）、P5e 簽核行為本身。總場景數 3086→**3125**；總文件數 98→**99**。
>
> **v2.48 MSSQL 全面遷移 P3d CR 優先分派 raw SQL 引擎移植測試設計（2026-07-08，P3 第四片，接續 P3c）**：新增 `infrastructure/AD-E07-42-P3d-test.md`（**58 個場景**）。對應 AD-E07-42 §2.4（CR 優先分派逐站點方言轉換清單）+ §5 P3d DoD：`cr-priority-sql.ts` 之 mssql 平行版本，JS↔MSSQL 逐列六元組等價（I-MSSQL-ENGINE-EQ-01）。承接已完成之 P3a/P3b/P3c（案件已在 `ob_monthly_run_result` 內、已有 `tier_level`、Stage 3/4 raw SQL 下推已可正確執行）。**核心紅線 1（🔴🔴 DISPATCH 缺口正式閉環，P3c impl log 已預告，本文件將 P3c 之 DISPATCH-003 負向守門翻轉為正向 MUST-FIX）**：test-designer 查證 `executeStage2to3PushdownMssql:1123-1204` 現行僅三步，L1169-1170 明文「刻意不呼叫 PG-only 之 runCrPrioritySql（DISPATCH-003 負向守門）」；本文件 §二 DISPATCH-001 要求擴充為四步（`runStage2and3SqlMssql`→`clearStage3Fields`→`runCrPrioritySqlMssql`→`runStage3to4RationSqlMssql`），順序對稱 I-CR-ORDER-01。**核心紅線 2（🔴🔴 日期型別查證逆轉，不可類推 P3c 結論）**：P3c 查證 `assignday` 為 varchar 不需日期轉換；本輪查證 `ob_monthly_run_result.appl_date` 實際型別為 **datetime2**（PG 對應 `timestamp`，經 `dateColumnType` helper），與 P3c 結論方向相反——`cr-priority-sql.ts` 步驟 1 之 `appl_date < :twoYearsAgo::date` 為必要轉換站點（`CAST(:twoYearsAgo AS DATE)`），已獨立立 §七 DATECAST 群組防止下游誤判省略轉換。**核心紅線 3（🔴🔴 步驟 3 三重疊加旗艦，AD §2.4 明文標示「建議此站點安排最高覆蓋率測試」）**：CTE（`empl_set_ranked`/`first_dept`）+ 視窗函式（`ROW_NUMBER() OVER (PARTITION BY emplid ORDER BY deptid_m ASC)`）+ UPDATE...FROM 重構三者疊加於單一陳述式，已設計 §五 STEP3 群組（7 案例，含 `STEP3-005` 完整 worked example 旗艦 + `STEP3-003` I-DET-CR-01 MUST-FIX 已知具體值斷言）。**其餘查證推翻類推假設之發現**：`empl_set_ranked` CTE 直接對真實表 `SELECT`（非 PG VALUES 語法糖），**不需要**如 P3c 三處 CTE 之 derived table 包裝改寫（GATE-006）；本檔全程無 `ration` 之 DECIMAL CAST 算術（僅作 `WHERE ration>0` 過濾），**不適用** P3b/P3c 之裸 `NUMERIC(18,0)` 精度截斷風險（GATE-004）；CR 前置步驟本身（`runCrPrioritySql`/`applyCrPriority`）**不產生任何 warning/skipped_cases**（回傳 `void`/`CrAssignment[]`，非 `RationWarning[]`），已於 §十 CRWARN 群組修正任務指示原始假設，改設計「CR 前置動態產生之 `is_cr='Y'` 案件如何正確影響 Stage 3/4 既有 warning 基數」之交互回歸（橋接 P3c CRFILTER 群組，本輪首次由真實機制而非手動 seed 產生）。**Harness 首次直接以 raw SQL JOIN 觸及 `ob_emphire`/`ob_empl_set`**（P3a~c 皆由呼叫端 TypeORM 查詢轉陣列傳入，dialect-agnostic），兩表皆屬既有 dbo 共用表集合，不需自建，僅需 regression 確認存在性（GATE-001）。分層：**GATE 6**（4 表存在性 + 🔴🔴appl_date 型別查證 + resign_date/sysDate 低風險核對 + 🔴無 DECIMAL 風險決策 + crEnabled=false 直接複用決策關卡 + 🔴CTE 免包裝查證）+ **DISPATCH 5**（🔴🔴 缺口閉環 MUST-FIX 由負轉正 + I-CR-ORDER-01 順序 + 三分支互斥 spy + DoD 核心觀察 + PG-only 零殘留）+ **STEP1 6 + STEP2 6**（含 🔴BR-F102-08 MUST-FIX + 🔴UPDATE...FROM 轉換）+ **STEP3 7**（🔴🔴 三重疊加旗艦 + I-DET-CR-01 MUST-FIX）+ **GATECR 3**（cr_enabled=false 閘控 + 跨切片整合）+ **DATECAST 3**（🔴🔴 appl_date 逆轉發現 + DATE/DATETIME2 隱式轉換待驗證 + 非午夜時間分量未驗證假設）+ **UPDATEFROM 2**（🔴防跨 run 污染旗艦）+ **WINDOWFN 3**（承 STEP3，決定性 + collation 待驗證）+ **CRWARN 2**（🔴查證推翻任務假設 + 交互回歸橋接 P3c）+ **EQ 5**（DoD 核心，含 🔴🔴 端對端跨切片旗艦 EQ-004）+ **IDEM 2 + REG 5 + STATIC 3**。**明確排除**：3e `fn_calc_tier_level` 收尾，待後續子切片。總場景數 2950→**3008**；總文件數 96→**97**。
>
> **v2.47 MSSQL 全面遷移 P3c Stage 3/4 比例分派 raw SQL 引擎移植測試設計（2026-07-08，P3 第三片）**：新增 `infrastructure/AD-E07-42-P3c-test.md`（**56 個場景**）。對應 AD-E07-42 §2.3（Stage 3/4 比例分派逐站點方言轉換清單）+ §5 P3c DoD：`stage3to4-ration-sql.ts` 之 mssql 平行版本，JS↔MSSQL 逐列四元組等價（I-MSSQL-ENGINE-EQ-01）。承接已完成之 P3a（Stage 1 篩選）+ P3b（Stage 2~3 計分，視為已驗證黑盒依賴：案件已在 `ob_monthly_run_result` 內且已有 `tier_level`）。**核心紅線 1（🔴🔴 P3b impl log 已預告之已知缺口，本文件為其正式閉環）**：test-designer 直接查證 `assignment-run-pipeline.service.ts:1113-1158` 之 `executeStage2to3PushdownMssql`（P3b 落地產物）函式本體，確認其僅補 score/card_level/tier_level 三欄，**完全未呼叫** `clearStage3Fields`／`runCrPrioritySql`／`runStage3to4RationSql`，對照 PG 完整鏈路 `executeStage2to4Pushdown`（四步）僅完成第一步，mssql 月名單分派 dept_id/emplid/assignday 現行恆 NULL；已設計 §二 DISPATCH-001 為刻意對現行未修改程式碼設計之紅燈 MUST-FIX 守門，逼實作方擴充呼叫鏈為三步（**不含** `runCrPrioritySql`，P3d 範圍外，已設計 DISPATCH-003 靜態守門確認此為刻意排除而非遺漏）。**核心紅線 2（🔴🔴 test-designer 查證出之全新 DECIMAL 精度旗艦缺陷，AD §2.3 表格未點名此具體站點，同型於 P3b DECIMAL-LOANRATE-001 之 FINDING-P4D-01 家族）**：`stage3to4-ration-sql.ts` 內兩處 `CAST(:param AS numeric)`（dept ration／empl ration）皆為裸 `numeric`、無精度宣告；查證來源型別 `ob_dept_pct.ration numeric(9,2)`／`ob_empl_set.ration numeric(10,2)`（皆帶 2 位小數），T-SQL 未指定精度之裸 `NUMERIC` 預設為 `NUMERIC(18,0)`，逐字翻譯會在 `VALUES` CTE 建構階段即把如 `33.67` 四捨五入為 `34`，使部門/員工實際分得比例系統性偏移且不拋錯；已設計 §五 DECIMAL-RATION-001 為 MUST-FIX 旗艦守門（已知具體數值：3 課 33.67/33.67/32.66、cnt=300 → 正確 `{102,101,97}`，誤用裸 CAST 會產生配額超額分配之偏移分佈）。**核心紅線 3（🔴 修正任務指示原始假設）**：任務指示原預期本切片含「ASSIGNDAY `::date`/日期運算→mssql(DATEADD/DATEDIFF/CAST AS DATE)」轉換站點，但 test-designer 逐行掃描 `stage3to4-ration-sql.ts` 全檔零命中任何日期方言轉換站點，`ob_monthly_run_result.assignday` 實際型別為 `varchar(100)`（非 date/datetime2），`casedt` 全程以字串處理，此類轉換實際發生於其他檔案（§2.1/§2.4 之 `:ccWorkdt::date`/`:twoYearsAgo::date`）非本檔範圍；已於 §一 GATE-004 記錄此修正，避免下游誤植不必要之轉換程式碼。**其餘查證發現**：`is_cr` 篩選子句（F102 落地時已疊加於 PG 版，非本輪新增業務邏輯）之 mssql 翻譯正確性獨立成 §十一 CRFILTER 群組（含對稱 F102 202606 live bug「CR 案 assignday 全空」之 MUST-FIX 旗艦 CRFILTER-002），但明確排除驗證 CR 完整業務流程本身（P3d 範圍）；`LIMIT 1`→`TOP(1)` 僅 1 處（`hasEmplRows`），獨立立 §七 TOPLIMIT 群組維持可追溯性；視窗函式（`ROW_NUMBER`/`SUM OVER`）AD 標「低風險」但仍設計 §四 WINDOWFN 群組驗證單一列 partition 之空框架語意（`ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING` 首列/唯一列邊界）。**Harness 範圍顯著小於 P3b**：本檔僅觸及 `ob_monthly_run_result`/`ob_pool_data`/`assignment_run` 三張表，皆屬 P3a 已確認共用 dbo 六表子集，**不需要**如 P3b 般自建 6 張計分專屬表；`ob_dept_pct`/`ob_empl_set`/`ob_calendar` 由呼叫端 TypeORM 查詢後轉陣列傳入（已是 dialect-agnostic 路徑，非本文件方言轉換範圍）。分層：**GATE 4**（三表存在性 regression + ration 精度決策前置 + VALUES-CTE 站點清單核對 + 🔴ASSIGNDAY 無日期轉換需求澄清）+ **DISPATCH 5**（🔴🔴 缺口閉環 MUST-FIX + clearStage3Fields 方言中立性決策 + CR 前置刻意排除靜態守門 + 三分支互斥 spy + DoD 核心觀察）+ **VALUESCTE 3**（3 處 derived table 包裝）+ **WINDOWFN 3**（🔴單一列空框架邊界）+ **DECIMAL 2**（🔴🔴 ration 精度旗艦）+ **UPDATEFROM 4**（🔴防跨 run 污染旗艦）+ **TOPLIMIT 2** + **DEPT 6 + EMPL 5 + ASGD 3**（對稱既有 F101 PG spec 手算 oracle）+ **CRFILTER 3**（🔴is_cr 篩選子句 mssql 翻譯，含對稱 F102 live bug 旗艦）+ **EQ 5**（DoD 核心，含含 CR 混合情境旗艦）+ **IDEM 2 + REG 6 + STATIC 3**。**明確排除**：3d CR 優先分派（`cr-priority-sql.ts` 本身之 mssql 化）、3e `fn_calc_tier_level` 收尾，皆待後續子切片。總場景數 2894→**2950**；總文件數 95→**96**。
>
> **v2.46 MSSQL 全面遷移 P3b Stage 2~3 計分 raw SQL 引擎移植測試設計（2026-07-08，P3 第二片，AD-E07-42 §6.1 明文「本 AD 風險最高單一區塊」）**：新增 `infrastructure/AD-E07-42-P3b-test.md`（**131 個場景**）。對應 AD-E07-42 §2.2（Stage 2~3 計分逐站點方言轉換清單）+ §5 P3b DoD：`stage2to4-sql-builder.ts`/`stage2to4-sql-executor.ts` 之 mssql 平行版本，JS↔MSSQL 逐列等價（I-MSSQL-ENGINE-EQ-01）。承接已完成之 P3a（Stage 1 篩選，視為已驗證黑盒依賴：Stage 1 已 INSERT 案件至 `ob_monthly_run_result`，P3b 僅在其上 UPDATE score/card_level/tier_level）。**核心紅線 1（🔴🔴 語意澄清，糾正 AD §2.2「高風險」標籤易造成的誤判）**：三處 `~` 正則站點（`SAFE_INT_CUS_SEX`/`IS_PERSONAL_GATING`/EDUCAT_BACK `numExpr`）皆為 `^[0-9]+$`（含 `$` 錨點，全字串驗證），與 P3a year-above 之 `^[0-9]+`（無錨點，前導擷取）語意層級不同——**可直接複用 P4a 已驗證公式**（`NOT LIKE '%[^0-9]%'` + `LEN>0` + `TRY_CAST`），非需要如 P3a 般自行推導 `PATINDEX` 新公式；但三處組合輸入互不相同（原始欄位 vs `COALESCE(NULLIF(...),'1')` 包裝 vs 巢狀 `CASE` 補零字串），仍需逐一分別驗證邊界，已獨立立 §三/四/五/六 REGEX-SAFESEX/GATING/EDUCAT/META 四群組。**核心紅線 2（🔴🔴 test-designer 查證出之全新 DISPATCH 陷阱，AD 完全未提及，同型於 P3a/P1c/P2b 已反覆出現）**：`resolveStage2to4Strategy`（`assignment-run-pipeline.service.ts:174-180`）現行為二元-ish gate（`DB_TYPE==='postgres'→pushdown`；其餘依 `ASSIGNMENT_PIPELINE_V2` 選 v1/v2 in-memory），`DB_TYPE='mssql'` 環境會靜默落入 in-memory JS 執行路徑（不拋錯，`executeV2` 為 DB-agnostic 純 TypeORM 查詢，但違反 I-NOLOAD-01 架構意圖），是「功能正確但架構退化」之隱蔽缺口，已設計 §二 DISPATCH 4 案 MUST-FIX 守門。**核心紅線 3（🔴🔴 `to_jsonb(o)->>'col'` 通用 fallback，live production path 非死碼，MSSQL 無等價單一 SQL 表達式，需架構性調整 I-MSSQL-DYNAMIC-FALLBACK-01）**：任何未 hardcode 之計分欄位皆走此路（P3b 範圍內唯一「非純語法轉換而是設計調整」站點），須改為 SQL 生成前 TS 端 `INFORMATION_SCHEMA.COLUMNS`（大寫）schema 檢查，存在→直接欄位參照；不存在（幽靈欄位）→SQL 生成時即產生字面值 `0`（不留待執行期動態解析），已設計 §七 FALLBACK 7 案（含 O(1) schema 查詢 MUST-FIX 守門 + 與 DECIMAL 群組交叉引用之精度風險）。**核心紅線 4（🔴 test-designer 查證出之獨立站點陷阱）**：Stage 2 計分之 AGE 與 Stage 1 篩選之 AGE 為**兩個獨立站點**，前者參考日為「今日」（PG `age(dob)` 單引數隱含 `CURRENT_DATE`，JS oracle `calcAgeYears(dob, new Date())`），後者參考日為 `:ccWorkdt`（月名單分派工作月）——複製貼上 P3a 已驗證 `DATEDIFF` 公式時極易誤植 `@ccWorkdt` 而非 `SYSDATETIME()`，此陷阱不會拋錯、僅在跨月份查驗時產生系統性偏差，已設計 §八 AGESCORE-META-001 MUST-FIX 旗艦守門（刻意以非當月工作月驗證計分結果不隨之變動）；CAR_YEAR 之 `EXTRACT(YEAR FROM CURRENT_DATE)` 站點同型，已於 §九 CARYEAR-002 同步守門。**核心紅線 5（🔴 test-designer 查證出之全新 DECIMAL 精度缺陷，AD 未點名此具體站點）**：LOAN_RATE `CAST(o.loan_rate AS numeric)`（無精度宣告）——查證 `ob_pool_data.loan_rate` 之 MSSQL baseline 型別為 `numeric(5,2)`，T-SQL 未指定精度之裸 `NUMERIC` 預設為 `NUMERIC(18,0)`，逐字翻譯會將小數四捨五入去除（`12.50`→`13`），使 range 計分比對系統性偏移且不拋錯，屬 I-MSSQL-DECIMAL-NORMALIZE-01 揭示之 FINDING-P4D-01 同型缺陷家族新發生位置，已設計 §十四 DECIMAL-LOANRATE-001 MUST-FIX 旗艦守門。**其餘查證發現**：`stage2to4-sql-builder.ts` 檔頭 docblock 仍描述已於 F101/AD-E07-29（I-NO-ST4-EXCHANGE）移除之 Stage 4 `st4_exchange` 邏輯，屬過時殘留註解，易誤導範圍判斷，已設計 §二十三 STATIC-005 靜態守門確認 P3b 實際範圍僅 `runStage2and3Sql`（不含任何 Stage 4 欄位）；`CROSS JOIN LATERAL`→`CROSS APPLY`（§十一）、`IS NOT DISTINCT FROM`→OR-NULL 重寫（§十二，對稱 F100 LEVTIER-002~005 五案）、`UPDATE...FROM` 目標併入 FROM（§十三，含防跨 run 污染旗艦案例 UPDATEFROM-002）三處結構性重構站點皆逐一驗證。**Harness 改善（依任務指示，本輪起消除 P3a 已知盲點）**：P3a impl log 明文記錄「正式 CI 需先 bootstrap dbo baseline」為已知盲點，本文件 §零 0.2 新增「`beforeAll` 冪等自建（零 drift DDL，逐字複製 baseline migration 陳述式）+ `afterAll` 條件式清理（本次自建之表 DROP 還原；原本已存在之共用表僅前綴 DELETE，絕不 DROP/TRUNCATE）」策略，涵蓋 P3b 新增依賴之 6 張計分專屬表（`ob_levelcard_version`/`column`/`score`/`level`/`ob_tier`/`ob_arreturndf_min_cap`）+ P3a 已依賴之 6 張，合計 12 表，使套件在全新/部分缺表 dbo 上仍可獨立完整重跑，已設計 §二十四 HARNESS 5 案（含冪等性守門）。**9 個 customer_core 計分維度完整 EQ**（AD §0 已解除過渡態限制）：§十五 CCDIM 群組對稱 F104 PG spec 之 SEX/BRANCH/EDU/CITY/PCD 矩陣 + 五欄 gating 交叉驗證 + FULLEQ 旗艦綜合案例；§二十 FULLEQ 群組另涵蓋 F103/F105 8 個綜合情境（含 §七 FALLBACK 通用 fallback、composite PROJECT_TP、SALES_STS 三值分流）。**DoD #4 真實月重跑跨引擎比對**：§二十一 MONTHRUN-DIFF 群組依 AD §4.2 建議設計，性質同既有 F101/F102/F104「202606 真實重跑」慣例，建議腳本/手動執行而非併入常駐 vitest 套件（記入 risks-and-gaps.md，非阻擋 P3b 核心 DoD）。分層：**GATE 6**（12 表存在性 + `resolveColumnSource` 簽章開放式決策點 + 型別事實核對 + LOAN_RATE 精度決策 + `year_produ` 欄寬限制決策 + customer_core P4d 殘留列可用性探測）+ **DISPATCH 4**（🔴🔴 三態化 MUST-FIX）+ **REGEX-SAFESEX 5 + REGEX-GATING 5 + REGEX-EDUCAT 6 + REGEX-META 2**（🔴🔴 三站點語意區分核心）+ **FALLBACK 7**（🔴🔴 唯一架構性調整站點）+ **AGESCORE 6**（🔴🔴 參考日期 MUST-FIX 旗艦）+ **CARYEAR 5**（複用 P3a PATINDEX 手法 + 參考日期同型守門）+ **PJTP 4** + **CROSSAPPLY 3** + **DISTINCTFROM 5**（對稱 LEVTIER-002~005）+ **UPDATEFROM 4**（🔴 防跨 run 污染旗艦）+ **DECIMAL 5**（🔴🔴 LOAN_RATE 精度旗艦）+ **CCDIM 18**（🔴 9 維度完整 EQ 矩陣：矩陣子案例 17 + FULLEQ 旗艦 1）+ **SCORE 7 + CJOIN 3 + AR 4 + S2CLEAN 2**（對稱既有 F100/F102 PG spec）+ **FULLEQ 8**（DoD 核心）+ **MONTHRUN-DIFF 2**（DoD #4，非常規自動化）+ **CHARSET 3** + **STATIC 7**（🔴 零殘留 PG token 掃描核心）+ **HARNESS 5**（Harness 改善驗證）+ **REG 5**。**明確排除**：3c Stage 3/4 比例分派、3d CR 優先分派、3e `fn_calc_tier_level` 收尾，皆待後續子切片。總場景數 2763→**2894**；總文件數 94→**95**。
>
> **v2.45 MSSQL 全面遷移 P3a Stage 1 篩選 raw SQL 引擎移植測試設計（2026-07-08，P3 首片，跳過 spec-writer）**：新增 `infrastructure/AD-E07-42-P3a-test.md`（**73 個場景**）。對應 AD-E07-42 §2.1（Stage 1 逐站點方言轉換清單）+ §5 P3a DoD：`stage1-sql-builder.ts`/`stage1-customer-core-clause.ts`/`stage1-sql-executor.ts` 之 mssql 平行版本，JS↔MSSQL 逐案件等價（I-MSSQL-ENGINE-EQ-01）。**核心紅線 1（🔴🔴 本文件最高優先級發現，保證語法錯誤，AD 完全未提及、直接推翻 AD §1.1「executor 層不需重新設計」表述）**：`stage1-sql-executor.ts:100` 之 `'CR' || cremp.emp_nm`（CR 業代姓名組裝）為 PG 專屬 `||` 字串串接運算子，T-SQL 不支援、100% 拋語法錯誤；已設計 §三 CONCAT（3 案例，含原始碼靜態守門 + 中文姓名旗艦 EQ）。**核心紅線 2（🔴🔴 AD 建議公式引數順序反轉，套用後年齡計算得負值）**：AD §2.1 表格建議之 AGE 轉換公式 `DATEDIFF(YEAR,@ccWorkdt,cc.date_of_birth)` 引數順序反轉（應為 `DATEDIFF(YEAR,cc.date_of_birth,@ccWorkdt)`），逐字套用會使 30 歲客戶算出 −30；已設計 §四 AGE-MSSQL-001 為 MUST-FIX 旗艦紅燈守門（已知年齡具體數值斷言，非僅「有值」）。**核心紅線 3（中風險，不可逐字複用 P4a 既有正則轉換公式）**：year-above 之 `SUBSTRING(col FROM '^[0-9]+')`（無 `$` 錨點，前導數字**擷取**）與 P4a 已驗證之 `^[0-9]+$`（全字串**驗證**）語意層級不同，MSSQL 版需自行以 `PATINDEX` 推導擷取公式並處理空字串/全數字/首字元非數字三種 `PATINDEX` 回傳 0 之邊界混淆；已設計 §五 YEARABOVE（8 案例，逐一對稱 PG PORT-001~007 + 1 補充）。**核心紅線 4（Harness 環境依賴，AD 未涉及）**：test-designer 查證 MSSQL baseline migration 已於 `dbo` 建有 `ob_pool_data`/`ob_pool_data_list`/`ob_list_definition`/`assignment_run`/`ob_monthly_run_result`/`customer_core` 六張與 P1b2/P4 系列共用之表，Stage 1 raw SQL 產出之裸表名僅能解析至 `dbo`（無法比照 P1b1 獨立 schema 隔離），既有 F099/F109 PG spec 之「DROP+re-synchronize」模式移植至 MSSQL `dbo` 會破壞其餘套件——已於 §零 Harness 設計改採「共用既有表 + 前綴隔離寫入列 + 精準 DELETE（禁止 DROP/TRUNCATE）」策略，並設計 §十四 STATIC-001 靜態守門此禁令。**核心紅線 5（二元 gate MUST-FIX，同型於 AD-E07-38 P1c DISPATCH-001 / AD-E07-40 P2b DISPATCH-001~003）**：`assignment-run-pipeline.service.ts:1437-1440`／`stage0-estimate.service.ts:809` 現行皆為 `DB_TYPE==='postgres'` 二元 gate，MSSQL 環境未接線會誤回退至現行 JS chain 路徑（違反 I-NOLOAD-01）；已設計 §二 DISPATCH（4 案例，spy 驗證 + 三分支互斥）。**開放式決策點（不預設答案）**：`buildCustomerCoreClause` 現行由 `buildStage1Sql`（PG 下推）與 `executeStage1Chain`（JS oracle）共用同一函式，MSSQL 上 `executeStage1Chain` 若未 dialect-aware 化，customer_core 條件之 EQ oracle 需改用測試檔內手算 JS oracle（非重用 `executeStage1Chain`）——已於 §0.3 記錄兩種路徑，§十 CCEQ 依預設路徑（不改動 `stage1-filter-chain.ts`）設計。分層：**GATE 4**（query-composer 雙引號識別碼 regression 確認 + dbo 六表存在性探測 + CC-EQ oracle 選擇記錄 + `source_customer_no` UNIQUE 約束查證）+ **DISPATCH 4** + **CONCAT 3** + **AGE 7**（含閏年 2/29 邊界）+ **YEARABOVE 8** + **EQ 14**（Stage 1 核心規則，對稱 PG EQ-001~014）+ **CCNULLEXC 6** + **CCJOIN 2** + **CCMISC 5**（CITY/AND/JOINCARD/DEACT）+ **CCEQ 6** + **RUNEST 2** + **IDEM 3** + **CHARSET 2** + **STATIC 4** + **REG 3**。**明確排除**：3b Stage 2~3 計分（含 AGE 之另一獨立站點、`~` 正則轉換三站點、`to_jsonb` fallback）、3c 比例分派、3d CR 優先分派、3e `fn_calc_tier_level` 收尾，皆待後續子切片。總場景數 2690→**2763**；總文件數 93→**94**。
>
> **v2.44 MSSQL 全面遷移 P4e raw Staging Bulk-Load 寫入端測試設計（2026-07-08，P4 最後一片，P4 全範圍至此完成）**：新增 `infrastructure/AD-E07-41-P4e-test.md`（**65 個場景**）。對應 AD-E07-41 §6（Bulk-Load：5 個來源表 raw Staging 寫入端）+ §9 P4e DoD（銜接 P4a/b/c/d，**跳過 spec-writer**）之收尾切片：`raw-data.service.ts` 之 `openCopyWriter`/`formatCopyValue`/`supportsCopy`（`pg-copy-streams`）改為新增 `openMssqlBulkWriter`/`supportsBulk`（tedious `Request.bulk`），來源端讀取（`mssql-executor.ts`）已相容不需改。**核心紅線 1（🔴🔴 本文件最高風險發現，直接阻擋 P4e 自身 DoD）**：`RawDataService` 現行 `isPostgres: boolean` 為二元 gate，`DB_TYPE=mssql` 時恆為 `false` 而誤入 SQLite 分支——`createRawTable()` 產生 `AUTOINCREMENT`（非 T-SQL 語法）、`tableExists()` 查詢 `sqlite_master`、`getTableColumns()` 呼叫 `PRAGMA table_info`，三者皆為 `executeExtraction()` 建表/schema-drift 比對之直接前置依賴，缺口不修則 bulk-load 連第一步都無法執行；已設計 §二 ISPG-GATE（9 案例，MUST-FIX 陷阱佐證 + 目標行為雙軌）。**核心紅線 2（🔴 AD 檔案改動清單缺口，同型於 AD-E07-40 P2b 先例）**：AD §6 僅描述 `raw-data.service.ts` 新增介面，完全未提及消費端 `extraction-execution.service.ts` 之 `canStream` 判定式（現行僅檢查 `supportsCopy()`）需同步擴充承認 `supportsBulk()`，否則新機制會成為「建置完成但從未被觸發」的死碼；已設計 §三 DISPATCH（6 案例，含陷阱佐證 + spy 驗證呼叫路徑）。**核心紅線 3（🔴🔴 TYPEMAP，呼應 FINDING-P4D-01 同型缺陷家族）**：三層型別鏈（來源型別→raw 表型別→tedious bulk Table 型別宣告）須逐一對應一致；`decimal`/`numeric`/`money` 家族若映射為固定精度 `DECIMAL(p,s)`，會在 raw staging DDL 建表層級重演 P4a-fix 已修正之 `I-MSSQL-DECIMAL-NORMALIZE-01` 精度溢位缺陷家族（只是發生位置從 type_cast 節點換成建表本身），已設計 `TYPEMAP-003` MUST-FIX 決策關卡建議映射為 `NVARCHAR(MAX)`。**核心紅線 4（🔴 formatCopyValue 誤用防線）**：`formatCopyValue()` 之文字跳脫邏輯專屬 PG COPY TEXT 協定語意，bulk 為型別化協定不需要（且不可誤用）此函式——若誤重用會使含真實 tab/newline 字元的值被寫入為反斜線字面轉義形式而非真實控制字元；已設計 §五 NOESCAPE-CHARSET MUST-FIX + §十一 STATIC-003（原始碼靜態掃描）雙重防線。**其餘查證發現（4 項不可假設，待 tdd-impl 真庫驗證）**：(a) TypeORM mssql QueryRunner 之 `databaseConnection` 底層物件是否可比照 `openCopyWriter` 手法曝露 tedious `Request`/`Table` API（本專案既有全部 `.mssql.spec.ts` 從未用過此手法）；(b) tedious `Request.bulk(table)` 是否可對同一物理表跨多次呼叫以達成記憶體有界（CLAUDE.md ETL 紅線）；(c) bulk Table 型別宣告與目標表實際型別輕微不符時之寬容度；(d) `abort()` 中途呼叫之已寫入列殘留行為。分層：**TYPEMAP 14**（三層型別矩陣 + `I-MSSQL-DECIMAL-NORMALIZE-01` 同型缺陷 MUST-FIX + IDENTITY PK 語法陷阱佐證）+ **ISPG-GATE 9**（🔴🔴 二元 gate MUST-FIX，僅涵蓋 bulk-load 直接前置依賴三方法）+ **DISPATCH 6**（🔴🔴 consumer 端接線 MUST-FIX + spy 驗證）+ **BULK-WRITE 7**（DoD 核心列數/逐欄值/NULL 語意 + `databaseConnection` 探測決策關卡）+ **NOESCAPE-CHARSET 5**（formatCopyValue 誤用陷阱佐證 + 中文 round-trip）+ **BATCH 5**（🔴🔴 記憶體有界探測決策關卡 + DoD 核心逐步增加佐證）+ **E2E-EXTRACT 6**（DoD 核心全量擷取完整跑通 + schema-drift + 冪等）+ **PIPELINE-READ 3**（銜接 P4a `ExtractHandlerMssql`，P4e 特有新整合點，非 P4d fixture 手法）+ **PERF 2**（觀察性，非阻擋）+ **REG 5**（PG COPY 路徑逐字不變 + 既有套件不回歸 + tsc gate）+ **STATIC 3**。**明確排除**（同檔案同缺陷但非 bulk-load 直接依賴，記入 `risks-and-gaps.md`）：`getColumnMetadata`/`getIndexedColumns`/`getRawData` 資料查詢之 `isPostgres` 缺口；`insertBatch` 非 full-mode（incremental）路徑之 mssql 相容性（含 2100 參數上限）。**MSSQL 全面遷移 P4（P4a/b/c/d/e）全範圍至此完成測試設計**；總場景數 2625→**2690**；總文件數 92→**93**。
>
> **v2.43 MSSQL 全面遷移 P4d customer_core 56 節點端對端測試設計（2026-07-08，P4 收官切片）**：新增 `infrastructure/AD-E07-41-P4d-test.md`（**51 個場景**）。對應 AD-E07-41 §8（EQ/端對端測試策略）+ §9 P4d DoD（銜接 P4a/P4b/P4c 已完成之全 9 個 mssql handler 單元/整合測試，**跳過 spec-writer**，QUOTE-003/CLEANUP-003/DISPATCH-001 三項決策沿用不重議、不重測任一 handler 內部 SQL 正確性）之收官切片：全部 9 個 handler 首次透過真實 `NodeDispatcher`+`PipelineRunner.run()` 協同執行「ETL for Customer Core」真實 DAG，並與 PG 版本端對端結果比對（I-MSSQL-ETL-EQ-01）。**核心紅線 1（🔴 節點數量事實修正）**：test-designer 逐檔查證 `etl-pipelines.json` 確認真實節點數為 **56**（`raw_data_extract:5, derived_field:7, lookup:31, merge:4, dedup:3, type_cast:2, field_mapping:2, conditional:1, target_load:1`）、55 條邊，與 AD/任務書沿用之舊估算「53 節點」不符，本文件一律以真實 56 為準（§十三 STATIC-001 事實鎖定，非阻擋，僅提醒 AD 未來修訂同步）。**核心紅線 2（🔴🔴 本文件最高風險發現，raw fixture 範圍大幅擴大）**：任務書「5 來源 2 ZZIP+3 MLMC」僅描述 5 個 extract 節點對應之 raw 表，但 31 個 lookup 節點另依賴 **9 張獨立 lookup 來源 raw 表**（`ZZIP_BAMCODE_D`×2/`ZZIP_BAMPOST_M`/`MLMCODE`×3/`MLSTDINDUMF`×3），若僅建 5 張表 31 個 lookup 節點會 100% 拋錯無法端對端跑通，已於 §零 Harness 設計全數納入（14 張 raw fixture 表，雙側 DB 對稱建構）。**核心紅線 3（🔴🔴 dry-run 陷阱，MUST-FIX）**：查證 `target-load-handler(-mssql).ts` 皆有 `isTestRun===true` 時完全跳過寫入但 `nodeLogs.outputRowCount` 仍顯示正常數字之陷阱，若 E2E harness 誤用 `triggerTest`（而非 `triggerExecute`）或誤設 `PipelineRunnerConfig.isTestRun=true`，會產生極具欺騙性的假陽性（全部節點 completed 但 customer_core 零寫入）；已設計 §四 ISTESTRUN 群組（執行期斷言 + 陷阱佐證對照組）+ §十三 STATIC-003（原始碼靜態掃描）雙重防線。**核心紅線 4（🔴🔴 TIEBREAK 業務級偵測，AD §4.3 落地）**：C-TIE fixture（同一 extract 表內 2 筆列，`CUSTO_NO`/`UPDATE_DATE` 完全相同）首次在真實 56 節點 DAG 中觸發 `d1` 之 `_seq IDENTITY` tie-breaker（P4c 僅以孤立 handler harness 驗證過）；`TIEBREAK-003` 為跨引擎（PG vs MSSQL）勝出列比對之探測型案例，依 AD §4.3 裁定**不預設答案**、兩種結果（一致/不一致）皆為合法通過，僅要求 tdd-implementation 於 impl log 明確記錄實際觀察結果供比照 F067 差異報告揭露慣例回報使用者。**核心紅線 5（🔴 PG 側 degradable 政策，依本專案真實先例）**：P4a impl log RESOLVE-002 已記錄「5433（postgres-test）於前次實測不可達，5432 為 dev DB 不可注入測試資料」之真實先例，本文件 §六 EQ-PG 與 §五 TIEBREAK-003 依此設計為 best-effort（5433 不可達即整組 `describe.skip`，不阻擋 P4d 核心 DoD），§三 E2E-RUN（MSSQL-only）為唯一不可退讓之硬性 DoD。**Harness 設計核心決策**：§0.2 DISPATCH-E2E 決策關卡（方案甲 service-level `triggerExecute` smoke vs 方案乙直接建構 dispatcher，建議方案乙為 DoD 核心、方案甲為 1 案例補充驗證）；§0.3/§0.4 14 張 raw fixture 表 + 9 客戶 fixture 矩陣設計原則（不規定確切字面值）；沿用 `_p4c-target-tables.ts` 之 idempotent 自建模式並擴充至 `datasources`/`extraction_tasks` 存在性守門。分層：**GATE 4**（raw 表欄位清單衍生方式決策關卡 + datasources/extraction_tasks 存在性 + lookup 值域涵蓋決策關卡 + DAG 無環回歸）+ **DISPATCH-E2E 2**（決策關卡 + service-level `triggerExecute` 補充 smoke，非 `triggerTest`）+ **🔴E2E-RUN 7**（DoD 核心：56 節點零錯誤 + 9 handler 類型全覆蓋 + `##` 中途取樣 + customer_core 實際列數 + 容錯路徑 + 耗時觀察性 + 交叉核對）+ **🔴🔴ISTESTRUN 3**（MUST-FIX：靜態守門 + 陷阱佐證對照組 + PG 對稱回歸）+ **🔴🔴TIEBREAK 5**（DoD 核心決定性 + 3 次重跑穩定 + 跨引擎業務級探測 + 對照組 + NULLS LAST 全 DAG 落地）+ **EQ-PG 6**（degradable，旗艦逐欄逐列比對 + C-BOTH 衝突解決 + C-NULLBIZ 排除 + lookup miss 一致性 + skip 機制自我驗證）+ **CHARSET 3**（DoD 核心中文 round-trip + lookup 中文描述 + 中途取樣）+ **LOOKUPHIT/LOOKUPMISS 3**（基準對照組前提 + DoD 核心未命中 NULL + 隔離性）+ **NULLEXC 3**（DoD 核心 + 多欄同理 + 節點順序回歸防線）+ **IDEMPOTENT 3**（DoD 核心重跑不疊加 + tie 客戶冪等交叉 + logId 隔離回歸）+ **🔴CLEANUP-E2E 3**（DoD 核心成功/失敗兩路徑全清 + LIKE 萬用字元防禦性掃描）+ **REG 5**（tsc gate + PG/sqlite 路徑不受影響 + P4a/b/c 套件不回歸 + 共用 cdmp_test DB 隔離 + PG 黑箱契約不變）+ **STATIC 4**（🔴節點數/邊數/nodeType 分佈事實鎖定 + raw 表命名清單鎖定 + 🔴MUST-FIX isTestRun 靜態掃描 + 凍結清單最終確認）；不需 spec-writer，不重測任一 handler 內部 SQL 正確性（P4a/b/c 已覆蓋）。**MSSQL 全面遷移 P4（P4a/b/c/d）至此全數完成測試設計**（P4e bulk-load 為明確排除範圍，另待）；總場景數 2574→**2625**；總文件數 91→**92**。
>
> **v2.42 MSSQL 全面遷移 P4c ETL Handler 群組三（dedup/target-load）測試設計（2026-07-08，P4 第三片，P4 最複雜切片）**：新增 `infrastructure/AD-E07-41-P4c-test.md`（**92 個場景**）。對應 AD-E07-41 §4（`DISTINCT ON`+`ctid`→`ROW_NUMBER()`+`IDENTITY` tie-breaker）+ §5.1（customer_core UPSERT `ON CONFLICT`→兩段式）之 P4c 切片（銜接 P4a/P4b，**跳過 spec-writer**，QUOTE-003/CLEANUP-003 沿用不重議；**DISPATCH-001 於本輪落地**：P4a/P4b 皆延後之 `createDispatcher()` `DB_TYPE` 分支，本輪 9 個 handler 全數到齊，正式接線）。**核心紅線 1（🔴🔴 範圍擴大核心，本文件查證出之最高風險）**：真實 `etl-pipelines.json` 顯示 `target-load-handler.ts` 被 **6 條 pipeline 共用**（customer_core UPSERT ×1 + `fullMode` 全量替換 ×4：`ob_arreturndf_min_cap`/`ob_calendar`/`ob_emphire`/`ob_pool_data` + `loadMode='partition_replace'` ×1：`ob_pool_data_list`），AD §5.1/§9 P4c DoD 僅描述 customer_core UPSERT 一種路徑，`fullMode`/`partition_replace` 完全未被提及；由於 `createDispatcher()` 之 `DB_TYPE` 分支本輪首次接線，cutover 後這 5 條既有生產 pipeline（E03/E04 raw data landing 既有機制）會直接呼叫 mssql 版 handler，若僅實作 customer_core 路徑，會在 MSSQL 上 100% 失敗且無任何既定測試（P4d 端對端僅限 customer_core）能在上線前攔截。已擴大範圍新增 §四 FULLMODE（11 案例，含 composite PK 旗艦案例）+ §五 PARTITION（6 案例）。**核心紅線 2（🔴🔴 TLDEDUP 群組，本文件查證出之第二高風險，AD 完全未涵蓋）**：`target-load-handler.ts` 內部另有兩處 `DISTINCT ON` 去重站點（`fullMode` PK dedup + customer_core `source_customer_no` dedup），與 AD §4/I-MSSQL-DEDUP-TIEBREAK-01 所述 `dedup-handler.ts` 之 `ctid` 決勝角色相同，但 AD 文字完全未涵蓋，`ORDER BY` 皆僅含 key 本身、無次要排序鍵；PG 原始碼註解明確承認「`NULLIF(TRIM())` 正規化會製造碰撞」（`'A12345 '` 與 `'A12345'`），即勝出列內容確實可能不同，tie-breaker 選擇有實質影響。已獨立立 §二 TLDEDUP（11 案例，含 `TLDEDUP-EQ-001` 旗艦真實碰撞案例）。**核心紅線 3（🔴 DEDUP 群組，AD §4 明確設計之核心）**：`dedup-handler.ts` 之 `_seq IDENTITY` tie-breaker 改寫，21 個案例涵蓋真實 customer_core 三組 key 配置（d1 `CUSTO_NO`/d2 `CUSTID`/d3 `source_customer_no`）+ 決定性核心群組（同 timestamp、NULL timestamp、§4.3「決定性選出恰一列」非「同一實體列」語意落地）。**其餘查證發現**：`getPrimaryKeyColumns()`（`fullMode` 專屬，composite PK `ordinal_position` 排序）與 `inputColumnTypes`/`varcharColumns`（`NULLIF(TRIM())` 判斷用）兩處 catalog 站點 AD 完全未提及，後者需要既有共用 helper `MssqlTempTableColumn` 未涵蓋之 `data_type` 資訊，已設計 `CATALOG-GATE-001` 決策關卡（擴充 helper vs 另寫查詢）；target-load 內部 enriched `tempTable`/`dedupTable` 因回傳 `tempTable=''` 從未向 `NodeOutputStore` 註冊，P4a `CLEANUP-003` 機制天生不覆蓋，須獨立 `try/finally` 管理，已設計 §七 CLEANUP（5 案例）MUST-FIX + 決策關卡。**Harness 設計**：`dbo` schema 佔用範圍第四度擴大（P1b2/P1b3→P4-0/P4a→P4b→本輪 P4c），且本輪首次直接對真實既有生產 baseline 表（`ob_calendar`/`ob_emphire`/`ob_arreturndf_min_cap`/`customer_core`）寫入測試資料而非僅合成 fixture，§0.2 已設計隔離/清理策略；`ob_pool_data`/`ob_pool_data_list` 因逾百欄改用合成 throwaway `dbo` 表驗證 PK/分區形狀。**明確排除**：53 節點端對端（P4d）、bulk-load（P4e）。總場景數 2482→**2574**；總文件數 90→**91**。
>
> **v2.41 MSSQL 全面遷移 P4b ETL Handler 群組二（merge/lookup，含 UPDATE...FROM 重構）測試設計（2026-07-08，P4 第二片）**：新增 `infrastructure/AD-E07-41-P4b-test.md`（**66 個場景**）。對應 AD-E07-41 §3.2 merge/lookup 兩列（銜接 P4a，**跳過 spec-writer**，QUOTE-003/CLEANUP-003/DISPATCH-001 三項 P4a 決策沿用不重議）之 P4b 切片：`merge-handler.ts`（CTAS→`SELECT INTO ##`，FULL OUTER JOIN 本身不需改）+ `lookup-handler.ts`（`UPDATE...FROM`/`DELETE...WHERE NOT EXISTS`/`ALTER TABLE ADD COLUMN` 三種 `noMatchStrategy` 分支全面改寫）。**核心紅線 1（🔴🔴 ALTERCOL 群組，本文件查證出之最高風險站點，AD 與任務書皆完全未提及）**：`lookup-handler.ts` 之 `ALTER TABLE "${inputTable}" ADD COLUMN IF NOT EXISTS "${alias}" TEXT`——T-SQL `ALTER TABLE ADD` 無 `COLUMN` 保留字、無 `IF NOT EXISTS` 子句（PG 9.6+ 專屬語法糖），且 `TEXT` 為已棄用型別；真實 customer_core pipeline **31 個 lookup 節點每節點皆觸發**，逐字複製 PG 語法 100% 語法錯誤，風險優先權高於任務書已點名的 `UPDATE...FROM` 重構本身（保證編譯期失敗 vs 語意風險）。已設計 `ALTERCOL-UNIT-001~003`（MUST-FIX）+ `ALTERCOL-GATE-001`（決策關卡：欄位存在性冪等檢查機制不預設）+ `ALTERCOL-TRAP-001`（陷阱佐證，手動組裝 naive SQL 對真實 MSSQL 執行證實拋錯）。**核心紅線 2（🔴 UPDATEFROM 群組，任務書明確點名，本文件查證出具體翻譯陷阱）**：PG `UPDATE "input" _src SET ... FROM (subquery) _lk WHERE ...` 之 `_src` 為 PG 特有「就地宣告別名」語法（target 不需列於 FROM），若逐字翻譯僅替換 `::text`→`CAST` 而未把 target 併入 `FROM`/`JOIN`，T-SQL 會因別名未宣告拋 `Invalid object name`；已設計 `UPDATEFROM-UNIT-001`（MUST-FIX）+ `UPDATEFROM-TRAP-001`（陷阱佐證）+ `UPDATEFROM-EQ-001`（🔴 旗艦案例，多列 lookup 來源逐列取值防笛卡兒積）等 6 個 EQ 案例。**真實資料查證發現**：grep `etl-pipelines.json` customer_core 53 節點確認 31 個 lookup 節點 **100% legacy mode + 100% `noMatchStrategy='null'`**（dual-input/`skip_row`/`default_value` 三者 0% 真實觸發，比照 P4a FIELDMAP-UNIT-004 精神列為防禦性 UNIT）；**25/31（81%）使用 `lookupFilter`**（AD 未提及，已納入 `UPDATEFROM-EQ-003`）；4 個 merge 節點 **100% `sameKeyName=true`**，且 `m2→m3` 為鏈式合併（`m3` 須正確跳過 `m2` 產生的 `_left`/`_right` 衍生欄位），已設計 `MERGE-EQ-005`（🔴 旗艦案例）。**其餘查證發現**：`merge-handler.ts` 之 `getColumns()`／`lookup-handler.ts` 之 raw 表存在性檢查，兩處皆為 AD §3.2 表格文字未明說但確實存在之 catalog 查詢站點（比照 P4a `EXTRACT-UNIT-002` 之 `INFORMATION_SCHEMA.TABLES` 大寫 + 具名參數模式），已分別納入 `MERGE-UNIT-002`／`RESOLVE-003`；`lookup-handler-mssql.ts` 之 legacy mode 解析須複用 P4a 已預留之 `resolveRawTableMssql`（非重新實作），已設計 `RESOLVE-001` 靜態守門。**DELETE...WHERE NOT EXISTS 澄清**：AD 判定「ANSI 相容不需改」經查證屬實（T-SQL `DELETE FROM table AS alias WHERE ...` 原生支援別名宣告，與 `UPDATE` 之限制不同），已於 `SKIP-UNIT-001` 明確記錄此不對稱性。**Harness 設計**：`dbo` schema 佔用範圍第三度擴大（lookup legacy-mode 為 100% 真實用法，須落於 `dbo`，同 P4a EXTRACT 群組限縮設計），merge 與 lookup 之防禦性分支完全以 `##` fixture 繞開。**明確排除**：`dedup-handler.ts`/`target-load-handler.ts`（P4c）、53 節點端對端（P4d）、bulk-load（P4e）。總場景數 2416→**2482**；總文件數 89→**90**。
>
> **v2.40 MSSQL 全面遷移 P4a ETL Handler 群組一測試設計（2026-07-08，P4 首片）**：新增 `infrastructure/AD-E07-41-P4a-test.md`（**96 個場景**）。對應 AD-E07-41 §3/§5/§9（P4-spike-2 已於 2026-07-08 全數通過、P4-0 customer_core schema 已獨立完成，前置閘已過，**跳過 spec-writer**）之 P4a 切片：§3.1 共用 temp table helper 補齊（`createMssqlTempTable`/`getMssqlTempTableColumns`/`countMssqlTempTableRows`，additive 擴充 P4-spike-2 已落地之 `dropMssqlTempTableIfExists` 所在檔案）＋§3.2 對應 5 個群組一 handler（`extract-handler.ts`/`field-mapping-handler.ts`/`derived-field-handler.ts`/`type-cast-handler.ts`/`conditional-handler.ts`）＋§5.2 Pattern B＋§5.3 `TRY_CAST`＋§5.4 `~` 正則覆核＋§5.5 `INFORMATION_SCHEMA` 大小寫。**核心紅線 1（🔴 QUOTE 群組，本文件設計之最高優先決策關卡）**：test-designer 查證發現 5 個 handler 之私有 SQL 組裝方法**全數**內嵌 `"${col}"` 雙引號識別碼，而本專案既有全部 `.mssql.spec.ts` 從未以原始 `queryRunner.query()` 測試過雙引號識別碼語法（grep 零命中）——若 MSSQL BIN collation + tedious driver 不支援雙引號分隔識別碼，將導致全部 5 個 handler 產生的 SQL 100% 無法執行，此為比任何個別方言轉換站點更基礎的未驗證前提，AD-E07-41 全文未提及，已設計 `QUOTE-001~003` 決策關卡並置於文件最優先位置。**核心紅線 2（🔴 DERIVED-EQ-001 旗艦案例，本文件查證出之 AD 建議公式錯誤）**：AD §3.2 建議之 `LPAD → RIGHT(REPLICATE(char,n)+col,n)` 轉換公式在輸入字串長度 ≥ n 時與 PG `LPAD` 語意不一致（PG 從左截斷保留前 n 碼，AD 建議公式從右截斷保留後 n 碼），已設計 `DERIVED-UNIT-002`（MUST-FIX 紅燈守門）要求正確公式須含 `LEN(col)>=n THEN LEFT(col,n)` 分支。**核心紅線 3（🔴 CAST-EQ-002 旗艦案例，`LIKE` 空匹配真值陷阱）**：任務書明確點名之空字串邊界——naive T-SQL 翻譯 `'' NOT LIKE '%[^0-9]%'` 求值為 `TRUE`（空字串內不存在「非數字字元」），若遺漏 `LEN(col)>0` 額外守門會導致空字串被誤判為合法整數，與 PG `+` 量詞「至少一位數字」語意相反；本文件覆核結論（`CAST-UNIT-003`）：`getValidationRegex` 三目標型別（DECIMAL/INTEGER/DATE）**皆屬簡單字元類別型，可用 LIKE/SUBSTRING/LEN 達成，無需 lookahead/alternation**。**核心紅線 4（🔴 CLEANUP 群組，AD 明文未定案掛載位置）**：AD §1.3 (iii) 僅稱「建議統一收在 pipeline-runner 層級 try/finally，或各 handler 自理」，test-designer 查證 `pipeline-runner.ts`（AD §1.2 明文凍結不可修改）已透過 `NodeOutputStore.cleanupAll()` 貫穿成功/失敗兩路徑，且該檔**不在**凍結清單內，天然為現成收斂點；比照既有 P2c `MOUNT-001` 決策關卡精神，設計 `CLEANUP-001/002` 黑盒 spy 驗證（不預設呼叫者位置）+ `CLEANUP-003`（🔴 MUST-FIX 決策記錄，要求 impl log 明確記錄掛載選擇）。**其餘查證發現**：`resolve-raw-table.ts`（extract-handler 共用依賴）需要獨立 `resolve-raw-table-mssql.ts` 新檔（AD §3.2 完全未提及，含 `$n`/`NULLS LAST`/`LIMIT 1` 站點，已設計 DUAL-DB 排序 EQ 案例）；`derived-field-handler.ts` 之 `mergePhone()` 內嵌 `~ '^0+$'` 正則（customer_core 53 節點中實際出現 **7 次**，為該 handler 最高頻表達式，AD 僅提及 padStart 卻遺漏此站點）與 `gen_random_uuid()`（AD 表格未列出）；`field-mapping-handler.ts` 之 `toSqlLiteral` 對 boolean defaultValue 產生裸 `TRUE`/`FALSE`（同型於 P1b3 已踩雷之裸布林字面值，惟已 grep 確認真實 customer_core 7 個 field_mapping 節點皆無此路徑，僅 UNIT 層防禦）；`createDispatcher()` 現行完全無驅動分支（與 P1c/P2b 之「既有二元 gate 被誤吞」不同型），已設計 `DISPATCH-001` 決策關卡建議延後至 P4c（全部 9 handler 到齊）才接線，P4a 測試手法全數繞過 dispatcher、直接實例化 handler class（同既有 `engine-node-executors.spec.ts` 手法）。**Harness 設計**：`dbo` schema 佔用範圍隨 P4 系列擴大（extract-handler/resolve-raw-table 之裸表名無法透過 TypeORM schema 選項重新導向，同 P1b3 raw SQL 腳本之限制），僅 EXTRACT 群組落 `dbo`，其餘 4 個 handler 完全以 `##` fixture 繞開；EQ 驗證分層（UNIT mock SQL 文字 + MSSQL EQ 手算 oracle + resolveRawTable 排序案例採 DUAL-DB 雙邊真實連線比對）。**明確排除**：`merge-handler.ts`/`lookup-handler.ts`（P4b）、`dedup-handler.ts`/`target-load-handler.ts`（P4c）、53 節點端對端（P4d）、bulk-load（P4e）。總場景數 2320→**2416**；總文件數 88→**89**。
>
> **v2.39 MSSQL 全面遷移 P2c Expire Sweep 整合 + 兩層回收一致性 + 全 F098 套件整體回歸測試設計（2026-07-07，P2 最後一片）**：新增 `infrastructure/AD-E07-40-P2c-test.md`（**25 個場景**）。對應 AD-E07-40 §2.3/§4.3/§7（銜接 P2a/P2b，**跳過 spec-writer**）之 P2c 收尾切片：`MssqlQueueService.expireSweep()`（P2a 已完成黑盒依賴，不重測）之**定時掛載**（AD §4.3 明文掛載機制未定案，交 tdd-implementation 依現行風格擇一，本文件刻意設計為黑盒 spy 驗證、不預設具體 class/method）＋**🔴 兩層回收獨立且一致（旗艦群組，AD §7 DoD #2）**＋**全 F098 套件於 mssql 分支之最終整體重跑（AD §7 DoD #3）**＋`docker-compose.yml` `RUN_QUEUE_POLL_INTERVAL_MS` 環境變數補齊（AD §7 DoD #4）。**核心紅線 1（🔴 test-designer 查證出之關鍵技術澄清）**：`MssqlQueueService.expireSweep()` 之 P2a 定案簽章**無** `now` 參數，與 `OrphanReaper.reap(now)` 之注入時鐘機制**不對稱**——sweep 的時間控制須沿用 P2a `SWEEP-001` 已驗證之技術（直接 SQL 竄改種子列 `expire_at` 至過去），而非期待兩者共用同一套注入時鐘 harness，已於 §0.3 明確記錄避免下游誤用。**核心紅線 2（🔴 RECOVERY 旗艦群組，DoD #2 核心）**：`RECOVERY-001~003` 正面證明兩層回收各自成立且順序無關；`RECOVERY-004`/`005` 為**獨立性直接反證**（僅執行其中一層，證明另一層完全不受影響，互相不依賴）；`RECOVERY-010` 額外驗證一個 AD 未明講但邏輯上真實存在的第三類孤兒情境——**「入列但從未被 claim」之孤兒**（`queue_job` 仍為 `created`，sweep 之 `WHERE state='active'` 條件天然不影響它，只有業務層 `OrphanReaper` 之 pending 分支會抓到它），並串連 P2a claim 語意 + P2b `processPayload` 既有防線（`PAYLOAD-004`）+ P2c `OrphanReaper` 三個切片，驗證此邊界下 worker 恢復後仍不會誤跑 pipeline。**核心紅線 3（REG 群組排除機制設計）**：`REG-001` 要求失敗案例清單與已知排除集合**恰好相等**（非僅比對失敗數量），避免「已知失敗被替換成另一個不相關失敗但總數剛好還是 1」之誤判；`REG-002` 為此排除理由本身持續有效性之獨立靜態守門（確認 `CreatePgBossSchema` migration 檔持續不存在，沿用 `AD-E07-40-P2b-impl.md` 已記錄之 pre-existing 失敗根因）。**已查證之現況缺口**：`docker-compose.yml` `worker:` service 現行**零個** `RUN_QUEUE_*` 環境變數（不僅 `RUN_QUEUE_POLL_INTERVAL_MS`，其餘 4 個既存 tuning 欄位亦未於 compose 曝露），`STATIC-001` 依 AD §7 DoD #4 字面範圍僅要求補上 `RUN_QUEUE_POLL_INTERVAL_MS`，其餘缺口記入 `risks-and-gaps.md`（非阻擋）。分層：**MOUNT 6**（🔴 決策關卡文件化守門 + 啟動立即掃 + 定期觸發 + 零週期防呆 + 🔴 destroy 清 timer DoD 核心 + unref 靜態）+ **🔴 RECOVERY 10**（旗艦群組：DoD #2 正面三案例 + 獨立性反證兩案例 + 冪等終態 + retry=0 整合語境重新確認 + 對照組防誤殺 + 批次崩潰 + 🔴 三片段協同之「未消費孤兒」邊界）+ **STATIC 4**（🔴 docker-compose DoD #4 核心 + 單一位置守門鏈延續兩案例 + 新增週期設定 env 覆蓋建議性決策關卡）+ **REG 5**（🔴 DoD #3 全套件整體重跑 + 排除理由有效性守門 + postgres/sqlite 不受影響 + tsc gate）。**P2（P2a/P2b/P2c）全範圍測試設計至此完成**，MSSQL 全面遷移 P1+P2 累計場景數達 2320；總文件數 87→**88**。
>
> **v2.38 MSSQL 全面遷移 P2b Producer/Consumer 接線 + 輪詢 Loop + processPayload 共用測試設計（2026-07-07，P2 第二片）**：新增 `infrastructure/AD-E07-40-P2b-test.md`（**44 個場景**）。對應 AD-E07-40 §3/§4/§5（銜接 P2a，**跳過 spec-writer**）之 P2b 切片（`RunQueueProducer.send`/`cancel` 新增 mssql 分支 + `RunQueueConsumer.onModuleInit` driver 分支 + 單一 worker 輪詢 loop `startMssqlPolling`/`pollOnce` + `polling` reentrancy guard + `OnModuleDestroy` 清 `pollTimer` + `processPayload` 業務邏輯共用，**不重測 `MssqlQueueService` 本身**——P2a 已完成 59/59 綠，本輪視為已驗證黑盒依賴）。**核心紅線 1（🔴 MUST-FIX，本輪最重要發現）**：**一、DISPATCH 群組**——查證現行 `RunQueueProducer.send`/`cancel`/`RunQueueConsumer.onModuleInit` 皆為「`this.boss` 是否為 `null`」二元 gate，而 `DB_TYPE='mssql'` 環境下 `this.boss` **必然為 null**（pg-boss 本不支援 MSSQL，與現行 sqlite 測試環境未 override 時的訊號完全相同）——若僅在三處程式碼內部新增 mssql 分支邏輯而未把既有二元 gate 改為三分支，mssql 分支會被現行程式碼直接吞掉（send 誤拋錯／cancel 誤靜默略過／onModuleInit 誤 warn+return 完全不啟動輪詢，且無任何錯誤提示），此為 `AD-E07-38-P1c` `DISPATCH-001`（`isPostgres()` 二元 gate 陷阱）之同型態重演；DISPATCH-001~003 對現行未修改程式碼刻意設計為紅燈。**核心紅線 2（🔴 MUST-FIX，test-designer 查證 AD 文件本身之檔案改動清單缺口）**：AD §4.2 檔案改動清單**僅列 `assignment-worker.module.ts` providers 加入 `MssqlQueueService`**，但 `RunQueueProducer`（`send`/`cancel` mssql 分支之直接依賴）依現行 `assignment.module.ts` 既有結構是註冊在 **API 程序**（`TS-F098-WORKER-004` 既有靜態守門已確認），若不同步在 `assignment.module.ts` 也提供 `MssqlQueueService`，API 程序之月名單分派觸發（`send`）在 mssql 環境下會因缺少依賴而故障；`DISPATCH-006`（新增，MUST-FIX 靜態守門）專守此缺口，已記入 `risks-and-gaps.md`。**核心紅線 3（🔴 I-MSSQL-QUEUE-SERIAL-01 reentrancy guard）**：`POLL-007` 以 `vi.useFakeTimers()` + 可外部控制 resolve 時機的 `claimNext` fake 精確重現「上一輪 tick 尚未完成、下一輪計時器已到期」之窗口，證明 `polling` guard 使 `claimNext` 於重疊期間恰被呼叫 1 次（非 2 次）。**核心紅線 4（🔴 processPayload 共用 DoD）**：`PAYLOAD-001` 以單一 `vi.spyOn` 掛在同一實例的 `processPayload` 方法上，分別驅動 pg-boss 路徑（`handleJobs`）與 mssql 路徑（`pollOnce`）各觸發一次，證明兩路徑呼叫的是同一函式參照而非各自兩份相似實作；`PAYLOAD-008` 額外揪出 pg-boss 與 mssql 兩路徑**唯一的資料形狀落差**——pg-boss 之 `job.data` 已是解析後物件，mssql 之 `claimed.payload`（P2a 已驗證）是**原始 JSON 字串**，轉接層漏做 `JSON.parse` 會使 `processPayload` 收到 `undefined` 欄位。**分層設計決策（成本考量）**：DoD #1 明文授權 test-designer 判斷是否新增獨立 `.mssql.spec.ts` 或擴充既有矩陣——查證 DISPATCH/PROD/POLL/PAYLOAD/STATIC 五組驗證對象皆為 Node.js 控制流程邏輯（driver 分支/計時器/共用函式呼叫），與 SQL Server 鎖行為無關，改用 fake `MssqlQueueService`+`vi.useFakeTimers()`（比照既有 `f098-producer.spec.ts`/`f098-consumer.spec.ts` fake boss 風格）即可完整驗證，**僅 E2E 群組需要真實 MSSQL 容器**，新增兩檔：`ad-e07-40-p2b.spec.ts`（39 案例，免真實連線，CI 恆常執行）+ `ad-e07-40-p2b.mssql.spec.ts`（5 案例）。**E2E harness 設計偏離既有 F098 PGINT 精神之處**：既有 `f098-pg-integration.spec.ts` 為驗證「pg-boss 套件本身是否可靠」而繞過 `RunQueueProducer`/`RunQueueConsumer` 直接操作原生 pg-boss API；P2b 待測的正是這兩個類別本身新增的 mssql 分支接線邏輯，故 E2E 群組**直接建構真實** `RunQueueProducer`/`RunQueueConsumer`（注入真實 `MssqlQueueService`），僅 `pipeline` 以簡化 stub 取代，避免拖入完整 ETL/計分依賴；`E2E-003` 延伸 P2a impl log 之 D-CONC-01「反覆輪詢終能全部領完」單 worker 語意至**輪詢 loop 整體**層級（P2a CONC 群組驗證的是 `claimNext` 本身，E2E-003 驗證的是整條輪詢 loop 是否真的會反覆 tick 直到 drain 完畢，兩者驗證對象不同、互補）。分層：**DISPATCH 6**（🔴 三處二元 gate MUST-FIX 守門 + `assignment.module.ts` 缺口 MUST-FIX 靜態守門）+ **PROD 5**（producer mssql 分支正確轉接 `MssqlQueueService`，retryLimit=0/回傳 jobId/cancel 吞錯語意對齊 DoD #1）+ **POLL 10**（onModuleInit driver 分支互斥 + 輪詢間隔可注入 + pollOnce 正常/例外路徑 + 🔴 reentrancy guard 核心 + tick 例外不中斷 loop + 🔴 OnModuleDestroy 清 timer DoD #4 核心 + SIGTERM 驗證範圍界定決策關卡）+ **PAYLOAD 8**（🔴 DoD 核心單一函式參照證明 + 單一位置靜態守門 + run 不存在/取消快路徑/正常呼叫/pipeline 拋錯吞錯 4 案例於新 signature 重新驗證既有 CONS-001/001b/004/005 語意 + 🔴 mssql 路徑 JSON.parse 轉接守門）+ **E2E 5**（🔴 DoD #3 核心：send→輪詢→completed + 業務失敗→failed 但佇列層仍 completed + 🔴 drain 語意延伸 + pollIntervalMs 延遲量測（AD §9.1 非阻擋觀察性）+ cancel-before-consume 全鏈路）+ **ZERO 3**（OrphanReaper/CancellationPoller 既有測試原樣通過 + 兩檔案零異動靜態守門）+ **STATIC 3**（命名鎖定 + P2a STATIC-003 前瞻性守門於接線後重跑確認仍僅命中一處 + pollIntervalMs tuning 靜態）+ **REG 4**（tsc gate + postgres 路徑黑箱契約不變 + sqlite 不受影響 + 既有 f098-static-guards 全數不回歸）。**明確排除**：`MssqlQueueService` 五操作本身（P2a 已完成，本輪視為已驗證黑盒依賴）、expire sweep 定時掛載整合與雙層回收一致性、F098 全套件於 mssql 分支下之最終整體重跑（皆留待 P2c）。
>
> **v2.37 MSSQL 全面遷移 P2a queue_job Schema + MssqlQueueService 五操作 + 併發正確性 Harness 測試設計（2026-07-07，P2 首片）**：新增 `infrastructure/AD-E07-40-P2a-test.md`（**59 個場景**）。對應 AD-E07-40（system-architect 產出，**跳過 spec-writer**——純底層儲存/驅動置換，無新業務行為，比照 AD-E07-38 §3 D-7 先例）之 P2a 切片（`dbo.queue_job` entity + 手寫 filtered index baseline migration 兩軌 + 五個原子 T-SQL 操作封裝為 `MssqlQueueService` + 併發正確性 harness，**獨立驗證層，不碰 Producer/Consumer**——P2b/P2c 留待各自測試設計）。**與 P1c 的關鍵前提差異（AD §0）**：佇列 claim 為純 T-SQL DML（`WITH (READPAST,ROWLOCK,UPDLOCK)`+CTE+`OUTPUT`），P0 smoke 已證本機 Linux 容器可行、不受 `sp_getapplock` 之 17750 DLL 缺失影響，**本文件全部案例（含 CONC 群組）皆可對真實 MSSQL 容器完整執行，不需等待 Windows 環境**。核心紅線：**CONC-001~008（🔴 本切片最有料的旗艦群組）**——test-designer 查證 `node_modules/typeorm/driver/sqlserver/SqlServerConnectionOptions.d.ts` 確認 mssql `DataSourceOptions.pool.max` **預設值為 1**，故 AD §6.2 之「pool 太小恐變相序列化」並非「可能」而是「未顯式設定必然發生」；CONC-001 為前置守門（斷言 CONC 群組專屬 `DataSource` 明確設 `pool.max≥10`）、CONC-004 為併發性直接證據（K 個 claim 呼叫起始時間戳重疊 < 50ms + 批次總耗時遠小於「單次基準×K」）、**CONC-006（🔴 決策關卡，比照既有 PARITY-009 comparator 敏感度驗證精神延伸至併發 harness）**故意以 TypeORM 預設 `pool.max=1` 重跑同組計數斷言，證明「恰 M 次成功、無重複」本身對序列化退化**無鑑別力**（預期仍會通過），必須靠 CONC-004 的時間戳證據才能抓到 pool 設定被誤刪/調低。**★ test-designer 額外查證發現之 comparator 誤用陷阱**：`queue_job` 之 Path A（synchronize 一般索引）與 Path B（baseline migration filtered index）**同名但欄位組成刻意不同**（`idx_queue_job_pending`：Path A=(queue_name,state) vs Path B=(queue_name,created_at) WHERE state='created'），既有 `schema-parity.ts` 之 `diffIndexSets`/`IndexRecord` 不含 `has_filter`/`filter_definition` 欄位、套用於本表會產生誤導性 diff，**SCHEMA-010 已設計為決策關卡文件化守門**，改以 SCHEMA-011/012 兩組獨立明確斷言取代「diff 應為空」判定。分層：**SCHEMA 15**（兩軌建表 + uuid/varchar/nvarchar(MAX)/datetime2 型別矩陣 + CURRENT_TIMESTAMP smoke（沿用 P1a 既有驗證不重複探測）+ 欄位層 parity DoD 核心 + comparator 敏感度守門 + **索引 comparator 誤用陷阱決策關卡** + 兩路徑索引獨立斷言 + ALL_ENTITIES/glob 靜態守門 + PG 無害空表靜態守門）+ **UNIT 6**（免真實連線 SQL 文字/參數 spy，CI 恆常執行；**UNIT-003 為 I-MSSQL-QUEUE-CLAIM-01 核心守門**：claim 恰 1 次 query 呼叫 + 同時含 READPAST/ROWLOCK/UPDLOCK/OUTPUT，杜絕兩段式模擬）+ **SEND 4** + **CANCEL 4**（含 §6.3.2 cancel-before-claim 核心）+ **CLAIM 6**（含 §6.3.3 claim-after-claim 核心 + §6.3.4 FIFO + 跨佇列隔離 + payload round-trip）+ **COMPLETE 3** + **SWEEP 5**（含 §6.3.1 expire 後不重派兩案例核心）+ **CONC 8**（DoD 紅線）+ **STATIC 3**（命名鎖定 + P2a DoD #4 獨立性守門 + 前瞻性單一位置守門供 P2b 沿用）+ **REG 5**（tsc gate + F098 既有套件 + sqlite/PG 不回歸 + 既有全部 `.mssql.spec.ts` schema 不交叉污染）。**Schema 隔離策略刻意偏離 P1b2/P1b3 慣例**：Path B 改用獨立 `p2a_baseline` schema（非 `dbo`），僅驗證單表結構本身正確，不驗證新 migration 疊加進 `dbo` 既有 36 表鏈之整合順序（範圍缺口已記入 `risks-and-gaps.md`，低風險非阻擋）。
>
> **v2.36 MSSQL 全面遷移 P1c Pattern B（$n→具名參數）＋ sp_getapplock 跨 driver 鎖測試設計（2026-07-07，P1 最後一片）**：新增 `infrastructure/AD-E07-38-P1c-test.md`（**48 個場景**）。對應 AD-E07-38 §3 D-5/D-6（P1c 切片，銜接 P1a/P1b1/P1b2/P1b3，**跳過 spec-writer**）之核心 4 檔 6 處中站點 1/2/3/4（站點 5 native cursor 明確排除，移交 Phase 3/4）。核心紅線：**LOCK-009（🔴 決策關卡：`@LockOwner='Transaction'` 前置條件未處於顯式交易之真實 MSSQL 行為，AD 聲稱「直接報錯」但不預設答案，若資料庫不主動擋須新增程式碼層防禦性斷言）**+ **DISPATCH-001（🔴 MUST-FIX 守門：現行 `isPostgres()` 為二元 gate，`DB_TYPE='mssql'` 會被現行程式碼誤判與 sqlite 同路徑「完全跳過鎖」，對現行未修改程式碼預期為紅燈，逼 tdd-implementation 改為三分支）**+ **PARAM-003（站點 1 `customer_core` 為 AD-E07-37 裁定之 PG-only 表，MSSQL 上錯誤恆定發生，測試驗證的是「錯誤被正確分類為表不存在」而非資料等價）**。**★ test-designer 逐檔查證發現 AD 原文未區分之可測性落差**：`customer_core`（站點 1 來源表）全域 grep entities/MSSQL baseline 皆零命中確認為 PG-only；`ob_arreturndf_min_cap`（站點 2 來源表）已在 MSSQL baseline（`1751884800000-MssqlBaselineSchema.ts:74`）故可完整跨 driver 資料等價驗證——兩站點可測性程度不同，AD 原文一視同仁未區分。**★ scopeClause 巢狀 $n 稽核**（AD 要求交付項）：確認 `buildExportQuery` 於 `scopedByCreator=true` 且轄區非空時，`scopeClause` 動態產生 `$2..$(1+N)`，須與主參數 `:runId` 同一次 `escapeQueryWithParameters` 呼叫展開，不可拆兩段。分層：**ESCAPE 5**（driver 層 `escapeQueryWithParameters` 純函式驗證，**免真實 MSSQL 連線**，CI 恆常執行，含混合具名參數編號順序決定性驗證）+ **PARAM 17**（站點 1 四案例含 PG-only 錯誤分類 + 站點 2 六案例含 MSSQL 完整資料等價 + 站點 4 七案例含巢狀 $n 稽核 + SQL-only MSSQL 驗證繞過 cursor 排除）+ **LOCK 12**（🔴 P1c 最有料：DECLARE+EXEC+SELECT 批次回傳形狀探測 + 單次成功 commit/rollback 自動釋放 + 雙連線並發逾時降級/等待後取得 + 三種非鎖錯誤碼 rethrow + 前置條件探測決策關卡 + resource 字串免 hashtext 等價 + 不同 listNo 不互相阻塞）+ **DISPATCH 5**（🔴 三分支黑箱契約：mssql 分支確實觸發＋postgres/sqlite 不回歸＋成功/逾時兩種情境跨 driver 回傳形狀一致，逾時情境改用 mock 避免真實等待 production 5000ms）+ **REG 6**（tsc gate + F084 auto-advance 30 案例 + personnel-ratio 既有單元 + report service/scope 既有單元 + F103/F104 pg.spec 呼叫端不退化）+ **STATIC 3**（AD 交付物「Pattern B 完整站點清單」驗收 + fs+regex 殘留 $n 字面值零命中 + 命名鎖定）。測試環境：完全沿用既有 `mssql-env-preload.ts`/`.env.test.mssql`/`pg-env-preload.ts`，**不新增**環境設定；ESCAPE 群組刻意設計為不需任何 gating 之常駐單元測試。**P1（P1a/P1b/P1c）至此全數完成測試設計**，總場景數 2144→**2192**；總文件數 84→85。
>
> **v2.35 MSSQL 全面遷移 P1b3 Bootstrap/Seed 三支腳本改寫 + B2 npm alias 修復驗證測試設計（2026-07-07）**：新增 `infrastructure/AD-E07-39-P1b3-test.md`（**54 個場景**）。對應 AD-E07-39 §7/§8（銜接 P1b2，**跳過 spec-writer**）之 P1b3 切片（`seed.ts`/`seed-datasource.ts`/`prod-data-seed.ts` 三支腳本改寫使其對 MSSQL 可執行 + 修復 P1b2 impl log §4.2 記錄之 B2 `npm run migration:run`/`bootstrap` npm alias 於本機 Node 24 + workspace hoisting 下失效問題）。**★ 逐檔實際 grep 後新增查證出 5 類 AD §7 原文未列出、但阻擋 DoD #1 成立的站點類別**（AD 僅列 `$n`→named param、`LIMIT`→`TOP`）：三支腳本 `main()` 皆硬編碼 `type:'postgres'`（完全無視 `DB_TYPE`，比任何 SQL 語法問題更早阻擋）、PG 專屬函式 `NOW()`（10+ 處）、PG 專屬子句 `RETURNING id`（1 處）、PG 專屬型別轉換 `::text`（1 處）、PG 專屬 NULL-safe 運算子 `IS NOT DISTINCT FROM`（reconcile 引擎核心，6 張計分卡表共用）、裸布林字面值 `true`（1 處，T-SQL 不接受）。分層：**DIALECT 4**（DataSource 建構依 `DB_TYPE` 切換，前提）+ **ALIAS 6**（🔴 DoD #1 核心：字面 `npm run migration:run`/`migration:revert`/`seed`/`seed-datasource`/`data-seed`/`bootstrap` 全部 exit 0，非 P1b2 之 node launcher workaround）+ **SITE 12**（逐站點行為驗證：具名參數/`TOP`/`LOWER()`/`NOW()`時間窗/**NULL-safe 自然鍵**〔`ob_levelcard_score` 449 筆中 212 筆 `level1=NULL` 真實資料，最高風險項〕/reconcile INSERT/漂移偵測+repair/`column_label` 條件式 UPDATE/`RETURNING`→FK 可用性/JSON round-trip/布林欄位/`UPDATE...AS alias`+`EXISTS`）+ **BOOT 5**（bootstrap E2E：4 帳號登入/datasource 空殼/etl_pipelines FK/extraction_tasks 無懸空 FK）+ **COUNT 12**（含 **🔴 2 個 SCOPE GAP 決策關卡**：`roles`/`pooldata_field_whitelist`/`pooldata_field_option` 三表資料唯一來源為 PG-only `1711360000001-BaselineReferenceData.ts`，**不在 P1b1/P1b2/P1b3 任一輪改動範圍**，MSSQL 側 bootstrap 後預期仍為 0 筆，DoD #2 原文範圍與現有程式碼範圍不一致）+ **IDEM 5**（重跑兩次冪等 + drift-repair 冪等）+ **REG 5**（tsc gate + PG reconcile spec 不退化 + P1a/P1b1/P1b2 107 案例不回歸）+ **PROBE 2**（`qr.query()` UPDATE 回傳形狀探測，非阻擋 + 決策關卡彙整）+ **STATIC 3**（PG-only 語法零殘留 + 站點清單文件產出物驗收）。**Harness 設計核心發現**：raw SQL 腳本之裸表名無法透過 TypeORM `schema` 選項重新導向（與 P1b1/P1b2 entity 層測試不同），**必定**落在 `dbo`——升級 P1b2 test 文件既有 `R-MSSQL-P1B2-03`（原「低風險、現況無污染」）為「中風險、P1b3 為第二個需獨佔 dbo 的套件，理論風險成為現實執行順序需求」，建議新增序列化執行 lane（比照既有 `.pg.spec.ts` 序列慣例）涵蓋全部 `*.mssql.spec.ts`。**明確排除** `sp_getapplock`/Pattern B（P1c）；P1b1/P1b2 已完成項目不重複驗證。
>
> **v2.34 MSSQL 全面遷移 P1b2 Prod Baseline Migration + Dev/Prod Parity 驗證測試設計（2026-07-07）**：新增 `infrastructure/AD-E07-39-P1b2-test.md`（**39 個場景**）。對應 AD-E07-39 §6/§8/§9（銜接 P1b1，**跳過 spec-writer**）之 P1b2 切片（prod 手寫 T-SQL baseline migration 產出 + dev/prod 兩軌結構化 parity 驗證）。**核心紅線**：**PARITY-001~007**（不變式 I-MSSQL-BASELINE-PARITY-01：`INFORMATION_SCHEMA.COLUMNS`/`sys.indexes`+`sys.index_columns`/`sys.check_constraints` 兩路徑結構化 diff 為空，含欄位/索引集合對稱差、複合索引欄位順序守門）+ **PARITY-009（🔴 comparator 敏感度驗證：人工注入合成差異必須被偵測到，防止「永遠回報空 diff」的無效比對工具）**+ **STATIC-001（🔴 P1b1 impl log §5.6 本機 MSSQL 容器 `sp_executesql` 拋 17750 caveat 之直接靜態守門）**。**★ 兩路徑 Harness 方案**（回應核心設計挑戰）：因 `docker/mssql-init.sql` 查證 `cdmp` login 僅獲 `db_owner`（資料庫層級角色），**無**伺服器層級 `CREATE DATABASE` 權限，故不採「兩個獨立資料庫」方案，改於同一 `CDMP_TEST` 資料庫內以 **schema 區隔**：Path A（synchronize）＝新建專屬 schema `p1b2_sync`（TypeORM `schema` 連線選項自動前綴 DDL）；Path B（baseline migration）＝**直接使用 `dbo`**（不新建 schema——因手寫 migration 為 raw SQL，TypeORM 不會改寫其中的表名前綴，會落在連線 session 的 default schema，即 `dbo`，此**恰好與 prod 真實部署路徑完全一致**，非模擬近似）；`dbo` 於 `CDMP_TEST` 設為本文件獨佔保留 schema，其餘測試套件不得使用。分層：**BASELINE 6**（字面 CLI `npm run migration:run` 於 `NODE_ENV=production` 建出 36 表零錯誤 + migration 冪等 + FK 正確 + `dbo` 前置乾淨守門）+ **PARITY 10**（DoD 核心：欄位/索引屬性 diff + 欄位/索引集合對稱差 + check_constraints 聯合零值 + 複合索引欄位順序 + comparator 自我一致性 + **comparator 敏感度驗證** + 結構化輸出格式）+ **TIERFN 3**（`fn_calc_tier_level` 動態 OBJECT_ID 為 NULL + 靜態掃描 + 雙路徑對照）+ **FILTER 3**（F-2：`has_filter=1` 兩路徑皆零 + 靜態掃描）+ **COLLATE-BASELINE 3**（I-MSSQL-COLLATE-01 於 baseline 路徑：collation 唯一值 + 無逐欄覆寫 + 靜態掃描 `COLLATE` 關鍵字零命中）+ **CASE-BASELINE 2**（I-MSSQL-CASE-01 於 baseline 路徑）+ **HASH-BASELINE 3**（B1 於 baseline 路徑：`token_hash binary(32)` 結構 + PK 寬度 32 bytes + 與 PARITY 交叉引用零 diff）+ **STATIC 4**（`sp_executesql` 零命中 + 非 pg_dump 專屬語法（`::`/`SERIAL`/`RETURNING`/`gen_random_uuid()`等）零命中 + migration 檔案合法性 + 建議項 `down()` round-trip）+ **REG 5**（tsc gate + P1a/P1b1/既有套件不回歸 + `dbo` 清理閉環驗證）。表數沿用 P1b1 已修正之 **36**（非 AD 原文 37，算術 off-by-one）。**明確排除** P1b3（bootstrap/seed 三支腳本改寫）與 P1c（`sp_getapplock`/Pattern B），留待各自測試設計；P1b1 已完成之 entity 層/synchronize 路徑驗證本輪不重複，僅在 HASH-BASELINE 群組驗證 B1 於**新路徑**（baseline migration）之正確性。
>
> **v2.33 MSSQL 全面遷移 P1b1 全 Entity 型別修正測試設計（2026-07-07）**：新增 `infrastructure/AD-E07-39-P1b1-test.md`（**43 個場景**）。對應 AD-E07-39（system-architect 產出，銜接 AD-E07-38，**跳過 spec-writer**，AD-E07-38 §3 D-7 已裁定）之 P1b1 切片（全 37 entity 型別轉換 47 處 + B1 `token_blocklist` 改 `token_hash binary(32)` + D1 全 entity 統一載入 + varchar 中文編碼 smoke test）。核心紅線：**TYPE-005/006/007**（🔴F-1：4 處裸 `type:'timestamp'` 字面值為 `rowversion` 舊式同義詞之靜默地雷，修復後須明確驗證為 `datetime2` 而非 `rowversion`，並以全表掃描 + 正面 Date 寫入雙重佐證）+ **CHI-DECISION-001**（🔴F-4：中文編碼 test-first 決策閘門，「借新還舊」/「中古車商」逐字元 round-trip，兩種結果皆已定義後續行動，「不符」分支並標出後續 varchar→nvarchar 測試設計方向供未來接手）+ **HASH-E2E-001**（B1 DoD 核心：`token_blocklist` 改 hash PK 後之真實 JWT 撤銷流程端對端測試，login→logout→同 token 再請求應 401，且不再受 P1a 已實測發現之 900-byte 索引鍵上限限制）+ **PKWIDTH-001/002**（新不變式 I-MSSQL-PK-BYTELIMIT-01：以動態查詢 `sys.indexes`/`sys.index_columns` 取代人工估算表格，長期防禦未來新增欄位違反索引鍵寬度上限）。分層：**CHI 5**（varchar 中文編碼 smoke，可最先獨立執行）+ **TYPE 8**（uuid 18 處/text 17 處/boolean 8 處矩陣驗證 + F-1 timestamp 4 處紅線 + 全域掃描 + bigint 2 處）+ **HASH 12**（`hashToken` 決定性/SHA-256 正確性 + token_hash 型別 + PK 寬度正面驗證 + 既有測試遷移 + 4 個 E2E 撤銷流程案例 + 三 driver helper 回歸）+ **ENTITY 6**（D1：`app.module.ts`/`worker-app.module.ts` mssql 分支統一載入全 37 entity，取代 P1a 過渡態之硬寫 4-entity 陣列；I-MSSQL-ENTITY-LIST-PARITY-01 靜態守門）+ **DEFAULT 2**（8 處 `CURRENT_TIMESTAMP` default smoke）+ **CASE/COLLATE 4**（I-MSSQL-CASE-01/COLLATE-01 全 37 表擴大版）+ **PKWIDTH 2** + **REG 4**（tsc gate + 既有 pg.spec 套件回歸 + **P1a 既有案例 `TS-MSSQL-P1A-CRUD-003b` 因 B1 欄位改名產生之遺留矛盾處置** + I-MSSQL-HELPER-SCOPE-01 全 37 entity 延伸）。**明確排除** P1b2（`I-MSSQL-BASELINE-PARITY-01`、`fn_calc_tier_level` 最終驗證、prod baseline migration）與 P1b3（bootstrap/seed 三支腳本改寫）及 P1c（`sp_getapplock`/Pattern B），留待各自測試設計。測試環境：完全沿用 P1a 既有 `mssql-env-preload.ts`/`.env.test.mssql`，新增 `p1b1` SQL schema 隔離慣例（與 `p1a` 並存）。總場景數 2008→**2051**；總文件數 81→82。
>
> **v2.32 MSSQL 全面遷移 P1a 最小可連線＋登入測試設計（2026-07-07）**：新增 `infrastructure/AD-E07-38-P1a-test.md`（**30 個場景**）。對應 AD-E07-38（system-architect 產出，**跳過 spec-writer**——P1 全範圍為純底層儲存/驅動置換，無新業務行為，AD §3 D-7 已裁定）之 P1a 切片（三處 TypeORM 設定點 mssql 分支 + auth 最小 4-entity 子集 `User`/`Role`/`TokenBlocklist`/`PasswordResetToken` + `column-types.ts` 既有 3 helper 擴充 mssql 分支）。核心紅線：**TYPE-001/002**（D-1 明確標註「不可假設，須真實容器驗證」之兩個型別探測點：裸 `'uuid'` 是否映射 `uniqueidentifier`、裸 `'text'` 是否誤落已棄用 `TEXT` 而非 `nvarchar(max)`；結果直接決定 `uuidColumnType`/`longTextColumnType` 兩個新 helper 是否必要，P1a DoD #5）+ **CASE-001~003**（I-MSSQL-CASE-01：BIN collation 下大小寫敏感守門，含 CASE-003 陽性對照組驗證守門非巧合）。分層：**CONN 6**（三設定點啟動 + P1a 範圍刻意限定僅 4 表 synchronize，不可提前擴大至 P1b 全 37 entity + encrypt/trustServerCertificate 獨立可配置）+ **TYPE 7**（uuid/text/bigint/datetime2/bit 五型別 sys.columns 探測 + CreateDateColumn 隱式型別探索 + 決策關卡）+ **LOGIN 3**（種子 admin 登入回傳合法 JWT + 密碼錯誤 401 + 跨 driver JWT payload 等價）+ **CRUD 7**（uuid PK round-trip + bcrypt 密碼欄位 + 長字串 token + datetime2 毫秒精度 + varchar PK + nullable date + bigint 邊界值）+ **CASE 3** + **REG 3**（tsc gate + helper 新增 mssql 分支後 sqlite/postgres 既有值不變 + 未知 DB_TYPE 現況記錄）+ **HELPER 1**（I-MSSQL-HELPER-SCOPE-01：4 entity 內無重複 `process.env.DB_TYPE` 判斷）。**明確排除**（分流至 P1b/P1c 各自測試設計）：全 37 entity baseline（I-MSSQL-BASELINE-PARITY-01）、collation 全表一致性（I-MSSQL-COLLATE-01）、`sp_getapplock`（I-MSSQL-LOCK-01）、Pattern B named param（I-MSSQL-PARAM-01）。測試環境設計：`*.mssql.spec.ts` 命名慣例（比照既有 `.pg.spec.ts` gating-skip 模式，連不上即整檔 skip 不假綠）+ `mssql-env-preload.ts`（比照 `pg-env-preload.ts`）+ 建議新增 `.env.test.mssql`。**已識別風險**：現行 `docker-compose.yml` 之 `mssql`/`mssql-init` 僅建單一 dev 用 `CDMP` 資料庫，無如 `postgres-test`（5433/`cdmp_test`）之獨立 test-only port/DB 分離，已記入 `risks-and-gaps.md`。
>
> **v2.31 F109 新增「客戶資料」來源篩選欄位（2026-07-02）**：新增 F109 test spec（**94 個場景**）。F075 白名單引入第二個資料來源「客戶資料」（`customer_core`），新增 8 個篩選欄位（性別/年齡/職業別/教育程度/婚姻狀況/身分別/收入區間/居住城市）。**架構裁定（AD-E07-37）：customer_core 為 PG-only**（無 TypeORM Entity、僅存在 PostgreSQL），所有含 customer_core 條件之測試一律 `.pg.spec.ts`（**約 38 案例強制需 Postgres**，與 F098~F104 pg.spec 序列執行）；純案件資料 regression guard 維持 SQLite。核心紅線：**NULLEXC 群組**（AC-8 三變體：無對應客戶／客戶欄本身 NULL／無客戶條件不觸發，三者獨立驗證）+ **JOIN-003「EMPTY_CONDITIONS 陷阱」**（AD 特別點名：僅含 customer_core 條件之名單不可被誤判整批 skip）+ **EQ 群組**（BR-10 三處消費一致 DoD：`buildStage1Sql` PG 下推 ↔ `executeStage1Chain` 同一 PG DB 逐列等價）+ **AGE-004 決定性**（BR-5：同一 `project_workym` 重跑年齡結果一致，供 F067 apples-to-apples）。分層：**WL 8**（data_source schema + API dataSource + seed-only 邊界）+ **OPT 10**（7 categorical 欄位可選值 seed，dev 實測真實值 3/55/8/5/4/9/22）+ **DESC 2**（6 個 `_desc` 欄 value=label 查詢層對比）+ **GENDER 3**（唯一 code→label 欄位）+ **DATASRC 7**（OQ-F109-01：`resolveConditionDataSource` 雙層機制 + `stampConditionDataSource` 寫入路徑）+ **JOIN 8**（AC-11/BR-2：條件式 LEFT JOIN 觸發 + EMPTY_CONDITIONS 陷阱 + 命名空間隔離）+ **NULLEXC 7**（AC-8/BR-3/BR-4 核心紅線）+ **AGE 8**（AC-6/BR-5：衍生 AGE 運算式 + 前端 min/max 驗證 + 決定性）+ **CITY 4**（AC-7/BR-6：`LEFT(cpost_city,3)` 縣市級）+ **AND 3**（AC-10/BR-8：跨來源 AND）+ **DEACT 3**（AC-9/BR-9：停用不回溯）+ **EQ 6**（BR-10 DoD）+ **PARAM 2**（I-CC-PARAM-NS-01）+ **JOINCARD 2**（I-CC-JOIN-CARD-01：`source_customer_no` UNIQUE 索引基數保證）+ **COMPSCOPE 2**（I-CC-COMPOSER-SCOPE-01：composer 不得內建 `cc.` 前綴邏輯）+ **API 2**+ **MIGSEED 3**（m305/m306 冪等）+ **FRONTEND 7**（M06 資料來源欄 + F050/F051 來源分組 dropdown + 年齡表單）+ **REG 5**（既有 40+ composer 測試 + F100~F104 pg.spec 不退化 + tsc gate）+ **STATIC 2**（命名鎖定）。所有 5 個架構 Open Question（OQ-F109-01~05）已由 AD-E07-37 裁定；5 個不變式（I-CC-DATASOURCE-01/JOIN-CARD-01/NULL-EXCLUDE-01/COMPOSER-SCOPE-01/PARAM-NS-01）逐一對應測試場景。
>
> **v2.30 F108 匯出新增「樞紐分析」頁籤（2026-06-29）**：新增 F108 test spec（**40 個場景**）。延伸 F064 v2.1 xlsx 匯出，新增第 2 頁籤「樞紐分析」靜態交叉表（% of parent row，部門×員編×名單代號）。DoD = **PARENTROW-001**（spec §6.2 Worked Example oracle 全 18 格，浮點誤差 < 1e-9）+ **SCOPE-001**（SCOPE 紅線：樞紐不洩漏轄區外部門/員編）+ **REGRESSION 群組**（第 1 頁 23 欄不受影響 + CSV 不含樞紐）+ `tsc --noEmit` 乾淨。分層：**SHEET 4**（2 頁結構 + CSV 不含樞紐 + 靜態 grep）+ **HEADER 4**（R1 部門代號/(全部) / R2 空 / R3 計數-案號/欄標籤 / R4 列標籤/listNo 升冪/總計）+ **PARENTROW 7**（oracle 全 18 格 + 部門列加總 100% + 員編列加總 100% + 總計列 + 總計欄部門層 + 總計欄員編層 + numFmt='0.0%'）+ **ZEROBLANK 3**（0/正數→0.0% / 0/0→null / 部門層 vs 員編層區分）+ **DET 4**（listNo 升冪 / 部門 localeCompare / 員編升冪 / (空白) 最後）+ **BLANK 3**（emphire join-miss + §6.4 oracle 重算 + emplid null 歸組）+ **SCOPE 3**（section_chief scoped + director bypass + 靜態 grep）+ **EMPTY 3**（0 列時 2 頁仍存在 + 標頭 + HTTP 200）+ **REGRESSION 4**（23 欄 + 列數 + CSV + 靜態 grep，DoD 紅線）+ **STATIC 3**（commit 時序 + 記憶體安全 + tsc gate）+ **PG 2**（選配：真實 run 84486ddd + 32/34/15/18% 分佈）。**2 個案例需 Postgres**（PG-001/002，選配慢速套件）。Mock 策略：沿用 f064-export-23col.spec.ts 模式（mock `cursorRows`，用 exceljs `Workbook.xlsx.load(buffer)` 讀回斷言）。TC-171-01~12 全部覆蓋（AC-1~8 + I-PIV-SHEET-01/PARENTROW-01/SOURCE-01/MEM-01/DET-01 全覆蓋）。
>
> **v2.29 F049 v2.0 Stage 0 業務化重設計 Part B（2026-06-29）**：更新 `features/F049-test.md`，**新增 43 個場景**。DEPT 4（手算 oracle）+ GAP 6（缺口機制 + 捨入容差）+ SCOPE 6【SECURITY-CRITICAL：SCOPE-002 JSON.stringify 全掃非轄區字串 MUST-HAVE】+ FEAS 6（人均、headcount=0、超門檻、threshold=null、TRIM 移植性、scope headcount 隔離）+ INVAR 2（I-RUN-EST-01 spy + list_total 物化/fallback）+ EDGE 3（partial timeout / 0 名單 / 轄區無比例）+ AGG 5（AGG-001~005 前端 RTL+MSW）+ FE 6（FE-001~006：矩陣/gap/處長 banner/scope=null/overThreshold/派案日曆）+ TERM 5（TERM-001 DOM 黑名單 MUST-RUN regression + TERM-002~005）。全部 OQ 已裁定；oracle 手算寫定；所有 43 個場景 SQLite 可覆蓋（無強制 PG）。總場景數 1801→**1844**；F049-test.md YAML spec_version=2.0 + covers US-166~170。
> **v2.28 F104 Stage 2 全欄對齊 legacy SP（2026-06-24）**：新增 F104 test spec（**82 個場景**）。F103 等於對齊了一份有偏差的 AD-E07-10-L；本輪修正兩路徑（PG `resolveColumnSource`/`buildStage2ScoreExpr` + JS `resolveColumnValue`/`computeScore`）使其對齊 legacy SP 真語意。DoD = **EQ 12**（JS↔PG 逐列等價，PG 真庫，§8 矩陣全覆蓋）+ **cus_sex NULL-safe cast**（髒值 'C'/'D' 不拋例外，高嚴重度）+ **兩 default 分離**（計分 3 / gating 個人）+ **per-card default 逐格**（AD-E07-33 矩陣）。分層：**KW 6**（PROJECT_TP 借新還舊 + SALES_STS 中古車商，舊關鍵字全清）+ **SEX 4**（CUS_SEX category→range + safe-cast）+ **BRANCH 10**（五欄 isCorp 分流：個人取自身/法人 0-default/空→個人/髒值→法人）+ **SAFE 6**（cus_sex NULL-safe cast SQL 不拋例外）+ **AGE100 5**（>100 排除 + 法人 0）+ **EDU 7**（補零 + range 字串 BETWEEN + per-card default S→'02'/S5→'08'）+ **CITY 10**（三縣市欄 LEFT3 + per-card default）+ **PCD 8**（LIST_MONTH/LOAN_RATE per-card default）+ **EQ 12**（DoD）+ **SIG 4**（簽章加 cardType + CustomerCoreRow 改名）+ **UPGRADE 5**（202606 重跑 + 10 筆抽樣手算）+ **REG 5**（F103/F100/F101/F102 不退化 + tsc gate + 舊欄名靜態掃描）。**約 48 案例需 Postgres**（與 F098~F103 pg.spec 序列）。**test-designer 連 DB 查證**：EDUCAT_BACK=range（0 cat/29 range，level2 字串 BETWEEN）✅、三縣市欄=category ✅、CUS_SEX=range ✅、cc 新欄存在 ✅、cus_sex 髒值分佈（'C'/'D'/'8'/空）✅。**legacy SP 查證**：'C' 髒值 → 法人（`NOT IN('1','2')`），**OQ-TDS-F104-01 待 architect 確認**（AD line 4103 散文與算式矛盾，建議釘法人）；E5 無 CAREA_NO1 唯有 CELLULAR、HM 複用 M 設定。命名鎖定：`resolveColumnSource(columnName, cardType)` / `resolveColumnValue(pool, columnName, cc, arCap, cardType)` / `isCorporate` / `calcAgeYears` 加 >100 守門 / CustomerCoreRow 新欄名（cus_sex/carea_no1/carea_no2/cellular/hpost_city/cpost_city/co_city）。
> **v2.27 F103 月名單分派計分引擎欄位來源修正（2026-06-24）**：新增 F103 test spec（**59 個場景**）。修正 Stage 2 計分引擎兩條路徑（PG 下推 `resolveColumnSource` + JS oracle `resolveColumnValue`）未完整對齊 AD-E07-10-L 映射表導致系統性低估（H 卡 score 81~152，理論上界 255，無案件達 card C 門檻 185 → 全 card D → 全 T3）。DoD = **EQ 8**（JS `computeScore` ↔ PG `buildStage2ScoreExpr` 生成 SQL，相同輸入 score 完全相等，誤差=0，PG 真庫）。分層：**AR 5**（I-SCORE-AR-JOIN-01：ADD_UN_CAPITAL `needsArCapital` flag + LEFT JOIN ob_arreturndf_min_cap + JS arCap=null/有值）+ **EQ 8**（DoD 紅線：ADD_UN_CAPITAL/cc=null/arCap=null/PROJECT_TP 含與不含「專案」/SALES_STS/AGE 三邊界/通用 fallback 各 1 場景）+ **AGE 4**（I-SCORE-AGE-01：生日前一天/當天/後一天 `calcAgeYears` 純函式 + cc=null fallback 0）+ **FALLBACK 5**（I-SCORE-FALLBACK-01：PG 有值/幽靈 key/非數值文字 + JS 有值/幽靈 key）+ **GHOST 4**（I-SCORE-GHOST-01：不拋例外/logger.warn/+0/月名單分派繼續，PG+JS 各測）+ **COMMISSION 4**（I-SCORE-COMMISSION-01：resolveColumnSource/resolveColumnValue/MAPPED_SCORING_COLUMNS 不含 COMMISSION/含 ADD_UN_CAPITAL 靜態掃描）+ **CC 10**（AC-8 JS oracle 全補齊：CUS_SEX/CAREA_NO1/CAREA_NO2/CELLULAR/AGE/EDUCAT_BACK/HPOST_NUM_NM/CPOST_NUM_NM/CO_NUM_NM/LOAN_RATE 各欄 null/有值）+ **PROJECT_TP 4**（BR-F103-03：JS 含/不含「專案」/null fallback/PG CASE WHEN 靜態）+ **AUDIT 5**（AC-5/7：MAPPED_SCORING_COLUMNS 完整性/CAREA 語意/ADD_UN_CAPITAL range/FALLBACK 永不 undefined）+ **PREFETCH 3**（I-SCORE-PREFETCH-01：customer_core IN 1 次/arreturndf IN 1 次/10 案件規模）+ **UPGRADE 3**（AC-11/12：202606 重跑定性 ≥2 種 card_level + tier 含 T1/T2/異常時空值率量測/score MAX 提升）+ **REG 4**（AC-13：F100/F101/F102 全綠/tsc gate/`pnpm test` 序列）。**43 案例強制需 Postgres**（EQ 8 + AR 3 + FALLBACK 3 + GHOST 2 + PREFETCH 3 + UPGRADE 3 + REG 2）。已裁定決策：OQ-1（呼叫端 batch pre-fetch merge，`cc`/`arCap` 新參數）/ OQ-2（同 OQ-1 流程）/ OQ-3（SQLite 單元測試無需建 cc/ar 表）/ OQ-156-02（通用 fallback 本輪納入）/ OQ-157-01（AGE 統一演算法）/ OQ-158-01（定性驗收）/ OQ-158-02（本輪根因，不推延）。命名鎖定：`computeScore(pool, cardType, cardVersion, activeColumns, allScores, cc: CustomerCoreRow | null, arCap: ArCapitalRow | null)` / `resolveColumnValue` / `resolveColumnSource` / `calcAgeYears(dateOfBirth, now)` / `needsArCapital` / `MAPPED_SCORING_COLUMNS`。
> **F097 作業月語意統一新增（2026-05-27）**：新增 F097 test spec（48 個場景）。涵蓋後端 `SystemService.getDefaultTargetWorkYm()`（一般月 +1 / 跨年邊界 / OVERRIDE）、`POST /api/v1/assignment/runs` DTO `workYm` 三分支驗證（缺省 400 / 格式錯 422 `WORK_YM_INVALID_FORMAT` / 過去月 422 `RUN_WORKYM_PAST`）、過去月 guard 邊界（`>=` 語意：目標月 1 號當天合法）、三 controller `computeCurrentWorkYm()` 移除 regression、`project_workym` 寫入目標月（非執行月）驗證、Stage 1 去重視窗 `workdt` 對齊（`project_workym='202606'` → 上界 `2026-05-31`，後移一月 regression）、`computeDedupWindow` 函式不改靜態 git diff 驗證（AC-20）、前端 `AssignmentWorkYmContext` 四頁同步 / `run-history` 獨立 / 處長 MonthPicker disabled / 下游結果頁無 MonthPicker 靜態月份、UI 標籤「分派作業月份」regression、forward-only 注釋存在性、E2E 全鏈整合。命名鎖定對齊 glossary.md（`current_work_ym` / `target_work_ym` / `project_workym` / `workdt` / `AssignmentWorkYmContext` / `getDefaultTargetWorkYm` / `RUN_WORKYM_PAST`）。
> **Stage 1 精確化工程 Phase A/B 更新（2026-05-27）**：F090 v2.0（data_source 單源化 `etl_load` + ETL Delete 全量放寬 + 月名單分派不再寫本表 Regression）；F091 v2.0（特例 DELETE trigger 關鍵字 SP bug fix：期中機車 / 期中 / 年以上，v1.0 誤判字中結/強案/年資/滿已廢棄；去重上界動態計算 `MIN(MAX(assignday), workdt-1)`；year_produ 改 parseInt）；新增 F094（ob_monthly_run_result migration + Stage 1/3/4 落點切換 + FK CASCADE）；新增 F095（appliedSpecialRules[] 讀時推導 + 觸發一致性 + 前端唯讀 Component RTL）；新增 F096（pooldata_field_whitelist list_type 停用 m293 + available-columns regression + 既有條件相容）。⚠️ **F091 v2.0 SP bug fix 為最高風險變更**：v1.0 SD-002~006（中結/強案/年資觸發）全面廢棄，以 SDv2 系列取代；所有 mock `list_nm` 字串須更新為 v2.0 正確繁體中文（期中機車/期中/年以上）。
> **Stage 1 精確化工程新增（2026-05-26）**：新增 3 個 test spec 檔（F090 / F091 / F092）。Phase 1（F090）ETL + data_source schema 13 個場景；Phase 2（F091）Stage 1 三步驟補完整 30 個場景（MONTH_CNT × 6 + 去重 × 5 + 特殊 DELETE × 8 + 封裝 × 5 + Regression × 3 + 誤差 × 3）；Phase 3（F092）dry-run 精確估算 26 個場景（唯讀 × 4 + 一致性 × 3 + estimateListCount 升級 × 4 + F049/F088 升級 × 4 + Regression × 3）。⚠️ F091 為唯一改變 production 月名單分派案件數的階段（deploy 後立即生效、無 flag）；既有 Stage 1 pipeline integration test baseline 需同步更新。
> **M01 v2.0~v2.3 Kanban 重構 + Detail Snapshot + Signal（2026-05-21）**：新增 9 個 test spec 檔（F048/F049/F052/F061 v1.4/F077/F081/F085/F089；F050 升版 v2.2）。新增場景合計 **103 個**：F050 v2.2 +19（SS 群 12 + SIG 群 7）、F048 v2.0 新建 15（K 群 8 + 搜尋 2 + Drawer 3 + Banner 2）、F049 v1.1 新建 5（CTA-001~005）、F052 v2.1 新建 3（TXT-001~003）、F077 v1.3 新建 23（矩陣 15 + 橫切 6 + Integration 2）、F081 v1.3 新建 6（Integration 4 + Component 2）、F085 v1.3 新建 6（Integration 4 + Component 2）、F089 v1.3 新建 7（Integration 4 + Component 3）、F061 v1.4 新建 3（CTA-001~003）。Deprecated 標記：F048 v1.0 表格列 AC-1 + 頁籤 AC-5；F052 「停」縮寫按鈕斷言。
>
> **F050 v2.1.1 補強（2026-05-20）**：F050 新增 45 個場景（TS-F050-A01~K01c）：US-126 card-type dropdown 建立頁（A/D/E/H 群組）、US-127 card-type dropdown 編輯頁（I 群組）、US-128 prodBest 欄位移除（B/C/F 群組）、US-129 best_case Y/N options seed（A/G 群組）、E2E 整合（J 群組）、Regression Guard fs+regex（K 群組）。F076 v1.6 新增 3 個場景（TS-F076-009~011）：best_case Y/N seed 正確性、N 標籤覆寫驗證（UPSERT DO UPDATE）、冪等。F075 v1.6 新增 3 個場景（TS-F075-051~053）：M-A1 whitelist seed 驗證、冪等、API 整合。共 +50 個場景。
> **E07 M08 Whitelist-Driven 新增（2026-05-20）**：新增 F050 v2.1（30 場景）、F051 v2.1（19 場景）、F076 v1.5（8 場景）、F068-deprecated（9 場景）測試設計，共 66 個場景。新增 M01-whitelist-driven-integration-test.md（21 場景，含 OQ-TEST-001 caseyear wildcard 3 個 + OQ-TEST-002 _backfill_empty skip 2 個）及 M01-migration-test.md（26 場景，M1~M5）。M06-regression-guards.md 更新 v2.0（+3 個 F068 廢棄 guard）。F075 +2 個 v1.5 配套場景（TS-F075-049/050）。GAP 47/47 + §18.10 10/10 + K1~K5 100% 覆蓋。
> **E07 M06 篩選欄位管理新增（2026-05-18）**：新增 F075（POOLDATA 篩選欄位白名單管理 v1.4）測試設計，共 48 個場景。涵蓋 `GET /api/v1/pooldata-fields/available-columns` 端點（AC-10~AC-15）、`getAvailableColumns()` service 單元測試（4 場景）、`_inferSuggestedFieldType()` pure function 逐型別驗證（14 場景）、SQLite E2E 權限矩陣 / Feature Flag / 路由排序（8 場景）、PostgreSQL Test Container 過濾邏輯（2 場景）、前端 dropdown / hint 狀態機 / toast（16 場景）、跨模組整合（2 場景）、命名漂移 regression guard（2 場景）。環境策略：方案 C 分層（Guard/路由 → SQLite；過濾邏輯 → `pooldata-available-columns.integration-spec.ts`）。新增 M06 regression guard 文件（`regression/M06-regression-guards.md`）。
> **E07 M07 角色整合補修 v2.0（2026-05-16）**：business_role 合併重構對齊 AD-E07 v3.0。移除 TC-ORTHO-400~407（正交維度 section，is_sales_manager 廢棄）；TC-E02-100~108 endpoint 更名為 `/business-role`；TC-AUTH-200~205 claim 更名為 `businessRole`；Guard 更名為 DirectorOrAdminGuard / SectionChiefOrAboveGuard，移除 SalesManagerGuard 相關場景。新增：TC-MERGED（合併互斥約束，10 場景）、TC-MIG（m14 遷移，8 場景）、TC-LEGACY（legacy JWT，5 場景）、TC-DEPRECATED（廢棄端點，5 場景）。總場景數由 43 增至 63（+20）。Fixture builder 更新：新增 buildLegacyUser()、移除 buildUserOrthogonalSectionChief() / buildUserWithSalesManagerFlag() / buildUserE07Null()，更名 buildDirectorUser / buildSectionChiefUser / buildRegularUser。新增開放問題 OQ-MIG-001 / OQ-DEPR-001。
> **E07 M07 角色整合 v1.0（2026-05-16）**：初版 43 場景，涵蓋 PATCH `/accounts/:id/e07-role` 端點（TC-E02-100~112）、JWT payload e07_role claim（TC-AUTH-200~205）、Guard 單元測試（TC-GUARD-300~315）、正交維度 regression（TC-ORTHO-400~407）
> **E07 M02 計分設定新增（2026-05-13）**：新增 F053（查看計分維度，13 場景）、F054（編輯計分維度與分數，24 場景）、F055（編輯 CARD_LEVEL 門檻，21 場景）、F056（編輯 TIER_LEVEL 對應表，28 場景），涵蓋覆寫式編輯語意、月名單分派鎖（pending/running）、稽核 log before/after、Fallback CARD_TYPE（M5/M3/HC/C3）、fn_calc_tier_level NULL fallback 跨層整合、S5 兩級 vs H 四級等級數差異、BR-9 card_level 長度驗證、PUT/POST 端點語意分離
> **F002SM 新增（2026-05-13）**：新增 Sales Manager 旗標顯示於 Top Bar 測試設計（25 場景），涵蓋後端 Login API `isSalesManager` 欄位補充、Frontend TopBar Badge 元件、JWT payload 驗證、edge case（舊 token / 旗標升降級）
> **E06 Customer 360 新增（2026-04-13）**：新增 F046（客戶搜尋與清單，31 場景）與 F047（單一客戶詳情，38 場景），涵蓋 Full-Text Search、精確比對、遮罩規則（Admin/User）、風控旗標高亮、ETL 資料新鮮度、客戶類型適應顯示、404 錯誤處理
> **E06 角色精簡更新（2026-04-13）**：US-017 角色從 8 種（2 系統角色 + 6 業務角色）精簡為 2 種（admin / user）。F045 更新（15→13 場景）；F004 精簡（14→9 場景）；F005 精簡（14→10 場景）；F008 精簡（20→12 場景）；移除業務角色相關測試案例
> **F029/F043 更新（2026-03-31）**：新增 Lookup 節點雙輸入重設計測試：F029 補充 6 個前端 Lookup UI 場景（TS-F029-032~037，31→37 場景），F043 補充 14 個 LookupExecutor 場景（TS-F043-045~058，44→58 場景），涵蓋 US-042 AC-7a~7d 與 US-058 AC-1~6
> **F042~F044 新增**：2026-03-27 新增 ETL 執行引擎測試設計：F042 核心框架（21 場景）、F043 節點執行器（44 場景）、F044 Target Load（17 場景）
> **F039~F041 新增**：2026-03-27 新增 ETL Pipeline 編輯器「節點欄位變化」測試設計：F039 Badge（22 場景）、F040 Inspector Diff（6 場景）、F041 Tooltip（12 場景）
> **F036 更新**：2026-03-25 依 US-049 修訂版重新設計，目標表由 4 個改為 1 個（customer_core，85 欄位），場景數由 20 增至 40（新增 ETL 轉換規則、衝突解決、前端介面測試）
> **v2.20 F098 月名單分派 Worker 抽離 P1（2026-06-02）**：新增 F098 test spec（**51 個場景**，**僅 P1**，不含 P2/P3 SQL 下推）。涵蓋 AD-E07-28 P1「月名單分派由 cdmp-api 同程序 `setImmediate` 改為入列 pg-boss → 獨立 `cdmp-worker` 容器消費」。分層：TRIG 6（I-TRIGGER-01 核心：`triggerRun` 改入列、`runPipeline` 0 次、立即回 202）+ CONS 6（worker 消費 → status pending→running→completed/failed）+ RETRY 3（`retryLimit=0` OQ-AD28-04）+ SER 3（單 worker 序列化 OQ-AD28-05 + `assertNoRunningRun` 不回歸）+ CANCEL 7（`CancellationPoller` 修現有「背景不真停」bug：偵測 failed → 拋 `RunCancelledException` → 不寫快照/result；list 級取消粒度）+ ORPHAN 7（`OrphanReaper` 殭屍 running 回收 + 誤殺邊界 + 不新增 schema 欄位 OQ-AD28-02）+ PGINT 5（真 pg-boss 入列/消費/冪等/expiration + schema migration OQ-AD28-01）+ NFR 3（月名單分派期間 API 仍可回應，解 F1）+ WORKER 4（worker entrypoint 不掛 HTTP / 共用 flag）+ RG 5（回歸基準 + `setImmediate` 移除 grep + tsc gate）+ OQ 2（OQ-F098-01 待裁）。**18 個案例強制需 Postgres**（PGINT/NFR + RETRY/SER/CANCEL/ORPHAN 之 PG 子案例），連動 CI 須能起 Postgres Test Container。命名鎖定：`RunQueueProducer` / `RunQueueConsumer` / `CancellationPoller` / `OrphanReaper` / `RunCancelledException` / queue `'assignment-run'` / payload `{ runId, ym }` / error_message `'worker 中斷，請重新觸發'`。
>
> **v2.24 F102 月名單分派 CR 優先分派（2026-06-12，含 I-CR-ASSIGNDAY-01 修正）**：新增 F102 test spec（**55 個場景**）。補足 F101 simplified is_cr 缺口：在 Stage 2 之後、F101 Stage 3 之前插入 CR 前置三步驟（失效清空 + CR 優先指派 + 扣量）。DoD = EQ 7（JS `applyCrPriority` ↔ PG `runCrPrioritySql` 逐列六欄等價，含 EQ-007 CR ASSIGNDAY 散佈）+ 邊界 oracle 誤差=0（STEP1/STEP2 嚴格小於）+ I-CR-ORDER-01 靜態驗證 + AC-12 靜態掃描（cr_reassignment_enabled=0）。⚠️ **Bug Fix（I-CR-DEDUCT-01 修正 + I-CR-ASSIGNDAY-01 新增）**：`is_cr='Y'` 排除僅作用於 Stage 3/4 配額基數，ASSIGNDAY 散佈案件池 = `emplid IS NOT NULL`（含 CR 案件）；CR 案 assignday 全非 NULL（legacy 202606 驗證 2,079/2,079）。分層：**GATE 6** + **STEP1 5** + **STEP2 5** + **STEP3 5** + **DEDUCT 7**（原 4 + 新增 DEDUCT-005 CR 案 assignday 全非 NULL / DEDUCT-006 散佈千分比 / DEDUCT-007 雙重斷言）+ **EQ 7**（原 6 + 新增 EQ-007 CR ASSIGNDAY 等價）+ **IDEM 3** + **S2CLEAN 2** + **S1SRC 3** + **ORDER 2** + **DET 3** + **REG 4** + **UPGR 3**。**52 案例強制需 Postgres**。**F101 受影響測試**：TS-F101-EMPL-005 / TS-F101-REG-004 / TS-F101-IDEM-001 / TS-F101-EQ-008 需更新 seed（指定 cr_enabled=false）。命名鎖定：`applyCrPriority` / `runCrPrioritySql` / `clearStage3Fields` / `CrCase` / `CrAssignment` / `CrPriorityContext` / twoYearsAgo='YYYY-MM-DD' 字串計算。
>
> **v2.23 F101 Stage 3/4 真實比例分派（2026-06-05）**：新增 F101 test spec（**51 個場景**）。取代 F100 placeholder Stage 4（dept[0]+defaultEmpl → 比例真實分派）。DoD = EQ 8（JS↔SQL 逐列四元組等價，PG 真庫）+ 手算 oracle 誤差=0（DEPT/EMPL/ASGD）。**I-NO-ST4-EXCHANGE**：st4_exchange（10% T1/T2→senior）永久廢除（SP 202408起硬編碼 `RETURN`），3 個靜態 grep guard + 2 個行為回歸守住。警告通道（OQ-F101-05）= `skipped_cases.warnings[]`（JSONB）+ `warning_summary`（VARCHAR 100），不擴 audit_log。所有 OQ（01~05）均已裁定。44 案例強制需 Postgres；F098/F099/F100/F101 pg.spec 序列執行（共用 cdmp_test DB）。oracle seed 寫死：Seed 1（101件，AI000=51/AM000=30/B0000=20）/ Seed 2（73件，diff=2）/ Seed A（51件，E1=21/E2=18/E3=12）/ ASSIGNDAY E1（最末日2件其餘各1件）。
>
> **v2.22 F100 Stage 2~4 SQL 下推 + v2 真實計分引擎 P3（2026-06-02）**：新增 F100 test spec（**52 個場景**，**僅 P3**，不含 P1/P2（已完成提交））。涵蓋 AD-E07-28 P3「Stage 2 計分（`ob_levelcard_score` 區間/類別權重 `SUM(CASE…)` + `LEFT JOIN customer_core` 補完客戶屬性欄位）/ score→card_level→tier_level（`LEFT JOIN`）/ Stage 3 CR（`EXISTS`）/ Stage 4 st4_exchange（`ROW_NUMBER()` 視窗 + `CEIL(×0.1)` 保底 1）由 JS 下推為 SQL，並把計分引擎由 v1 簡化版升級為 v2 真實版」。**與 P1/P2 本質差異：P3 非純等價變更**——現行 `computeScore` 對 customer_core 欄位回 `''` 不計分（標 v2.1 補完），P3 以 LEFT JOIN 補上。**故 golden oracle = 依計分卡規則 + customer_core 屬性手算之預期值（寫死於 §一矩陣、人複核），非跑 v1 JS**（跑 v1 會把升級補上的正確分判為 fail）。分層：**EQ 8（P3 DoD AC-8 — SQL 逐列 == 升級後手算預期，每案標 (a) 升級差異 / (b) 下推等價）** + SCORE 7（`SUM(CASE…)` 區間/類別/NULL vs 0）+ CJOIN 4（LEFT JOIN match/NULL，攔 INNER JOIN 漏案）+ LEVTIER 5（score NULL vs card_level NULL 之 tier fallback 分歧）+ CR 5（`EXISTS` cr_enabled 開/關）+ **EXCH 8（st4_exchange：`CEIL` 非 SP `ROUND`、保底 1、`PARTITION BY list_no`、deterministic 選案精確比對、單一 senior；SP 主管↔專員配對交換 out-of-scope 不測）** + RUNEST 2 + NOLOAD 3 + IDEM 3 + **UPGR 4（F067 計分升級差異報告 + 業務驗收 gate，§9/NFR-005，上線硬性前置）** + RG 3。**約 40 個案例強制需 Postgres**（視窗函式 / `SUM(CASE…)` / `LEFT JOIN` / `EXISTS` 在 SQLite 不具代表性），沿用 postgres-test 容器。**4 個待確認 open question**：OQ-F100-T1（transaction 範圍 spec 未明 → IDEM-003 blocked）、OQ-F100-T2（score=NULL 時 tier 走 fallback T3 或 NULL，spec 未逐字明列）、OQ-F100-T3（customer_core entity 目前不存在於 entities 目錄 → P3 LEFT JOIN 前置）、OQ-F100-T4（CUS_SEX/AGE 計分欄位映射對齊 §3.10 表，tdd 交接項）。命名鎖定：`executeV2` / `computeScore` / `collectCrCandidates`；oracle=手算矩陣（禁跑 v1 當 (a) 案件 oracle）；st4_exchange `Math.max(1, Math.ceil(n*0.1))`。
>
> **v2.21 F099 Stage 1 SQL 下推 P2（2026-06-02）**：新增 F099 test spec（**38 個場景**，**僅 P2**，不含 P1（F098 已完成）/P3（F100））。涵蓋 AD-E07-28 P2「`executeStage1Chain` 全載 + 應用層 filter → 單一 set-based `buildStage1Sql(list, workdt)`（run `INSERT…SELECT` 與 estimate `SELECT COUNT(*)` 共用同一 WHERE/JOIN core）」。分層：**EQ 14（P2 Definition of Done — JS↔SQL 逐 list 結果集精確等價，PG 真庫逐列 PK `toEqual`，assignday 恆 NULL 斷言）** + RUNEST 4 + **PORT 7（I-PORT-01：year-above `year_produ` 前導數字/空字串/非數字/null/cutoff 邊界，PORT-004/005/007 守三個 PG 實作陷阱，**強制 PG 禁 SQLite**）** + NOLOAD 3 + IDEM 3 + SQLG 4 + GMT 3（**作廢 `RGv2-005`/`SDv2-*` JS-pin guard**）。**26 個案例強制需 Postgres**（EQ 14 + PORT 7 + RUNEST(PG) 2 + IDEM 3）。**所有 OQ 已裁定（OQ-F099-01/02/03 均 ✅ RESOLVED）**：oracle=JS、assignday 恆 NULL、CI 必起 PG。命名鎖定：`buildStage1Sql`；保留 JS `executeStage1Chain` 為等價 oracle 不可刪。
>
> **v2.19 US-144 best_case 系統固定篩選條件（2026-05-28）**：補強 3 個 test spec（F050 v2.3.1 / F051 v2.2.1 / F075 v1.7）。共新增 **50 個場景**：F050 +33（L 群 5：createList 注入 + tamper；M 群 4：min-count 排除 system-fixed；N 群 4：updateList 注入；O 群 10：m295/m296 migration；P 群 1：Stage 1 整合；Q 群 6：建立頁前端 best_case 鎖定列；R 群 3：編輯頁前端鎖定列）、F051 +6（TS-F051-020~025：updateList 注入 + 正規化 + min-count v2.2.1 + LEGACY guard ordering）、F075 +11（TS-F075-v17-001~010 + 002b：seed / API isSystemFixed / deactivation guard / M06 UI disabled button）。命名鎖定：`injectSystemFixedConditions`、`SYSTEM_FIXED_FIELD_CANNOT_DEACTIVATE`（422）、`is_system_fixed` DB / `isSystemFixed` API、`condition-row-best_case` / `remove-condition-best_case` / `value-best_case` / `btn-disable-best_case` / `field-row-best_case` prototype testid。
>
> **v2.26 F064 v2.1 pool 表換源修正（2026-06-17）**：F064 spec_version 升至 v2.1；追加 **LINEAGE 5 案例**（+STATIC-005）共 **+6 個場景**（63→**69**），總場景 1736→**1742**。核心修正：pool join 表從 `ob_pool_data_list`（3-key INNER JOIN）改為 `ob_pool_data`（2-key: orgno+appl_no），消除 live run 11.5% 掉列（55,863→49,425 問題）。新增 invariant **I-EXP-LINEAGE-01**（匯出列數 = ob_monthly_run_result 列數，DoD 紅線）與 **BR-F064-16**。新案例：**LINEAGE-001**（ob_pool_data 50-row 全數匯出 vs ob_pool_data_list 40-row 差異辨別）；**LINEAGE-002**（ob_pool_data_list 缺少列不影響匯出）；**LINEAGE-003**（靜態 grep: buildExportQuery 含 ob_pool_data 不含 ob_pool_data_list）；**LINEAGE-004**（pool 屬性值回歸：dept_name/pro_rate/month_cnt 換表後不變）；**LINEAGE-005**（ob_pool_data.appl_date timestamp→Date 物件→YYYY/MM/DD）；**STATIC-005**（全 exportResult 路徑靜態掃描不含 ob_pool_data_list 字串）。修改案例：**COLSRC-001** 更名「INNER JOIN ob_pool_data 雙欄鍵命中（v2.1 修正）」。DoD 紅線擴充：REGRESSION + **LINEAGE-001** + COLSEQ-001/002 + STATIC-002/004 全綠。
>
> **v2.25 F064 匯出分派結果 23 欄對齊 legacy（2026-06-17）**：新增 F064 test spec（**63 個場景**）。對應 F064 v2.0（US-155，supersedes US-084）三項 SCHEMA GAP 修正：(GAP-1) 移除 `custo_no`/`cust_name`/`card_level`/`score`；(GAP-2) 資料來源改 `ob_monthly_run_result` 多表 join；(GAP-3) 進件日 source = `ob_pool_data.appl_date`（v2.1 前誤為 ob_pool_data_list）。所有 OQ 已裁定（AD-E07-31）。分層：**COLSRC 6** + **COLSEQ 4** + **REGRESSION 6** + **FMT 8** + **CR 4** + **JOINMISS 5** + **OVERDUE 2** + **STREAM 5** + **SCOPE 5** + **STATUS 4** + **AUDIT 3** + **DET 2** + **APLDATE 2** + **STATIC 4** + **AUTH 3**。Mock 換向：`snapshotRepo.find()` → `DataSource.query()`/`queryRunner.stream()`。
>
> **最後更新**：2026-06-29

---

## 1. 範圍總覽

### 涵蓋範圍

- 45 個 Feature（F001–F026、F027、F028、F029、F030、F031、F032、F033、F034、F035、F036、F037、F038、F045、F046、F047、F053–F056），分屬 7 個 Epic（F038 跨 E04/E05）：
  - **E01 驗證與登入**：F001、F002、F003
  - **E02 帳號與角色管理**：F004、F005、F006、F007、F008、F009、F010、F045、F113（員工編號作為登入識別碼，跨 E01 登入分支）
  - **E03 資料來源管理**：F011、F012、F013、F014、F015、F016
  - **E04 資料擷取**：F017、F018、F019、F020、F021、F022、F023、F024、F025、F026
  - **E05 ETL Pipeline 管理**：F027、F028、F029、F030、F031、F032、F033、F034、F035、F036、F037、F110（Code Decode 節點）
  - **E04+E05 系統啟動修復**：F038（孤兒任務回收，跨 E04/E05）
  - **E06 Customer 360**：F046（客戶搜尋與清單）、F047（單一客戶詳情）
  - **E07 客戶名單分派（M01 名單定義）**：F048（查看本月名單清單 Kanban v2.0）、F049（Stage 0 業務化重設計 v2.0）、F050（新增名單定義 v2.2）、F051（編輯名單定義 v2.1）、F052（停用名單定義 v2.1）、F061（月名單分派計分執行 v1.4）、F077（月份切換與五階段總覽 v1.3）、F081（Rollback 至草稿 v1.3）、F085（Rollback 至部門比例 v1.3）、F089（Rollback 至簽核 v1.3）、F111（分派總覽儀表板，模組新首頁，US-177／AD-E07-46）
  - **E07 客戶名單分派（M03a 部門比例設定階段）**：F117（部門比例設定頁僅提供「有在職處長」之部門設定，US-180／AD-E07-48；F079 v1.3 之精煉）
  - **E07 客戶名單分派（M01 名單定義「從上月複製」子流程）**：F118（從上月複製名單顯示「已複製過」提示，US-181／AD-E07-48；F050 v2.4 §4 AC-5 之精煉）
  - **E07 客戶名單分派（M01 名單定義「條件建構子」子流程）**：F119（類別型篩選欄位新增文字比對運算子「包含／不包含／完全等於」，US-183／AD-E07-50；F050 v2.4 §5.4 之加性擴充，本輪不含 e2e/mutation/metric）
  - **E07 客戶名單分派（M02 計分設定）**：F053（查看計分維度）、F054（編輯計分維度與分數）、F055（編輯 CARD_LEVEL 門檻）、F056（編輯 TIER_LEVEL 對應表）
  - **E07 客戶名單分派（M06 篩選欄位管理）**：F075（POOLDATA 篩選欄位白名單管理 v1.6）、F076（類別型欄位可選值管理 v1.6）、F109（新增「客戶資料」來源篩選欄位，customer_core 8 欄）
  - **E07 客戶名單分派（M07 角色整合）**：F073/F074（E02 角色整合）
- 2 項非功能需求（NFR-001 安全性、NFR-002 效能），共 10 個子需求（含 NFR-002.6 E04 清單、NFR-002.7 E04 儀表板、NFR-002.8 E04 排程；新增 F026 raw data 預覽效能）
- 65 個錯誤碼的驗證覆蓋（新增 C360_CUSTOMER_NOT_FOUND、C360_SEARCH_MIN_LENGTH for E06；新增 PIPELINE_VERSION_ALREADY_PUBLISHED for F037；累計含 PIPELINE_VERSION_NOT_FOUND、PIPELINE_PUBLISH_REQUIRES_TEST、PIPELINE_INVALID_CONNECTION、PIPELINE_NAME_EXISTS、PIPELINE_NOT_FOUND、PIPELINE_RUNNING、PIPELINE_NO_DEFINITION、PIPELINE_DRAFT_CANNOT_ENABLE、PIPELINE_TARGET_TABLE_NOT_FOUND、VALIDATION_INVALID_CRON、DATASOURCE_SCHEMA_LOAD_FAILED、DATASOURCE_TABLE_LOAD_FAILED、EXTRACTION_RAW_TABLE_NOT_FOUND、EXTRACTION_TABLE_CREATE_FAILED、EXTRACTION_BATCH_WRITE_FAILED）

### 排除項目

- Phase 2 功能（SSO/LDAP、進階稽核日誌、連線池）
- 帳號鎖定機制（MVP 不提供）
- CSV 批量帳號匯入
- 特定技術棧的實作測試（規格維持技術中立）

### 假設

- JWT 為 Session 管理的唯一機制
- 系統共有 2 種角色：admin、user，均為 Seed Data，不開放 Admin 自訂新增或刪除（US-017）
- 資料來源僅支援三種 RDBMS（MySQL、PostgreSQL、SQL Server）
- 密碼規則僅有最小長度 8 字元（無複雜度要求）
- 前端為 SPA 架構
- Token 失效策略採用 Refresh Token + 短效 Access Token

---

## 2. 覆蓋率摘要表

| Feature ID | Feature Name | Priority | Test File | Scenarios | Status |
|------------|-------------|----------|-----------|-----------|--------|
| F001 | Admin 登入 | P0-MVP | [F001-test.md](features/F001-test.md) | 8 | Draft |
| F002 | User 登入 | P0-MVP | [F002-test.md](features/F002-test.md) | 7 | Draft |
| F002SM | Sales Manager 旗標顯示於 Top Bar | P0-MVP | [F002SM-test.md](features/F002SM-test.md) | 25 | Draft |
| F003 | 登出 | P0-MVP | [F003-test.md](features/F003-test.md) | 6 | Draft |
| F004 | 建立帳號 | P0-MVP | [F004-test.md](features/F004-test.md) | 9 | Draft |
| F005 | 查看帳號清單 | P0-MVP | [F005-test.md](features/F005-test.md) | 10 | Draft |
| F006 | 編輯帳號 | P0-MVP | [F006-test.md](features/F006-test.md) | 8 | Draft |
| F007 | 停用／啟用帳號 | P1 | [F007-test.md](features/F007-test.md) | 7 | Draft |
| F008 | 指派／變更角色 | P0-MVP | [F008-test.md](features/F008-test.md) | 12 | Draft |
| F045 | 系統角色定義（系統預設角色） | P0-MVP | [F045-test.md](features/F045-test.md) | 13 | Draft |
| F113 | 員工編號作為登入識別碼（employee_no 選填欄位、登入 Email/員工編號擇一；US-179／AD-E02-5；不 trim/大小寫敏感登入/兩軌唯一性/⚠️mssql-p1b2 companion 必要修改） | P1 | [F113-test.md](features/F113-test.md) | 105 | Draft |
| F009 | 自助式密碼重設 | P0-MVP | [F009-test.md](features/F009-test.md) | 10 | Draft |
| F010 | Admin 重設使用者密碼 | P0-MVP | [F010-test.md](features/F010-test.md) | 6 | Draft |
| F011 | 新增資料來源 | P0-MVP | [F011-test.md](features/F011-test.md) | 8 | Draft |
| F012 | 查看資料來源清單 | P0-MVP | [F012-test.md](features/F012-test.md) | 7 | Draft |
| F013 | 編輯資料來源 | P0-MVP | [F013-test.md](features/F013-test.md) | 8 | Draft |
| F014 | 刪除資料來源 | P1 | [F014-test.md](features/F014-test.md) | 5 | Draft |
| F015 | 測試連線 | P0-MVP | [F015-test.md](features/F015-test.md) | 8 | Draft |
| F016 | 狀態監控儀表板 | P1 | [F016-test.md](features/F016-test.md) | 10 | Draft |
| **E04 資料擷取** | | | | | |
| F017 | 建立擷取任務 | P0-MVP | [F017-test.md](features/F017-test.md) | 26 | Draft |
| F018 | 查看擷取任務清單 | P0-MVP | [F018-test.md](features/F018-test.md) | 9 | Draft |
| F019 | 編輯擷取任務 | P0-MVP | [F019-test.md](features/F019-test.md) | 26 | Draft |
| F020 | 啟用／停用擷取任務 | P0-MVP | [F020-test.md](features/F020-test.md) | 8 | Draft |
| F021 | 立即執行／重新執行 | P0-MVP | [F021-test.md](features/F021-test.md) | 26 | Draft |
| F022 | 查看擷取日誌 | P0-MVP | [F022-test.md](features/F022-test.md) | 13 | Draft |
| F023 | 排程自動執行 | P0-MVP | [F023-test.md](features/F023-test.md) | 8 | Draft |
| F024 | 擷取監控儀表板 | P1 | [F024-test.md](features/F024-test.md) | 11 | Draft |
| F025 | 刪除擷取任務 | P1 | [F025-test.md](features/F025-test.md) | 7 | Draft |
| F026 | 查看擷取資料預覽 | P0-MVP | [F026-test.md](features/F026-test.md) | 41 | Draft |
| **E05 ETL Pipeline 管理** | | | | | |
| F027 | 查看 Pipeline 列表 | P0-MVP | [F027-test.md](features/F027-test.md) | 22 | Draft |
| F028 | 建立 Pipeline | P0-MVP | [F028-test.md](features/F028-test.md) | 17 | Draft |
| F029 | 視覺化轉換編輯器 | P0-MVP | [F029-test.md](features/F029-test.md) | 37 | Draft |
| F030 | 執行 Pipeline | P0-MVP | [F030-test.md](features/F030-test.md) | 20 | Draft |
| F031 | 啟用／停用 Pipeline | P0-MVP | [F031-test.md](features/F031-test.md) | 14 | Draft |
| F032 | 查看 Pipeline 日誌 | P0-MVP | [F032-test.md](features/F032-test.md) | 21 | Draft |
| F033 | Pipeline 版本管理 | P1 | [F033-test.md](features/F033-test.md) | 29 | Draft |
| F034 | 刪除 Pipeline | P1 | [F034-test.md](features/F034-test.md) | 15 | Draft |
| F035 | Pipeline 監控儀表板 | P1 | [F035-test.md](features/F035-test.md) | 21 | Draft |
| F036 | 目標表 Domain-Oriented 規劃 | P0-MVP | [F036-test.md](features/F036-test.md) | 40 | Draft |
| F037 | 發布 Pipeline 版本 | P0-MVP | [F037-test.md](features/F037-test.md) | 37 | Draft |
| F110 | Code Decode 節點（泛用單趟多欄位代碼解碼，US-173／AD-E05-7；31 個 lookup 收斂為 9 個 code_decode 節點）——設定驗證＋語意契約（SQL 生成/真實等價/效能/migration 驗證見 [infrastructure/AD-E07-41-P4-codedecode-test.md](infrastructure/AD-E07-41-P4-codedecode-test.md) 85 場景） | P0-MVP | [F110-test.md](features/F110-test.md) | 28 | Draft |
| **E05 小計** | | | **12 files** | **301** | |
| **E04+E05 系統啟動修復** | | | | | |
| F038 | 孤兒任務回收（系統啟動時自動修復 running 狀態） | P0-MVP | [F038-test.md](features/F038-test.md) | 45 | Draft |
| **ETL Editor 前端功能** | | | | | |
| F039 | 節點欄位變化統計 Badge | P0-MVP | [F039-test.md](features/F039-test.md) + [F039-test-strategy.md](features/F039-test-strategy.md) | 22 | Draft |
| F040 | Inspector Panel 欄位 Diff | P1 | [F040-test.md](features/F040-test.md) | 6 | Draft |
| F041 | Badge Hover Tooltip | P2 | [F041-test.md](features/F041-test.md) | 12 | Draft |
| **E05 ETL 執行引擎** | | | | | |
| F042 | ETL 執行引擎核心框架 | P0-MVP | [F042-test.md](features/F042-test.md) | 21 | Draft |
| F043 | ETL 節點執行器（8 種節點含 Lookup） | P0-MVP | [F043-test.md](features/F043-test.md) | 58 | Draft |
| F044 | ETL Target Load + UPSERT | P0-MVP | [F044-test.md](features/F044-test.md) | 17 | Draft |
| **E06 Customer 360** | | | | | |
| F046 | Customer 360 — 客戶搜尋與清單 | P0-MVP | [F046-test.md](features/F046-test.md) | 31 | Draft |
| F047 | Customer 360 — 單一客戶詳情 | P0-MVP | [F047-test.md](features/F047-test.md) | 38 | Draft |
| **E06 小計** | | | **2 files** | **69** | |
| **E07 M02 計分設定** | | | | | |
| F053 | 查看計分維度設定 | P0-MVP | [F053-test.md](features/F053-test.md) | 13 | Draft |
| F054 | 編輯計分維度與分數 | P0-MVP | [F054-test.md](features/F054-test.md) | 24 | Draft |
| F055 | 編輯 CARD_LEVEL 分級門檻 | P0-MVP | [F055-test.md](features/F055-test.md) | 21 | Draft |
| F056 | 編輯 TIER_LEVEL 對應表 | P0-MVP | [F056-test.md](features/F056-test.md) | 28 | Draft |
| **E07 M02 小計** | | | **4 files** | **86** | |
| **E07 M07 角色整合（E02 整合）** | | | | | |
| F073-F074-E02 | E07 角色指派 E02 整合（PATCH /business-role / Guard / JWT / 合併約束 / m14 遷移 / legacy / deprecated） | P0-MVP | [F073-F074-e02-integration-test.md](features/F073-F074-e02-integration-test.md) | 63 | Draft |
| **E07 M07 小計** | | | **1 file** | **63** | |
| **E07 M06 篩選欄位管理** | | | | | |
| F075 | POOLDATA 篩選欄位白名單管理（含 v1.4 available-columns 端點、suggestedFieldType 推斷、dropdown Modal；v1.5 +2；v1.6 +3 M-A1 seed；**v1.7 +11 is_system_fixed / deactivation guard / M06 UI**） | P0-MVP | [F075-test.md](features/F075-test.md) | 64 | Draft |
| F109 | 新增「客戶資料」來源篩選欄位（`customer_core` 8 欄；data_source 概念 + 條件式 LEFT JOIN + NULL 排除三變體 + AGE/LEFT3 衍生 + PG-only 邊界 AD-E07-37） | P1 | [F109-test.md](features/F109-test.md) | 94 | Draft |
| F112 | 類別型篩選欄位可選值自動建議（distinct-values 查詢 + options/bulk 批次新增；US-178／AD-E07-47；就緒優先於存在性/500非504逾時/RBAC碼查證/稽核彙總單筆） | P2 | [F112-test.md](features/F112-test.md) | 81 | Draft |
| **E07 M06 小計** | | | **3 files** | **239** | |
| **E07 M08 Whitelist-Driven 重構** | | | | | |
| F050 | 建立名單定義（v2.1 30；v2.1.1 +45；v2.2 +19 SS/SIG；**v2.3/v2.3.1 +33 US-144 injectSystemFixedConditions / min-count / migration m295/m296 / Stage1 / frontend locked row**） | P0-MVP | [F050-test.md](features/F050-test.md) | 127 | Draft |
| F051 | 編輯名單定義（v2.1 19；**v2.2/v2.2.1 +6 US-144 updateList inject / tamper-normalization / min-count**） | P0-MVP | [F051-test.md](features/F051-test.md) | 25 | Draft |
| F076 | 類別型欄位可選值管理（v1.5 seed 重構 +8 場景；v1.6 +3 場景 best_case Y/N options 配套） | P0-MVP | [F076-test.md](features/F076-test.md) | 11 | Draft |
| F068-deprecated | 指派代碼查詢（已廢棄，9 個廢棄驗證場景） | P0-MVP | [F068-deprecated-test.md](features/F068-deprecated-test.md) | 9 | Draft |
| M01-INT | Whitelist-Driven 條件 Payload 端對端整合測試（含 OQ-TEST-001/002） | P0-MVP | [integration/M01-whitelist-driven-integration-test.md](integration/M01-whitelist-driven-integration-test.md) | 21 | Draft |
| M01-MIG | F050 v2.1 Migration M1~M5 測試設計 | P0-MVP | [migration/M01-migration-test.md](migration/M01-migration-test.md) | 26 | Draft |
| **E07 M08 小計** | | | **6 files** | **219** | |
| **E07 Stage 1 精確化工程** | | | | | |
| F090 | OBPOOLDATA_LIST ETL 載入與 data_source 單源化標記（Phase 1 / Phase B，v2.0 更新）⚠️ | P0-MVP | [F090-test.md](features/F090-test.md) | 18 | Draft |
| F091 | Stage 1 補完整（MONTH_CNT + 去重 v2.0 動態上界 + 特例 DELETE SP bug fix）（Phase 2，v2.0 更新）⚠️ | P0-MVP | [F091-test.md](features/F091-test.md) | 38 | Implemented |
| F092 | Stage 1 完整鏈 Dry-run 精確估算（Phase 3） | P0-MVP | [F092-test.md](features/F092-test.md) | 26 | Draft |
| F094 | 月名單分派結果表 ob_monthly_run_result（單源化 Phase A：pipeline 落點切換）⚠️ | P0-MVP | [F094-test.md](features/F094-test.md) | 17 | Implemented |
| F095 | 名單套用之系統特例規則前端唯讀呈現（appliedSpecialRules[] 讀時推導）| P1 | [F095-test.md](features/F095-test.md) | 23 | Implemented |
| F096 | POOLDATA 篩選欄位白名單 list_type 停用（期別篩選唯一路徑澄清）| P1 | [F096-test.md](features/F096-test.md) | 9 | Draft |
| **E07 Stage 1 精確化 / Phase A/B 小計** | | | **6 files** | **131** | |
| F097 | 作業月語意統一（SystemService getDefaultTargetWorkYm / workYm DTO / 過去月 guard / Stage 1 去重視窗 / AssignmentWorkYmContext） | P0-MVP | [F097-test.md](features/F097-test.md) | 48 | Draft |
| **E07 作業月語意** | | | **1 file** | **48** | |
| **E07 月名單分派執行模型重構（AD-E07-28）** | | | | | |
| F098 | 月名單分派 Worker 抽離 P1（pg-boss 入列 + cdmp-worker 容器 + cancellation 真生效 + OrphanReaper；**僅 P1**，不含 P2/P3 SQL 下推） | P0-MVP | [F098-test.md](features/F098-test.md) | 51 | Draft |
| F099 | Stage 1 SQL 下推 P2（`buildStage1Sql` set-based + estimate≡run 共用 core + JS↔SQL 逐 list 等價 PG 真庫；**僅 P2**，不含 P1/P3） | P0-MVP | [F099-test.md](features/F099-test.md) | 38 | Draft |
| F100 | Stage 2~4 SQL 下推 + v2 真實計分引擎 P3（`SUM(CASE…)` 區間/類別計分 + `LEFT JOIN customer_core` 補完 + score→level→tier + CR `EXISTS` + st4_exchange 視窗函式；**僅 P3**，不含 P1/P2；oracle=手算預期，非跑 v1 JS）⚠️ | P0-MVP | [F100-test.md](features/F100-test.md) | 52 | Draft |
| F101 | Stage 3/4 真實比例分派（dept ration + empl ration + ASSIGNDAY 千分比；取代 F100 placeholder；oracle=手算 FLOOR+差額補足；JS↔SQL 逐列等價 DoD；I-NO-ST4-EXCHANGE senior swap 廢除回歸；所有 OQ 已裁定）⚠️ | P0-MVP | [F101-test.md](features/F101-test.md) | 51 | Draft |
| F102 | 月名單分派 CR 優先分派（失效清空 × 2 + CR 優先指派 + 扣量；per-list cr_enabled 閘控；EQ 7（JS applyCrPriority ↔ PG runCrPrioritySql 六欄逐列等價）；I-CR-ORDER-01；I-CR-ASSIGNDAY-01；Bug Fix I-CR-DEDUCT-01；所有 OQ 已裁定）⚠️ | P0-MVP | [F102-test.md](features/F102-test.md) | 55 | Draft |
| F103 | 月名單分派計分引擎欄位來源修正（ADD_UN_CAPITAL 補 JOIN + 通用 fallback + PROJECT_TP 衍生 + COMMISSION 移除 + JS oracle 補齊 cc + 202606 重跑驗收；EQ 8（JS↔SQL score 完全相等，誤差=0）；I-SCORE-AR-JOIN-01/FALLBACK-01/EQ-01/AGE-01/GHOST-01/COMMISSION-01；OQ-1/2/3/156-02/157-01/158-01/02 全裁定）⚠️ | P0-MVP | [F103-test.md](features/F103-test.md) | 59 | Draft |
| **E07 月名單分派執行模型 小計** | | | **6 files** | **306** | |
| **E07 M04 分派匯出** | | | | | |
| F064 | 匯出分派結果（23 欄 legacy 對齊，**v2.1**）——COLSRC 6 + COLSEQ 4 + REGRESSION 6（破壞性排除 DoD 紅線）+ FMT 8（日期格式邊界）+ CR 4 + JOINMISS 5 + OVERDUE 2 + STREAM 5 + SCOPE 5 + STATUS 4 + AUDIT 3 + DET 2 + APLDATE 2 + STATIC **5** + AUTH 3 + **LINEAGE 5**（v2.1：pool 表換源 ob_pool_data 不掉列 DoD 紅線）；GAP-1/2/3 + OQ F064-1~4 全部裁定；BR-F064-16 join ob_pool_data 2-key；不讀 snapshot；CSV PassThrough streaming；scope WHERE SQL 注入；DoD 紅線 = REGRESSION + LINEAGE-001 + 23 欄表頭 + tsc 乾淨 | P0-MVP | [F064-test.md](features/F064-test.md) | 69 | Draft |
| F108 | 匯出新增「樞紐分析」頁籤（xlsx 第 2 頁靜態交叉表；% of parent row；部門×員編×名單代號；格式 0.0%；0/0→空白；I-PIV-SHEET-01/PARENTROW-01/SOURCE-01/MEM-01/DET-01；SCOPE 紅線；TC-171-01~12 全覆蓋）——SHEET 4 + HEADER 4 + PARENTROW 7（spec §6.2 oracle 全 18 格）+ ZEROBLANK 3 + DET 4 + BLANK 3（§6.4 oracle）+ SCOPE 3（SCOPE 紅線 DoD）+ EMPTY 3 + REGRESSION 4（DoD 紅線）+ STATIC 3 + PG 2（選配）；mock cursorRows 模式；exceljs load(buffer) 讀回斷言；**2 案例需 PG**（選配）；DoD 紅線 = PARENTROW-001 oracle + SCOPE-001 紅線 + REGRESSION 全綠 + tsc 乾淨 | P0-MVP | [F108-test.md](features/F108-test.md) | 40 | Draft |
| **E07 M04 小計** | | | **2 files** | **109** | |
| **E07 M01 分派總覽儀表板（模組新首頁）** | | | | | |
| F111 | 分派總覽儀表板（US-177／AD-E07-46；單一薄型唯讀聚合端點 `GET /assignment/overview?ym=`，內容橫跨 M01/M03d/M04/M05）——CTRL 11（Guard/角色/路由，含 I-OVW-GUARD-PROPAGATE-01 紅線）+ SVC-COMPOSE 2 + ISO 8（區塊獨立失敗 DoD 紅線）+ EMPTY 9（empty≠zero≠error 三態，含 2 處陷阱守門）+ DEDUP 4（I-OVW-DEDUP-01）+ HAL 1 + SCOPE 5 + UNSCOPED 2 + RISK 2（AD §11 殘留議題邊界）+ UTIL 10（純函式）+ RECENT 4 + STATIC 4（後端合計 62）+ 前端 ROLE 4 + MONTH 3 + STATE 6 + PANEL 23 + READONLY 1 + SIDEBAR 5（前端合計 42）；全數可 mock、無 PG/MSSQL 依賴，CI 常駐 | P1 | [F111-test.md](features/F111-test.md) | 104 | Draft |
| **E07 M01 分派總覽 小計** | | | **1 file** | **104** | |
| **E07 M03a 部門比例設定階段（F117）** | | | | | |
| F117 | 部門比例設定頁僅提供「有在職處長」之部門設定（US-180／AD-E07-48／F117 v1.1 已核可；F079 v1.3 之精煉，疊加可設定範圍限縮，衝突時以 F079 為準）——三分類 GET `requireDirector` flag（有處長部門可編輯／孤兒部門鎖定顯示／無關部門隱藏，BR-2）+ PUT 孤兒列伺服器端保留（BR-4，覆寫式 DELETE+INSERT 前強制併回）+ 鎖定列 payload 竄改一律忽略（BR-5）+ 無處長新配置攔截 422 `RATIO_DEPT_DIRECTOR_REQUIRED`（BR-6）+ 加總驗證範圍改為最終持久化集合（BR-7）+ 稽核完整性含保留孤兒列（BR-9）；OQ-F117-B1 業務裁決＝顯示鎖定＋伺服器保留、不做強制歸零；I-F117-DIRECTOR-SINGLE-SOURCE-01（`computeActiveDirectorMap` 唯一實作，GET/PUT 共用）——BE 12（GET 三分類/在職判定含哨蛘值迴歸/PUT 孤兒保留核心案例）+ INT 6（真實 HTTP+Guard+SQLite DB round-trip）+ FE 8（孤兒鎖定列/已隱藏資訊列/空狀態含儲存亦停用 AC-7 v1.1）+ E2E 5（Playwright route-mock 對照 prototype 29a 六個 demo 場景）；AC-9（處長唯讀）/AC-10（既有消費端回歸：準備完成摘要 excludeZeroRatio / Detail Drawer）為 regression 錨點，不變更既有測試 | P1 | [F117-test.md](features/F117-test.md) | 31 | Draft |
| **E07 M03a 部門比例設定 小計** | | | **1 file** | **31** | |
| **E07 M01 名單定義「從上月複製」子流程（F118）** | | | | | |
| F118 | 從上月複製名單顯示「已複製過」提示（US-181／AD-E07-48／F118 v1.1 已核可；F050 v2.4 §4 AC-5 之精煉，宿主流程不變，疊加唯讀提示+二次確認）——判定採語意等價（方案 b，重用 `findActiveConditionDuplicate` 之 `normalizeConditionPayload`，BR-1）+ 新端點 `GET /api/v1/assignment/lists/copy-duplicate-check?prevYm&currentYm`（常數 3 次查詢，AC-7/BR-3）+ `findActiveConditionDuplicate` 補 `ORDER BY list_no ASC`（BR-4 決定性，對既有已上線程式碼之唯一修改點）；不新增錯誤碼、不新增欄位、無 migration（BR-2）——BE 11（真實 SQLite；★核心 AC-2 雙向一致性 BE-002/003、★核心 N+1 防範 BE-009、回歸 BE-011）+ RBAC 9（路由順序 + Guard 矩陣）+ E2E 4（真實 HTTP+Guard+SQLite DB round-trip）+ FE 9（徽章/二次確認彈窗/AC-10 降級）+ PAGE 5（AC-5/AC-11 頁面整合）+ E2E Fidelity 4（Playwright route-mock 對照 prototype 27a）；AC-6（不引入快取，方法簽章本身保證）/AC-8（既有複製欄位帶入行為）/AC-9（舊遷移名單過濾）為 regression 錨點，不變更既有測試 | P1 | [F118-test.md](features/F118-test.md) | 42 | Draft |
| **E07 M01「從上月複製」子流程 小計** | | | **1 file** | **42** | |
| **E07 M01 名單定義「條件建構子」子流程（F119）** | | | | | |
| F119 | 類別型篩選欄位新增文字比對運算子「包含／不包含／完全等於」（US-183／AD-E07-50／F119 v1.1 已核可；F050 v2.4 §5.4 之加性擴充，`condition_payload` 純加性擴充 `operator`/`keyword` 兩個 optional key，無 migration、無新端點、無新錯誤碼）——單一 SQL 落點 `buildCategoricalOperatorFragment` 供 composer/customer_core（PG+MSSQL）/customer_financial 四處呼叫（I-CATOP-SINGLE-FRAGMENT-01）+ BR-6 NULL 八格矩陣（僅 `ob_pool_data`×`not_contains` 顯式保留 NULL，唯一例外）+ 跳脫超集 `\`/`%`/`_`/`[`/`]`/`^`（I-CATOP-ESCAPE-SINGLE-01）+ `:catop:` 簽章區段與向後相容硬性要求（I-CATOP-SIG-BACKCOMPAT-01）+ `caseyear` 排除文字運算子（I-CATOP-CASEYEAR-EXCLUDE-01）+ 互斥驗證置於 service 層（I-CATOP-VALIDATION-LAYER-01）+ 顯示層單一格式化來源 `formatConditionSummary()`（I-CATOP-DISPLAY-SINGLE-01）——**本輪依 team lead 指示僅產出 vitest/jest 單元＋整合測試，不含 Playwright e2e fidelity／Stryker mutation／dependency-cruiser metric gate**：後端 stage1 SQL 語意層 49（`f119-categorical-operator-fragment.spec.ts` 28，含★核心 BR-6 NULL 八格矩陣真實 SQL 執行 + AC-9 未跳脫必紅案例；customer_core/financial 各 8/6，含 I-CATOP-NULL-MATRIX-01 靜態掃描；真實 MSSQL `f119-categorical-collation.mssql.spec.ts` 7，BR-8 大小寫/全半形敏感度）+ 後端 assignment-list 黑箱/驗證層 30（`previewHitCount`/`createList` 黑箱 7/13 + 簽章向後相容/重複判定 10，★核心 T-11/T-12/T-13~16）+ 前端 `_utils` 純函式 13（`OPERATOR_LABEL`/`operatorLabel()` 6 + `formatConditionSummary()` 7，後者採 AC-15 例句而非 AD §3.6 樣板）+ 前端 Page 整合 23（建立/編輯草稿頁 12+4、Kanban chip + Detail Drawer 3、Stage0 warning 渲染 4）+ 後端真實 MSSQL 7（`f119-categorical-collation.mssql.spec.ts`：BR-8 大小寫/全半形敏感度，依 team lead 環境情報回饋補上）——2026-08-18 tdd-implementation 提兩件測試側爭議，已裁決：F119-FE-010 testid 誤植修正（`value-checkbox-0-0`→`value-checkbox-0-01`，對齊本檔既有 optionValue 慣例）、F119-EDIT-002 依 prototype `setCondOperator()` 之 crossForm+willLose 分支改為驗證二次確認 modal（附錄 C C-3~C-5），新增 F119-EDIT-002b 覆蓋兩側皆空之對照組，詳見 risks-and-gaps.md R-F119-08——**2026-08-18 R-F119-04 缺口補強**（AC-14 五路徑一致性，前輪僅驗共用函式本身+一條可達路徑，本輪補三檔共 12 案例）：`f119-ac14-cross-path-parity.mssql.spec.ts` 4（★核心，真實 MSSQL，路徑1 MSSQL 下推 vs 路徑3 JS filter chain 對真實 167 萬列 `ob_pool_data` 逐 PK 相等，含 AC-3 NULL 不對稱；不斷言絕對數字，改採路徑相等+自 seed 列歸類，因 dev pool 非空表）+ `f119-ac14-stage0-delegation.spec.ts` 4（SQLite，spy 驗證路徑4 Stage0 估算委派路徑3 時 operator/keyword 逐欄位不失真，AD-E07-50 §2.1 查證路徑4 非獨立實作）+ `f119-ac14-pg-semantic-equivalence.spec.ts` 4（路徑2 PG 下推，本機無 PG 可跑，改採 SQL 字串/語意等價，已誠實標記驗證層級低於其餘三條真實執行路徑）；四路真實/可執行覆蓋，路徑5（草稿抽樣估算）沿用既有 `f119-preview-hit-count-text-operators.spec.ts`，R-F119-04 詳見 risks-and-gaps.md 更新版 | P1 | [F119-test.md](features/F119-test.md) | 127 | Draft |
| **E07 M01「條件建構子」子流程 小計** | | | **4 files** | **127** | |
| **Infrastructure — 資料庫平台遷移（cross-cutting，非 F-numbered）** | | | | | |
| AD-E07-38-P1a | MSSQL 全面遷移 P1a 最小可連線＋登入（三設定點 mssql 分支 + auth 4-entity 子集 + 型別探測 uuid/text/bigint/datetime2/bit + I-MSSQL-CASE-01 大小寫守門 + I-MSSQL-HELPER-SCOPE-01；不需 spec-writer，AD §3 D-7 裁定） | P0-MVP | [infrastructure/AD-E07-38-P1a-test.md](infrastructure/AD-E07-38-P1a-test.md) | 30 | Draft |
| AD-E07-39-P1b1 | MSSQL 全面遷移 P1b1 全 Entity 型別修正（全 37 entity 型別轉換 47 處含 F-1 timestamp 紅線 + B1 token_blocklist 改 token_hash binary(32) + D1 全 entity 統一載入 + F-4 varchar 中文編碼 test-first 決策閘門 + I-MSSQL-PK-BYTELIMIT-01 動態守門；不需 spec-writer） | P0-MVP | [infrastructure/AD-E07-39-P1b1-test.md](infrastructure/AD-E07-39-P1b1-test.md) | 43 | Draft |
| AD-E07-39-P1b2 | MSSQL 全面遷移 P1b2 Prod Baseline Migration + Dev/Prod Parity 驗證（I-MSSQL-BASELINE-PARITY-01 結構化 diff 核心 + comparator 敏感度驗證 + fn_calc_tier_level 未建立 + 無 filtered index + collation/case/B1 於 baseline 路徑守門 + sp_executesql/pg_dump 語法靜態規避；兩路徑 harness＝schema 隔離非獨立 DB，因 cdmp login 無 CREATE DATABASE 權限；不需 spec-writer） | P0-MVP | [infrastructure/AD-E07-39-P1b2-test.md](infrastructure/AD-E07-39-P1b2-test.md) | 39 | Draft |
| AD-E07-39-P1b3 | MSSQL 全面遷移 P1b3 Bootstrap/Seed 三支腳本改寫 + B2 npm alias 修復驗證（DIALECT DataSource 依 DB_TYPE 切換 + ALIAS 字面 npm script 修復（DoD#1 核心）+ SITE 12 逐站點行為驗證（含 NULL-safe 自然鍵最高風險項）+ BOOT E2E + COUNT 含 2 個 SCOPE GAP 決策關卡（roles/pooldata_field_whitelist/option 不在任一輪範圍）+ IDEM 冪等 + 逐檔查證出 5 類 AD §7 未列出站點：`type:'postgres'`硬編碼/NOW()/RETURNING/::text/IS NOT DISTINCT FROM/裸布林字面值；不需 spec-writer） | P0-MVP | [infrastructure/AD-E07-39-P1b3-test.md](infrastructure/AD-E07-39-P1b3-test.md) | 54 | Draft |
| AD-E07-38-P1c | MSSQL 全面遷移 P1c（P1 最後一片）Pattern B（$n→具名參數）＋ sp_getapplock 跨 driver 鎖（核心 4 檔 6 處之站點 1/2/3/4，站點 5 native cursor 排除）——ESCAPE 5 driver 層純函式驗證免真實連線 + PARAM 17（customer_core PG-only 錯誤分類 vs ob_arreturndf_min_cap 完整跨 driver 等價 + scopeClause 巢狀 $n 稽核）+ 🔴LOCK 12（sp_getapplock 雙連線並發逾時降級/等待取得 + 前置條件探測決策關卡 I-MSSQL-LOCK-01）+ 🔴DISPATCH 5（現行 isPostgres() 二元 gate 缺口 MUST-FIX 守門）+ REG 6 + STATIC 3（站點清單交付物）；不需 spec-writer，AD §3 D-5/D-6 裁定 | P0-MVP | [infrastructure/AD-E07-38-P1c-test.md](infrastructure/AD-E07-38-P1c-test.md) | 48 | Implemented（tdd 落地 2026-07-07；LOCK 真實取鎖 6 案因本容器 sp_getapplock 17750 DLL 缺失 skip，OQ-MSSQL-P1C-01；PG 等價 5 案 skip 因 5433 未起） |
| AD-E07-40-P2a | MSSQL 全面遷移 P2a（P2 首片，自建 T-SQL 佇列）queue_job Schema + MssqlQueueService 五操作 + 併發正確性 Harness（獨立驗證層，不碰 Producer/Consumer）——SCHEMA 15（兩軌建表 + 型別矩陣 + 🔴索引 comparator 誤用陷阱決策關卡 + parity DoD）+ UNIT 6（免真實連線 SQL spy，🔴 I-MSSQL-QUEUE-CLAIM-01 恰 1 次 query 守門）+ SEND 4 + CANCEL 4（cancel-before-claim）+ CLAIM 6（claim-after-claim + FIFO）+ COMPLETE 3 + SWEEP 5（expire 後不重派）+ 🔴CONC 8（旗艦群組：pool.max 預設=1 已查證、時間戳重疊證據、決策關卡反證計數斷言無鑑別力）+ STATIC 3 + REG 5；不需 spec-writer，佇列 claim 為純 T-SQL DML 不受 sp_getapplock 17750 影響，全部案例可對真實 MSSQL 容器執行 | P0-MVP | [infrastructure/AD-E07-40-P2a-test.md](infrastructure/AD-E07-40-P2a-test.md) | 59 | Draft |
| AD-E07-40-P2b | MSSQL 全面遷移 P2b（P2 第二片）Producer/Consumer 接線 + 輪詢 Loop + processPayload 共用（不重測 MssqlQueueService 本身，P2a 已驗證黑盒依賴）——🔴DISPATCH 6（三處既有二元 boss-null gate MUST-FIX 守門，同型於 AD-E07-38-P1c DISPATCH-001 + AD §4.2 檔案改動清單缺口：assignment.module.ts 亦須供應 MssqlQueueService）+ PROD 5（producer mssql 分支正確轉接、retryLimit=0、cancel 吞錯語意對齊 DoD #1）+ POLL 10（driver 分支互斥 + 輪詢間隔可注入 + 🔴reentrancy guard 核心 I-MSSQL-QUEUE-SERIAL-01 + 🔴OnModuleDestroy 清 timer DoD #4 + SIGTERM 驗證範圍界定決策關卡）+ 🔴PAYLOAD 8（processPayload 單一函式參照 DoD 核心 I-MSSQL-QUEUE-PAYLOAD-UNITY-01 + 既有 CONS-001/001b/004/005 語意於新 signature 重新驗證 + 🔴mssql 路徑 JSON.parse 轉接守門）+ E2E 5（真實 MSSQL，🔴DoD #3 核心 send→輪詢→completed/failed + 🔴drain 語意延伸 D-CONC-01 + pollIntervalMs 延遲量測非阻擋 + cancel-before-consume）+ ZERO 3（OrphanReaper/CancellationPoller 零改動回歸）+ STATIC 3（命名鎖定 + P2a STATIC-003 前瞻性守門重跑）+ REG 4（tsc gate + postgres 黑箱契約不變 + sqlite/既有 static-guards 不回歸）；分層免真實連線（39 案例）vs 真實 MSSQL（E2E 5 案例）；不需 spec-writer | P0-MVP | [infrastructure/AD-E07-40-P2b-test.md](infrastructure/AD-E07-40-P2b-test.md) | 44 | Draft |
| AD-E07-40-P2c | MSSQL 全面遷移 P2c（P2 最後一片）Expire Sweep 整合 + 兩層回收一致性 + 全 F098 套件整體回歸（不重測 expireSweep() SQL 本身，P2a 已驗證黑盒依賴）——MOUNT 6（🔴掛載機制未定案決策關卡，AD §4.3 明文交 tdd-implementation 擇一 + 啟動立即掃 + 定期觸發 + 零週期防呆 + 🔴destroy 清 timer + unref 靜態）+ 🔴RECOVERY 10（旗艦群組，AD §7 DoD #2：兩層回收正面三案例（含順序無關性）+ 獨立性直接反證兩案例 + 冪等終態 + retry=0 整合語境重新確認 + 正常完成對照組防誤殺 + 批次崩潰 + 🔴「入列未消費」孤兒邊界串連 P2a claim+P2b processPayload 防線+P2c OrphanReaper 三片段）+ STATIC 4（🔴docker-compose RUN_QUEUE_POLL_INTERVAL_MS 缺口 DoD #4 核心 + 單一位置守門鏈延續兩案例 + 新增週期 env 覆蓋建議性決策關卡）+ REG 5（🔴DoD #3 全套件整體重跑，失敗清單須恰等於已知排除集合 + TS-F098-PGINT-002 排除理由持續有效性守門 + postgres/sqlite 不回歸 + tsc gate）；🔴 test-designer 查證：expireSweep() 無 injectable now 參數，與 OrphanReaper.reap(now) 注入時鐘機制不對稱，時間控制須沿用 P2a SWEEP-001 之直接竄改 expire_at 技術；不需 spec-writer，P2 全範圍至此完成 | P0-MVP | [infrastructure/AD-E07-40-P2c-test.md](infrastructure/AD-E07-40-P2c-test.md) | 25 | Draft |
| AD-E07-41-P4a | MSSQL 全面遷移 P4a（P4 首片，ETL 引擎 MSSQL 化）Handler 群組一（extract/field_mapping/derived_field/type_cast/conditional，CTAS→SELECT INTO ##global temp）——🔴QUOTE 3（最優先決策關卡：雙引號識別碼跨 driver 相容性，AD 未提及、既有套件零經驗證）+ HELPER 11（3 個新共用 helper：createMssqlTempTable/getMssqlTempTableColumns/countMssqlTempTableRows，UNIT 5 + MSSQL 6，含🔴巢狀 FROM 插入點正確性守門）+ 🔴CLEANUP 6（I-MSSQL-TEMPTABLE-CLEANUP-01 落地，AD 明文未定案掛載位置，黑盒 spy 驗證 + MUST-FIX 決策記錄，查證 NodeOutputStore.cleanupAll() 為天然收斂點）+ DISPATCH 3（createDispatcher() 現行無驅動分支，決策關卡建議延後至 P4c 全部 9 handler 到齊才接線）+ EXTRACT 14（UNIT 7 + RESOLVE 4〔resolve-raw-table.ts 獨立 mssql 版，AD 未提及〕+ EQ 3）+ FIELDMAP 10（UNIT 6〔含🔴裸布林字面值守門〕+ EQ 4）+ 🔴DERIVED 16（旗艦群組：UNIT 6 + EQ 10，含🔴DERIVED-EQ-001 padStart/LPAD 截斷方向公式錯誤旗艦案例 + mergePhone 正則轉換，customer_core 最高頻表達式 7/12，AD 未提及）+ 🔴CAST 17（UNIT 6 + EQ 11，含🔴CAST-EQ-002 空字串 LIKE 陷阱旗艦案例 + getValidationRegex 三型別覆核結論：皆簡單字元類別可用 LIKE/SUBSTRING 達成 + DATE 無錨點寬鬆語意防過度修正）+ COND 7（UNIT 4 + EQ 3，最低風險群組，真實 customer_core 表達式驗證）+ REG 5 + STATIC 4；不需 spec-writer，P4-spike-2（併發+崩潰清理）已於 2026-07-08 全數通過、P4-0（customer_core schema）已獨立完成，前置閘已過 | P0-MVP | [infrastructure/AD-E07-41-P4a-test.md](infrastructure/AD-E07-41-P4a-test.md) | 96 | Draft |
| AD-E07-41-P4b | MSSQL 全面遷移 P4b（P4 第二片，ETL 引擎 MSSQL 化）Handler 群組二（merge/lookup，含 UPDATE...FROM 重構）——🔴🔴ALTERCOL 8（本切片最高風險，AD/任務書皆未提及：`ALTER TABLE ADD COLUMN IF NOT EXISTS ... TEXT` 為 T-SQL 保證語法錯誤，真實 31 個 lookup 節點每次月名單分派皆觸發；UNIT 3 MUST-FIX + GATE 1 決策關卡 + MSSQL 3 冪等性 + TRAP 1 陷阱佐證）+ 🔴UPDATEFROM 10（任務書點名之核心重構：PG `UPDATE tbl alias SET...FROM...` 就地宣告別名為 PG 特有語法，逐字翻譯拋 Invalid object name；UNIT 3 MUST-FIX + TRAP 1 + EQ 6 含🔴防笛卡兒積旗艦案例 EQ-001 + lookupFilter 誤配防禦 EQ-003）+ CLEANUP 4（延伸 P4a NodeOutputStore 機制至 merge 新建表／lookup 原地修改表兩種模式）+ DISPATCH 2（nodeType 契約 + createDispatcher 沿用 P4a 選項甲不重議）+ MERGE 15（UNIT 7 + EQ 8，含🔴MERGE-EQ-005 真實 m2→m3 鏈式合併旗艦案例、4 個真實節點 100% sameKeyName=true）+ RESOLVE 3（複用 P4a resolveRawTableMssql，🔴raw 表存在性檢查 INFORMATION_SCHEMA 大寫化，AD 未提及）+ SKIP 5（skip_row 分支，DELETE...WHERE NOT EXISTS 經查證 ANSI 相容屬實不需改，真實 pipeline 0% 觸發，防禦性覆蓋）+ DEFAULT 2（default_value 分支，同 0% 真實觸發）+ LOOKUP 8（通用防禦 UNIT 3 + EQ 5，真實 customer_core 代表情境）+ REG 5 + STATIC 4；不需 spec-writer，沿用 P4a 已落地 QUOTE-003/CLEANUP-003/DISPATCH-001 三項決策不重議，真實 etl-pipelines.json 查證 31 個 lookup 節點 100% legacy mode + noMatchStrategy='null'、81% 使用 lookupFilter | P0-MVP | [infrastructure/AD-E07-41-P4b-test.md](infrastructure/AD-E07-41-P4b-test.md) | 66 | Draft |
| AD-E07-41-P4c | MSSQL 全面遷移 P4c（P4 第三片，ETL 引擎 MSSQL 化，P4 最複雜切片）Handler 群組三（dedup/target-load，含 tie-breaker + customer_core UPSERT 兩段式 + target-load 三種 loadMode 全覆蓋）——🔴DEDUP 21（AD §4 核心：`_seq IDENTITY` tie-breaker，真實 3 組 key 配置 d1/d2/d3 + 決定性核心群組含同 timestamp/NULL timestamp/§4.3「決定性非同一實體列」語意落地）+ 🔴🔴TLDEDUP 11（本切片最高風險之一，AD 完全未涵蓋：target-load 內部另有兩處未記載 DISTINCT ON 站點——fullMode PK dedup + customer_core source_customer_no dedup，PG 註解明確承認 NULLIF(TRIM()) 正規化真實碰撞；UNIT 2 MUST-FIX + GATE 1 + MSSQL 3 + EQ 3 含🔴TLDEDUP-EQ-001 旗艦真實碰撞案例 + TRAP 1）+ UPSERT 13（AD §5.1：兩段式 UPDATE...FROM+INSERT...WHERE NOT EXISTS，UNIT 5 + TRAP 1 + EQ 7 含新列/既有列/ghost gate/冪等 DoD 核心案例）+ 🔴🔴FULLMODE 11（🔴🔴範圍擴大核心：AD/任務書完全未提及，4 個真實 pipeline 依賴 TRUNCATE+批次 INSERT，含 composite PK 旗艦案例仿 ob_pool_data）+ 🔴PARTITION 6（範圍擴大：AD/任務書未提及，ob_pool_data_list 唯一依賴之 partition_replace 路徑）+ CATALOG 6（getPrimaryKeyColumns/getColumns 雙路徑/inputColumnTypes 三站點 AD 未提及，🔴CATALOG-GATE-001 決策關卡：data_type 資訊超出既有 helper 範圍）+ 🔴CLEANUP 5（target-load 內部暫存表因回傳 tempTable='' 從未向 NodeOutputStore 註冊，P4a CLEANUP-003 機制天生不覆蓋，須獨立 try/finally 管理）+ LITERAL 5（系統字面值 cast + ghost gate LENGTH→LEN）+ 🔴DISPATCH 4（DoD 核心：createDispatcher() DB_TYPE 分支於本輪落地，P4a/P4b 兩度延後之決議）+ REG 5 + STATIC 5；不需 spec-writer，沿用 P4a/P4b 已落地 QUOTE-003/CLEANUP-003 決策不重議；真實 etl-pipelines.json 查證 target_load 被 6 條 pipeline 共用（非僅 customer_core） | P0-MVP | [infrastructure/AD-E07-41-P4c-test.md](infrastructure/AD-E07-41-P4c-test.md) | 92 | Draft |
| AD-E07-41-P4d | MSSQL 全面遷移 P4d（P4 收官切片）customer_core 56 節點端對端（真實 DAG 執行 + PG EQ 比對 + tie-breaker 業務級偵測，不重測 handler 內部 SQL，P4a/b/c 已覆蓋）——GATE 4（raw 表欄位清單衍生決策關卡 + datasources/extraction_tasks 存在性 + lookup 值域涵蓋決策關卡 + DAG 無環回歸）+ DISPATCH-E2E 2（方案甲/乙分工決策關卡 + triggerExecute 補充 smoke，非 triggerTest）+ 🔴E2E-RUN 7（DoD 核心：56 節點零錯誤 + 9 handler 類型全覆蓋 + ## 中途取樣 + customer_core 實際列數 + 容錯路徑 + 耗時觀察性 + 交叉核對）+ 🔴🔴ISTESTRUN 3（MUST-FIX：isTestRun=true 會使 target-load 靜默跳過寫入但 nodeLogs 顯示正常之陷阱，靜態守門 + 陷阱佐證對照組 + PG 對稱回歸）+ 🔴🔴TIEBREAK 5（AD §4.3 業務級偵測落地：C-TIE fixture 於真實 56 節點 DAG 中首次觸發 d1 之 _seq tie-breaker，DoD 核心決定性 + 3 次重跑穩定 + 跨引擎業務級探測不預設答案 + 對照組 + NULLS LAST）+ EQ-PG 6（degradable，5433 不可達即 skip 不阻擋核心 DoD，依 P4a RESOLVE-002 真實先例：旗艦逐欄逐列比對 + C-BOTH 衝突解決 + C-NULLBIZ 排除 + lookup miss 一致性 + skip 機制自我驗證）+ CHARSET 3（DoD 核心中文 round-trip + lookup 中文描述 + 中途取樣）+ LOOKUPHIT/LOOKUPMISS 3（基準對照組前提 + DoD 核心未命中 NULL + 隔離性）+ NULLEXC 3（DoD 核心 + 多欄同理 + 節點順序回歸防線）+ IDEMPOTENT 3（DoD 核心重跑不疊加 + tie 客戶冪等交叉 + logId 隔離回歸）+ 🔴CLEANUP-E2E 3（DoD 核心成功/失敗兩路徑全清 + LIKE 萬用字元防禦性掃描）+ REG 5（tsc gate + PG/sqlite 路徑不受影響 + P4a/b/c 套件不回歸 + 共用 cdmp_test DB 隔離 + PG 黑箱契約不變）+ STATIC 4（🔴節點數/邊數/nodeType 分佈事實鎖定：真實 56 節點/55 邊，非 AD 沿用之舊估算 53 + raw 表命名清單鎖定 + 🔴MUST-FIX isTestRun 靜態掃描 + 凍結清單最終確認）；不需 spec-writer；🔴🔴 raw fixture 範圍由任務書「5 來源」擴大至 14 張（5 extract + 9 lookup 來源表，31 個 lookup 節點依賴）；**P4（P4a/b/c/d）全範圍至此完成測試設計**，P4e bulk-load 明確排除另待 | P0-MVP | [infrastructure/AD-E07-41-P4d-test.md](infrastructure/AD-E07-41-P4d-test.md) | 51 | Draft |
| AD-E07-41-P4e | MSSQL 全面遷移 P4e（P4 最後一片）raw Staging Bulk-Load 寫入端（`raw-data.service.ts` 之 `openCopyWriter`/`formatCopyValue`/`supportsCopy`〔pg-copy-streams〕改為新增 `openMssqlBulkWriter`/`supportsBulk`〔tedious Request.bulk〕，來源端讀取已相容不需改）——🔴🔴TYPEMAP 14（三層型別鏈決策關卡：來源型別→raw 表型別→bulk Table 型別宣告，含 decimal/numeric/money 呼應 FINDING-P4D-01 同型缺陷家族 MUST-FIX + IDENTITY PK 語法陷阱佐證 + bulk 型別寬容度 Probe）+ 🔴🔴ISPG-GATE 9（本文件最高風險：現行 `isPostgres` 二元 gate 使 `DB_TYPE=mssql` 誤入 SQLite 分支，`createRawTable`/`tableExists`/`getTableColumns` 三者為 bulk-load DoD 直接前置依賴，缺口不修則 P4e 連第一步都無法執行，陷阱佐證 + 目標行為雙軌）+ 🔴🔴DISPATCH 6（同型於 AD-E07-40 P2b 先例之 AD 檔案改動清單缺口：`extraction-execution.service.ts` 之 `canStream` 判定式未同步擴充承認 `supportsBulk()`，新機制淪為未被觸發之死碼，陷阱佐證 + spy 驗證呼叫路徑）+ BULK-WRITE 7（DoD 核心列數/逐欄值/NULL 語意正確性 + 🔴 TypeORM mssql `databaseConnection` 底層物件曝露方式 Probe 決策關卡，本專案既有 mssql spec 從未用過此手法）+ NOESCAPE-CHARSET 5（🔴 formatCopyValue 文字跳脫邏輯不可誤用於型別化 bulk 協定之陷阱佐證 + 中文 round-trip 對齊 I-MSSQL-COLLATE-01）+ 🔴🔴BATCH 5（tedious Request.bulk 是否可跨多次呼叫達成記憶體有界之 Probe 決策關卡，呼應 CLAUDE.md ETL 紅線 + DoD 核心逐步增加佐證）+ E2E-EXTRACT 6（DoD 核心全量擷取完整跑通 + schema-drift 重建 + 冪等重跑，合成來源表驗證機制本身）+ PIPELINE-READ 3（銜接 P4a 已驗證 `ExtractHandlerMssql`，P4e 特有新整合點，非 P4d fixture 手法）+ PERF 2（吞吐量 POC 觀察性，AD 明文不要求對齊 PG COPY 數字，非阻擋）+ REG 5（🔴 DoD 核心 PG COPY 路徑既有三個 describe 區塊逐字不變 + 既有套件不回歸 + tsc gate）+ STATIC 3（🔴 formatCopyValue 未被呼叫之原始碼靜態掃描，與 NOESCAPE 陷阱佐證形成雙重防線）；不需 spec-writer；明確排除同檔案同缺陷但非 bulk-load 直接依賴之 `getColumnMetadata`/`getIndexedColumns`/`getRawData` 查詢與 `insertBatch` 非 full-mode 路徑（記入風險文件）；**MSSQL 全面遷移 P4（P4a/b/c/d/e）全範圍至此完成測試設計** | P0-MVP | [infrastructure/AD-E07-41-P4e-test.md](infrastructure/AD-E07-41-P4e-test.md) | 65 | Draft |
| AD-E07-42-P3a | MSSQL 全面遷移 P3a（P3 首片，Raw SQL 引擎移植）Stage 1 篩選（`stage1-sql-builder.ts`/`stage1-customer-core-clause.ts`/`stage1-sql-executor.ts` mssql 平行版本，JS↔MSSQL 逐案件等價 I-MSSQL-ENGINE-EQ-01）——🔴🔴CONCAT 3（本文件最高優先級發現：`stage1-sql-executor.ts:100` 之 `'CR' \|\| cremp.emp_nm` PG 專屬字串串接運算子，T-SQL 保證語法錯誤，AD 完全未提及、推翻 AD §1.1「executor 不需重新設計」表述；靜態守門 + 中文姓名旗艦 EQ）+ 🔴🔴AGE 7（AD §2.1 建議之 AGE 轉換公式 `DATEDIFF(YEAR,@ccWorkdt,dob)` 引數順序反轉會得負值年齡，MUST-FIX 旗艦紅燈守門用已知數值斷言攔截 + 閏年 2/29 邊界 + NULL 傳播 + 決定性 + workdt 驅動）+ YEARABOVE 8（中風險：year-above 前導數字**擷取**〔`^[0-9]+` 無錨點〕與 P4a 已驗證之全字串**驗證**〔`^[0-9]+$`〕語意層級不同，不可逐字複用，需自行以 PATINDEX 推導 + 空字串/全數字/首字元非數字三態邊界，逐一對稱 PG PORT-001~007）+ 🔴DISPATCH 4（`assignment-run-pipeline.service.ts`/`stage0-estimate.service.ts` 現行皆為二元 gate，同型於 P1c DISPATCH-001/P2b DISPATCH-001~003，spy 驗證 + 三分支互斥）+ GATE 4（query-composer 雙引號識別碼複用 QUOTE-003 regression 確認 + 🔴 dbo 六張表〔ob_pool_data/ob_pool_data_list/ob_list_definition/assignment_run/ob_monthly_run_result/customer_core〕與 P1b2/P4 系列共用之 Harness 環境依賴決策關卡 + CC-EQ oracle 方法論開放式選擇記錄 + customer_core UNIQUE 約束查證）+ EQ 14（Stage 1 核心規則，逐一對稱 PG EQ-001~014，含中文 LIKE round-trip）+ CCNULLEXC 6 + CCJOIN 2（EMPTY_CONDITIONS 陷阱）+ CCMISC 5（CITY/AND/JOINCARD/DEACT）+ CCEQ 6（依 GATE 決策路徑，預設改用測試檔內手算 JS oracle 而非 `executeStage1Chain`，因後者對 MSSQL 執行 customer_core 片段會拋 PG-only 語法錯誤）+ RUNEST 2 + IDEM 3 + CHARSET 2 + STATIC 4（含 dbo 共用表禁止 DROP/TRUNCATE 之 MUST-FIX 靜態守門）+ REG 3；不需 spec-writer，AD §5「是否需要 spec-writer」已裁定；🔴 Harness 核心限制：Stage 1 raw SQL 產出裸表名僅能解析至連線 login 之 DEFAULT_SCHEMA（dbo），無法比照 P1b1 獨立 schema 隔離，亦不可比照既有 F099/F109 PG spec 之「DROP+re-synchronize」（會破壞 P1b2/P4 系列共用之 dbo baseline），改採「共用既有表 + 前綴隔離寫入列 + 精準 DELETE」策略；明確排除 3b Stage 2~3 計分／3c 比例分派／3d CR 優先分派／3e `fn_calc_tier_level` 收尾 | P0-MVP | [infrastructure/AD-E07-42-P3a-test.md](infrastructure/AD-E07-42-P3a-test.md) | 73 | Draft |
| AD-E07-42-P3b | MSSQL 全面遷移 P3b（P3 第二片，本 AD §6.1 明文「風險最高單一區塊」）Stage 2~3 計分（`stage2to4-sql-builder.ts`/`stage2to4-sql-executor.ts` mssql 平行版本，JS↔MSSQL 逐列等價 I-MSSQL-ENGINE-EQ-01；承接已完成 P3a，視為已驗證黑盒依賴）——🔴🔴REGEX 三站點語意澄清 18（SAFESEX 5 + GATING 5 + EDUCAT 6 + META 2：三處 `~ '^[0-9]+$'` 皆為含 `$` 錨點全字串驗證，與 P3a `^[0-9]+` 無錨點前導擷取語意層級不同，**可直接複用 P4a 已驗證公式**非需自行推導 PATINDEX 新公式，但三處組合輸入互不相同需逐一驗證）+ 🔴🔴DISPATCH 4（test-designer 全新查證：`resolveStage2to4Strategy:174-180` 現行二元-ish gate，`DB_TYPE='mssql'` 會靜默落入 in-memory JS 路徑而非 SQL 下推，不拋錯但違反 I-NOLOAD-01，AD 完全未提及，同型於 P3a/P1c/P2b 已反覆出現之陷阱）+ 🔴🔴FALLBACK 7（test-designer 查證：`to_jsonb(o)->>'col'` 通用 fallback 為 live production path 非死碼，MSSQL 無等價單一 SQL 表達式，改為 SQL 生成前 TS 端 `INFORMATION_SCHEMA.COLUMNS` schema 檢查〔大寫〕，存在→直接欄位參照；不存在→SQL 生成時即產生字面值 `0`，P3b 範圍內唯一「非純語法轉換而是架構調整」站點，含 O(1) schema 查詢 MUST-FIX 守門）+ 🔴AGESCORE 6（test-designer 查證：Stage 2 AGE 與 Stage 1 AGE 為獨立站點，前者參考日為「今日」`SYSDATETIME()`/`age(dob)` 單引數隱含 `CURRENT_DATE`，非 `@ccWorkdt`，複製貼上 P3a 公式易誤植，MUST-FIX 旗艦守門以非當月工作月驗證計分結果不隨之變動）+ CARYEAR 5（複用 P3a `mssqlLeadingYearExpr` PATINDEX 手法 + 參考日期同型守門 + `year_produ varchar(4)` 欄寬限制改直接表達式測試）+ PJTP 4（`TRIM(CAST(...AS text))`→NVARCHAR，F105 composite 逐 score row 動態展開）+ CROSSAPPLY 3（`CROSS JOIN LATERAL`→`CROSS APPLY`，恆 1 列語意保留）+ DISTINCTFROM 5（`IS NOT DISTINCT FROM`→OR-NULL 重寫，對稱 F100 LEVTIER-002~005，含反面邊界防假性 fallback 匹配）+ 🔴UPDATEFROM 4（目標表併入 FROM + 🔴防跨 run 污染旗艦案例 UPDATEFROM-002）+ 🔴🔴DECIMAL 5（test-designer 全新查證：LOAN_RATE `CAST(...AS numeric)` 無精度宣告，T-SQL 裸 `NUMERIC` 預設 `NUMERIC(18,0)` 會四捨五入去除小數〔`ob_pool_data.loan_rate numeric(5,2)`〕，AD 未點名此具體站點，屬 FINDING-P4D-01 同型缺陷家族新發生位置，MUST-FIX 旗艦守門）+ 🔴CCDIM 18（9 個 customer_core 計分維度完整 EQ，AD §0 已解除過渡態限制，對稱 F104 SEX/BRANCH/EDU/CITY/PCD 矩陣 + 五欄 gating 交叉驗證）+ SCORE 7 + CJOIN 3 + AR 4 + S2CLEAN 2（對稱既有 F100/F102/F103 PG spec）+ FULLEQ 8（DoD 核心綜合情境）+ MONTHRUN-DIFF 2（AD §4.2 建議之真實月重跑跨引擎比對，性質同既有 F101/F102/F104 慣例，建議腳本/手動執行）+ CHARSET 3 + 🔴STATIC 7（零殘留 PG token〔`::`/`~`/`to_jsonb`/`->>` /`CROSS JOIN LATERAL`/`IS NOT DISTINCT FROM`〕掃描核心 + docblock stale Stage 4 殘留註解守門）+ HARNESS 5（依任務指示新增之 Harness 改善：`beforeAll` 冪等自建零 drift DDL + `afterAll` 條件式清理，消除 P3a 已知「CI 需先 bootstrap dbo」盲點，涵蓋 P3b 新增 6 張計分專屬表 + P3a 已依賴 6 張，合計 12 表）+ GATE 6 + REG 5；不需 spec-writer；明確排除 3c Stage 3/4 比例分派／3d CR 優先分派／3e `fn_calc_tier_level` 收尾 | P0-MVP | [infrastructure/AD-E07-42-P3b-test.md](infrastructure/AD-E07-42-P3b-test.md) | 131 | Draft |
| AD-E07-42-P3c | MSSQL 全面遷移 P3c（P3 第三片）Stage 3/4 比例分派（`stage3to4-ration-sql.ts` mssql 平行版本，JS↔MSSQL 逐列四元組等價 I-MSSQL-ENGINE-EQ-01；承接已完成 P3a+P3b，視為已驗證黑盒依賴）——🔴🔴DISPATCH 5（test-designer 直接查證 `executeStage2to3PushdownMssql:1113-1158` 現行完全未呼叫 `clearStage3Fields`/`runCrPrioritySql`/`runStage3to4RationSql`，對照 PG `executeStage2to4Pushdown` 四步僅完成第一步，mssql 月名單分派 dept_id/emplid/assignday 恆 NULL，P3b impl log 已預告之已知缺口，本文件為其正式閉環；DISPATCH-001 紅燈 MUST-FIX + clearStage3Fields 方言中立性決策關卡 + CR 前置刻意排除靜態守門非遺漏 + 三分支互斥 spy + DoD 核心觀察）+ 🔴🔴DECIMAL 2（test-designer 全新查證：dept/empl ration 兩處 `CAST(:param AS numeric)` 皆無精度宣告，來源 `ob_dept_pct.ration numeric(9,2)`/`ob_empl_set.ration numeric(10,2)`，T-SQL 裸 `NUMERIC` 預設 `NUMERIC(18,0)` 會於 VALUES CTE 建構階段即四捨五入去除小數，AD 未點名此具體站點，屬 FINDING-P4D-01 同型缺陷家族新發生位置；DECIMAL-RATION-001 已知具體數值旗艦守門：3 課 33.67/33.67/32.66、cnt=300→正確 `{102,101,97}`）+ 🔴GATE-004（修正任務指示原始假設：逐行掃描確認本檔零命中 `::date`/DATEADD/DATEDIFF 等日期方言轉換站點，`ob_monthly_run_result.assignday` 實際型別為 `varchar(100)` 非 date/datetime2，避免下游誤植不必要轉換）+ VALUESCTE 3（dept_pct/empl_set/cal 三處 `WITH x(cols) AS (VALUES...)`→derived table 包裝）+ WINDOWFN 3（AD 標「低風險」仍驗證單一列 partition 之 `ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING` 空框架邊界）+ 🔴UPDATEFROM 4（3 道 UPDATE...FROM 重構 + 🔴防跨 run 污染旗艦案例 UPDATEFROM-004）+ TOPLIMIT 2（`LIMIT 1`→`TOP(1)`，`hasEmplRows` 存在性檢查）+ DEPT 6 + EMPL 5 + ASGD 3（對稱既有 F101 PG spec 手算 oracle）+ 🔴CRFILTER 3（`is_cr` 篩選子句〔F102 落地時已疊加於 PG 版〕mssql 翻譯正確性，CRFILTER-002 對稱 F102 202606 live bug「CR 案 assignday 全空」MUST-FIX 旗艦；明確排除驗證 CR 完整業務流程本身，屬 P3d 範圍）+ 🔴EQ 5（DoD 核心，EQ-005 旗艦含 CR 預指派案件混合情境）+ IDEM 2 + REG 6 + STATIC 3；不需 spec-writer；🔴 Harness 範圍顯著小於 P3b：僅觸及 `ob_monthly_run_result`/`ob_pool_data`/`assignment_run` 三張表，皆屬 P3a 已確認共用 dbo 六表子集，不需自建新表；明確排除 3d CR 優先分派（`cr-priority-sql.ts` 本身）／3e `fn_calc_tier_level` 收尾 | P0-MVP | [infrastructure/AD-E07-42-P3c-test.md](infrastructure/AD-E07-42-P3c-test.md) | 56 | Draft |
| AD-E07-43-P5b | MSSQL 全面遷移 P5b（P5 全量 CI + 業務簽核，第二片）其餘 5 條生產 ETL Pipeline 端對端驗證（customer_core 以外：`E07-OBARRETURNDF_MIN_CAP-Load`/`E07-OBCALENDAR-Load`/`E07-OBEMPHIRE-Load`/`E07-OBPOOLDATA-Load`/`E07-OBPOOLDATA_LIST-Load`，9 handler 本身已於 P4a~c 驗證，本文件僅端對端接線驗證，工作量遠小於 P4）——GATE 5（raw fixture 欄位衍生決策關卡 + datasources/extraction_tasks 存在性 + DAG 無環 + 5 表 PK 事實核對 + loadMode 設定值鎖定）+ DISPATCHE2E 2（`createDispatcher()` 補充 smoke + handler 集合回歸）+ 🔴🔴E2E-RUN 16（DoD 核心：5 條 pipeline 各自節點完成 + 目標表交叉列數 + 抽樣內容正確性，含 114/122 欄兩條旗艦規模 pipeline + handler 類型涵蓋確認，本文件僅用 4 種 handler 類型〔raw_data_extract/field_mapping/conditional/target_load〕，與 P4d 9 種形成對比）+ FULLMODE-WIRING 5（4 條 fullMode 全量替換語意接線確認，composite PK/單欄 PK dedup 決定性、閏年邊界，機制本身沿用 P4c 已驗證結論不重新推導）+ PARTITION-WIRING 4（`ob_pool_data_list` DELETE+INSERT 分區隔離接線確認 + 月名單分派專屬 7 欄不受本 pipeline 觸及之事實鎖定）+ 🔴🔴ATOMIC 6（**本文件最高風險發現，test-designer 全新查證**：逐行核對 `pipeline-runner.ts` 全檔零交易保護，`target-load-handler-mssql.ts` fullMode/partition_replace 皆為「先 TRUNCATE/DELETE、後單一 INSERT」兩段式且無 NOT NULL 業務欄防呆〔ghostGate 僅 UPSERT 路徑獨有〕，P5b 5 條 pipeline 又皆無 type_cast 節點防線，MUST-FIX 決定性探測「INSERT 失敗後目標表是否被留在已清空狀態」，不預設答案但要求務必執行並記錄結論，PG 版同構對稱確認為兩引擎共通既有架構特性非 MSSQL 專屬缺陷）+ ISTESTRUN 3（沿用 P4d 已驗證陷阱機制，補齊 fullMode/partition_replace 兩路徑之陷阱佐證，UPSERT 路徑已由 P4d 驗證不重複）+ FIELD 10（🔴`ob_emphire.resign_date` 哨兵 9999-12-31→NULL 之 `cd1` conditional 節點正確性，與既有專案記憶 `feedback_emphire_active_resign_sentinel` 存在字面張力之開放性問題交裁定 + 中文 round-trip + numeric/date 隱式轉換 happy path round-trip + NOT NULL 業務欄 happy path + 系統欄位逐表事實核對）+ IDEMPOTENT 5 + CLEANUP 4 + EQ-PG 6（degradable，5433 可達才比） + REG 6（含🔴REG-006 執行順序協調決策關卡：本文件 fullMode TRUNCATE 語意與 P3a/P3c/P3d 既有依賴之 dbo 共用表「禁 DROP/TRUNCATE」原則存在直接張力，需明確記錄協調策略）+ STATIC 6；不需 spec-writer，比照 P1~P4 系列先例；🔴 P5b 為 P5c（MONTHRUN-DIFF）之前置依賴，本文件 5 條 pipeline 完成後 `ob_pool_data`/`ob_pool_data_list`/`ob_emphire`/`ob_calendar`/`ob_arreturndf_min_cap` 五表方有真實資料供月名單分派比對 | P0-MVP | [infrastructure/AD-E07-43-P5b-test.md](infrastructure/AD-E07-43-P5b-test.md) | 78 | Draft |
| AD-E07-43-P5c | MSSQL 全面遷移 P5c（P5 全量 CI + 業務簽核，第三片）MONTHRUN-DIFF 真實完整月名單分派跨引擎逐列比對（AD §3.1 定調 manual/script，非新 CI 套件；F067 式簽核之技術附件；I-MSSQL-ENGINE-EQ-01 完整月名單分派層級最終驗收 + I-MSSQL-SIGNOFF-GATE-01 條件 (a) 之直接證據）——🔴🔴GATE 5（Tier 1 佐證力道決策關卡 GATE-002：JS oracle vs MSSQL 是否足以滿足 AD 字面「PG/MSSQL」要求交 architect/業務裁定 + 欄位計數聯集 10 欄裁定 + customer_core 繞開機制確認 + Harness 資料庫策略沿用 CDMP_TEST dbo 決策）+ HARNESS 5（fixture 矩陣 + Stage 1 單次執行雙寫兩個 run_id 機制 + 資料公平性前提守門）+ 🔴🔴CHAIN-EQ 10（DoD 核心旗艦：兩側皆連同一 MSSQL DataSource、複用既有 golden `executeV2`/`executeStage2to3PushdownMssql` 全鏈路徑、逐列 0 差異為判定基準，含分名單 + CR 專項×2 + ration=0 + BR-F102-08 + 🔴Stage2→Stage3/4 組合缺口專項 CHAINEQ-007 + customer_core 名單 + 冪等 + 🔴🔴差異根因分析框架決策關卡）+ DATECAST-BOUNDARY 3（承 P3d DATECAST-003，完整鏈路層級延伸揭露 `appl_date` 非午夜時間分量邊界，Probe 不預設答案，交 P5d 業務裁示）+ DIST 4（輔助性分佈摘要，比照 F067 §2/§8④ 格式，非核心判定依據）+ 🔴REPORT 5（DoD 核心：報告結構聲明與 F067 方法論明確區隔 + 案件級記錄格式 + 🔴🔴REPORT-004 Tier 範圍聲明決策關卡供 P5e 業務簽核判斷佐證力道）+ PG-ENHANCE 3（degradable：Tier 2 `pgPortReachable()` 5433 可達時真實 PG 執行比對 + Tier 3 既有 PG 快照加強建議記錄性）+ STATIC 2 + REG 2；不需 spec-writer，比照 P1~P5b 系列先例；本文件正式取代並擴大 P3b §二十一 `MONTHRUN-DIFF-001/002` stub（原僅 3 欄無方法論）；**MSSQL 全面遷移 P5（P5a/b/c）技術驗證主體至此完成**，P5d（datetime2 業務裁示）/P5e（F067 式簽核）/P5f（部署 bootstrap，可選）留待業務/維運流程 | P0-MVP | [infrastructure/AD-E07-43-P5c-test.md](infrastructure/AD-E07-43-P5c-test.md) | 39 | Draft |
| AD-E07-43-P5g | MSSQL 全面遷移 P5g（P5 核心範圍外之新增子切片，AD §7.4 裁定不阻擋 P5a~f/P5c/P5e，但使用者裁定「切換前必做」）ETL target_load ATOMIC 資料完整性修法——`target-load-handler(.ts/-mssql.ts)` fullMode（TRUNCATE+INSERT）/partition_replace（DELETE+INSERT）交易包裝（架構師推薦方案 (a)，新增不變式 I-ETL-ATOMIC-LOAD-01），本文件為 P5b §六 ATOMIC 探測（凍結清單 report-not-fix）之正式解凍修法驗證——🔴🔴GATE 4（交易管理位置決策關卡 + isolation level 不可退化為 READ UNCOMMITTED + Harness 沿用確認 + 暫存表建立時機決策）+ 🔴🔴TXNCORE 7（DoD 核心：MSSQL 側沿用 P5b 4 種壞值形狀壞值注入手法之修法後回歸，分支 A 資料遺失→分支 B 資料保留 + 可觀察性不劣化 + 🔴可恢復性重跑）+ PGTXN 6（degradable `P5B_PG_DB`：4 種壞值形狀 PG 對稱版 + 兩引擎對稱結論鎖定旗艦 + skip 自我驗證，落實 I-ETL-ATOMIC-LOAD-01「兩引擎共通不得僅修其一」）+ 🔴🔴UPSERTATOMIC 3（**本文件最高風險新查證發現**：I-ETL-ATOMIC-LOAD-01 文字僅列 fullMode/partition_replace，未涵蓋 customer_core UPSERT 兩段式 `UPDATE...FROM`+`INSERT...WHERE NOT EXISTS`〔MSSQL〕之部分套用不一致風險，MUST-FIX 驗證 updateSql 成功但 insertSql 失敗時 update 亦須回滾 + PG 單句天生原子回歸 + 不變式範圍決策關卡建議 architect 擴大文字）+ 🔴🔴SCOPE 6（DoD 核心旗艦：`queryRunner.isTransactionActive`〔已查證 TypeORM 既有公開 API〕黑盒操作化「交易不可跨節點邊界殘留」，落實任務指示「非整個 pipeline」要求 + 上游節點/前置唯讀查詢不受影響回歸 + 🔴7.8M 列是否分批決策關卡，不預設答案，若分批需交 architect 確認與不變式文字之潛在張力 + `##` 表相容性）+ 🔴🔴ISOLATION 4（**test-designer 新查證，真實環境驗證架構師 §7.2 聲明**：雙連線手動編排 harness 繞開 handler 直接驗證資料庫引擎隔離語意，PG MVCC 非阻塞確認 + MSSQL 預設 Read Committed 無 RCSI〔`mssql-init.sql` 查無 SNAPSHOT 設定〕是否阻塞查詢之不預設答案探測 + 業務體感記錄性決策關卡 + 規模無關性說明）+ REGRESSION-SUCCESS 6（DoD 核心：4 條 fullMode + partition_replace + customer_core UPSERT 乾淨路徑不破壞回歸 + dedup 決定性 + 冪等 + 觀察性效能）+ IMPACT 4（🔴 grep 全部 pipeline 鎖定 6 條 target_load 使用路徑全集 + 異動點界定交 GATE-001 + 🔴🔴P5b 既有 ATOMIC-001~006 案例將與修法後行為直接矛盾之 MUST-FIX 防誤判回歸守門 + node-dispatcher 不受影響）+ 🔴🔴CLEANUPTXN 4（**test-designer 新查證**：PG 交易內陳述式失敗後連線進入 aborted 狀態、僅接受 ROLLBACK，若 handler 未先 rollback 即 re-throw 會使 `pipeline-runner.ts` 外層 `outputStore.cleanupAll()` 連帶失敗，MUST-FIX 黑盒清理階段可觀察錯誤守門 + MSSQL 無 `XACT_ABORT` 對稱行為不預設答案探測 + MSSQL 既有 finally 回歸 + PG 既有暫存表清理缺口記錄性不擴大範圍）+ STATIC 4（交易呼叫靜態掃描 MUST-FIX + `pipeline-runner.ts`/`node-dispatcher.ts` 未改回歸 + 呼叫站點對稱 + 其餘 8 handler 未改）+ REG 5（tsc + P5b 既有套件同步更新非單純不回歸 + PG/sqlite 路徑不變 + 共用表隔離慣例 + 月名單分派套件不受影響）；不需 spec-writer；Harness 完全沿用既有基礎設施不新建（MSSQL 側 P5b `CDMP_P5B`、UPSERT 側 P4d `CDMP_TEST`、PG 側 P5b `P5B_PG_DB`+`pgPortReachable()`）；明確排除 P5b 探測工作本身（已完成）／P5c MONTHRUN-DIFF／P5d datetime2／P5e F067 簽核（互不阻擋）／7.8M 列真實效能數字（決策關卡留待 tdd-implementation 真實量測） | P0-MVP（使用者裁定切換前必做） | [infrastructure/AD-E07-43-P5g-test.md](infrastructure/AD-E07-43-P5g-test.md) | 53 | Draft |
| AD-E07-41-P4-followup | MSSQL 全面遷移 P4 收尾 tech debt — raw-data.service 完整性（P4/P5 期間刻意 scope-out 之收尾：CREATETABLE-FINDING 字串來源 PK 修法 + `getColumnMetadata`/`getIndexedColumns`/`getRawData` 分頁 `LIMIT?OFFSET?`/`insertBatch` 非 full-mode 路徑，四者現行二元 `isPostgres` 誤入 SQLite 分支之修法驗證）——**★★ 設計階段直接對本機 dev MSSQL 2022 容器 `cdmp-mssql` 實測 13 項關鍵假設（V1-V13），取代理論推導**：V1/V2 推翻任務書「450 字元為 DDL 成功門檻」隱含假設（任意 `n≤4000` 皆成功，唯一決定因素是是否為 `MAX`）+ V3/V4 精確邊界（clustered 900 bytes／nonclustered 1700 bytes，皆 INSERT 時依實際值判定非宣告長度）+ V5 複合鍵 900-byte 全欄加總（225+225 OK／400+400 FAIL）+ V6 無靜默截斷（`#8016`/`#2628` 兩種拒絕路徑）+ V7 MSSQL 參數上限確切為 2098（非官方文件字面 2100，`#8003`）+ V8/V9 `OFFSET...FETCH` 語法（`#153`）與分頁邊界正確性 + V10-V12 catalog 查詢形狀（含 `information_schema` 小寫物件名稱拋 `#208`，強化 `I-MSSQL-CATALOG-CASE-01` 至物件名稱層級）+ V13 現行陷阱程式碼確切錯誤代碼（`#102`）——🔴🔴PKFINDING 10（三候選決策關卡：任務書 (a) 有界 `NVARCHAR`/(b) `_cdmp_id` surrogate + 本文件真庫實測發現之候選 (c) `NONCLUSTERED PK`+`_cdmp_id` `CLUSTERED`，V1-V6 全數真庫驗證 MUST-FIX）+ GETCOLMETA 5 + GETIDXCOLS 5（含 >100,000 列 Observability 非阻擋）+ 🔴🔴GETRAWDATA-PAGE 9（`_cdmp_id` 非保證存在之澄清，`_cdmp_extracted_at` 為唯一無條件存在欄位）+ 🔴🔴INSERTBATCH 9（援用既有 Pattern B `escapeQueryWithParameters` 慣例）+ E2E-API 6（含查證修正 P4e `DISPATCH-005`「僅 incremental」文字精確度，full-mode 非 streaming 來源同樣觸發）+ 🔴🔴REG 5（test-designer 查證 postgres/sqlite 於此 4 方法亦零既有覆蓋，重新定調為新建基準）+ STATIC 3；不需 spec-writer，比照 P1~P5 系列先例 | P2-TechDebt（非阻擋 cutover） | [infrastructure/AD-E07-41-P4-followup-rawdata-test.md](infrastructure/AD-E07-41-P4-followup-rawdata-test.md) | 52 | Draft |
| AD-E07-43-P5-followup-display | MSSQL 全面遷移 P5 收尾顯示層兩項 follow-up（命名更正：任務指示原稱「P4-followup」，逐行查證兩項來源皆為 AD-E07-43/P5h/P5i，非 P4，已更正為 P5-followup）——**CRNM**：`ob_monthly_run_result.cr_nm` 於 MSSQL baseline 現為 `varchar(50)`（P5i「非 schema 產生器 entity」而未被機械掃描涵蓋之明文 out-of-scope follow-up），沿用既有 `nvarcharColumnType` helper 修為 `nvarchar(50)`——🔴🔴test-designer 真庫新查證（dev CDMP + docker cdmp-postgres 唯讀）：★發現 1 此並非單純顯示截斷風險，而是 Stage 1 set-based `INSERT...SELECT` 整批寫入失敗風險（真庫證實 SQL Server 對 varchar 溢位採明確拋錯、非靜默截斷）；★發現 2 現行風險機率極低（PG production 實測 cr_nm 現有最長值僅 5 字元，`ob_monthly_run_result` 有 26,695 筆真實非空值列，非死欄位）——GATE 4（inline 組裝／fixture 深度／sqlite 分支／baseline 更新方式決策關卡）+ CRNM-SCHEMA 5（現況鎖定+防範圍誤擴大 MUST-FIX，已用真 INFORMATION_SCHEMA 驗證）+ CRNM-ROUNDTRIP 4（沿用 P5i round-trip/contrast 手法，含真庫已驗證逐字錯誤訊息）+ 🔴🔴CRNM-WRITEPATH 5（本文件核心新增群組：透過真實 `stage1-sql-executor(-mssql).ts` 生產寫入路徑〔非孤立探針表〕驗證修法前後對照，含整批失敗決定性佐證）+ CRNM-REG 4；**APLFMT**：`assignment-run-report.service.ts::formatApplDate()`（P5h §7 AD-3 明文記錄之 follow-up：JS Date getter 對 PG/MSSQL 無單一正解）改為 SQL 端 `CONVERT`/`to_char` 格式化——🔴🔴★發現 3 真庫量化：production `appl_date`（PG cdmp_dev，1,679,489 列）之 wall-clock ≥16:00 危險帶佔 **15.4%**（258,461 列），非邊緣案例；★發現 4 真 MSSQL 逐秒邊界驗證（15:59:59 現行正確／16:00:00 現行 +1 日錯誤）+ `CONVERT`/`FORMAT` 兩函式全樣本皆正確；★發現 5 既有 `f064-export-23col.spec.ts` sliceFn 靜態測試對修法程式碼位置之隱性約束（須 inline，非外部 helper）——APLFMT-BOUNDARY 6（🔴🔴 DoD 核心，真庫逐秒驗證表格）+ APLFMT-EXPORT 5（DoD 核心跨引擎一致性 + F108 樞紐頁不受影響回歸）+ 🔴🔴APLFMT-STATIC 4（MUST-FIX：既有 `TS-F064-APLDATE-002` 等 4 項 sliceFn 靜態測試相容性守門）+ APLFMT-REG 3（含既有 `pattern-b.mssql.spec.ts` PARAM-016 真 MSSQL 語法驗證天然涵蓋確認）；不需 spec-writer，比照 P1~P5 系列先例；MSSQL 尚無業務資料（dev CDMP 現況空庫），本文件真實生產規模/分佈佐證改以唯讀查詢 docker `cdmp-postgres` 取得 | P2-TechDebt（非阻擋 cutover） | [infrastructure/AD-E07-43-P5-followup-display-test.md](infrastructure/AD-E07-43-P5-followup-display-test.md) | 40 | Draft |
| AD-E07-41-P4f-codedecode | MSSQL 全面遷移 P4f（F110/US-173 `code_decode` 節點，接續 P4a~e，SQL 生成正確性 + 真實等價安全網 + 效能 + Migration 驗證）——GATE 5（fixture 沿用 P4b 決策 + `_cdmp_id` 存在性逐表核對 + EQ 代表性群組選擇 + migration 事實來源 + Harness 沿用確認）+ SQLGEN 18（兩 dialect 各自驗證 I-CODEDECODE-JOIN-FILTER-01/DEDUP-TIEBREAK-01/NORMALIZE-01/COLLISION-01 SQL 結構 + `SELECT INTO` 非就地 ALTER+UPDATE + MSSQL `OPTION (HASH JOIN)`/PG 無 hint 負向確認）+ DISPATCH 4（第 10 對 handler 註冊，`lookup`/`code_decode` 並存不覆蓋）+ 🔴🔴EQ-MSSQL 20（DoD 核心安全網，US-173 AC-2：3 個代表性字典群組──單一等式/複合條件/無 filter──涵蓋 happy-path/NULL/TRIM 邊界/collation 契約/重複 key 取首筆一致性/9-mapping 旗艦/防笛卡兒積/中文 round-trip/冪等；兩側皆在同一顆 MSSQL 引擎執行，不受 5433 不可達先例影響，非 degradable）+ EQ-PG-BYTEIDENTICAL 7（degradable，AC-9/BR-11）+ PERF-NFR 4（live-verified 非 CI 斷言，US-173 AC-3 門檻定義）+ MIGRATION 14（`etl-pipelines.json` 收斂定義核對 + `step_count`=34/`version`=14 + `up()`/`down()` 冪等回滾 + I-CODEDECODE-MIGRATION-01 PG/MSSQL byte-identical + mojibake description 替換）+ REG-LOOKUP 5（AC-8，既有 F043/P4b lookup 套件不回歸）+ STATIC 4 + REG 5；不需 spec-writer（F110 spec 已完成）；姊妹文件 [features/F110-test.md](../features/F110-test.md)（28 場景，dialect-neutral 設定驗證/語意契約） | P0-MVP | [infrastructure/AD-E07-41-P4-codedecode-test.md](infrastructure/AD-E07-41-P4-codedecode-test.md) | 85 | Draft |
| **Infrastructure 小計** | | | **22 files** | **1319** | |
| **總合計**（含本輪 F119 +1 file/+115；2026-08-18 R-F119-04 補強 +3 files/+12） | | | **111 files** | **3742** | |

---

## 3. 自動化就緒度評估

### 適合自動化的場景

| 類別 | 場景數 | 說明 |
|------|--------|------|
| API 端點測試（Unit + Integration） | ~192 | 所有 CRUD 操作、驗證邏輯、錯誤碼回傳（含 E04 擷取任務 API、F026 raw data 預覽 API、新增 GET /datasources/:id/schemas 及 GET /datasources/:id/schemas/:schema/tables；新增 F027 統計 API 與列表 API；新增 F029 GET/PUT definition API、GET raw-tables API；新增 F033 版本清單/詳情/Diff/回滾/發布 5 個端點，涵蓋 PIPELINE_VERSION_NOT_FOUND 與 PIPELINE_PUBLISH_REQUIRES_TEST 錯誤碼；新增 F037 PATCH publish 端點，涵蓋 PIPELINE_VERSION_ALREADY_PUBLISHED 錯誤碼） |
| 安全性測試（RBAC / Token / SQL Injection） | ~26 | 角色權限驗證、Token 失效、輸入消毒；新增 raw data 表名安全（BR-13）與欄位名稱 sanitize；F027 RBAC（User 403）；F029 RBAC（User 403 for GET/PUT definition）；F033 RBAC（User 403 for 全部版本管理端點） |
| 資料驗證（邊界值、格式） | ~26 | 密碼長度、Port 範圍、Email 格式、cron 格式；新增 F026 分頁 limit 白名單驗證；F027 todayProcessed 時區邊界；F029 changeSummary 500 字元邊界 |
| 排程邏輯測試（E04） | ~8 | 使用 scanAndExecute(fakeNow) injectable time 參數直接呼叫排程邏輯 |
| raw data 落地驗證（E04 F021） | ~12 | 動態建表、批次寫入、全量 TRUNCATE、增量追加，使用 Test Container 驗證 AppDB 資料 |
| F026 raw data 預覽 API | ~16 | 分頁、排序、錯誤碼；使用 Test Container 建立受控 raw data 表 |
| F042 ETL 執行引擎核心單元測試 | ~14 | 拓撲排序、Dispatcher 分派、輸入收集邏輯、記憶體回收均為純函數，不需 DB，可全部自動化 |
| F042 節點狀態回寫整合測試 | ~7 | 需要 Test Container 驗證 node_logs 即時回寫 DB；含 running/completed/failed/skipped 狀態轉移 |
| F043 節點執行器單元測試 | ~52 | 除 RawDataExtract 外，其他 7 種節點（含 Lookup）均為 In-Memory 純函數，全部可不依賴 DB 自動化；Lookup 向下相容模式需 Mock queryRunner（~10 場景） |
| F043 RawDataExtract 整合測試 | ~4 | 需 Mock queryRunner 或 Test Container 驗證批次 SELECT 與表存在性檢查 |
| F043 LookupExecutor 整合測試 | ~2 | TS-F043-058（扇出場景）需 F042 ExecutionEngine 框架支援，屬整合測試範疇 |
| F044 Target Load 整合測試 | ~13 | UPSERT INSERT/UPDATE、customer_id 不覆蓋、ETL 追蹤欄位、批次邊界均需 Test Container |

### 需手動或半自動測試的場景

| 類別 | 場景數 | 說明 |
|------|--------|------|
| E2E 瀏覽器流程 | ~18 | 完整登入→操作→登出流程；新增 F026 從日誌 Drawer 導航至預覽頁面的 E2E 流程；新增 F029 視覺化編輯器前端場景（未儲存離開確認對話框、非法連線視覺提示） |
| 視覺驗證 | ~8 | 儀表板圖表渲染、狀態色彩標示；F026 水平捲動表格；F027 狀態 Badge 顏色；F029 畫布節點顏色/圖示（Extract=藍色、Transform=橘色、Load=綠色）、連線箭頭、紅色非法連線提示 |
| 效能測試 | ~8 | 負載測試、並發測試需專用工具；F026 百萬筆分頁查詢效能（需 Test Container + 1,000,000 筆受控資料集） |

### 環境依賴

| 依賴項 | 測試影響 | Mock 策略 |
|--------|---------|-----------|
| 目標資料庫（MySQL / PostgreSQL / SQL Server） | F015、F016 連線測試 | Mock DB Driver 或 Test Container |
| Email 服務（SMTP / SendGrid） | F009 密碼重設 Email | Mock Email Service |
| 時鐘 | Token 過期、Reset Token 過期 | Clock Mock / Time Travel |
| 排程計時器（E04） | F023 排程觸發 | scanAndExecute(fakeNow) injectable time 參數，無需真實計時器 |
| 非同步執行（E04） | F021 執行結果驗證 | waitForTaskStatus(taskId, status, timeoutMs=5000) polling helper，interval=300ms |
| 外部資料庫（E04 F021） | raw data 動態建表、批次寫入驗證 | 使用 Test Container（MySQL / PostgreSQL）模擬外部來源，搭配 AppDB Test Container 驗證資料落地 |
| 外部資料庫（E04 F017/F019 schema/table 查詢） | GET /schemas 及 GET /tables 端點連線失敗場景 | Mock Datasource Service 的 `getSchemas()` / `getTables()` 方法；或使用 DS_PG_DISCONNECTED 搭配 Test Container 停用 PG 實例 |
| raw data 效能測試（E04 F026） | F026 百萬筆分頁查詢 | Test Container + controlled dataset（1,000,000 筆）；僅於 QA 環境執行，不納入 CI Pipeline |
| 時區（E04 / E05） | F018 今日統計、F024 今日統計、F027 todayProcessed | todayInTaipei() 種子資料工廠函式，CI 設定 TZ=Asia/Taipei |
| 視覺化畫布函式庫（F029） | F029 前端 E2E 場景（TS-F029-029 ~ 031） | 依賴 React Flow（或同等函式庫）；建議搭配 Playwright 或 Cypress 進行拖拉操作模擬 |
| ETL 執行引擎（F042-F044） | 節點狀態回寫、UPSERT 寫入、customer_core 驗證 | Test Container（AppDB PostgreSQL）；F043 RawDataExtract 需額外 raw table 模擬；F044 部分批次失敗需 Mock queryRunner 或注入錯誤觸發機制 |
| `customer_core` PG-only（F109） | 含 customer_core 條件之 Stage 1 篩選測試（NULLEXC/AGE/CITY/AND/EQ/JOINCARD 群組） | 無 TypeORM Entity、僅存在 PostgreSQL；一律 `.pg.spec.ts` 真實連線，SQLite 環境不建表；與 F098~F104 pg.spec 共用 `cdmp_test` DB 序列執行 |
| MSSQL 容器（AD-E07-38 P1a） | CONN/TYPE/LOGIN/CRUD/CASE 群組全數需真實 MSSQL 連線 | `docker compose --profile mssql up -d mssql mssql-init`（`localhost:1433`/`CDMP`，collation `Chinese_Taiwan_Stroke_BIN`）；建議新增 `.env.test.mssql` + `mssql-env-preload.ts`（比照 `.pg.spec.ts` gating 模式），連不上時 `.mssql.spec.ts` 整檔 skip，不假造綠燈。**已知缺口**：現行 `docker-compose.yml` 之 `mssql` 服務無如 `postgres-test` 之獨立 test-only port/DB 分離（僅單一 dev 用 `CDMP` 資料庫），見 `risks-and-gaps.md` R-MSSQL-P1A-01 |
| MSSQL 容器（AD-E07-39 P1b1） | CHI/TYPE/HASH/ENTITY/DEFAULT/CASE/COLLATE/PKWIDTH 群組全數需真實 MSSQL 連線 | 完全沿用 P1a 之 `mssql-env-preload.ts`/`.env.test.mssql`（`DB_NAME=CDMP_TEST`，已解決 R-MSSQL-P1A-01 之 dev/test 污染風險）；新增專屬 SQL schema `p1b1`（與 P1a 之 `p1a` 並存不衝突）；全 37 表 `synchronize:true` 耗時明顯高於 P1a 之 4 表，建議評估提高 `beforeAll` timeout（見 `infrastructure/AD-E07-39-P1b1-test.md` §零.3） |

---

## 4. Agent Loading Guide

### TDD Developer Agent

**必讀檔案：**
1. `test-index.md`（本文件）— 瞭解整體範圍與優先級
2. 對應的 `features/F###-test.md` — 取得具體測試場景

**建議載入順序：** F001 → F002 → **F002SM** → F003 → **F045** → F004 → F005 → F006 → F008 → **F073-F074-E02**（E07 角色整合先行，Guard 設計影響後續 E07 實作） → F009 → F010 → F007 → F011 → F012 → F013 → F015 → F014 → F016 → F017 → F018 → F019 → F020 → F021 → F022 → F023 → F024 → F025 → F026 → F027 → F028 → F029 → F030 → F031 → F032 → F033 → F037 → F034 → F035 → F036 → F038 → F042 → F043 → F044 → **F046 → F047** → **F053 → F054 → F055 → F056** → **F075 → F076 → F109**（F109 依賴 F075「data_source」欄位擴充 + F076 可選值 seed 機制，須在兩者之後載入；F109 另依賴 F050/F051 條件選單分組 UI）

**F002SM Sales Manager Badge 特殊注意：**
- 後端 `LoginResult.user` DTO 須補充 `isSalesManager: boolean`；TS-F002SM-001 / TS-F002SM-006 驗證欄位存在性與 boolean 型別（不可為字串）
- 前端 auth-store `User` interface 須新增 `isSalesManager?: boolean`（optional，相容舊 token）
- Badge className 必須完整符合 prototype 27 line 123：`inline-flex items-center gap-1.5 px-2.5 py-1 text-xs font-medium bg-amber-50 text-warning rounded-md border border-amber-200`；`text-warning` 非標準 Tailwind class，需確認 tailwind.config 已定義（RISK-F002SM-004）
- DOM 不渲染原則：TS-F002SM-010 ~ 013 驗證 `false` / `undefined` / `null` / admin 角色時 badge 元素在 DOM 中完全不存在，非 `display:none`
- 建議 Badge 元素掛 `data-testid="sales-manager-badge"` 供自動化定位
- 種子帳號：manager@cdmp.test / P@ssw0rd123（role=user, is_sales_manager=true）已存在於 dev seed

**E07 M07 角色整合特殊注意（F073-F074-E02，v2.0 AD-E07 v3.0 對齊）：**
- **重構重點**：`is_sales_manager` + `e07_role` 已合併為 `business_role` enum；SalesManagerGuard 廢棄；Guard 名稱更名為 DirectorOrAdminGuard / SectionChiefOrAboveGuard
- **端點更名**：PATCH `/accounts/:id/business-role`（舊 `/e07-role` 已廢棄，TC-DEPRECATED-002 驗證回 404）
- **JWT claim 更名**：`businessRole`（舊 `e07_role` claim 不再出現）
- **廢棄端點 regression**：TC-DEPRECATED-001~005 含 Static Analysis grep test，確認 SalesManagerGuard / is_sales_manager / e07_role 在 src/ 為零存在
- **Fixture 更新**：使用 buildAdminUser / buildDirectorUser / buildSectionChiefUser / buildRegularUser / buildLegacyUser；移除 buildUserOrthogonalSectionChief（正交已廢棄）
- **Legacy JWT**：TC-LEGACY-001~005 驗證無 businessRole claim 的舊 token 不導致 500；TC-LEGACY-003 確認 admin role bypass 仍有效（不依賴 claim）
- **m14 遷移隔離**：TC-MIG-m14-001~008 需獨立 migration test DB（與主 test suite 隔離，確保 schema 狀態可控）
- **冪等性行為（TC-E02-103）**：待 OQ-E02-001 確認；TDD Developer 需在 F073 v1.2 更新後才能最終實作
- **三欄位原子性（TC-E02-100~102）**：須在同一斷言中驗證 `business_role` + `password_changed_at` + audit_log 同時更新（transaction 原子性保證）

**E02 角色定義特殊注意（F045 / F004 / F005 / F008）：**
- F045 必須先於 F004/F005/F008 載入：F045 的 Seed Data 初始化（TS-F045-001）是後三者所有角色相關測試的前置條件
- 系統角色僅有 2 種：admin、user；role_code 超出此範圍回傳 422 VALIDATION_INVALID_ROLE
- F005 角色篩選 `?role=` 參數支援 admin / user；回傳欄位格式為 `{ roleCode, displayName }` 而非單純字串
- F008 最後 Admin 保護（AC-2）：系統僅 1 個 Admin 時，降級為 user 回傳 HTTP 422 ACCOUNT_LAST_ADMIN
- F008 前端確認對話框（TS-F008-FE-002）：對話框須顯示中文顯示名稱（含括號別名），不顯示 role_code

**E04 資料擷取特殊注意：**
- F017/F019 連鎖下拉選單：新增 `GET /datasources/:id/schemas` 與 `GET /datasources/:id/schemas/:schema/tables` 兩個端點測試；連線失敗回傳 503（DATASOURCE_SCHEMA_LOAD_FAILED / DATASOURCE_TABLE_LOAD_FAILED）
- F017/F019 前端測試：驗證連鎖下拉選單的初始停用狀態、載入順序、重置邏輯、連線失敗停用、無手動輸入 fallback
- F019 前端特殊：編輯表單初始化時並行呼叫兩個 API（GET /schemas + GET /tables），預選既有值；已執行任務變更 schema/table 時顯示警告 Modal
- F021 非同步測試使用 `waitForTaskStatus(taskId, expectedStatus, timeoutMs=5000)`（interval=300ms）
- F021 SQL 組合格式：`"source_schema"."source_table"`（含雙引號）；sourceSchema=null 時僅使用 `"source_table"`
- F021 raw data 落地驗證需雙 Test Container（外部 DB + AppDB）；測試完成後驗證 AppDB 中 `raw_{task_id_short}` 表內容
- F021 SQL Injection 安全測試（TS-F021-SEC-001、TS-F021-SEC-002、TS-F021-SEC-003）驗證表名格式、欄位名稱 sanitize 及 SQL 保留字引用
- F022 前端測試（TS-F022-009 ~ TS-F022-013）驗證「預覽資料」連結的顯示條件
- F023 排程測試使用 `scanAndExecute(fakeNow)` injectable time 參數（不依賴真實計時器）
- F026 meta 回應新增 sourceSchema 欄位；前端顯示格式為 `sourceSchema.sourceTable`（sourceSchema=null 時僅顯示 sourceTable）
- F026 效能測試（TS-F026-PERF-001 ~ 003）需 1,000,000 筆受控資料集，僅在 QA 環境執行
- F018 / F024 今日統計種子資料使用 `todayInTaipei()` 工廠函式（CI 設定 TZ=Asia/Taipei）

**E05 ETL Pipeline 管理特殊注意（F027 / F030 / F031 / F032 / F033 / F035）：**
- F027 todayProcessed 時區邊界測試與 F018/F024 相同，使用 `todayInTaipei()` 工廠函式，CI 設定 TZ=Asia/Taipei
- F027 統計卡片需驗證 total = active + running + draft + failed + disabled（數值一致性）
- F027 軟刪除驗證：stats API（total）與列表 data 兩處均不能出現 deleted_at IS NOT NULL 的記錄
- F027 伺服器錯誤降級：stub DB 拋出例外，驗證回傳 SYSTEM_INTERNAL_ERROR 且不含 stack trace
- F030 排程測試使用 `scanAndExecute(fakeNow: Date)` injectable time 參數（與 F023 相同模式，不依賴真實計時器）
- F030 非同步執行使用 `waitForPipelineStatus(logId, expectedStatus, timeoutMs=10000)` polling helper（interval=300ms）
- F030 測試執行（is_test_run=true）的 processed_count 不計入 EtlPipeline.processed_count 累計；EtlPipelineVersion.status 成功後從 draft 更新為 testing
- F030 狀態回歸驗證：active→running→active（執行成功）；draft→running→draft（測試執行成功）；running→failed（執行失敗）
- F031 排程引擎測試 seam：停用（removeJob）與啟用（addJob/registerJob）的排程引擎呼叫需透過 spy/mock 驗證；排程引擎若無法注入，TS-F031-002 / TS-F031-004 / TS-F031-006 應退為手動整合測試
- F031 冪等性行為：對已 disabled 的 Pipeline 再送出 enabled=false 的預期結果未定義，需向 Architecture 確認後補充場景
- F031 狀態轉換邊界：`draft → disabled` 是否允許、以及 running Pipeline 的後端 toggle 防護均為待確認項目（見 F031-test.md Risks and Notes）
- F032 軟刪除 Pipeline 日誌存取（BR-5 vs PIPELINE_NOT_FOUND）：`GET /api/v1/etl/pipelines/:id/logs` 在 Pipeline 已軟刪除時，規格 BR-5 說明日誌不清除，但錯誤碼表定義「已刪除」回傳 404；兩者衝突，需向 Architecture 確認（見 TS-F032-015）
- F032 logId 不存在的錯誤碼（TS-F032-018）：`GET /api/v1/etl/logs/:logId` 的 404 錯誤碼未定義（PIPELINE_LOG_NOT_FOUND vs PIPELINE_NOT_FOUND），需向 Architecture 確認後補充
- F032 nodeLogs 節點排列順序：規格未說明是按 definition 節點順序或實際執行起始時間排列，需確認後更新 TS-F032-006 驗證邏輯
- F035 儀表板統計（stats / trend / failures / slowest）均須排除 `is_test_run = true` 的 EtlPipelineLog；測試資料集必須同時含兩種 is_test_run 值以驗證隔離邏輯
- F035 今日統計時區邊界：今日 / 昨日邊界種子資料使用 `todayInTaipei()` 工廠函式，CI 設定 TZ=Asia/Taipei（與 F018/F024/F027 相同模式）
- F035 Polling 測試（TS-F035-021）：使用 fake timer（sinon / jest fake timers）控制 5 秒間隔，不依賴真實計時器
- F035 progressPercent 邊界：totalCount=0 時 progressPercent=0.0，不發生除以零錯誤（TS-F035-016）
- F035 successRate 精度：保留一位小數且四捨五入（75.0、88.9），分母為零時回傳 0.0（TS-F035-020）
- F033 版本清單（GET /versions）不含 `definition` 欄位；版本詳情（GET /versions/:versionId）才回傳完整 JSONB definition
- F033 Diff API 路由：`GET /versions/diff?from=N&to=M`；路由器需確保 "diff" 字串不被誤解為 versionId（路由優先順序需在實作層驗證）
- F033 回滾（POST /versions/:versionId/rollback）建立新版本，版本號為現有最大版本號 + 1；changeSummary 格式為「回滾自版本 N」
- F033 發布前置條件：版本 status 必須為 "testing"（代表已有 is_test_run=true AND status="completed" 的 EtlPipelineLog），否則回傳 PIPELINE_PUBLISH_REQUIRES_TEST（422）
- F033 發布後需驗證兩處：etl_pipeline_versions.status="published" 且 etl_pipelines.version=新版本號（BR-6）
- F033 發布新版本不改變舊 published 版本狀態（同一 Pipeline 可同時有多個 published 版本，排程以最大 version 號為準）
- F033 排程引擎選版本邏輯（TS-F033-017）：以 version 欄位最大值（非 created_at）選取最新 published 版本，與 F030 TS-F030-010 相同模式
- F037 發布版本（PATCH /versions/:versionId/publish）：僅允許 testing 狀態版本發布；draft → 422 PIPELINE_PUBLISH_REQUIRES_TEST；published → 422 PIPELINE_VERSION_ALREADY_PUBLISHED（新增錯誤碼）
- F037 Transaction 原子性（TS-F037-003 / TS-F037-020）：version.status 更新與 pipeline.version 更新必須在同一 Transaction；失敗需完整回滾，驗證需使用 Test Container（不可純 mock）
- F037 published_at 欄位：現有 EtlPipelineVersion entity 僅有 created_at，規格回應含 publishedAt 欄位；實作前需確認是否新增欄位或重用 created_at，已列為 Risks
- F037 前端 Toast 計時器（TS-F037-032）：使用 fake timer 控制 3 秒，不依賴真實計時器
- F037 端到端發布→啟用流程（TS-F037-008）：驗證發布後 F031 toggle 不再阻擋（PIPELINE_DRAFT_CANNOT_ENABLE 消除）

**E05 ETL Pipeline 管理特殊注意（F029）：**
- F029 連線驗證（BR-2 ~ BR-5）在後端 PUT definition 時執行：Extract 只能連 Transform；Transform 可連 Transform 或 Load；Load 為終端節點；禁止逆向循環連線
- F029 連線驗證錯誤碼：PIPELINE_INVALID_CONNECTION（HTTP 422），detail 欄位說明具體違反規則
- F029 step_count 更新（BR-6）：每次 PUT definition 後，etl_pipelines.step_count = definition.nodes.length；需同時驗證 API 回應 stepCount 與 DB 欄位
- F029 Transform 節點採樣策略：13 種 Transform 節點中，Lookup 已因雙輸入重設計（US-042 AC-7a~7d）提升為獨立覆蓋（TS-F029-032~037）；其餘採樣 Merge、Filter、Masking 三種進行 JSONB 儲存/還原完整性驗證（TS-F029-019~021），其餘 9 種節點結構以規格文件為準
- F029 重複 Extract 來源（BR-8 邊界）：同一 rawTableId 出現兩次時回傳 PIPELINE_INVALID_CONNECTION（422）；需向 Arch 確認是否有獨立錯誤碼
- F029 前端 E2E 場景（TS-F029-029 ~ 031、TS-F029-032 ~ 036）：依賴畫布函式庫（建議 React Flow）行為，需搭配 Playwright/Cypress 執行
- F029 changeSummary 邊界：500 字元合法（TS-F029-027），501 字元回傳 VALIDATION_ERROR（TS-F029-028）
- **F029 Lookup 雙輸入 UI（US-042 AC-7a~7d，2026-03-31 新增）**：
  - TS-F029-032：Lookup 節點兩個輸入端口（main-input top:33%, lookup-input top:67%）
  - TS-F029-033：lookup-input 連線後 edge 含 `targetHandle:"lookup-input"`；連線視覺樣式須與主資料流區別
  - TS-F029-034：雙輸入模式時 `lookupSource` / `lookupFilter` **不渲染於 DOM**（非 CSS 隱藏）
  - TS-F029-035：向下相容模式顯示 `lookupSource` / `lookupFilter` 及升級提示訊息
  - TS-F029-036：刪除 lookup-input 連線後面板自動回到向下相容模式
  - TS-F029-037：雙輸入模式 Lookup JSONB 儲存與還原（Integration，需驗證 edge.targetHandle="lookup-input"）

**E05 ETL 執行引擎特殊注意（F042 / F043 / F044）：**
- F042 拓撲排序與 Dispatcher 分派邏輯為純函數，不需 DB，可直接以 Vitest 進行單元測試，不需 Test Container
- F042 節點狀態回寫整合測試需使用 Test Container（AppDB PostgreSQL），驗證 running/completed/failed/skipped 四種 node_logs 狀態轉移
- F042 循環依賴偵測：排序結果長度 < 節點總數即為循環，驗證 error_message 包含「循環依賴」字樣
- F043 RawDataExtractExecutor：Mock queryRunner.query 時需分別 Mock 表存在性查詢（information_schema）與資料查詢（SELECT *）兩種 SQL 行為
- F043 MergeExecutor：FULL JOIN 欄位命名規則為左側欄位保留原名，右側衝突欄位加 `_right` 後綴；JOIN key 同名時僅保留一個欄位（取非 null 者，left 優先）
- F043 DedupExecutor：null timestampColumn 視為最舊（排在最後，不被保留）；時間戳相同時保留 index 最小者
- F043 DerivedFieldExecutor：gen_random_uuid 每列產生獨立 UUID，不可複用；CASE WHEN 中 `right.{col}` 對應 `{col}_right` 欄位（_right 後綴為 merge 節點輸出的欄位命名慣例）
- **F043 LookupExecutor（US-058，2026-03-31 新增）**：
  - 雙輸入模式（TS-F043-045~049）：`inputs['lookup-input']` 存在時，LEFT JOIN 純記憶體執行，不查詢 DB；重複 key 取首筆；null key 無匹配補 null；空對照集全部補 null
  - 向下相容模式（TS-F043-050~052）：`inputs['lookup-input']` 不存在時，Mock queryRunner.query 回傳 lookupSource 表資料；lookupFilter 空字串時不加 WHERE
  - 模式切換驗證（TS-F043-053~054）：舊版 Pipeline 定義可正常執行；雙輸入模式下 queryRunner 不被呼叫
  - 錯誤處理（TS-F043-055~057）：主資料流缺失 → `inputs` 無 `default` 亦無 `main-input`；matchColumn 不存在 → message 含欄位名與「主資料集」；lookupMatchColumn 不存在 → message 含欄位名與「對照資料集」
  - 扇出整合測試（TS-F043-058）：1 Extract → 3 Filter → 3 Lookup（各自 lookup-input 獨立）；需 F042 ExecutionEngine 框架支援
- F044 TargetLoadExecutor：customer_id 不在 DO UPDATE SET 中（保留原值）；_etl_loaded_at 與 _etl_pipeline_id 由引擎自動附加，不從輸入資料列取得
- F044 批次大小公式：`actualBatchSize = min(configuredBatchSize, floor(65535 / columnsPerRow))`；customer_core 欄位數 85，實際 batch size = 771
- F044 部分批次失敗：已寫入批次不回滾，outputRowCount 記錄已成功筆數；需注入錯誤機制（Mock queryRunner.query 在第 N 次呼叫拋錯）
- F044 is_test_run=true：目標表存在性仍需驗證（步驟 1-2 執行），僅跳過 UPSERT SQL（步驟 4-8）

**F038 孤兒任務回收特殊注意（跨 E04/E05）：**
- F038 無 HTTP 端點，測試入口為 `OrphanRecoveryService.onApplicationBootstrap()`
- Unit 測試使用 Jest + mock Repository（4 個）+ mock DataSource；Integration 測試使用 Test Container PostgreSQL（僅 AppDB，無需外部 DB）
- Integration 測試需透過 `@nestjs/testing` 的 `Test.createTestingModule()` 建立完整模組，不可直接 `new OrphanRecoveryService()`
- `duration_ms` 計算使用 PostgreSQL 專屬語法 `EXTRACT(EPOCH FROM (NOW() - started_at)) * 1000`，SQLite 不相容；Integration 測試只驗證 `IS NOT NULL`（不驗證精確值），需使用 Test Container PostgreSQL
- `etl_pipelines` 無 `error_message` 欄位（BR-10）：測試需驗證 update `.set()` 中**不含** `error_message` 鍵
- E04 和 E05 回收各自獨立 Transaction；E04 失敗不阻斷 E05，測試需覆蓋雙向獨立失敗場景（TS-F038-021, TS-F038-022）
- 冪等性測試（TS-F038-027, TS-F038-044）：第二次執行時 DB 中無 `status='running'` 記錄，回收靜默通過，驗證無副作用
- Module 順序驗證（TS-F038-040）：`AppModule.imports` 中 `OrphanRecoveryModule` 必須在 `SchedulerModule` 之前（架構靜態驗證）
- 回收後操作驗證（TS-F038-041 ~ 043）：觸發 `triggerRun`、`deleteTask`、`triggerExecute` 時不應再拋出 `EXTRACTION_RUNNING` / `PIPELINE_RUNNING`，需依賴 E04 和 E05 的業務 Service

**E06 Customer 360 特殊注意（F046 / F047）：**
- F046 前置條件：GIN 全文搜尋索引（AddCustomerCoreFullTextIndex Migration）必須在測試環境執行完畢，否則 Full-Text Search 場景（TS-F046-004、TS-F046-005、TS-F046-010）將失敗
- F046 / F047 遮罩測試：需分別建立 Admin Token 與 User Token 兩組種子帳號（與 E01 相同模式）；遮罩邏輯在 API 層（Service/Serializer）處理，前端直接渲染 API 回傳值
- F046 搜尋優先邏輯（BR-3）：同時傳入 keyword 與 idNumber 時，idNumber 優先；TS-F046-007 需種子資料同時含符合 keyword 但不符合 idNumber 的客戶，才能驗證 keyword 確實被忽略
- F046 type 篩選：後端 API 查詢（非前端本地過濾）；前端測試需以 spy 驗證 API 呼叫含正確 type 參數
- F047 欄位映射：85 欄位分 8 個分類（A=5、B=13、C=10、D=10、E=12、F=14、G=15、H=5）；欄位名稱使用 camelCase；時間欄位回傳 ISO 8601 UTC 格式
- F047 風控旗標：只以 CHAR(1) 值 `'Y'`（大寫）觸發警告色 Badge；`'N'`、`'1'`、null 均不觸發；address_anomaly_flag 與 mainland_flag（SMALLINT 型別）不列入風控高亮
- F047 資料新鮮度：計算公式 `Math.ceil((now - _etl_loaded_at) / (24 * 60 * 60 * 1000))`；前端測試使用 clock mock 控制當前時間；閾值為 7 天（超過則顯示警告）
- F047 客戶類型適應：customer_type_code='01'（個人）與 '04'（外籍）均在 G 分類顯示「本分類不適用」；'02'（企業）才顯示企業欄位
- F046 / F047 共用遮罩函式：maskIdNumber（前 3 + 後 2）、maskPhone（前 4 + 後 2）、maskEmail（@ 前保留前 2 字元）；NULL 值回傳 null，不套用遮罩
- F047 UUID 路徑參數驗證：非 UUID 格式的 customerId（如 'invalid-id'）應回傳 400 或 422，不得回傳 500

**E07 M02 計分設定特殊注意（F053 / F054 / F055 / F056）：**
- 所有 E07 計分設定 API 均需 `is_sales_manager = true`（SalesManagerGuard + @RequireSalesManager()）；非 Sales Manager Token 回傳 403 AUTH_FORBIDDEN
- 月名單分派鎖適用 F054 / F055 / F056 的全部寫入端點：`assignment_run.status IN ('pending', 'running')` 時回 409 SCORING_VERSION_LOCKED；兩種狀態均需各自測試（TS-F054-010/011、TS-F055-007/008、TS-F056-010/011）
- ob_levelcard_column.status 欄位由 Migration 1711360000143 補建（VARCHAR(10) NOT NULL DEFAULT 'active'）；測試 seed 不依賴 dev DB 狀態，直接植入已知 status 值
- ob_levelcard_version 的 active 判斷以 `status='active'` 為準（非 sdate/edate 計算），seed 直接寫 status='active'，避免時鐘依賴
- F054 PUT 覆寫語意：card_version 不遞增（BR-2，dump 觀察 6 種 CARD_TYPE 均為 v1）；TS-F054-001 需顯式驗證 DB card_version 仍為 1
- F055 等級數差異：S5 型 2 級（A/B）vs H/S/E/E5/M 型 4 級（A/B/C/D）；TS-F055-002/004 使用 S5 seed，驗證 API/UI 不硬編碼 4 級邏輯
- F055 PUT 依 `(card_type, card_version, card_level)` 三欄定位 UPDATE，不傳 surrogate id；TS-F055-005 顯式驗證此行為
- F055 preview 快取（BR-2，60 秒）：CI 只驗計算正確性（TS-F055-013/014）；快取行為移至 Manual QA（除非實作提供 injectable TTL）
- F055 preview URL encoded levels 參數：直接使用 spec 5.2 範例字串（`levels=%5B%7B%22cardLevel%22%3A%22A%22...%7D%5D`）
- F056 PUT（批次 UPSERT） vs POST（單筆 INSERT）語意分離：TIER_LEVEL_DUPLICATE 在 PUT 是「body 內 PK 重複」（TS-F056-007），在 POST 是「DB 已存在」（TS-F056-013）
- F056 Fallback CARD_TYPE：M5（標準 fallback，OBTIER dump 第 28 列）、M3/HC/C3（過渡期 fallback，OQ-E07-28 決策，OBMLISTDF 仍有名單）；HM 不再為 fallback（OQ-E07-27 決策，需補獨立計分卡）
- F056 BR-9 card_level 長度檢查：ob_tier.card_level VARCHAR(5) vs ob_levelcard_level.card_level VARCHAR(1)；輸入 'AB'（2 字元）應回 422 CARD_LEVEL_NOT_FOUND（TS-F056-015）
- F056 BR-10 fn_calc_tier_level NULL fallback：跨層整合測試（TS-F056-019/020）需直接以 SQL 呼叫 PostgreSQL function；Migration 1711360000141 必須先執行於 Test Container
- Audit Log 通用驗證：所有 F054/F055/F056 寫入操作後，查詢 assignment_audit_log 最新一筆確認 action / entity_type / entity_id / before_value / after_value；before_value 不可為 null（需記錄舊值）
- F053 BE-F053-001（停用維度連鎖）應在 F054 的整合 test suite 中串聯：F054 停用維度 → 呼叫 F053 GET /scoring 確認該維度不出現（TS-F054-009）
- F054 停用維度端點路徑：spec 未明確定義（PATCH vs DELETE），測試設計標記為待確認；實作後對齊測試

**E07 M06 篩選欄位管理特殊注意（F075 v1.4/v1.5）：**
- `GET /api/v1/pooldata-fields/available-columns` 受 `DirectorGuard`（非 `DirectorOrSectionChiefGuard`）+ `FeatureFlagGuard('ENABLE_E07_REFACTOR_PHASE3')` 雙重保護；處長呼叫回 403，Feature Flag 關閉回 503
- **路由排序**：Controller 內 `@Get('available-columns')` 必須宣告在 `@Get(':columnName')` 之前（NestJS 靜態路由優先）；TS-F075-E2E-008 為路由排序回歸測試
- **information_schema 環境限制**：`_inferSuggestedFieldType` 純函數單元測試使用 mock；過濾邏輯（TS-F075-INT-BE-001/002）需在獨立 PostgreSQL Test Container（`pooldata-available-columns.integration-spec.ts`）執行，**不可**在 SQLite E2E 測試
- **decimal 邊界（TS-F075-BE-024）**：spec §5.5 文件列 `decimal`，但 PostgreSQL information_schema 實際回傳 `numeric`；`'decimal'` 字串輸入期望 `categorical`（保守原則），正確生產路徑由 TS-F075-BE-010（`'numeric'` → `'numeric'`）覆蓋
- **hint 狀態機**：React state 需維持 `hasUserOverridden: boolean` flag；`dropdown` 重選時 `setHasUserOverridden(false)`（重置 suggested）；radio `onChange` 一律 `setHasUserOverridden(true)`；**點回原 suggestedFieldType 值不重置 state**（TS-F075-FE-010 驗證此行為）
- **dropdown-column-name-empty testid 缺失**：prototype 原始空態元素（`#columnDropdownEmpty`）無 data-testid；實作時需補充 `data-testid="dropdown-column-name-empty"`（RISK-F075-004）
- **Regression Guard v2.0**：`regression/M06-regression-guards.md` v2.0 含 5 個 guard（NAMING-001/002 + F068-001/002 + SIDEBAR-001），每次 F075 / F068 相關 PR 後需重新執行
- **v1.5 新增（2026-05-20）**：TS-F075-049（whitelist seed 含 case_status，共 6 筆）、TS-F075-050（GET /whitelist?active=true 回傳 case_status）

**E07 M06 F109「客戶資料」來源篩選欄位特殊注意（AD-E07-37 PG-only 裁定，2026-07-02）：**
- **PG-only 核心限制**：`customer_core` 無 TypeORM Entity、僅存在 PostgreSQL（SQLite 測試 DB 無此表）。**所有含 customer_core 條件的測試一律寫在 `.pg.spec.ts`**；純案件資料 regression guard（`customerCoreJoin === null`）維持 SQLite。**不可**在既有 SQLite composer/chain 單元測試中加入 customer_core 情境（會因表不存在直接失敗）。
- **「JS oracle」正名**：`executeStage1Chain` 本質上也是透過 TypeORM 對真實 DB 執行 SQL，非記憶體 JS 述詞評估；EQ 群組「PG 下推 vs JS oracle 等價」測試**兩條路徑皆須連同一 PG DB**，不是「PG vs SQLite」比較。
- **EMPTY_CONDITIONS 陷阱（JOIN-003，高風險）**：composer 對純 customer_core 名單本身仍會回報 `EMPTY_CONDITIONS`（因迴圈全部 `continue`），呼叫端須同時檢查 `customerCoreClause.whereFragments` 才能正確判斷是否真正 skip；否則「僅性別 IN [1]」這類純客戶條件名單會被誤判整批 skip。
- **NULL 排除語意不得 COALESCE**：客戶條件欄位一律不得 COALESCE 為可匹配之預設值（與 `ob_pool_data` 欄位之 COALESCE JS-parity 策略相反）；NULL 排除語意由 SQL 三值邏輯自然達成。
- **CI 序列執行**：F109 pg.spec 須與 F098/F099/F100/F101/F102/F103/F104 pg.spec 共用 `cdmp_test` DB 序列執行（禁並行）。
- **命名鎖定**：`resolveConditionDataSource` / `stampConditionDataSource` / `CUSTOMER_CORE_COLUMN_NAMES` / `buildCustomerCoreClause` / `customerCoreJoin`（`Stage1SqlCore` 新欄位）/ 參數前綴 `cc`（`ccCat{n}`/`ccAgeMin`/`ccAgeMax`/`ccWorkdt`）/ `gender`（**非** F104 之 `cus_sex`，兩者為 `customer_core` 上獨立欄位）。

**E07 M08 Whitelist-Driven 重構特殊注意（F050/F051/F076/F068-deprecated）：**
- **新 error codes（v2.1）**：`CONDITION_COLUMN_NOT_IN_WHITELIST`（422）、`RESERVED_FIELD_IN_CONDITIONS`（400）、`LEGACY_LIST_CONDITION_READONLY`（422）、`LEGACY_LIST_NOT_COPYABLE`（422）；`WHITELIST_OPTION_INACTIVE` 為非阻擋 warning（201/200 + warnings array）
- **廢棄 error codes**：`LIST_FILTER_FIELD_NOT_IN_WHITELIST`、`CASE_STATUS_REQUIRED`；仍可能共存於舊 code path，需確認刪除範圍
- **caseyear wildcard（OQ-TEST-001 解答）**：values 含 `99` → Stage 1 完全省略 year_cnt 條件（不加任何 IN / = / BETWEEN）；見 IT-M01-013/014/015
- **_backfill_empty（OQ-TEST-002 解答）**：conditions=[] 且 _backfill_empty=true → Stage 1 skip + Logger.warn + assignment_run 不 fail；result summary status='skipped' reason='EMPTY_CONDITIONS'；見 IT-M01-016/017、MT-M2-003
- **M5 不可逆 + 部署閘門**：M5（DELETE ob_code_df）需先確認 F069 已切換來源（TC-GUARD-M06-F068-001/TS-F068-DEP-008）；M5 只在 staging/CI 執行測試；M5 down() 為 no-op（不可還原，需備份）
- **spec_tp 52 筆 / TBL_ID='12'（TEST-RISK-005 ✅ Resolved 2026-05-21 v2）**：實際 SPEC_TP 在 OBMCODEDF TBL_ID='12'，共 **52 筆**（option_value=TBL_CD、option_label=TBL_DESC1）；MT-M3-001 / TS-F076-003 已對齊 count=52
- **backward-compat 衍生規則**：categorical → `values.join('$')`；numeric → `${min}$${max}`；date_range → `${dateStart}$${dateEnd}`；欄位不在 conditions 中 → DB 欄位為 NULL
- **Path A/B Stage 1 切換**：condition_payload IS NOT NULL → Path A（conditions 解析）；IS NULL → Path B（entity 欄位 fallback）；兩條路徑均需獨立 IT-M01 驗證（IT-M01-009/011）
- **K1~K5 五階段保護**：condition_payload 僅允許在 draft stage 寫入（K1）；rollback 還原 draft 可編輯性（K3）；Stage 1 只讀 ready stage 名單（K5）
- **F068 廢棄驗證**：3 端點回 404（TS-F068-DEP-001~003）+ 模組目錄刪除（TC-GUARD-M06-F068-001）+ 錯誤碼刪除（TC-GUARD-M06-F068-002）+ Sidebar 入口刪除（TC-GUARD-M06-SIDEBAR-001）
- **整合測試位置**：M01 整合測試 → `integration/M01-whitelist-driven-integration-test.md`；Migration 測試 → `migration/M01-migration-test.md`
- **建議載入順序補充**：F050/F051 實作前需先完成 F075/F076 + M3/M4 migration；F068 廢棄需在 F069 切換完成後執行 M5

**E07 Stage 1 精確化工程特殊注意（F090 / F091 / F092）：**
- **建議載入順序**：F090 → F091 → F092（三者相依，依序 deploy）
- **F090 migration no-op 慣例**：migration `1711360000291-AddObPoolDataListDataSource` 在 SQLite 環境為 no-op（DB_TYPE 分支）；PostgreSQL TestContainer 才執行 ALTER TABLE / CREATE INDEX
- **F090 ETL fullMode 護欄**：`E07-OBPOOLDATA_LIST-Load.fullMode` **必須為 false**（不可 TRUNCATE 全表，否則清除月名單分派輸出）；TS-F090-ETL-005 靜態 grep 驗證
- **F091 PRODUCTION 行為變更（⚠️ 必讀）**：F091 是三階段唯一改變 production 月名單分派案件數的階段，deploy 後立即生效（無 flag）。既有 `assignment-run-pipeline.service.spec.ts` 等 Stage 1 pipeline Integration test 的**案件數 baseline 必須在 F091 PR merge 前更新**（詳見 TS-F091-RG-002）
- **F091 mock 注意**：`list_nm` 字串比對（中結 / 強案 / 年資 / 白牌）mock 必須含**真實繁體中文**；`assignday` mock 必須為 `yyyyMMdd` 字串格式（與 F090 ETL 格式一致）
- **F091 特殊 DELETE 型別**：`deal_num` 在 entity 為 `string | null`（NUMERIC），比較前必須 `Number()` 轉換；`year_produ` 為 `string | null`，字串比較（固定 4 碼，等效數值比較）
- **F091 執行順序**：詐騙白牌（SP L69）在近 3 個月去重（SP L77）**之前**執行；中結強案（SP L90）在中結（SP L98）之前（忠實複刻 SP 順序）
- **F091 + F092 互依**：TS-F091-CH-004（dry-run ≡ run 一致性）是 F092 AC-3 的前置驗證；F091 CH-004 必須先 pass，F092 才能合理驗收
- **F092 estimateListCount 升級**：升級後呼叫 `executeStage1Chain({ dryRun: true })`；舊版 `buildPoolCountQuery` / `buildStage1WhereConditions().count()` 路徑應移除（TS-F092-RG-001 grep 驗證）
- **F092 dry-run 去重查詢**：dry-run 允許讀取 `ob_pool_data_list`（去重 SELECT），但**不寫入**（TS-F092-DR-001 spy 驗證區分讀寫）
- **F049 BR-6 語意矛盾**：F092 部署後「估算為上界」描述已過時；TS-F092-RG-002 標注需更新 F049-test.md + F049 spec BR-6（由後續 spec-writer 或本輪使用者確認後處理）

**E07 月名單分派 Worker 抽離 P1 特殊注意（F098，AD-E07-28 P1）：**
- **範圍嚴格限 P1**：F098-test.md 只設計「執行容器抽離 + cancellation + orphan 回收」；P2/P3 SQL 下推（F099/F100）之 JS↔SQL 等價測試、I-RUN-EST-01 SQL core 斷言**不在 F098 範圍**，勿提前實作
- **I-TRIGGER-01 為驗收紅線**：`triggerRun` 改入列後，spy `AssignmentRunPipelineService.runPipeline` 必須 **0 次**；現行 `assignment-run.service.ts` L119 `kickoffPipeline` → L257 `setImmediate(runPipeline)` 路徑須移除（保留 dead code 會讓 TS-F098-RG-001 grep guard 失敗）
- **pg-boss = Postgres 專屬，必須分層**：unit 層以 **mock/fake** `RunQueueProducer`（`send` spy）驗「有入列 + payload `{ runId, ym }` + 不執行 pipeline」；真實入列→消費→冪等→job expiration→schema migration 一律 **PG Integration（Test Container 強制）**。**18 個案例強制需 Postgres**（TS-F098-PGINT-001~005、NFR-001/003，及 RETRY-002 / SER-002 / CANCEL-006/007 / ORPHAN-007）
- **mock 須模擬真實 pg-boss contract**（feedback_mock_real_system_contract）：`send` 回傳 jobId（非 void）；work handler 收到 `{ id, name, data }`，payload 在 `job.data`（非 handler 參數本身）；`retryLimit=0` → handler 拋錯後**不自動重派**（mock 不可自動重呼）；`teamConcurrency=1` 序列化（mock 不可平行呼叫）
- **cancellation 修現有 bug**：`cancelRun`（L173-216）API 側不變（標 failed + audit CANCEL）；**新增** worker 側 `CancellationPoller` 於 list 迴圈（`runPipeline` L151）/ stage 邊界輪詢 → 偵測 failed 拋 `RunCancelledException` → 快照寫入（L215 起）0 次。取消粒度為 **list 級**（單 list 內無讓出點，TS-F098-CANCEL-004 誠實揭露）
- **OrphanReaper**：結構可參考 F038 `OrphanRecoveryService`（`onApplicationBootstrap` + Test Container），但本案在 **worker** 程序啟動；不新增 `worker_id`/`heartbeat_at` 欄位（OQ-AD28-02），靠 pg-boss job expiration + **可注入閾值**；誤殺邊界（執行中 run 不回收）為核心（TS-F098-ORPHAN-003/004）；error_message 精確 = `'worker 中斷，請重新觸發'`
- **poller / reaper 閾值與週期必須可注入**（env/config），否則測試只能等真實逾時 → 不可行
- **AssignmentRun seed 四欄位必填**（run_id / project_workym / triggered_by / created_at），PG Integration / orphan seed 缺一即 NOT NULL 失敗（feedback_assignment_run_e2e_seed）
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（TS-F098-RG-005）：vitest 不檢型別，pg-boss 型別錯誤會潛伏至 prod build（US-144 登入 500 教訓）
- **回歸基準**：P1 不改演算法 → `assignment-run-pipeline.service.spec.ts`（案件數 baseline）+ `assignment-run.service.spec.ts`（併發/readiness/cancel）須維持綠燈；至多改 harness（pipeline 改由 worker 注入），不改期望結果
- **待裁**：OQ-F098-01（入列失敗之 pending run 是否由 OrphanReaper 涵蓋）→ TS-F098-OQ-001/002 待拍板後啟用

**E07 Stage 1 SQL 下推 P2 特殊注意（F099，AD-E07-28 P2）：**
- **範圍嚴格限 P2**：F099-test.md 只設計「Stage 1 set-based SQL 下推 + estimate≡run 共用 core」；P1（F098 worker 抽離，已完成）與 P3（F100 Stage 2~4 下推）不在範圍
- **EQ 群組是 P2 驗收紅線（Definition of Done）**：14 個「JS↔SQL 逐 list 結果集精確等價」案例（PG 真庫、逐列 PK `toEqual`，非僅 count）全綠才可上線。**以現行 JS `executeStage1Chain` 為唯一 golden oracle**；禁止以「SQL 自我斷言預期值」取代與 JS 的逐 list 比對（SQL 與 JS 同錯則假綠）。**JS 版 `executeStage1Chain` 不可刪除**——保留為 oracle
- **I-PORT-01 強制 PG，禁 SQLite**：year-above（`year_produ` 數值化）之 PORT 群組（6 案例）+ EQ 全群組一律對 postgres-test 容器跑；現行 `stage1-filter-chain.integration.spec.ts` 的 **better-sqlite3** harness 對 year-above / 數值 CAST 不具代表性（SQLite `CAST('N/A')`=0、無 POSIX regexp），會假綠
- **year-above 對齊現行 JS，不是 SP（除非 OQ-F099-02 改判）**：`null`→排除（退化 1900）、`''`/`'N/A'`→**保留**（JS `parseInt`→NaN，`NaN<cutoff`=false）。⚠️ **勿直接照搬 AD 建議的 `NULLIF(REGEXP_REPLACE(...),'')`**——那會把 `''`/`'N/A'` 也誤判為排除（與 JS 不符）。建議 `CASE WHEN year_produ IS NULL THEN 1900 WHEN year_produ ~ '^[0-9]+$' THEN year_produ::int ELSE NULL END < :cutoff`
- **去重用 `NOT EXISTS`/anti-join，不用 `NOT IN`**（含 NULL 子查詢「全部不 match」陷阱，spec A-1）；custo_no=NULL 不被去重誤排（EQ-012）；去重視窗上界 `MIN(MAX(assignday), workdt−1)` 語意不變，只是計算移入 SQL（C-2）
- **I-RUN-EST-01（F049 老坑）**：單一 `buildStage1Sql`，run（`INSERT…SELECT`）/ estimate（`SELECT COUNT(*)`）只差最外層包裝；RUNEST-001 結構斷言「兩路徑 SQL core 同源」+ RUNEST-002/003「run 列數==estimate COUNT」（含 year-above 不漏套）
- **I-NOLOAD-01 無例外**：下推路徑禁 `getMany()`/`find()` 全載 `ob_pool_data`（含 year-above）；現行 L408 `qb.getMany()` 須移出下推路徑（NOLOAD-001/002 靜態 grep guard）
- **guard 移轉防假綠**：作廢 F091-test 之 `RGv2-005`（grep `includes('小資')`/`includes('白牌')`）與整套 `SDv2-*`（pin JS array filter）**前**，須先確認對應 EQ 案例已綠（GMT-001/002 + SDv2→EQ 映射表）；`special-rules.spec.ts`（trigger 仍 JS，C-1）維持綠燈（GMT-003）
- **冪等 I-IDEM-01**：下推實質寫入前先 `DELETE FROM ob_monthly_run_result WHERE run_id=:runId`（FK CASCADE 為輔）；IDEM 群組（3 案例）守同 run_id 重跑列集合一致
- **26 個案例強制需 Postgres**（EQ 14 + PORT 7 + RUNEST(PG) 2 + IDEM 3），沿用 `docker-compose.test.yml` postgres-test（F038/F075/M01/F098 慣例）；CI 未起 PG → P2 DoD 無法驗收
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（vitest 不檢型別；US-144 登入 500 教訓）
- **所有 OQ 已裁定（無待裁項目）**：OQ-F099-01（CI 必起 PG）✅、OQ-F099-02（oracle=現行 JS，含前導數字 `'1980abc'`→1980，PORT-007 守陷阱）✅、OQ-F099-03（assignday 恆 NULL，下推 SELECT 寫 NULL，EQ 步驟 4 斷言）✅

**E07 Stage 2~4 SQL 下推 + v2 真實計分引擎 P3 特殊注意（F100，AD-E07-28 P3）：**
- **範圍嚴格限 P3**：F100-test.md 只設計「Stage 2 計分 / Stage 3 CR / Stage 4 st4_exchange SQL 下推 + v2 計分升級」；P1（F098）與 P2（F099）已完成提交、不在範圍
- **⚠️ golden oracle = 手算預期，非跑 v1 JS（P3 與 P1/P2 最大差異）**：P3 把計分由 v1 簡化版升級為 v2 真實版——現行 `computeScore` 對 customer_core 欄位回 `''` 不計分，P3 以 LEFT JOIN 補上。若以「跑 v1 JS」當 oracle，凡有 customer_core 計分欄位之案件，v1 會少算 → SQL 正確補上反被判 fail。**正解：用確定性 seed 把計分卡規則寫死，依規則手算每筆 score/level/tier（F100-test §一矩陣，寫死數字、人複核）當 oracle**
- **(a) 升級差異 vs (b) 下推等價 必須分流**：(b) 案件（無 customer_core 欄位 / LEFT JOIN 無 match）SQL 須 == 手算 == v1 JS（三方相等，守 regression）；(a) 案件 SQL == 手算 v2 **且** ≠ v1 JS（證明補完生效）。EQ 矩陣每案已標 (a)/(b)
- **customer_core 用 `LEFT JOIN` 非 `INNER JOIN`**（RISK-F100-002）：無對應客戶之案件不可整列消失（漏案）；屬性 NULL 不取分
- **st4_exchange oracle = `Math.max(1, Math.ceil(n*0.1))`（CEIL + 保底 1），非 SP 的 `ROUND`**（OQ-F100-01 對齊現行 JS）：下推 SQL 用 `CEIL(count*0.1)`；EXCH-006（11→2）/ EXCH-004（保底 1）守此。`PARTITION BY list_no`（非 SP per-主管）+ deterministic `ORDER BY orgno, appl_no` → 「哪些被交換」可精確 `toEqual` 比對；交換對象單一 `seniorEmpls[0]`。**SP 主管↔專員等量配對交換 / 寄信告警 / 整批回滾 out-of-scope、不測**
- **score NULL（無 active version）vs score 0（有 active 0 命中）分案**（SCORE-006/007）；**score NULL vs card_level NULL 之 tier fallback 分歧**（LEVTIER-003 tier=T3 vs LEVTIER-004 tier=NULL）——下推 SQL 易把兩種 NULL 混為一談
- **CR `EXISTS` 來源表對齊現行 `collectCrCandidates`（A-2）**：oracle = JS 未成交集合等價，不綁定子查詢對 snapshot 或對 result 表；「未成交」= `result_status='PENDING'` 或無 status
- **I-NOLOAD-01：Stage 2~4 下推路徑禁 `pool.map(computeScore)` 全物化 heap**（NOLOAD-001）；現行 `executeV2` L465 `scoredPool = pool.map(...)` 須移出下推路徑。**I-RUN-EST-01 延續**：estimate 只跑 Stage 1 COUNT，不含 Stage 2~4 計分 join（A-4）
- **F067 升級差異報告 + 業務驗收（UPGR）為上線硬性前置**：EQ 全綠（技術正確）+ UPGR-004 業務簽核（業務接受升級結果變化）並列 P3 上線門檻（§9 / NFR-005）。UPGR 不斷言 v1==v2（升級本就改變結果），而是「差異已量化 + 業務簽核接受」
- **約 40 個案例強制需 Postgres**（EQ 8 + SCORE 7 + CJOIN 4 + LEVTIER 5 + CR 5 + EXCH 8 + IDEM 2 + UPGR 3），沿用 `docker-compose.test.yml` postgres-test（F099 `stage1-sql-pushdown.pg.spec.ts` 連線/skip 模式可複用）；**禁 better-sqlite3**（視窗函式 / `SUM(CASE…)` / `LEFT JOIN` / `EXISTS` 在 SQLite 不具代表性）
- **4 個待確認 open question（tdd 實作前釐清）**：OQ-F100-T1（P3 是否單一 transaction、失敗全回滾 → spec/AD 未明 → IDEM-003 blocked）、OQ-F100-T2（score=NULL 時 tier 走 fallback T3 或 NULL → spec 未逐字明列，本案以現行 JS 推導為基準）、OQ-F100-T3（`customer_core` entity 目前不存在於 `apps/api/src/database/entities/` → P3 LEFT JOIN 前置 blocker）、OQ-F100-T4（CUS_SEX/AGE 計分欄位映射對齊 architecture-spec.md §3.10 表 → tdd 交接項，§一矩陣欄位名為測試 seed）
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（vitest 不檢型別；US-144 登入 500 教訓）
- **SP 為 UTF-16LE**（feedback_sp_utf16le_decode）：spec §5 已據解碼結果完成 OQ-06 推導，tdd 沿用結論；中文版 Stage4 mojibake 不採信

**E07 Stage 3/4 真實比例分派特殊注意（F101，AD-E07-29）：**
- **範圍**：F101 取代 F100 placeholder Stage 4（dept[0] + defaultEmpl）；F100 之 Stage 2 計分 / Stage 3 CR `EXISTS` / score→tier 演算法**不受影響**。本 feature 僅替換 dept ration / empl ration / ASSIGNDAY 分派邏輯。
- **⚠️ 所有 OQ 已裁定（AD-E07-29 全部 RESOLVED）**：OQ-F101-01（確定性鍵：差額 `obdeptid`/`emplid` 升冪；案件 `(orgno, appl_no)` 升冪；EMP_ORD `PARTITION BY emplid ORDER BY orgno, appl_no`；DIVIDE_LEFT `PARTITION BY emplid ORDER BY tier_level, orgno, appl_no`）/ OQ-F101-02（st4_exchange 廢除：SP 自 202408 已硬編碼 `RETURN`）/ OQ-F101-03（ob_assign_set 標 vestigial，F101 不引用）/ OQ-F101-04（冪等粒度：per-list auto-commit，Stage 3 前清 dept_id/emplid/assignday，is_cr 保留）/ OQ-F101-05（警告通道：`assignment_run.skipped_cases.warnings[]` + `warning_summary`，不擴 audit_log）。
- **golden oracle = 手算 FLOOR + 確定性差額補足**：oracle 件數寫死於 F101-test §一~§三，禁止以「跑舊 JS placeholder」（非比例分派）當 oracle；禁止「SQL 自我斷言預期值」（同錯假綠）。
- **I-NO-ST4-EXCHANGE（回歸紅線）**：st4_exchange（T1/T2→senior 10% swap）在 F101 範疇完全廢除；`runStage4Sql` senior swap CTE 移除；三個靜態 grep guard（DET-001/002/003）守住。REG-002/003 行為層驗證 senior swap 不發生。
- **EQ 群組（JS↔SQL 逐列等價）= F101 DoD 門檻（AC-15）**：8 個代表性名單涵蓋：多分處/多 Tier/差額觸發/兩階段補足/無 ration/無員工/無 calendar/is_cr 混合。逐列四元組（dept_id, emplid, emplid_deptid, assignday）`toEqual`。未全綠不上線。
- **警告格式（OQ-F101-05 裁定）**：`skipped_cases.warnings[]`（JSONB 合并，非覆蓋既有 cases 鍵）+ `warning_summary`（含警告事件碼，`|` 分隔）。三類事件碼：`STAGE3_NO_DEPT_RATION` / `STAGE4_NO_EMPL_WARN` / `ASSIGNDAY_NO_CALENDAR_WARN`。
- **CI 序列執行（F098/F099/F100/F101 pg.spec 共用 cdmp_test DB）**：共用 postgres-test 容器 + DROP/synchronize，必須 `--runInBand` 或分 step，**禁並行**。
- **44 個案例強制需 Postgres**（DEPT 8 + EMPL 9 + ASGD 5 + EQ 8 + IDEM 3 + FALL(PG) 5 + REG(PG) 4 + UPGR 2；DET 3 純靜態掃描無需 DB）。
- **手算 oracle 矩陣已驗算（可直接用作測試期望值）**：見 F101-test §一（Stage 3，4 個分組 Seed 1~4）/ §二（Stage 4，Seed A/B/C/D）/ §三（ASSIGNDAY，E1/E2/E3）。
- **ob_assign_set 永不引用**：AC-18 / BR-F101-18；DET-002 靜態掃描 `grep -rE "ob_assign_set|ObAssignSet|OBASSIGNSET"` 期望命中 0。
- **estimate≡run（I-RUN-EST-01 延伸）**：ASSIGNDAY 千分比與 Stage 0 試算共享同一 `calculateDailyEstimate(ym)` 呼叫路徑；ASGD-004 驗證工作日清單完全一致。
- **FALL-005 JSONB 合并**：`skipped_cases` 現有 `cases` 子鍵不受 `warnings` 寫入影響（使用 JSONB merge，非覆蓋）。
- **實作後必跑 `tsc --noEmit -p tsconfig.build.json`**（vitest 不檢型別，US-144 登入 500 教訓；`feedback_vitest_no_typecheck`）。
- **SP 解碼**：`reference/SP/SP_INFOT_ASSIGNEXPORTNAMELIST_st2_dept.sql` / `_st3_emplid.sql` 為 **UTF-16LE**，須 `node -e "require('fs').readFileSync(path).toString('utf16le')"` 解碼（`feedback_sp_utf16le_decode`）。
- **UPGR（NFR-005）為上線硬性前置**：EQ 全綠（技術）+ UPGR-002/003 業務驗收簽核（分派比例符合設定）並列上線門檻；UPGR-004 自動驗證 assignday 工作日分散性。

**E07 月名單分派計分引擎欄位來源修正特殊注意（F103，AD-E07-v3.5）：**
- **EQ DoD 為硬性門檻**（I-SCORE-EQ-01）：TS-F103-EQ-001~008（8 場景，PG 真庫）JS `computeScore` 與 PG `buildStage2ScoreExpr` 生成 SQL 對相同 `(pool, cc, arCap)` fixture 的 score 完全相等（整數，差異=0）；未全綠不得上線。
- **`computeScore` 簽章擴充（OQ-1 裁定）**：新增 `cc: CustomerCoreRow | null` / `arCap: ArCapitalRow | null` 兩個尾參數；所有呼叫端必須更新，REG-003 `tsc --noEmit` 守門。SQLite 單元測試直接傳入 fixture 物件，**無需建 customer_core / ob_arreturndf_min_cap 表**（OQ-3 裁定）。
- **AGE 統一演算法（I-SCORE-AGE-01）**：`calcAgeYears(dateOfBirth: Date, now: Date)` 須精確對齊 PG `EXTRACT(YEAR FROM age(date_of_birth))` 語意（精確到月，本年生日未到者不計當年）；**`now` 參數必須可注入**（非 hardcode `new Date()`），否則 EQ-007 在跨日邊界非確定性。三邊界：1990-06-23 → 36 / 1990-06-24 → 36（當天算已過）/ 1990-06-25 → 35。
- **通用 fallback（I-SCORE-FALLBACK-01）**：`resolveColumnSource` switch `default` 分支**永不回 `undefined`**；未 hardcode 的 range 欄位一律回 `COALESCE((to_jsonb(o)->>lower(col))::numeric, 0)`；AUDIT-005 靜態掃描確認原始碼無 `return undefined`。
- **幽靈欄位（I-SCORE-GHOST-01）**：PG 端 `to_jsonb(o)` 無此 key → COALESCE(NULL,0) = 0，靜默；JS 端 `pool[key] == null` → `logger.warn`（含 column_name + card_type）+ 回傳 0；**兩路徑皆不拋例外、不阻擋月名單分派**。
- **COMMISSION 移除（I-SCORE-COMMISSION-01）**：`resolveColumnSource` / `resolveColumnValue` switch 無 `case 'COMMISSION'`；`MAPPED_SCORING_COLUMNS` 不含 'COMMISSION'，含 'ADD_UN_CAPITAL'；COMMISSION-001~004 靜態掃描驗證。
- **needsArCapital flag（I-SCORE-AR-JOIN-01）**：`Stage2ScoreSql` interface 擴充 `needsArCapital: boolean`；executor 依 flag 條件注入 `LEFT JOIN ob_arreturndf_min_cap ar ON ar.appl_no = o.appl_no`；無 ADD_UN_CAPITAL active 欄時 **不注入** JOIN（效能保護，AR-002 驗證）。
- **batch pre-fetch N+1 禁止（I-SCORE-PREFETCH-01）**：每個 list 恰好 customer_core IN clause 1 次 + arreturndf IN clause 1 次；PREFETCH-001~003 驗證（spy query call count）。
- **202606 驗收為定性（OQ-158-01）**：card_level 出現 ≥2 種值、tier 含 T1/T2；仍異常時本輪內根因（AC-12，OQ-158-02 不推延）—量測 customer_core 空值率。
- **F100/F101/F102 回歸**：F103 修正後 score / tier 分佈改變；F101 seed 固定 tier（T1/T2/T3 seed）不受影響，但需確認無隱性「全 T3」假設（REG-001/002 確認）。
- **實作後必跑 `tsc --noEmit -p tsconfig.build.json`**（vitest 不檢型別；computeScore 簽章變更特別容易有隱性呼叫端漏補；feedback_vitest_no_typecheck）。
- **F103 pg.spec 序列執行**：與 F098/F099/F100/F101/F102 共用 cdmp_test DB，必須序列執行，禁並行。

**MSSQL 全面遷移 P1a 特殊注意（AD-E07-38，跳過 spec-writer）：**
- **範圍嚴格限 P1a**：`infrastructure/AD-E07-38-P1a-test.md` 只設計「三處 TypeORM 設定點 mssql 分支 + auth 最小 4-entity 子集（`User`/`Role`/`TokenBlocklist`/`PasswordResetToken`）+ 型別探測 + 登入 e2e + entity round-trip + 大小寫守門」；全 37 entity baseline（P1b）、`sp_getapplock`/Pattern B named param（P1c）**不在範圍**，勿提前實作。
- **P1a 之 mssql 分支 entities 陣列必須刻意限定為 4 個 auth entity**，不可沿用 sqlite/postgres 分支的完整 37-entity 陣列——其餘 33 個 entity 尚未完成 D-2 型別修正（37 處裸 uuid/bigint/text 字面值），提前全量 synchronize 會失敗或誤把 P1b 範圍提前納入。
- **兩個「不可假設」型別探測為核心紅線**（TS-MSSQL-P1A-TYPE-001/002）：裸 `'uuid'` 是否映射 `uniqueidentifier`、裸 `'text'` 是否誤落已棄用 `TEXT`（而非 `nvarchar(max)`）。**結果直接決定** `uuidColumnType`/`longTextColumnType` 兩個新 helper 是否必要（P1a DoD #5）；不可憑文件推斷，須以真實 MSSQL 容器實測後才能定案（若真實容器不可達，全檔 `.mssql.spec.ts` 應 skip，不可假設一個分支當作既定答案矇混過關）。
- **TYPE-002/003 使用測試專屬合成 probe 表**（非 production entity）：P1a 範圍之 4 個 auth entity 皆未使用裸 `'text'`/`bigint` 字面值，但 P1a DoD #1 文字仍要求驗證此二型別 → 測試檔內定義拋棄式表（`queryRunner.createTable`/`dropTable`），測試結束即清除，不擴大 production entity 範圍。
- **`.mssql.spec.ts` gating 慣例比照既有 `.pg.spec.ts`**：連不上 MSSQL 容器時整檔 `describe.skip` + 印 SKIP_REASON，**不可假造綠燈**；建議新增 `mssql-env-preload.ts`（比照 `pg-env-preload.ts` 之 `restoreDbType()` 模式）與 `.env.test.mssql`（比照 `.env.test` 之 PG 版本）。
- **AD 文件本身章節編號有漂移**：AD-E07-38 的 Agent Loading Guide 表所寫的 §6/§7/§8 與實際標題編號不符（P1a/b/c DoD 實為 §3 之 D-6 子節；不變式實為 §5；測試邊界實為 §6；無 §8）。已於 `infrastructure/AD-E07-38-P1a-test.md` Agent Loading Guide 段落註記；test-designer 已依實際內容（非依錯位號碼）產出本測試設計，tdd-implementation 引用 AD 時同樣以標題文字定位，不依號碼。
- **AD §3 D-1 pseudocode 仍有隱式 fallback**：三分支改寫後，未知 `DB_TYPE` 值仍落入最後一個無條件 `return {type:'postgres',...}`（非顯式 exhaustive 拋錯）；TS-MSSQL-P1A-REG-003 僅記錄現況，未斷言此為正確行為，是否需改為顯式拋錯留待未來裁定（見 risks-and-gaps.md）。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（TS-MSSQL-P1A-REG-001；vitest 不做型別檢查，`feedback_vitest_no_typecheck`）。

**MSSQL 全面遷移 P1b1 特殊注意（AD-E07-39，銜接 P1a，跳過 spec-writer）：**
- **範圍嚴格限 P1b1**：`infrastructure/AD-E07-39-P1b1-test.md` 只設計「全 37 entity 型別轉換 47 處（含 F-1 timestamp 4 處紅線）+ B1 token_blocklist 改 token_hash + D1 全 entity 統一載入 + F-4 varchar 中文編碼 test-first」；prod baseline migration/dev-prod parity（P1b2）、bootstrap/seed 三支腳本改寫（P1b3）、`sp_getapplock`/Pattern B（P1c）**不在範圍**，勿提前實作。
- **🔴 F-1 為最高優先修正項**：`ob-assign-config.entity.ts:25`、`ob-arreturndf-min-cap.entity.ts:16`、`assignment-run-stage-log.entity.ts:27,30` 共 4 處裸 `type:'timestamp'` 字面值繞過既有 `dateColumnType` helper——MSSQL 的字面值 `timestamp` **不是日期型別**，是 `rowversion` 的舊式同義詞（唯讀 8-byte 二進位版本戳記），**不保證拋錯，可能靜默建出完全錯誤的欄位**。TS-MSSQL-P1B1-TYPE-005/006/007 三案例分別從「已知站點型別確認」「全表未知站點掃描」「正面 Date 寫入佐證」三層防線驗證修復生效，缺一不可。
- **🔴 F-4 中文編碼 test-first，決策結果直接影響範圍**：CHI 群組（varchar 寫入「借新還舊」「中古車商」逐字元 round-trip）**不依賴其他群組**，可對全新 MSSQL 容器最先獨立執行。若判定「不符」（mojibake），觸發全面 `varchar`→`nvarchar` 轉換，波及面預估為本次 P1（P1a+P1b）中最大單一改動項（大於 47 處 uuid/text/boolean/timestamp 總和）——此決策點記錄於 `TS-MSSQL-P1B1-CHI-DECISION-001`，後續測試設計方向已標出（全庫 varchar 欄位盤點 / helper 化矩陣驗證 / 900-byte 索引鍵重算 / dev 既有資料重新 ETL 評估 / Stage 1 SQL 下推 LIKE 比對回歸），但本輪不預先撰寫，留待該決策點觸發後再開新測試設計。
- **B1（token_blocklist 改 hash）DoD 核心紅線**：`TS-MSSQL-P1B1-HASH-E2E-001`（login→logout→同 token 再請求應 401 TOKEN_REVOKED）為 P1b1 DoD #5 之硬性驗收；`TS-MSSQL-P1B1-HASH-005` 為對照 P1a 既有失敗案例 `TS-MSSQL-P1A-CRUD-003b`（2048 字元 token 因 900-byte 索引鍵上限 INSERT 失敗）之正面修復佐證。**下游 agent 需處置 `TS-MSSQL-P1A-CRUD-003b`**：B1 完成後 `token_blocklist.token` 欄位已改名為 `token_hash`，該既有案例若不更新會因欄位不存在而編譯/執行失敗（見 P1b1 test REG-003），建議直接移除並以 HASH-005 取代其驗證意圖。
- **I-MSSQL-PK-BYTELIMIT-01 為動態守門，非人工試算表**：`TS-MSSQL-P1B1-PKWIDTH-001` 以 `sys.indexes`/`sys.index_columns`/`sys.columns` 即時查詢計算全表索引鍵位元組寬度，未來任何新增 entity/欄位皆自動涵蓋，不需修改測試程式碼——此設計哲學亦適用於 F-4 若判定需 nvarchar 轉換後的欄寬加倍情境（自動重新驗證，非人工重算）。
- **I-MSSQL-ENTITY-LIST-PARITY-01（D1）**：`app.module.ts`/`worker-app.module.ts` 之 mssql 分支目前仍硬寫 P1a 過渡態之 4-entity 陣列（已於原始碼註解明確標註「P1a 過渡態」），P1b1 需改為與 sqlite/postgres 分支共用同一份 entity 清單來源（`app.module.ts` 建議顯式陣列 `ALL_ENTITIES`；`worker-app.module.ts` 建議改回引用既有 glob `entities` 變數，非另立陣列）。
- **測試環境完全沿用 P1a**：不新增 gating helper/`.env.test.mssql`，僅新增 `p1b1` SQL schema 隔離慣例（與既有 `p1a` 並存）。

**MSSQL 全面遷移 P1b2 特殊注意（AD-E07-39，銜接 P1b1，跳過 spec-writer）：**
- **範圍嚴格限 P1b2**：`infrastructure/AD-E07-39-P1b2-test.md` 只設計「prod 手寫 T-SQL baseline migration 產出 + dev/prod 兩軌結構化 parity 驗證 + fn_calc_tier_level 未建立 + 無 filtered index + collation/大小寫/B1 於 baseline 路徑守門 + sp_executesql/pg_dump 語法靜態規避」；bootstrap/seed 三支腳本改寫（P1b3）、`sp_getapplock`/Pattern B（P1c）**不在範圍**，勿提前實作。
- **🔴 權限約束直接決定 Harness 設計，非任意選擇**：已查證 `docker/mssql-init.sql`，`cdmp` login 於 `CDMP`/`CDMP_TEST` 僅獲資料庫層級 `db_owner` 角色，**未**授予伺服器層級 `dbcreator`/`sysadmin`，**無法 `CREATE DATABASE`**。故 parity harness 不得採「兩個獨立資料庫」方案（會於真實容器因權限不足直接失敗），必須在同一 `CDMP_TEST` 內以 schema 區隔兩路徑。
- **兩路徑 schema 刻意不對稱**：Path A（synchronize）新建專屬 schema `p1b2_sync`（TypeORM `schema` 連線選項自動前綴 DDL）；Path B（baseline migration）**直接使用 `dbo`，不新建 schema**——因手寫 migration 內的 raw SQL 字串不會被 TypeORM 改寫表名前綴，會落在連線 session 的 default schema（`dbo`，`mssql-init.sql` 建立 `cdmp` user 時未指定 `WITH DEFAULT_SCHEMA`），此**恰好與 prod 真實部署路徑完全一致**（`data-source.ts` 之 mssql 分支同樣未設定 `schema` 選項）。**下游 agent 不可誤解為「兩路徑應使用對稱命名的 schema（如 `p1b2_baseline`）」而額外新建 schema 給 Path B**——那樣反而會脫離「與 prod 實際路徑一致」的驗證意義。
- **`dbo` 於 `CDMP_TEST` 為本文件獨佔保留 schema**：`beforeAll` 須先斷言 `dbo` 目前為空（防止殘留污染），`afterAll` 須清空回空（含額外清理 `typeorm_migrations` bookkeeping 表，此表為 P1b1 `dropAllTablesInSchema` 從未處理過的新物件，因 P1b1 從未實際跑過 migration）；其餘既有/未來測試套件不得使用 `dbo`，見 `TS-MSSQL-P1B2-REG-005` 閉環驗證。
- **PARITY 群組為 DoD 核心，comparator 敏感度驗證（`PARITY-009`）不可省略**：「兩路徑 diff 為空」的結論唯有在證明 comparator 具備真實偵測能力（對人工注入的合成差異必須回報非空 diff）之後才可信；若跳過此案例直接驗收 PARITY-001~007 全綠，存在「comparator 永遠回報空 diff（形同 no-op）」而誤判通過的風險。
- **靜態守門延續 P1b1 caveat**：`TS-MSSQL-P1B2-STATIC-001`（`sp_executesql` 零命中）直接對應 P1b1 impl log §5.6 已查證之本機 MSSQL 容器已知限制（`EXEC sp_executesql` 拋 17750，一般 DDL/`EXEC('...')` 正常）；此案例失敗代表 migration 本身於本機測試容器無法正確執行，須於 code review/CI 階段攔截，而非等 BASELINE-001 執行期間才發現不易理解的錯誤代碼。
- **表數沿用 P1b1 已修正之 36**：AD-E07-39 §6/§8 原文仍用「37」，本文件與 P1b1 一致，一律動態對齊 `ALL_ENTITIES.length`/`entityMetadatas.length`，不寫死任何數字。
- **P1b1 已完成項目本輪不重複驗證**：全 entity 型別轉換 47 處、B1 entity 層設計、D1 全 entity 統一載入之原始驗證皆屬 P1b1 範圍；本文件僅在 `HASH-BASELINE` 群組驗證 B1 於**新路徑**（baseline migration 手寫 SQL）之正確性，其餘型別/D1 驗證不重跑。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P1B2-REG-001`；`feedback_vitest_no_typecheck`）。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（TS-MSSQL-P1B1-REG-001；`hashColumnType`/`ALL_ENTITIES`/`TokenBlocklist.token_hash: Buffer` 型別變更後所有呼叫端需同步更新）。

**MSSQL 全面遷移 P1b3 特殊注意（AD-E07-39，銜接 P1b2，跳過 spec-writer）：**
- **範圍嚴格限 P1b3**：`infrastructure/AD-E07-39-P1b3-test.md` 只設計「`seed.ts`/`seed-datasource.ts`/`prod-data-seed.ts` 三支腳本改寫使其對 MSSQL 可執行 + B2 npm alias 修復驗證」；`sp_getapplock`/Pattern B（P1c）**不在範圍**；P1b1/P1b2 已完成之 entity 型別/baseline migration/parity 驗證不重複。
- **🔴 AD §7 風險評估低估轉換範圍，本文件逐檔 grep 後補全**：AD 原文僅列 `$n`→named param、`LIMIT`→`TOP` 兩類；實際查證另有 5 類必須處理才能使 DoD #1 成立：三支腳本 `main()` 皆硬編碼 `type:'postgres'`（`DIALECT` 群組，比任何 SQL 語法問題更早阻擋，任何轉換皆無意義直到修復）、PG 專屬函式 `NOW()`（10+ 處）、PG 專屬子句 `RETURNING id`（`etl_pipelines` INSERT）、PG 專屬型別轉換 `::text`（`etl_pipeline_versions.definition`）、PG 專屬 NULL-safe 運算子 `IS NOT DISTINCT FROM`（reconcile 引擎核心 `findByKey`/repair UPDATE，6 張計分卡表共用）、裸布林字面值 `true`（`extraction_tasks.enabled`，T-SQL 不接受裸關鍵字）。詳見測試設計文件 §1 站點清單。
- **🔴 SITE-005 為最高風險項**：`ob_levelcard_score.json`（449 筆）中 212 筆 `level1=NULL`——真實生產種子資料大量依賴 `IS NOT DISTINCT FROM` 之 NULL-safe 語意；若轉換時誤用裸 `=` 取代，每次重跑 bootstrap 會在該表累積重複列（直接違反 DoD #3 冪等性），且此 bug 只在含 NULL 自然鍵分量的表才會出現，其餘 5 張計分卡表可能表面正常而掩蓋問題。
- **🔴 COUNT-011/012 為 SCOPE GAP 決策關卡，非必過案例**：`roles`（預期 2 筆）、`pooldata_field_whitelist`/`pooldata_field_option`（預期 17/186 筆）三表資料唯一來源為 PG-only 之 `1711360000001-BaselineReferenceData.ts` migration（`INSERT INTO public.roles ... ON CONFLICT ... DO NOTHING` 等 PG 專屬語法），**不在 P1b1/P1b2/P1b3 任一輪 AD 改動範圍內**，MSSQL baseline migration（P1b2）僅含 DDL 不含此類參考資料。AD-E07-39 §8 P1b3 DoD #2 原文列出這三表「筆數與 PG 版本一致」，**但三支 P1b3 腳本完全不觸及這三表**——此為 AD 文字與現有程式碼範圍的落差，非測試設計或 tdd-implementation 的失誤，需人類決策後續處置（納入 P1b3 收尾新增第四支腳本 / 追加至 MSSQL baseline migration / 明確記錄為技術債延後），**下游 agent 不可默默忽略此落差直接判定 DoD #2 已達成**。
- **Harness 設計核心限制**：raw SQL 腳本內未加 schema 前綴的裸表名，無法透過 TypeORM `schema` 連線選項重新導向（與 P1b1/P1b2 之 entity/`synchronize()` 層測試不同）——SQL Server 無等價 Postgres `SET search_path` 之連線期 schema 覆寫機制，一律依登入使用者的 `DEFAULT_SCHEMA`（`dbo`）解析。故本輪 ALIAS/SITE/BOOT/COUNT/IDEM 五群組皆**必定**落在 `dbo`，與 P1b2 既有「`dbo` 獨佔保留」假設直接衝突。
- **🔴 升級 P1b2 既有風險 `R-MSSQL-P1B2-03` 之嚴重度**：該項原標記「低風險、現況無實際污染，僅提醒未來擴充」；P1b3 是第二個需要獨佔 `dbo` 的套件，理論風險在本輪成為**現實存在**的執行順序需求。建議新增序列化執行 lane（如 `npm run test:mssql:serial`，`--no-file-parallelism` 涵蓋全部 `*.mssql.spec.ts`），比照既有 `.pg.spec.ts` 之 F098~F109 序列執行慣例（`feedback_pg_spec_parallel_timeout`）。
- **測試結構建議**：單次 `beforeAll` 建置（`migration:run` + 完整 bootstrap）供 SITE/BOOT/COUNT 群組共用唯讀斷言；IDEM 群組獨立於自己的 `it()` 內明確二次呼叫；`afterAll` 完整清空 `dbo`（擴充 P1b1 `dropAllTablesInSchema` 模式，含資料列與 `typeorm_migrations`）。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P1B3-REG-001`）。

**MSSQL 全面遷移 P1c 特殊注意（AD-E07-38 §3 D-5/D-6，銜接 P1a/P1b1/P1b2/P1b3，P1 最後一片，跳過 spec-writer）：**
- **範圍嚴格限 P1c**：`infrastructure/AD-E07-38-P1c-test.md` 只設計「核心 4 檔 6 處中站點 1/2/3/4」（pipeline `ANY($1)`×2 → `IN(:...arr)`／report `run_id=$1`→`:runId`+scopeClause 巢狀 $n／personnel-ratio `pg_advisory_xact_lock`→`sp_getapplock`）；站點 5（F064 native cursor）與其餘約 40 個 `$n` 站點（Stage 1/ETL/`postgresql-executor.ts`/c360）**不在範圍**，僅由 STATIC-001 交付物記錄清單供 Phase 3/4 引用。
- **🔴 站點 1/2 可測性程度不同，AD 原文未區分**：`customer_core`（站點 1）經 test-designer 逐檔查證（grep entities 目錄 + MSSQL baseline migration）確認為 AD-E07-37 裁定之 **PG-only 表**（無 entity、不在 MSSQL baseline）——MSSQL 上此查詢**恆定**因表不存在而失敗，測試設計刻意區分「具名參數轉換是否正確」與「表不存在」兩種錯誤來源（`PARAM-003`），避免誤判轉換失敗。`ob_arreturndf_min_cap`（站點 2）已確認在 MSSQL baseline（`1751884800000-MssqlBaselineSchema.ts:74`），可做**完整跨 driver 資料等價**驗證（`PARAM-007`）。
- **🔴 scopeClause 巢狀 $n 稽核已完成（AD 明確要求之稽核項）**：`assignment-run-report.service.ts` 之 `buildExportQuery` 於 `scope.shouldFilter(actor)===true` 且轄區 `emplids.length>0` 時，`scopeClause` 動態產生 `$2..$(1+N)`（N=轄區員編數）。`PARAM-012` 要求 `:runId` 與 `:...emplIds` 須由**同一次** `escapeQueryWithParameters` 呼叫展開，不可拆兩段（否則位置編號基準不同步）。
- **🔴 LOCK-009 為決策關卡，不預設答案**：AD 原文聲稱「`@LockOwner='Transaction'` 若呼叫端未處於顯式交易會直接報錯（可視為額外安全網）」，但此為 AD 文字本身之未驗證宣稱（非 test-designer 已查證之事實）。測試設計刻意不預設答案，依真實 MSSQL 實測結果分兩種後續行動：若確實報錯，資料庫已提供保護；若未報錯（可能為隱含單陳述式交易、鎖立即釋放），代表 I-MSSQL-LOCK-01 純為程式碼契約，tdd-implementation **必須**在服務層加防禦性斷言，否則此不變式形同虛設且不會有任何錯誤提示。
- **🔴 DISPATCH-001 為 MUST-FIX 守門，對現行未修改程式碼刻意設計為紅燈**：test-designer 查證 `personnel-ratio.service.ts:565-568` 之 `isPostgres()` 現行為**二元 gate**（`postgres` vs 其餘一律跳過），`DB_TYPE='mssql'` 在未修改的現行程式碼下會被誤判與 `sqlite` 同路徑「完全跳過鎖」，`sp_getapplock` 若只新增程式碼未同步把二元 gate 改為三分支會是死碼、永遠不會被觸發。`DISPATCH-001` 刻意針對「目標狀態」斷言，逼 tdd-implementation 確實接線。
- **LOCK 群組並發 harness**：兩個獨立 `dataSource.createQueryRunner()`（各自 `.connect()`+`.startTransaction()`），以 `Promise.all()`/交錯 `await` 控制時序；測試專用縮短 `@LockTimeout`（如 500ms，取代 production 5000ms）加速套件；DISPATCH 群組之逾時情境改用 unit mock（比照既有 TC-F084-014~016 之 55P03 mock 模式）避免真實等待 5 秒。
- **LOCK-001 為前提探測，決定後續案例可行性**：`DECLARE @lockResult INT; EXEC @lockResult = sp_getapplock ...; SELECT @lockResult` 多陳述式批次能否經 TypeORM `manager.query()` 正確取得回傳碼，尚未實測驗證；若形狀不可靠需改用 `mssql` 套件底層 output 參數機制，記入 risks-and-gaps.md 之 OQ-MSSQL-P1C-01。
- **ESCAPE 群組不需任何 gating**：`driver.escapeQueryWithParameters` 為純函式（僅需 `new DataSource({type:...})` 建構 driver 物件，不需 `.initialize()`），全數可離線驗證，建議獨立為一般 `.spec.ts`（無 `.mssql.`/`.pg.` 中綴），CI 恆常執行、訊號最快。
- **測試環境完全沿用既有**：`mssql-env-preload.ts`/`.env.test.mssql`/`pg-env-preload.ts` 全部不需新增，P1c 為 P1 全範圍最後一片，收斂既有 gating 慣例即可。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P1C-REG-001`）。

**MSSQL 全面遷移 P2a 特殊注意（AD-E07-40，P2 首片，跳過 spec-writer）：**
- **範圍嚴格限 P2a**：`infrastructure/AD-E07-40-P2a-test.md` 只設計「`dbo.queue_job` entity + 手寫 filtered index baseline migration 兩軌 + 五個原子 T-SQL 操作封裝為 `MssqlQueueService` + 併發正確性 harness」，**完全獨立於** `RunQueueProducer`/`RunQueueConsumer`；接線 Producer/Consumer + 輪詢 loop（P2b）、expire sweep 定時掛載整合 + `OrphanReaper` 雙層回收一致性 + F098 全套件於 mssql 分支整體重跑（P2c）**不在範圍**，勿提前實作。
- **🔴 已查證 `pool.max` 預設值為 1，非泛用提醒**：test-designer 直接查證 `node_modules/typeorm/driver/sqlserver/SqlServerConnectionOptions.d.ts`，確認 TypeORM mssql `DataSourceOptions.pool.max` 預設 = **1**。AD §6.2 原文措辭「若未設定足夠 pool size，K 個請求**可能**被排隊變相序列化」在本專案的真實情況是「**未顯式設定必然發生**」（並非機率性風險）。`CONC-001` 為前置守門（斷言 CONC 群組專屬 `DataSource` 明確設 `pool.max≥10`），程式碼建構處須引用 `I-MSSQL-QUEUE-TEST-CONCURRENCY-01`。
- **🔴 CONC-006 決策關卡：計數斷言對序列化退化無鑑別力**：故意以 `pool.max=1`（TypeORM 預設）重跑 M=5/K=10 併發 claim 之計數斷言（恰 5 次成功、無重複），**預期仍會通過**——列鎖本身在序列化下依然正確，只是變慢。此案例證明「恰 M 次成功、無重複」單獨不能作為併發性已驗證的證據，唯有 `CONC-004`（K 個 claim 呼叫起始時間戳重疊 < 50ms + 批次總耗時遠小於「單次基準×K」）才能偵測到 pool 設定被誤刪/調低的退化。
- **🔴 SCHEMA-010 決策關卡：既有索引 comparator 不可直接套用於 queue_job**：`queue_job` 之 Path A（synchronize 一般索引）與 Path B（baseline migration filtered index）**同名但欄位組成刻意不同**（如 `idx_queue_job_pending`：Path A=(queue_name,state) vs Path B=(queue_name,created_at) WHERE state='created'）。既有 `schema-parity.ts` 之 `diffIndexSets`/`IndexRecord`（P1b2 為 36 表完全一致而設計）不含 `has_filter`/`filter_definition` 欄位，套用於本表會產生誤導性 diff（欄位組成不同會被誤判為兩側各自獨有，掩蓋「同名但定義不同」的關鍵事實）。改用 `SCHEMA-011`/`SCHEMA-012` 兩組獨立明確斷言，不對索引集合套用「diff 應為空」判定。
- **Schema 隔離策略刻意偏離 P1b2/P1b3 之 `dbo` 慣例**：Path B 改用獨立 `p2a_baseline` schema（非 `dbo`），因 P2a 目的僅驗證單一新表結構本身正確，不需要重現「新 migration 疊加在既有 36 表 baseline 之上」的完整部署序列，避免與 P1b2/P1b3 既有的 `dbo` 獨佔慣例產生範圍重疊。此設計選擇之範圍缺口（未驗證新 migration 檔正確疊加進 `dbo` 既有鏈）已記入 `risks-and-gaps.md`，低風險非阻擋。
- **不需要「等 Windows」緩衝**：AD §0 已明確佇列 claim 為純 T-SQL DML，不經過 `sp_getapplock` 類系統預存程序，不受 P1c 已踩之 17750 DLL 缺失影響，本文件全部案例（含 CONC 群組）皆可對真實 MSSQL 容器完整執行。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P2A-REG-001`）。

**MSSQL 全面遷移 P2b 特殊注意（AD-E07-40，P2 第二片，跳過 spec-writer）：**
- **範圍嚴格限 P2b**：`infrastructure/AD-E07-40-P2b-test.md` 只設計「`RunQueueProducer`/`RunQueueConsumer` mssql 分支接線 + 單一 worker 輪詢 loop + `polling` reentrancy guard + `OnModuleDestroy` 清 timer + `processPayload` 業務邏輯共用」，`MssqlQueueService` 五操作本身**不重測**（P2a 已完成 59/59 綠，本輪視為已驗證黑盒依賴）；expire sweep 定時掛載整合 + `OrphanReaper`/`CancellationPoller` 雙層回收一致性 + F098 全套件於 mssql 分支之最終整體重跑（P2c）**不在範圍**，勿提前實作。
- **🔴 DISPATCH 群組為本輪最重要發現，與 P1c DISPATCH-001 同型陷阱重演**：test-designer 查證現行 `RunQueueProducer.send`/`cancel`/`RunQueueConsumer.onModuleInit` 三處皆為「`this.boss` 是否為 `null`」之二元 gate；`DB_TYPE='mssql'` 環境下 `this.boss` **必然為 null**（pg-boss 本不支援 MSSQL），與現行 sqlite 測試環境未 override 時的訊號完全相同。若僅在三處程式碼內部新增 mssql 分支邏輯而未把既有二元 gate 改為三分支，mssql 分支會被現行程式碼直接吞掉（send 誤拋錯／cancel 誤靜默略過／onModuleInit 誤 warn+return 完全不啟動輪詢），且沒有任何測試會自然失敗提示，除非測試明確 spy「呼叫了哪個依賴」而非只驗證最終回傳值。`DISPATCH-001~003` 對現行未修改程式碼刻意設計為紅燈。
- **🔴 DISPATCH-006 為 test-designer 查證出之 AD 文件本身缺口（非既有程式碼問題）**：AD §4.2 檔案改動清單僅列 `assignment-worker.module.ts` providers 加入 `MssqlQueueService`，未列 `assignment.module.ts`；但 `RunQueueProducer`（`send`/`cancel` mssql 分支之直接依賴）依現行結構註冊於 **API 程序**（`assignment.module.ts`，`TS-F098-WORKER-004` 既有靜態守門已確認），若不同步補上，API 程序之月名單分派觸發在 mssql 環境下會因缺少 `MssqlQueueService` 依賴而故障。已記入 `risks-and-gaps.md`，MUST-FIX。
- **🔴 POLL-007 reentrancy guard 為 I-MSSQL-QUEUE-SERIAL-01 核心**：以 `vi.useFakeTimers()` + 可外部控制 resolve 時機的 `claimNext` fake 精確重現「上一輪 tick 尚未完成、下一輪計時器已到期」之窗口，證明 `polling` guard 使 `claimNext` 於重疊期間恰被呼叫 1 次。
- **🔴 PAYLOAD-001 為 I-MSSQL-QUEUE-PAYLOAD-UNITY-01 DoD 核心**：單一 `vi.spyOn` 掛在同一實例的 `processPayload` 方法，分別驅動 pg-boss 路徑（`handleJobs`）與 mssql 路徑（`pollOnce`）各觸發一次，證明兩路徑呼叫同一函式參照，而非各自維護兩份相似業務邏輯；若 tdd-implementation 為兩路徑各寫一份相似方法，本案例之 spy 呼叫次數會是 0（明確可觀察之紅燈）。
- **🔴 PAYLOAD-008 揪出 pg-boss 與 mssql 兩路徑唯一的資料形狀落差**：pg-boss 之 `job.data` 已是 driver 自動解析後之物件，mssql 之 `claimed.payload`（P2a 已驗證）是**原始 JSON 字串**；轉接層若漏做 `JSON.parse`，`processPayload` 收到的會是字串而非物件，`payload.runId`/`payload.ym` 皆為 `undefined`，屬容易遺漏的細節陷阱。
- **分層設計決策（成本考量）**：DoD #1 明文授權判斷是否新增獨立 `.mssql.spec.ts`；查證 DISPATCH/PROD/POLL/PAYLOAD/STATIC 五組驗證對象皆為 Node.js 控制流程邏輯，與 SQL Server 鎖行為無關，改用 fake `MssqlQueueService` + `vi.useFakeTimers()` 即可完整驗證（新檔 `ad-e07-40-p2b.spec.ts`，39 案例，免真實連線，CI 恆常執行），**僅 E2E 群組需要真實 MSSQL 容器**（新檔 `ad-e07-40-p2b.mssql.spec.ts`，5 案例）。
- **E2E harness 刻意偏離既有 F098 PGINT 精神**：既有 `f098-pg-integration.spec.ts` 為驗證「pg-boss 套件本身是否可靠」而繞過 `RunQueueProducer`/`RunQueueConsumer` 直接操作原生 pg-boss API；P2b 待測的正是這兩個類別本身新增的 mssql 分支接線邏輯，故 E2E 群組直接建構真實 `RunQueueProducer`/`RunQueueConsumer`（注入真實 `MssqlQueueService`），僅 `pipeline` 以簡化 stub 取代業務邏輯，避免拖入完整 ETL/計分依賴。
- **POLL-010 為決策關卡，界定 DoD #4「SIGTERM 優雅關閉」之驗證範圍**：`worker-main.ts` 之 SIGTERM handler 為既有程式碼、P2b 不變動；本文件以「既有 `TS-F098-WORKER-001` 靜態守門（handler 存在）」+「`POLL-009`（`onModuleDestroy` 直接清 timer 之單元驗證）」組合滿足 DoD #4，不額外設計 spawn child process 送真實 OS SIGTERM 訊號之案例（現行 F098 P1 套件亦無此類先例，屬過度工程）。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P2B-REG-001`）。

**MSSQL 全面遷移 P2c 特殊注意（AD-E07-40，P2 最後一片，跳過 spec-writer）：**
- **範圍嚴格限 P2c**：`infrastructure/AD-E07-40-P2c-test.md` 只設計「`MssqlQueueService.expireSweep()` 定時掛載 + 兩層回收（佇列層/業務層）獨立性與一致性 + 全 F098 套件於 mssql 分支最終整體重跑 + `docker-compose.yml` `RUN_QUEUE_POLL_INTERVAL_MS` 補齊」；`expireSweep()` SQL 本身正確性（P2a `SWEEP-001~005` 已完成）、`RunQueueProducer`/`RunQueueConsumer` 接線與輪詢 loop（P2b 已完成）**不重測**。
- **🔴 關鍵技術澄清（避免下游誤用 harness）**：`MssqlQueueService.expireSweep()` 之 P2a 定案簽章為 `async expireSweep(): Promise<void>`，**無** `now` 參數，與 `OrphanReaper.reap(now: Date = new Date())` 之注入時鐘機制**不對稱**。sweep 之時間控制不能比照 `reap(now)` 傳入固定時間，須沿用 P2a `SWEEP-001` 已驗證的技術——直接以 SQL 竄改種子列 `expire_at` 至過去（DB 端 `SYSUTCDATETIME()` 判定）。已於本文件 §0.3 明確記錄。
- **🔴 掛載機制未定案，MUST 避免預設答案**：AD §4.3 原文明確**不強制**掛載方式（搭 `OrphanReaper` 既有 `reaperIntervalMs` 定時器 vs `MssqlQueueService` 自行啟動獨立 timer，兩者擇一交 tdd-implementation 依現行風格決定）。`MOUNT` 群組刻意設計為黑盒觀察式（spy `MssqlQueueService.expireSweep` 是否於預期時機被呼叫），不預設呼叫者之具體 class/method；`MOUNT-001` 要求 tdd-implementation 於 impl log 明確記錄實際選擇（接續 P2b impl log AD-1~AD-4 編號，本案編為 AD-5）。
- **🔴 RECOVERY 群組（旗艦，AD §7 DoD #2）核心設計**：`RECOVERY-001~003` 正面證明兩層回收各自成立且順序無關（先 sweep 後 reap／先 reap 後 sweep 結果一致）；`RECOVERY-004`/`005` 為獨立性**直接反證**（僅執行其中一層，另一層完全不受觸碰）；`RECOVERY-010` 額外驗證 AD 未明講但邏輯上真實存在的第三類孤兒——「入列但從未被 claim」（`queue_job` 仍為 `created`，sweep 之 `WHERE state='active'` 天然不影響它，只有 `OrphanReaper` 之 pending 分支會抓到），並串連 P2a claim 語意 + P2b `processPayload` 既有防線（`PAYLOAD-004`）+ P2c `OrphanReaper` 三個切片，驗證 worker 恢復後 `claimNext()` 仍會正常撈到該筆，但既有防線確保不重複執行 pipeline。
- **REG 群組排除機制設計**：`REG-001` 要求失敗案例清單與已知排除集合**恰好相等**（比對測試名稱字面，非僅比對失敗數量），避免「已知失敗被替換成另一個不相關失敗但總數剛好還是 1」的誤判；`REG-002` 為排除理由本身持續有效性之獨立靜態守門（`CreatePgBossSchema` migration 檔持續不存在，沿用 `AD-E07-40-P2b-impl.md` 已記錄之 pre-existing 失敗根因，若該檔案未來重新出現則排除清單須重新評估）。
- **已查證之 `docker-compose.yml` 現況缺口**：`worker:` service（`container_name: cdmp-worker`）現行**零個** `RUN_QUEUE_*` 環境變數（不僅 `RUN_QUEUE_POLL_INTERVAL_MS` 缺席，其餘 4 個既存 `RunQueueTuning` 欄位亦未曝露）。`STATIC-001` 依 AD §7 DoD #4 字面範圍僅要求補上 `RUN_QUEUE_POLL_INTERVAL_MS`；其餘缺口記入 `risks-and-gaps.md`，非本輪阻擋項。
- **P2（P2a/P2b/P2c）全範圍測試設計至此完成**，MSSQL 全面遷移 P1+P2 累計場景數達 2320。
- **實作後跑 `tsc --noEmit -p tsconfig.build.json`**（`TS-MSSQL-P2C-REG-005`）。

**輔助參考：**
- `test-data-strategy.md` — 測試資料準備
- `test-levels.md` — 各層級測試策略

### QA Agent

**必讀檔案：**
1. `test-index.md`（本文件）
2. `test-levels.md` — 測試層級與 NFR 驗證策略
3. `risks-and-gaps.md` — 風險與待決問題
4. 所有 `features/F###-test.md`

### CI/CD Agent

**必讀檔案：**
1. `test-index.md`（本文件）— 取得覆蓋率目標
2. `test-levels.md` — Unit / Integration / E2E 分層執行策略

### Product Analyst

**必讀檔案：**
1. `test-index.md`（本文件）— 確認覆蓋率
2. `risks-and-gaps.md` — 確認風險與待決問題是否可接受

---

## 5. 相關文件索引

| 文件 | 路徑 | 說明 |
|------|------|------|
| 測試層級策略 | [test-levels.md](test-levels.md) | Unit / Integration / E2E / NFR 策略 |
| 測試資料策略 | [test-data-strategy.md](test-data-strategy.md) | 種子資料、邊界值、Mock 策略 |
| 風險與缺口 | [risks-and-gaps.md](risks-and-gaps.md) | 不可測需求、架構限制、待決問題 |
| 功能規格索引 | [../spec-index.md](../spec-index.md) | 所有功能規格入口 |
| 架構規格 | [../architecture-spec.md](../architecture-spec.md) | 系統架構與模組定義 |
| 錯誤處理規範 | [../error-handling.md](../error-handling.md) | 49 個錯誤碼定義 |
| 非功能需求 | [../nfr.md](../nfr.md) | 安全性與效能閾值 |

---

## 更新紀錄

| 日期 | 變更內容 | 負責人 |
|------|---------|--------|
| 2026-08-18 | **F119 R-F119-04 缺口補強：AC-14 五路徑一致性（v2.62）**：F119 已實作完成並 merge 進 main；前輪 test-generator 誠實揭露之缺口——「AC-14 五路徑完整一致性未於本輪逐路徑數字驗證」（`risks-and-gaps.md` R-F119-04）——本輪補強。team lead 確認阻礙已消除（dev MSSQL `172.20.202.212:1433` 實測可連，既有 `f119-categorical-collation.mssql.spec.ts` 可用）。新增 3 檔共 12 案例，**四路真實/可執行覆蓋 + 一路誠實標記為較低驗證層級**：①`f119-ac14-cross-path-parity.mssql.spec.ts`（4 案例，★核心，**真實 MSSQL 執行**，非 mock）：路徑1（MSSQL 下推 `runStage1SqlInsertMssql`）vs 路徑3（JS filter chain `executeStage1Chain`）對真實 dev `ob_pool_data`（167 萬列）之 `contains`/`not_contains`/AC-9 字面值三情境，逐 PK 集合精確相等，含 AC-3 NULL 不對稱核心案例；**不斷言絕對數字**（team lead 明確指示：既有 `stage1-sql-pushdown.mssql.spec.ts` 15~16 個既有案例已因同一原因〔僅 seed 2~3 筆卻斷言 `toBe(1)`，而 dev pool 167 萬列早已非空表〕失效在案）——實測中途發現 `contains '勁便利'` 對真實資料命中達 112 列（非本檔僅 seed 之 1 列），已改為路徑相等 + 自 seed 列（依 PK）逐項歸類斷言，免疫於既有真實資料雜訊；另實測 `not_contains` 全表 NOT LIKE 掃描單案例達 ~90 秒，已依 `data-source.ts` `I-MSSQL-REQUEST-TIMEOUT-01` 慣例於測試自身連線設定 `requestTimeout:120000`（tedious 預設 15s 對此規模掃描不足）。②`f119-ac14-stage0-delegation.spec.ts`（4 案例，SQLite，恆可執行）：AD-E07-50 §2.1 查證「路徑4（Stage0 估算 `dryRunChainCount`）呼叫路徑3、非獨立實作」，故對路徑4 之正確驗證手法非重覆執行路徑3數字比對，而是以 `vi.spyOn` 證明委派不失真——`Stage0EstimateService.estimateListCount` 呼叫 `executeStage1Chain` 時，F119 新增之 `operator`/`keyword` 欄位逐欄位原封不動傳遞（含 AC-3 not_contains、AC-17 既有 in 條件回歸）。③`f119-ac14-pg-semantic-equivalence.spec.ts`（4 案例，**非真實 PG 執行，已於檔頭與本列誠實標記**）：本專案已全面遷移 MSSQL、環境無真實 PostgreSQL 可連線；改為直接呼叫生產碼 `buildStage1Sql()`（PG 建構器，純函式、mock `pdlRepo` 即可執行，非連接真實 DB）取得 WHERE 片段/參數，逐字元比對 `buildCategoricalOperatorFragment()` 之直接輸出，證明 PG 建構器未在共用函式輸出之外自行竄改字元——此為「呼叫路徑未走樣」層級之證據，非「PG 引擎真實行為」層級（後者無法驗證，已在 R-F119-04 明確記錄此殘留低風險）。路徑5（草稿抽樣估算 `previewHitCount`）沿用既有 `f119-preview-hit-count-text-operators.spec.ts`（SQLite 黑箱），AC-14 本身明文「抽樣估算不要求數字逐筆相等」，本輪未重複。**紅燈/綠燈驗證**：因 F119 生產碼已實作完成，本輪 12 案例皆為**對已上線程式碼的真實執行驗證**（非 TDD 紅燈-then-實作），首次撰寫時 2 案例因規則書寫錯誤而假紅（PG 語意等價之跳脫字元正則多轉了一層、equals 案例誤判「不含 LIKE」未排除既有詐騙白牌規則之無關 LIKE 子句），已修正並重跑確認全數 12/12 綠燈，其中真實 MSSQL 3 案例實測產出「dev pool 167 萬列真實命中 112 列」等真實數字，證明測試確有鑑別力而非空跑；`tsc --noEmit -p tsconfig.json` 型別檢查全數通過（一處 `condition_payload as {conditions:...}` 型別轉換過窄，改為 `as unknown as {...}` 修正）；兩目錄（`assignment/stage1/__tests__`、`assignment-list/__tests__`）之非-mssql 既有測試（44 檔 772 個）於新增後全數綠燈，零回歸；MSSQL 測試執行後已實測確認 dev DB 無 `F19` 前綴殘留列（`ob_pool_data`/`ob_list_definition`/`ob_monthly_run_result`/`assignment_run` 四表皆 0）。**R-F119-04 狀態**：由「未驗證」更新為「四路徑真實/可執行覆蓋 + 一路誠實分級（PG 語意等價）」，詳見 `risks-and-gaps.md` 更新版。總場景數 3861→**3873**；總文件數不變（新增 3 個測試檔，非新增 test-spec 文件） | Test Generator Agent |
| 2026-08-18 | **F119 tdd-implementation 團隊模式測試爭議裁決（v2.61）**：後端 7 檔 79 案例、前端 utils/kanban/stage0 已全綠後，implementer 提報兩件測試側爭議，test-generator 裁決如下（詳見 `risks-and-gaps.md` R-F119-08）。**爭議 1（採納，判定為測試自身缺陷）**：`list-create-draft-page.test.tsx` F119-FE-010 之 `value-checkbox-0-0` 與該 describe 之 mock（`prod_kind` optionValue `'01'`）不符，本檔既有慣例（L372/424/577）皆證實應為 `value-checkbox-0-01`；已修正兩處，AC-17 斷言本體未改動，implementer 實作已滿足。**爭議 2（採納，判定為測試遺漏真實 prototype 行為，非設計偏離）**：`list-edit-draft-page.test.tsx` F119-EDIT-002 原始寫法未驗證 prototype `27a` `setCondOperator()` 之 `crossForm && willLose` 分支——載入條件之關鍵字非空時跨形態切換（IN↔文字）須先彈出二次確認 modal（附錄 C C-3~C-5），原測試直接斷言面板立即切換，與 prototype 不符。test-generator 重讀 prototype 原始碼確認後改採 implementer 建議之選項 (b)：F119-EDIT-002 改為驗證「未確認前面板不得切換 + 損失清單含關鍵字內容 + 確認後才切換」，新增 F119-EDIT-002b 覆蓋「兩側皆空 → 不彈窗」對照組（獨立路徑，不耦合 EDIT-002 之確認流程，避免測試間依賴）。**兩案例調整後皆已對現行實作重跑驗證**：F119-FE-010 轉綠（確認 implementer 之 IN 形態 payload 不含 operator key 邏輯正確）；F119-EDIT-002 為真紅（implementer 目前暫以「立即切換、不確認」佔位，待補二次確認 modal）、F119-EDIT-002b 為真綠（該路徑 implementer 已正確實作）。`list-edit-draft-page.test.tsx` F119 案例數 3→**4**。總場景數 3860→**3861**；總文件數不變 | Test Generator Agent |
| 2026-08-18 | **F119 類別型篩選欄位新增文字比對運算子（包含／不包含／完全等於）測試設計新增（v2.60）**：新增 `features/F119-test.md`（**115 個場景**：後端 stage1 SQL 語意層 49（含真實 MSSQL 7）+ 後端 assignment-list 黑箱/驗證層 30 + 前端 `_utils` 純函式 13 + 前端 Page 整合 23），對應 US-183／F119 spec v1.1／AD-E07-50 v1.1。**本輪依 team lead 明確指示簡化範圍**：僅產出 vitest/jest 單元＋整合測試，不含 Playwright e2e fidelity、不含 Stryker mutation、不含 dependency-cruiser metric gate、未呼叫 `ring-setup` skill。核心裁定：BR-6 NULL 八格矩陣（僅 `ob_pool_data`×`not_contains` 顯式保留 NULL）由 `f119-categorical-operator-fragment.spec.ts` 以 better-sqlite3 真實執行 `buildCategoricalOperatorFragment()` 回傳之 fragment 逐格驗證**真實列選取結果**（非僅字串形狀），並設計「若未跳脫則測試必紅」案例（關鍵字 `100%` 不得誤命中 `1000元`、`A_B` 不得誤命中 `AXB`）；AC-17 向後相容以「顯式 `operator:'in'` 與缺漏 `operator` 之簽章/SQL/顯示三端輸出逐字相同」之回歸測試證明（非推論），前端 `formatConditionSummary()` 之 in 格式對照組（`OB202606004`/`OB202606011`）直接沿用 ui-ux-designer 已於原型內建之活體對照資料。**Test-generator 撰寫時嚴守 blind-to-impl 原則**：僅讀 F119 spec + AD-E07-50（本 feature 之架構決策文件，非生產碼）+ US-183 + `docs/ui-ux-design-overview.md` 附錄 C + `prototypes/27a/27b/27/30`，未讀 `stage1-query-composer.ts`／`stage1-customer-core-clause.ts`（及 -mssql）／`stage1-customer-financial-clause.ts`／`assignment-list.service.ts`／`condition-item.dto.ts`／`list-create-draft-page.tsx`／`list-edit-draft-page.tsx`／`list-definition-page.tsx`／`stage0-estimate-page.tsx`／`_components/ListDetailDrawer.tsx`／`_utils/labels.ts`／`_utils/condition-summary.ts` 生產碼；**wiring 例外**（非 assertion 內容）：讀取既有 `stage1-query-composer.spec.ts`／`stage1-customer-core-clause.spec.ts`／`stage1-customer-financial-clause.spec.ts`／`create-list-v2.1.spec.ts`／`validate-condition-payload.spec.ts`／`derive-backward-compat.spec.ts`／`preview-hit-count.spec.ts`／`list-create-draft-page.test.tsx`／`list-edit-draft-page.test.tsx`／`list-kanban-page.test.tsx`／`stage0-estimate-page.test.tsx`（皆為既有測試環，非生產碼）之 buildEnv pattern、mock DI 慣例、既有 data-testid 命名，未借此推論任何 F119 業務判斷之斷言內容；亦讀取 `ob-list-definition.entity.ts` 之 `ObListDefinitionConditionItem` interface（entity 型別定義，明確允許範圍）。**C-Q1 裁定沿用**：ui-ux-designer 已於附錄 C 記錄 AD §3.6 樣板字串（`「${欄位}」${標籤}「${keyword}」`）與 F119 AC-15 業務契約例句（`${欄位} ${標籤}「${keyword}」`，欄位名不加引號）不一致並裁定採 **AC-15 例句**（業務契約優先於 AD 樣板筆誤），本測試套件之 `formatConditionSummary()` 斷言依此裁定撰寫。**紅燈驗證**：全數 115 個場景已實際以 `vitest run` 執行確認（含後續依 team lead 環境情報回饋補上的 7 個真實 MSSQL BR-8 collation 案例）；後端 62 個純函式案例因新函式（`resolveCategoricalOperator`/`escapeLikeKeyword`/`buildCategoricalOperatorFragment`）尚未匯出而 TypeError，或驗證層/簽章擴充尚未實作而斷言失敗，紅燈原因正確；另有真實 MSSQL 7 個案例（`f119-categorical-collation.mssql.spec.ts`，BR-8 大小寫/全半形敏感度，2026-08-18 依 team lead 環境情報回饋補上）5 red（同一 TypeError 原因）/ 2 green（環境事實查證：dev MSSQL collation 確為 `Chinese_Taiwan_Stroke_BIN`，非行為紅燈，已確認單檔執行 1.33 秒完成、非併跑競爭偽紅）；前端 13 個因 `_utils/labels.ts` 之 `OPERATOR_LABEL`/`operatorLabel` 尚未匯出、`_utils/condition-summary.ts` 檔案尚未建立（模組解析失敗）；前端 Page 整合 23 個因新 `data-testid`（`condition-operator-{col}` 等）不存在於現行 DOM。**弱通過（weak-pass）揭露**：`f119-customer-core/financial-categorical-operator.spec.ts` 各有數個綠燈案例（既有 IN 行為回歸錨點，設計上即應保持綠燈），`f119-signature-backcompat-duplicate.spec.ts` 之 T-13/T-15/T-16（斷言「不觸發 422」）於實作前巧合為綠燈（因 keyword-based 條件現行產生空簽章、空簽章依既有規則永不衝突），已於 F119-test.md 明確記錄此非弱斷言之偽陽性，而是與配對之 T-14（真正紅燈證據）合讀方能證明 AC-16 完整雙向行為；`list-kanban-page.test.tsx` F119-KANBAN-002 與 `stage0-estimate-page.test.tsx` F119-STAGE0-002 為刻意設計之正控制組（既有行為之回歸基準），非「測試寫錯才通過」。**發現並記錄之測試邊界（非缺陷）**：composer 層對 caseyear 文字運算子之 defense-in-depth（AD §3.8 次要防線）因與既有 `EMPTY_VALUES` no-op 行為巧合重疊而無法產生有意義之紅燈證據（tautological pass），已改依 `I-CATOP-CASEYEAR-EXCLUDE-01` 本文明定之權威執行點（驗證層）撰寫 `CASEYEAR-CREATE-001`/`CASEYEAR-PREVIEW-001` 取代，未產出該弱測試檔；AC-14 五路徑一致性之完整逐路徑數字驗證需 MSSQL live 環境，超出本輪 SQLite unit/integration 範圍，已記錄待下輪補強。**型別檢查**：`tsc --noEmit -p tsconfig.json`（因 `tsconfig.build.json` 排除 `*.spec.ts` 不適用）僅出現「匯入尚未存在之生產碼匯出成員」之預期錯誤，測試檔自身邏輯無型別錯誤。**回歸確認**：後端 `assignment-list`/`stage1` 目錄（排除需真實 MSSQL 連線之 `.mssql.spec.ts`，其批次執行受 dev MSSQL 資源競爭影響〔既有 `stage3to4-ration-pushdown.mssql.spec.ts` 一例因 deadlock 導致 worker crash，與 F119 無關，純環境雜訊〕）36 個既有測試檔全數綠燈；前端 `apps/web/src/pages/assignment` 整目錄 869 個測試中 815 個既有通過案例全數不受影響。總場景數 3746→**3861**；總文件數 109→**110** | Test Generator Agent |
| 2026-08-04 | **F118 從上月複製名單顯示「已複製過」提示測試設計新增（v2.59）**：新增 `features/F118-test.md`（**42 個場景**：後端 Unit/Integration 11 + Route/RBAC 9 + E2E 4 + 前端 Component 9 + Page 整合 5 + E2E Fidelity 4），對應 US-181／F118 spec v1.1／AD-E07-48 §3.2/§5。核心裁定：「已複製過」判定採**語意等價**（方案 b，F118 §12.1 D-1）——重用 `AssignmentListService.normalizeConditionPayload`（既有 private method，`findActiveConditionDuplicate` 亦呼叫同一份），使判定與儲存端 422 `LIST_NO_DUPLICATE` **依建構即一致**（BR-1），故本測試套件之核心不是重新驗證正規化規則本身（已由既有 `derive-backward-compat.spec.ts` NCP-001~006 涵蓋），而是驗證 **AC-2 雙向一致性這個結構性不變式**（TS-F118-BE-002/003 ★核心：判定 false→原樣 createList 不得 422；判定 true→原樣 createList 必定 422 且 `conflictListNo===copiedToListNo`）。新端點 `GET /api/v1/assignment/lists/copy-duplicate-check?prevYm&currentYm` 為 v1.1 人工審閱閘自 AD-E07-48 v1.0 之 `POST`（前端傳候選）調整定案，理由是讓判定所用 `condition_payload` 與儲存端同源、免除「前端往返 payload 失真」風險；NestJS 路由順序陷阱（字面路徑須宣告於 `:listNo` 動態路由之前）由 TS-F118-RBAC-005（★核心）覆蓋。**Test-generator 撰寫時嚴守 blind-to-impl 原則**：僅讀 prototype `27a-list-create-draft.html` + F118/F050 spec + AD-E07-48 + error-handling.md，未讀 `assignment-list.service.ts`/`assignment-list.controller.ts`/`copy-from-prev-month-modal.tsx`/`list-create-draft-page.tsx`/`assignment-list.ts`（api client）生產碼；**wiring 例外**（非 assertion 內容）：因既有 `create-list-v2.1.spec.ts`/`derive-backward-compat.spec.ts`/`assignment-list.controller.spec.ts`/`copy-from-prev-month-modal.test.tsx`/`list-create-draft-page.test.tsx`/`f117-dept-ratio-director-filter.e2e-spec.ts` 為既有測試環（非生產碼），為求新增測試可實際掛載執行且延續既有 test-id 慣例（`copy-row-{listNo}`/`btn-use-{listNo}` 等），讀取其 mock DI 慣例、buildEnv pattern、既有 fixture builder（`apps/api/test/fixtures/users.fixture.ts`）與 app bootstrap 模式，未借此推論任何 F118 業務判斷之斷言內容；新定義之 test-id（`dup-confirm-modal`/`dup-confirm-target-list-no`/`btn-confirm-dup-copy`/`btn-cancel-dup-copy`/`btn-close-dup-reminder`，prototype 僅有原生 `id` 無 `data-testid`）記錄於 `docs/specs/contracts/F118-copy-duplicate-check.contract.ts`。**紅燈驗證**：全數 42 個場景已實際執行確認為紅燈——後端 11+9+4=24 個因 `checkCopyDuplicates`/路由/端點不存在而 TypeError 或 404（其中 TS-F118-BE-011 額外揭露一個**真實既有缺陷**：`findActiveConditionDuplicate` 目前無 `ORDER BY`，多筆歷史等價名單時 `conflictListNo` 依 SQLite 隱式順序回傳而非 `list_no` 最小者，與 AD-E07-48 §5.3/§2 事實 4 之描述完全吻合）；前端 9+5=14 個因元件/API client 尚未支援 `duplicateItems`/`checkCopyDuplicates` 而斷言失敗；E2E Fidelity 4 個以 `playwright test --list` 靜態驗證可正確解析列舉（無可用 live stack 執行，見 risks-and-gaps.md）。3 個前端測試（TS-F118-FE-002/008/009）為刻意設計之**回歸錨點**、非弱斷言——分別驗證「未複製過候選不顯示徽章」「未複製過候選點擊直接 onCopy」「AC-10 降級時全部候選皆視為未標示」三者皆須與 F118 實作前行為**逐字相同**，故在未實作狀態下即合法通過，實作後仍須保持綠燈（非「測試寫錯才通過」）。**Mutation/Metric gate**：`stryker.conf.json` `mutate` 追加 `assignment-list.service.ts`（既有 1580 行大檔，Stryker 無檔案內函式級 scoping，`vitest.mutation.config.ts` 之 include 隨之擴大為該模組既有全部 19 個 SQLite unit spec〔已於本輪確認全數綠燈，330 test / 7 秒〕+ 本輪新增 spec，避免既有邏輯被誤判 NoCoverage）；`eslint.ring.config.cjs` 追加 `assignment-list.{service,controller}.ts`（per-function 規則不受檔案總長度影響；`max-lines` 對 service.ts 設為現況 1580 + 合理增量 1650，而非套用 F117 之 400，避免對既有技術債重複告警，見 F118-test.md「max-lines 門檻特別說明」）；`gate:coverage:f118`（api + web）新增。**同輪查證發現**：既有 `assignment-list.service.ts`/`.controller.ts` 於本 gate 下已有 14 項 pre-existing complexity/max-lines-per-function 違規（`listLists`/`createList`/`updateList`/`getFullSnapshot`/`findActiveConditionDuplicate` 等），皆非 F118 造成，已如實記錄於 risks-and-gaps.md「F118」而非悄悄調高門檻掩蓋。總場景數 3704→**3746**；總文件數 108→**109** | Test Generator Agent |
| 2026-08-04 | **F117 部門比例設定頁僅提供「有在職處長」之部門設定測試設計新增（v2.58）**：新增 `features/F117-test.md`（**31 個場景**：後端 Unit 12 + Integration 6 + 前端 Component 8 + E2E Fidelity 5），對應 US-180／F117 spec v1.1／AD-E07-48。F117 為 F079 v1.3 之精煉（疊加可設定範圍限縮，衝突時以 F079 為準），核心為 §12.1 D-1~D-6 裁決偏離之可測契約化：三分類（有處長部門可編輯／孤兒部門鎖定顯示／無關部門隱藏）調和 US-180 AC-1（隱藏）與 AC-3（不得靜默刪除）之字面衝突；加總驗證範圍改為「最終持久化集合」而非畫面顯示列（BR-7）；新增 422 `RATIO_DEPT_DIRECTOR_REQUIRED`。**Test-generator 撰寫時嚴守 blind-to-impl 原則**：僅讀 prototype `29a-dept-ratio-config.html` + F117/F079 spec + AD-E07-48 + data-model.md，未讀 `dept-ratio.service.ts`/`dept-ratio.controller.ts`/`dept-ratio-form.tsx`/`dept-ratio-config-page.tsx` 生產碼；**wiring 例外**（非 assertion 內容）：因既有 `dept-ratio.service.spec.ts`/`dept-ratio-form.test.tsx`/`dept-ratio-config-page.test.tsx`/`f081-f085-f089-rollback.e2e-spec.ts` 為既有測試環（非生產碼），為求新增測試可實際掛載執行，讀取其 mock DI 慣例、test-id 命名、既有 fixture factory（`apps/api/test/fixtures/{users,ob-emphire}.fixture.ts`）與 app bootstrap 模式，未借此推論任何 F117 業務判斷之斷言內容。**後端 Integration 測試策略**：不依賴外部 MSSQL / docker，改用既有 `f081-f085-f089-rollback.e2e-spec.ts` 已驗證之 in-memory better-sqlite3 + 真實 `AuthModule`+`AssignmentStageModule` + `buildDirector()`/`buildSectionChief()`/`buildEmphire()` fixture builder 全自建資料，故可在無外部依賴環境下對 BR-4（孤兒列保留）取得真實 HTTP+DB round-trip 證據，非僅 mock。**E2E Fidelity 測試策略（記錄於 risks-and-gaps.md）**：因 dev 環境 base seed（`seed.ts`）未提供 `business_role=director/section_chief` 測試帳號、且無已知穩定存在於 dev DB 的 `dept_ratio` 階段名單可供 Playwright 對照，改採 `page.route()` 攔截 GET/PUT 回應（比照 prototype 內建 6 個 demo 場景）+ 真實瀏覽器導覽/建置驗證，兩者並存以在不依賴外部 MSSQL 之前提下捕捉建置/路由/proxy 層漂移；`admin` 帳號等價於 `director`（F079 BR-7），故 admin 可涵蓋絕大多數角色場景，僅 `section_chief` 真實登入場景暫缺（已於 risks-and-gaps.md 記錄，非本輪阻塞）。**同輪 ring-setup**：本專案先前無 Playwright/Stryker/dependency-cruiser，已透過 `ring-setup` skill 建立 `e2e/`（Playwright）+ `apps/api/stryker.conf.json`（scope: `dept-ratio.service.ts`）+ `apps/{api,web}/.dependency-cruiser.cjs` + `apps/api/eslint.ring.config.cjs`（複雜度 gate）。AC-9（處長唯讀）/AC-10（既有消費端回歸：準備完成摘要 excludeZeroRatio、Detail Drawer）為 regression 錨點，不修改任何既有測試斷言。總場景數 3673→**3704**；總文件數 107→**108** | Test Generator Agent |
| 2026-07-13 | **F113 員工編號作為登入識別碼測試設計新增（v2.57）**：新增 `features/F113-test.md`（**105 個場景**：後端 85 + 前端 20），對應 US-179／F113 spec／AD-E02-5。詳見上方版本歷史 v2.57 條目（登入分支不 trim/大小寫敏感核心紅線、既有 `auth.service.spec.ts` SQL Injection 測試必要修改之精確查證、`mssql-p1b2.mssql.spec.ts` 五項既有測試 CI 阻斷風險與索引層級白名單修法機制、兩軌唯一性 SQLite/MSSQL 測試邊界）。總場景數 3568→**3673**；總文件數 106→**107** | Test Designer Agent |
| 2026-07-09 | **F110 `code_decode` 節點（泛用單趟多欄位代碼解碼）測試設計新增（v2.54）**：新增 `features/F110-test.md`（**28 個場景**）+ `infrastructure/AD-E07-41-P4-codedecode-test.md`（**85 個場景**），對應 US-173／AD-E05-7／AD-E07-41 §13。詳見上方版本歷史 v2.54 條目（範圍分工、EQ-MSSQL 安全網設計洞察、六項不變式對照）。總場景數 3270→**3383**；總文件數 102→**104** | Test Designer Agent |
| 2026-07-08 | **MSSQL 全面遷移 P4e raw Staging Bulk-Load 寫入端測試設計新增（v2.44，P4 最後一片，P4 全範圍至此完成）**：新增 `infrastructure/AD-E07-41-P4e-test.md`（**65 個場景**），對應 AD-E07-41 §6（Bulk-Load：5 個來源表 raw Staging 寫入端）+ §9 P4e DoD（銜接 P4a/b/c/d，跳過 spec-writer，不重測任一 ETL handler 內部邏輯或 customer_core 56 節點端對端）之收尾切片：`raw-data.service.ts` 之 `openCopyWriter`/`formatCopyValue`/`supportsCopy`（`pg-copy-streams`）需新增對稱之 `openMssqlBulkWriter`/`supportsBulk`（tedious `Request.bulk`），來源端讀取（`mssql-executor.ts`）已相容不需改。**核心紅線 1（🔴🔴 本文件最高風險發現，直接阻擋 P4e 自身 DoD）**：逐檔查證 `RawDataService` 現行 `isPostgres: boolean` 為二元 gate，`DB_TYPE=mssql` 時恆為 `false` 而與 SQLite 分支混淆——`createRawTable()` 產生 `AUTOINCREMENT`（非 T-SQL 語法）、`tableExists()` 查詢 `sqlite_master`、`getTableColumns()` 呼叫 `PRAGMA table_info`，三者是 `executeExtraction()` 建表/schema-drift 比對之直接前置依賴，缺口不修則 bulk-load 連第一步都無法執行；已設計 §二 ISPG-GATE（9 案例：陷阱佐證 + 目標行為雙軌），範圍刻意限縮於此三方法（同檔案同缺陷但非直接依賴之 `getColumnMetadata`/`getIndexedColumns`/`getRawData` 資料查詢記入風險文件，不納入本輪）。**核心紅線 2（🔴 AD 檔案改動清單缺口，同型於 AD-E07-40 P2b 先例）**：AD §6 僅描述 `raw-data.service.ts` 本身新增介面，完全未提及消費端 `extraction-execution.service.ts` 之 `canStream` 判定式（現行僅檢查 `supportsCopy()`）需同步擴充承認新增的 `supportsBulk()`，否則 bulk-load 機制會成為「建置完成但從未被觸發」之死碼；已設計 §三 DISPATCH（6 案例，含陷阱佐證 + spy 驗證實際呼叫路徑而非僅驗證回傳值）。**核心紅線 3（🔴🔴 TYPEMAP，呼應 FINDING-P4D-01 同型缺陷家族）**：三層型別鏈（來源型別→raw 表型別→tedious bulk Table 型別宣告）須逐一對應一致，但 `ColumnMetadata` 介面完全不含 length/precision/scale 資訊；`decimal`/`numeric`/`money` 家族若映射為固定精度 `DECIMAL(p,s)`，會在 raw staging DDL 建表層級重演 P4a-fix 已修正之 `I-MSSQL-DECIMAL-NORMALIZE-01` 精度溢位缺陷家族（僅發生位置由 type_cast 節點換成建表本身），已設計 `TYPEMAP-003` MUST-FIX 決策關卡，建議映射為 `NVARCHAR(MAX)` 保留原始字面精度。**核心紅線 4（🔴 formatCopyValue 誤用防線）**：`formatCopyValue()` 之文字跳脫邏輯（`\t`/`\n`/`\r`/`\\`→反斜線轉義字面字串）專屬 PG COPY TEXT 協定語意，bulk 為型別化協定不需要（且不可誤用）此函式——若誤重用會使含真實控制字元之來源值被寫入為反斜線字面轉義形式而非還原成真實字元；已設計 §五 NOESCAPE-CHARSET MUST-FIX（執行期陷阱佐證）+ §十一 STATIC-003（原始碼靜態掃描）雙重防線。**其餘查證發現（4 項不可假設，明確標記待 tdd-implementation 真庫驗證，本文件不臆測）**：(a) TypeORM mssql QueryRunner 之 `databaseConnection` 底層物件是否可比照 `openCopyWriter` 既有手法曝露 tedious `ConnectionPool`/`Request`/`Table` API（本專案既有全部 P0~P4d `.mssql.spec.ts` 皆透過 `queryRunner.query()` 操作，從未使用過此手法，見 §四 `BULKWRITE-GATE-001`）；(b) tedious `Request.bulk(table)` 是否可對同一物理表跨多次呼叫以達成記憶體有界（CLAUDE.md ETL 紅線：不可用 in-memory 策略處理可能超出 RAM 的資料集，見 §六 `BATCH-GATE-001`）；(c) bulk Table 型別宣告與目標表實際型別輕微不符時之寬容度（見 §一 `TYPEMAP-PROBE-013`）；(d) `abort()` 中途呼叫之已寫入列殘留行為（見 §四 `BULKWRITE-006`）。**Harness 設計**：不新增基礎設施，沿用既有 `mssql-env-preload.ts`/`_p4a-mssql-harness.ts`（CDMP_TEST，同容器不同 DB 名隔離）+ 既有 `raw-data.service.spec.ts` 之 `makeService(type)` fake DataSource 手法（單元層）；§七 E2E-EXTRACT 之「來源」以 CDMP_TEST 自建合成表模擬（非連線真實外部 ZZIP/MLMC），因本輪待測物件是 `RawDataService`/`ExtractionExecutionService` 之新增接線，非已於背景確認相容的 `mssql-executor.ts`。分層：**TYPEMAP 14**+**ISPG-GATE 9**+**DISPATCH 6**+**BULK-WRITE 7**+**NOESCAPE-CHARSET 5**+**BATCH 5**+**E2E-EXTRACT 6**+**PIPELINE-READ 3**（銜接 P4a `ExtractHandlerMssql`，驗證 bulk-load 產出之物理表結構對既有已驗證 handler 相容，P4e 特有新整合點）+**PERF 2**（觀察性非阻擋，AD 明文不要求對齊 PG COPY 吞吐量數字）+**REG 5**（PG COPY 路徑既有三個 describe 區塊逐字不變為 DoD 核心）+**STATIC 3**。**MSSQL 全面遷移 P4（P4a/b/c/d/e）全範圍至此完成測試設計**（P0~P4 六階段遷移計畫中之 Phase 4，含全部子切片，累計本文件系列總場景數達 2690）；總場景數 2625→**2690**；總文件數 92→**93** | Test Designer Agent |
| 2026-07-08 | **MSSQL 全面遷移 P4c ETL Handler 群組三測試設計新增（v2.42，P4 第三片，P4 最複雜切片）**：新增 `infrastructure/AD-E07-41-P4c-test.md`（**92 個場景**），對應 AD-E07-41 §4（dedup tie-breaker）+ §5.1（customer_core UPSERT 兩段式）之 P4c 切片（銜接 P4a/P4b，跳過 spec-writer；`createDispatcher()` 之 `DB_TYPE` 分支本輪落地，P4a/P4b 兩度延後之決議）。**核心紅線 1（🔴🔴 範圍擴大核心）**：真實 `etl-pipelines.json` 查證 `target-load-handler.ts` 被 6 條 pipeline 共用（customer_core UPSERT ×1 + `fullMode` ×4 + `partition_replace` ×1），AD §5.1/§9 DoD 僅描述 customer_core 一種路徑，已擴大範圍新增 §四 FULLMODE（11 案例，含 composite PK 旗艦）+ §五 PARTITION（6 案例），否則 5 條既有生產 pipeline（E03/E04 raw data landing）於 MSSQL cutover 後會 100% 失敗且無既定測試攔截。**核心紅線 2（🔴🔴 TLDEDUP 群組）**：`target-load-handler.ts` 內部另有兩處未記載之 `DISTINCT ON` 去重站點（`fullMode` PK dedup + customer_core `source_customer_no` dedup），與 AD §4/I-MSSQL-DEDUP-TIEBREAK-01 所述 `dedup-handler.ts` 之 `ctid` 決勝角色相同但 AD 完全未涵蓋，PG 原始碼註解明確承認 `NULLIF(TRIM())` 正規化會製造真實碰撞，已設計 §二 TLDEDUP（11 案例，含旗艦真實碰撞案例 `TLDEDUP-EQ-001`）。**核心紅線 3**：`dedup-handler.ts` 之 `_seq IDENTITY` tie-breaker 改寫（AD §4 核心設計），21 個案例涵蓋真實 customer_core 三組 key 配置（d1/d2/d3）+ 決定性核心群組。**其餘查證發現**：`getPrimaryKeyColumns()`/`inputColumnTypes` 兩處 catalog 站點 AD 完全未提及，後者需要既有共用 helper 未涵蓋之 `data_type` 資訊，已設計 `CATALOG-GATE-001` 決策關卡；target-load 內部暫存表因回傳 `tempTable=''` 從未向 `NodeOutputStore` 註冊，P4a `CLEANUP-003` 機制天生不覆蓋，已設計 §七 CLEANUP（5 案例）MUST-FIX。**Harness 設計**：`dbo` schema 佔用範圍第四度擴大，本輪首次直接對真實既有生產 baseline 表（`ob_calendar`/`ob_emphire`/`ob_arreturndf_min_cap`/`customer_core`）寫入測試資料；`ob_pool_data`/`ob_pool_data_list` 因逾百欄改用合成 throwaway `dbo` 表驗證 PK/分區形狀。**明確排除**：53 節點端對端（P4d）、bulk-load（P4e）。總場景數 2482→**2574**；總文件數 90→**91** | Test Designer Agent |
| 2026-07-08 | **MSSQL 全面遷移 P4a ETL Handler 群組一測試設計新增（v2.40，P4 首片）**：新增 `infrastructure/AD-E07-41-P4a-test.md`（**96 個場景**），對應 AD-E07-41 §3/§5/§9（P4-spike-2 已於同日全數通過、P4-0 已獨立完成，前置閘已過，跳過 spec-writer）之 P4a 切片：§3.1 共用 temp table helper 補齊（3 個新函式，additive 擴充 P4-spike-2 已落地檔案）＋§3.2 對應 5 個 handler（extract/field_mapping/derived_field/type_cast/conditional）。**核心紅線 1（🔴 QUOTE 群組，本輪設計之最優先決策關卡）**：查證 5 個 handler 私有 SQL 組裝方法全數內嵌雙引號識別碼，本專案既有全部 `.mssql.spec.ts` 從未驗證過此語法於 BIN collation+tedious driver 下是否可行，AD 全文未提及，已置於文件最優先位置。**核心紅線 2（🔴 DERIVED-EQ-001 旗艦案例）**：AD §3.2 建議之 LPAD→`RIGHT(REPLICATE(char,n)+col,n)` 轉換公式於輸入長度≥n 時與 PG 語意相反（截斷方向錯誤），已設計 MUST-FIX 紅燈守門要求補 `LEFT` 分支。**核心紅線 3（🔴 CAST-EQ-002 旗艦案例）**：`getValidationRegex` 空字串邊界之 `LIKE` 空匹配真值陷阱（任務書明確點名）；覆核結論：三目標型別皆簡單字元類別型，無需 lookahead/alternation。**核心紅線 4（🔴 CLEANUP 群組）**：AD 明文未定案清理呼叫掛載位置，查證 `NodeOutputStore.cleanupAll()` 為現行天然貫穿成功/失敗兩路徑之收斂點且不在 AD 凍結清單內，設計黑盒 spy + MUST-FIX 決策記錄。**其餘查證發現**：`resolve-raw-table.ts` 需獨立 mssql 版（AD 未提及）；`mergePhone()` 正則（customer_core 最高頻表達式 7/12，AD 僅提及較少用的 padStart）與 `gen_random_uuid()`（AD 表格漏列）；`field-mapping-handler.ts` 裸布林字面值（同型 P1b3 已踩雷，實際不可達僅 UNIT 防禦）；`createDispatcher()` 決策關卡建議延後至 P4c 才接線。**明確排除** merge/lookup（P4b）、dedup/target-load（P4c）、53 節點端對端（P4d）、bulk-load（P4e）。總場景數 2320→**2416**；總文件數 88→**89** | Test Designer Agent |
| 2026-07-07 | **MSSQL 全面遷移 P2c Expire Sweep 整合 + 兩層回收一致性 + 全 F098 套件整體回歸測試設計新增（v2.39，P2 最後一片）**：新增 `infrastructure/AD-E07-40-P2c-test.md`（**25 個場景**），對應 AD-E07-40 §2.3/§4.3/§7（銜接 P2a/P2b，跳過 spec-writer）之 P2c 收尾切片。分層：MOUNT 6（🔴掛載機制未定案決策關卡，AD §4.3 明文交 tdd-implementation 依現行風格擇一，本文件刻意設計為黑盒 spy 驗證不預設具體 class/method + 啟動立即掃 + 定期觸發 + 零週期防呆 + 🔴destroy 清 timer + unref 靜態）+ 🔴RECOVERY 10（旗艦群組，AD §7 DoD #2：兩層回收正面三案例含順序無關性 + 獨立性直接反證兩案例 + 冪等終態 + retry=0 整合語境重新確認 + 正常完成對照組防誤殺 + 批次崩潰 + 🔴「入列未消費」孤兒邊界串連 P2a claim + P2b processPayload 既有防線 + P2c OrphanReaper 三片段）+ STATIC 4（🔴docker-compose `RUN_QUEUE_POLL_INTERVAL_MS` 缺口 DoD #4 核心 + 單一位置守門鏈延續兩案例（接續 P2a STATIC-003→P2b STATIC-002）+ 新增週期 env 覆蓋建議性決策關卡）+ REG 5（🔴DoD #3 全套件整體重跑，失敗清單須恰等於已知排除集合（比對測試名稱字面非僅數量）+ `TS-F098-PGINT-002` 排除理由持續有效性守門 + postgres/sqlite 不回歸 + tsc gate）。**核心紅線（🔴 test-designer 查證出之關鍵技術澄清）**：`MssqlQueueService.expireSweep()` 之 P2a 定案簽章**無** `now` 參數，與 `OrphanReaper.reap(now)` 之注入時鐘機制**不對稱**，sweep 時間控制須沿用 P2a `SWEEP-001` 已驗證之直接竄改 `expire_at` 技術，已於 §0.3 明確記錄避免下游誤用同一套 harness。**已查證現況缺口**：`docker-compose.yml` `worker:` service 現行零個 `RUN_QUEUE_*` 環境變數（不僅 `RUN_QUEUE_POLL_INTERVAL_MS`），已記入 `risks-and-gaps.md`。**P2（P2a/P2b/P2c）全範圍測試設計至此完成**。總場景數 2295→**2320**；總文件數 87→88 | Test Designer Agent |
| 2026-07-07 | **MSSQL 全面遷移 P1c Pattern B（$n→具名參數）＋ sp_getapplock 跨 driver 鎖測試設計新增（v2.36，P1 最後一片）**：新增 `infrastructure/AD-E07-38-P1c-test.md`（**48 個場景**），對應 AD-E07-38 §3 D-5/D-6（銜接 P1a/P1b1/P1b2/P1b3，跳過 spec-writer）之核心 4 檔 6 處中站點 1/2/3/4（站點 5 native cursor 明確排除，移交 Phase 3/4）。分層：ESCAPE 5（`driver.escapeQueryWithParameters` 純函式驗證，免真實 MSSQL 連線，CI 恆常執行）+ PARAM 17（站點 1 `customer_core` PG-only 錯誤分類 4 案例 + 站點 2 `ob_arreturndf_min_cap` 完整跨 driver 資料等價 6 案例 + 站點 4 `buildExportQuery` 含巢狀 $n 稽核 7 案例）+ LOCK 12（🔴 P1c 核心：DECLARE+EXEC+SELECT 批次回傳形狀探測 + 單次成功 commit/rollback 自動釋放 + 雙連線並發逾時降級/等待後取得 + 三種非鎖錯誤碼 rethrow + `@LockOwner='Transaction'` 前置條件探測決策關卡 + resource 字串免 hashtext 等價 + 不同 listNo 不互相阻塞）+ DISPATCH 5（🔴 MUST-FIX 守門：現行 `isPostgres()` 二元 gate 缺口，`DB_TYPE='mssql'` 現行程式碼下會被誤判與 sqlite 同路徑跳過鎖，對現行未修改程式碼刻意設計為紅燈）+ REG 6（tsc gate + F084 auto-advance 30 案例 + personnel-ratio/report service/scope 既有單元 + F103/F104 pg.spec 呼叫端不退化）+ STATIC 3（AD 交付物「Pattern B 完整站點清單」驗收 + fs+regex 殘留 $n 字面值零命中）。**★ test-designer 逐檔查證發現**：`customer_core`（站點 1 來源表）為 AD-E07-37 裁定之 PG-only 表（entities 目錄與 MSSQL baseline 皆零命中），`ob_arreturndf_min_cap`（站點 2 來源表）已在 MSSQL baseline（`1751884800000-MssqlBaselineSchema.ts:74`）——兩站點可測性程度不同，AD 原文未區分。**★ scopeClause 巢狀 $n 稽核完成**（AD 明確要求之交付項）：確認 `buildExportQuery` 於 `scopedByCreator=true` 且轄區非空時動態產生 `$2..$(1+N)`，須與主參數同一次 `escapeQueryWithParameters` 呼叫展開。**P1（P1a/P1b/P1c）全範圍測試設計至此完成**。測試環境完全沿用既有 `mssql-env-preload.ts`/`.env.test.mssql`/`pg-env-preload.ts`，不新增設定。總場景數 2144→**2192**；總文件數 84→85 | Test Designer Agent |
| 2026-07-07 | **MSSQL 全面遷移 P1b1 全 Entity 型別修正測試設計新增（v2.33）**：新增 `infrastructure/AD-E07-39-P1b1-test.md`（**43 個場景**），對應 AD-E07-39 P1b1（system-architect 產出，銜接 AD-E07-38 P1a，跳過 spec-writer）。分層：CHI 5（F-4 varchar 中文編碼 test-first 決策閘門，「借新還舊」/「中古車商」逐字元 round-trip，可最先獨立執行）+ TYPE 8（uuid 18/text 17/boolean 8 處矩陣驗證 + 🔴F-1 timestamp 4 處紅線三層防線：已知站點確認 + 全表未知站點掃描 + 正面 Date 寫入佐證 + bigint 2 處）+ HASH 12（B1 token_blocklist 改 token_hash binary(32)：`hashToken` 決定性/SHA-256 正確性 + PK 寬度正面驗證，對照 P1a 既有失敗案例 CRUD-003b + 既有測試遷移 auth.service.spec.ts/auth.guard.spec.ts + 4 個 E2E 撤銷流程案例 + 三 driver 回歸）+ ENTITY 6（D1：app.module.ts/worker-app.module.ts mssql 分支取代 P1a 過渡態之硬寫 4-entity 陣列，改為與 sqlite/postgres 共用同一份全 37 entity 清單；I-MSSQL-ENTITY-LIST-PARITY-01 靜態守門）+ DEFAULT 2（8 處 CURRENT_TIMESTAMP default smoke）+ CASE/COLLATE 4（I-MSSQL-CASE-01/COLLATE-01 全 37 表擴大版）+ PKWIDTH 2（新不變式 I-MSSQL-PK-BYTELIMIT-01：動態查詢守門取代人工估算表格）+ REG 4（tsc gate + 既有 pg.spec 套件回歸 + P1a 既有案例 TS-MSSQL-P1A-CRUD-003b 因 B1 欄位改名之遺留矛盾處置 + I-MSSQL-HELPER-SCOPE-01 全 37 entity 延伸）。明確排除 P1b2（I-MSSQL-BASELINE-PARITY-01、`fn_calc_tier_level` 最終驗證、prod baseline migration）、P1b3（bootstrap/seed 三支腳本改寫）、P1c（`sp_getapplock`/Pattern B），留待各自測試設計。測試環境完全沿用 P1a 既有 `mssql-env-preload.ts`/`.env.test.mssql`，新增 `p1b1` SQL schema 隔離慣例（與 `p1a` 並存）。總場景數 2008→**2051**；總文件數 81→82 | Test Designer Agent |
| 2026-07-07 | **MSSQL 全面遷移 P1a 最小可連線＋登入測試設計新增（v2.32）**：新增 `infrastructure/AD-E07-38-P1a-test.md`（**30 個場景**），對應 AD-E07-38 P1a（system-architect 產出，跳過 spec-writer——AD §3 D-7 裁定 P1 全範圍無新業務行為）。分層：CONN 6（三設定點啟動 + 4-entity 範圍邊界）+ TYPE 7（uuid/text/bigint/datetime2/bit 型別探測，含兩個「不可假設」核心紅線 + helper 決策關卡）+ LOGIN 3 + CRUD 7 + CASE 3（I-MSSQL-CASE-01）+ REG 3 + HELPER 1（I-MSSQL-HELPER-SCOPE-01）。明確排除 P1b（全 37 entity baseline / I-MSSQL-BASELINE-PARITY-01 / I-MSSQL-COLLATE-01）與 P1c（`sp_getapplock` / Pattern B / I-MSSQL-LOCK-01 / I-MSSQL-PARAM-01），留待各自測試設計。新增測試環境設計：`.mssql.spec.ts` 命名 + gating 慣例（比照 `.pg.spec.ts`）+ `.env.test.mssql`。風險已記入 `risks-and-gaps.md`（docker-compose mssql 服務無 test-only port/DB 分離、AD 章節編號漂移、未知 DB_TYPE 隱式 fallback）。總場景數 1978→**2008**；總文件數 80→81 | Test Designer Agent |
| 2026-07-02 | **F109 新增「客戶資料」來源篩選欄位測試設計新增（v2.31）**：新增 `features/F109-test.md`，**94 個場景**。F075 白名單引入第二個資料來源「客戶資料」（`customer_core`），新增 8 個篩選欄位（性別/年齡/職業別/教育程度/婚姻狀況/身分別/收入區間/居住城市）。**架構裁定（AD-E07-37）：customer_core 為 PG-only**（無 TypeORM Entity、僅存在 PostgreSQL），所有含 customer_core 條件之測試一律 `.pg.spec.ts`（**約 38 案例強制需 Postgres**，與 F098~F104 pg.spec 序列執行）；純案件資料 regression guard 維持 SQLite。核心紅線：NULLEXC 群組（AC-8 三變體，無對應客戶／客戶欄本身 NULL／無客戶條件不觸發）+ JOIN-003「EMPTY_CONDITIONS 陷阱」（僅含 customer_core 條件之名單不可誤判整批 skip，AD 特別點名）+ EQ 群組（BR-10 三處消費一致 DoD）+ AGE-004 決定性（BR-5，供 F067 apples-to-apples）。分層：WL 8 + OPT 10 + DESC 2 + GENDER 3 + DATASRC 7（OQ-F109-01 雙層機制）+ JOIN 8 + NULLEXC 7 + AGE 8 + CITY 4 + AND 3 + DEACT 3 + EQ 6 + PARAM 2（I-CC-PARAM-NS-01）+ JOINCARD 2（I-CC-JOIN-CARD-01）+ COMPSCOPE 2（I-CC-COMPOSER-SCOPE-01）+ API 2 + MIGSEED 3 + FRONTEND 7 + REG 5 + STATIC 2。所有 5 個架構 Open Question（OQ-F109-01~05）已由 AD-E07-37 裁定。總場景數 1884→**1978**；總文件數 79→80 | Test Designer Agent |
| 2026-06-29 | **F049 v2.0 Stage 0 業務化重設計 Part B 測試設計新增（v2.29）**：更新 `features/F049-test.md`，**新增 43 個場景**，涵蓋 F049 §14–§22 / US-166–170 / AD-E07-v3.6 之 Part B 測試設計。DEPT 4（BR-8 per-list ration 手算 oracle：DEPT-001 兩名單×兩部門完整驗算 / DEPT-002 休息日 deptCells=[] / DEPT-003 per-list regression guard vs MIN(LIST_NO) / DEPT-004 single-list 鑽探）+ GAP 6（BR-9/10/11：GAP-001 60% gap=20 手算 / GAP-002 70% gap=30 無名單層警示 / GAP-003 完全無比例 gap=orgTotal / GAP-004 gap=0 不顯示 / GAP-005 捨入容差不 assert Σ=orgTotal / GAP-006 org_total 不依賴比例 regression）+ SCOPE 6【SECURITY-CRITICAL】（BR-12/13/14 / I-DEPT-SCOPE-01：**SCOPE-002 安全紅線 regression guard** JSON.stringify 掃描非轄區字串完全不存在 / SCOPE-001 正向 / SCOPE-003 scope=null 200+warning / SCOPE-004 部長全部門 / SCOPE-005 後端為安全邊界不可繞過 / SCOPE-006 處長 orgTotal=null）+ FEAS 6（BR-15/16：FEAS-001 per_person=12 手算 / FEAS-002 headcount=0 perPerson=null+DEPT_HEADCOUNT_ZERO / FEAS-003 overThreshold=true / FEAS-004 threshold=null 降級 / FEAS-005 TRIM(dept_code) SQLite/PG 移植性 / FEAS-006 處長 scope 下 headcount 隔離）+ INVAR 2（I-RUN-EST-01：INVAR-001 spy computeWorkingDayRatios 被呼叫 / INVAR-002 stage0_estimate_count 物化 vs fallback 兩子場景）+ EDGE 3（EDGE-001 fallback 逾時 STAGE0_LIST_ESTIMATE_PARTIAL / EDGE-002 0 active 名單空結果 / EDGE-003 處長轄區無比例 deptCells=[]）+ AGG 5（AC-AGG：AGG-001 預設彙總 / AGG-002 切換 single-list / AGG-003 0 名單空狀態 / AGG-004 不寫 DB spy / AGG-005 月份切換重算）+ FE 6（FE-001 矩陣渲染+gap橘色 / FE-002 gap=0 缺口列不渲染 / FE-003 處長唯讀 banner / FE-004 scope=null 友善提示 / FE-005 overThreshold 紅色 / FE-006 派案日曆業務標籤功能不變）+ TERM 5（AC-TERM：**TERM-001 DOM 黑名單全文掃描 MUST-RUN** / TERM-002 KPI 卡片移除 / TERM-003 base+1 徽章不存在 / TERM-004 Pool 警示業務語言 / TERM-005 AD-E07-8 公式框不顯示）。**全部 OQ 已裁定**（AD-E07-v3.6 7 個 OQ 全解）；手算 oracle 寫定於 DEPT-001/GAP-001/GAP-002/GAP-005/FEAS-001/FEAS-003；所有場景可用 SQLite in-memory（無強制 PG）。**SCOPE-002 為最高優先 MUST-HAVE**：未來任何導致非轄區資料洩漏之修改均須使此 test FAIL。總場景數 1801→1844；F049-test.md YAML 更新 spec_version=2.0 + covers US-166~170 | Test Designer Agent |
| 2026-06-05 | **F101 Stage 3/4 真實比例分派測試設計新增（v2.23）**：新增 `features/F101-test.md`，**51 個場景**，取代 F100 placeholder Stage 4。分層：DEPT 8（Stage 3 手算 oracle 2分處×2Tier×3課，diff=0/1/2 邊界，OB202606001 多分處回歸，Pipeline 順序不變式）+ EMPL 9（Stage 4 手算 oracle 2課×2Tier×3員工，ADD_CNT 均攤，is_cr Y/N 同池，emplid/emplid_deptid 寫入，T5 不分流）+ ASGD 5（ASSIGNDAY per-casedt FLOOR，最末全吸收，DIVIDE_LEFT round-robin，estimate≡run，無 calendar fallback）+ EQ 8（**DoD 門檻 AC-15**：JS `executeV2` ↔ PG SQL 下推逐列四元組等價，8 代表性名單涵蓋多分處/多Tier/差額觸發/兩階段補足/無ration/無員工/無calendar/is_cr 混合）+ IDEM 3（Stage 3 前清除，is_cr 保留，兩次 run_id 四元組完全相同）+ FALL 6（三類警告 skipped_cases.warnings[] + warning_summary；JSONB 合并不覆蓋 cases；不寫 audit_log）+ REG 5（emplid≠NULL 回歸，**I-NO-ST4-EXCHANGE** senior swap 行為層驗證，is_cr 不改值）+ DET 3（**I-DET-01** 全程無 NEWID/random 靜態掃描，ob_assign_set 無引用 AC-18，senior swap CTE 移除）+ UPGR 4（分派差異報告，業務驗收 NFR-005）。**全部 OQ 已裁定**（OQ-F101-01~05，AD-E07-29 權威）；oracle 件數手算寫死於文件（Seed 1~4 Stage 3，Seed A~D Stage 4）；**44 案例強制需 Postgres**；CI 必須序列跑 F098/F099/F100/F101 pg.spec（禁並行）。總場景數 1567→1618；總文件數 70→71 | Test Designer Agent |
| 2026-06-02 | **F100 Stage 2~4 SQL 下推 + v2 真實計分引擎 P3 測試設計新增（v2.22）**：新增 `features/F100-test.md`，**52 個場景，僅涵蓋 AD-E07-28 P3**（Stage 2 計分 `SUM(CASE…)` + `LEFT JOIN customer_core` 補完 / score→level→tier / Stage 3 CR `EXISTS` / Stage 4 st4_exchange 視窗函式），不設計 P1/P2（已完成提交）。**與 P1/P2 本質差異：P3 非純等價變更**——計分引擎由 v1 簡化版升級為 v2 真實版（customer_core 欄位 v1 回 `''` 不計分 → v2 LEFT JOIN 補上），**故 oracle = 手算預期矩陣（§一，寫死數字人複核），非跑 v1 JS**（跑 v1 會把升級補上的正確分判 fail）。分層：EQ 8（DoD AC-8，每案標 (a) 升級差異 / (b) 下推等價）+ SCORE 7 + CJOIN 4 + LEVTIER 5 + CR 5 + EXCH 8（`CEIL` 非 SP `ROUND`、保底 1、`PARTITION BY list_no`、deterministic 選案；SP 配對交換 out-of-scope）+ RUNEST 2 + NOLOAD 3 + IDEM 3 + UPGR 4（F067 升級差異報告 + 業務驗收 gate，上線硬性前置）+ RG 3。**約 40 案例強制需 Postgres**（視窗函式 / `SUM(CASE…)` / `LEFT JOIN` / `EXISTS` 在 SQLite 不具代表性）。**4 個待確認 OQ**：OQ-F100-T1（transaction 範圍 spec 未明 → IDEM-003 blocked）、T2（score=NULL 之 tier fallback 語意）、T3（customer_core entity 不存在 → P3 前置 blocker）、T4（計分欄位映射對齊 §3.10 表）。總場景數由 1515 增至 1567；總文件數 69→70 | Test Designer Agent |
| 2026-06-02 | **F099 Stage 1 SQL 下推 P2 測試設計新增（v2.21）**：新增 `features/F099-test.md`，**38 個場景，僅涵蓋 AD-E07-28 P2**（`buildStage1Sql` set-based 下推 + estimate≡run 共用 core + JS↔SQL 逐 list 等價）。EQ 14（P2 DoD）+ RUNEST 4 + PORT 7（year-above 數值化，強制 PG 禁 SQLite）+ NOLOAD 3 + IDEM 3 + SQLG 4 + GMT 3（作廢 RGv2-005/SDv2-*）。26 案例強制需 PG。所有 OQ 已裁定 | Test Designer Agent |
| 2026-06-02 | **F098 月名單分派 Worker 抽離 P1 測試設計新增（v2.20）**：新增 `features/F098-test.md`，**51 個場景，僅涵蓋 AD-E07-28 P1**（執行容器抽離 + cancellation + orphan 回收），不設計 P2/P3 SQL 下推。核心不變式 I-TRIGGER-01（`triggerRun` 改入列、不在 API 程序跑 pipeline、`runPipeline` spy 0 次）；修復現行 `cancelRun` 自承「背景不真停」之 bug（`CancellationPoller` 於 list/stage 邊界輪詢 → `RunCancelledException` → 不寫快照/result）；`OrphanReaper` 殭屍 running 回收（不新增 schema 欄位，靠 pg-boss job expiration）；`retryLimit=0` + 單 worker 序列化 + `assertNoRunningRun` 不回歸；月名單分派期間 API 仍可回應之 NFR 驗證。**18 個案例強制需 Postgres**（pg-boss 為 Postgres 專屬，真實入列/消費/冪等/expiration/schema migration 須真庫；unit 層以 mock pg-boss 守 triggerRun 入列 + 不執行 pipeline）。連動 CI 決策：須能起 Postgres Test Container。總場景數由 1426 增至 1477；總文件數 67→68 | Test Designer Agent |
| 2026-05-26 | **Stage 1 精確化工程（F090/F091/F092）新增（v2.16）**：新增 3 個 test spec 檔，共 56 個測試場景。F090（13 場景）：migration m291 可逆 + ETL 歷史限定 + per-data_source 截斷不傷月名單分派 + 欄位映射完整性。F091（30 場景）：MONTH_CNT 期別過濾純函式 × 6、近 3 個月去重 × 5（含 Integration PG TC 聯集驗證）、特殊 DELETE 四類 SP 逐條對照 × 8、Stage1FilterChain 封裝與執行順序 × 5、Regression × 3（⚠️ 既有 Stage 1 pipeline baseline 需更新）。F092（26 場景）：dry-run 唯讀 × 4、dry-run ≡ run 精確一致（核心）× 3、estimateListCount 升級 × 4、F049/F088 升級 × 4、Regression × 3。版本升至 v2.16；總場景數由 1169 增至 1225 | Test Designer Agent |
| 2026-03-12 | 初版建立，16 個 Feature 測試設計 + 4 個策略文件 | Test Designer Agent |
| 2026-03-18 | 新增 E04 資料擷取（F017–F025）9 個 Feature 測試文件，共 79 個測試場景 | Test Designer Agent |
| 2026-03-18 | raw data 落地需求變更：更新 F017/F019（targetTable → sourceTable, rawTableName）、大幅更新 F021（動態建表/批次寫入/全量截斷/增量追加/SQL Injection）、更新 F022（「預覽資料」連結顯示條件）、新增 F026（raw data 預覽，36 個測試場景）；總測試場景數由 196 增至 247 | Test Designer Agent |
| 2026-03-18 | 連鎖下拉選單需求變更（v1.2）：sourceTable 單欄位 → sourceSchema + sourceTable；新增 GET /datasources/:id/schemas 與 GET /datasources/:id/schemas/:schema/tables 兩端點測試；更新 F017（+15 場景）、F019（+15 場景）、F021（+3 場景）、F026（+5 場景）；新增 4 項 SCHEMA-RISK；總場景數由 247 增至 294 | Test Designer Agent |
| 2026-03-20 | 新增 E05 ETL Pipeline 管理模組（F027–F036）共 10 個 Feature 測試文件，合計 210 個測試場景（F027:22、F028:17、F029:31、F030:20、F031:14、F032:21、F033:29、F034:15、F035:21、F036:20）；新增錯誤碼覆蓋：PIPELINE_NAME_EXISTS、VALIDATION_INVALID_CRON、PIPELINE_INVALID_CONNECTION、PIPELINE_DRAFT_CANNOT_ENABLE、PIPELINE_TARGET_TABLE_NOT_FOUND、PIPELINE_VERSION_NOT_FOUND、PIPELINE_PUBLISH_REQUIRES_TEST；總場景數由 308 增至 518 | Test Designer Agent |
| 2026-03-20 | 整合 test-index.md（v2.0）：統一版本號、修正場景合計、新增 E05 小計列；更新 test-levels.md（新增 E05 Unit/Integration/E2E/NFR 章節）；更新 test-data-strategy.md（新增 E05 種子資料與邊界值）；更新 risks-and-gaps.md（新增 E05-RISK-001~008） | Test Designer Agent |
| 2026-03-23 | 新增 F037 發布 Pipeline 版本測試設計（37 個測試場景，含後端 Unit/Integration、前端 Unit、端到端流程、邊界條件）；新增錯誤碼覆蓋：PIPELINE_VERSION_ALREADY_PUBLISHED；總場景數由 518 增至 531 | Test Designer Agent |
| 2026-03-25 | 新增 F038 孤兒任務回收測試設計（45 個測試場景：Unit 27 個 + Integration 17 個 + 效能 1 個）；涵蓋 E04 擷取任務回收、E05 Pipeline 回收、無孤兒靜默通過、Transaction 原子性、獨立 Transaction 隔離、Logger 驗證、冪等性、回收後操作解鎖（triggerRun/deleteTask/triggerExecute）及 NFR 效能閾值；總場景數由 531 增至 576 | Test Designer Agent |
| 2026-04-02 | E02 角色擴充（US-017 業務角色定義）：新增 F045 業務角色定義測試設計（15 個場景）；更新 F004（+6 場景，8→14，新增業務角色建立、無效 role_code、前端角色選單）；更新 F005（+7 場景，7→14，新增依業務角色篩選、角色欄位中文顯示名稱驗證）；更新 F008（+14 場景，6→20，新增業務角色間互相變更、Admin 降級為業務角色、最後 Admin 保護擴充、前端確認對話框）；移除「僅 Admin 與 User 兩種角色」的假設；總場景數由 736 增至 778 | Test Designer Agent |
| 2026-04-13 | E06 角色精簡（US-017 回歸 2 種角色）：業務角色（business/marketing/customer_service/analyst/supervisor/backend_ops）全部移除；F045（15→13 場景）、F004（14→9 場景）、F005（14→10 場景）、F008（20→12 場景）；移除業務角色相關測試案例；總場景數由 778 降至 743 | Test Designer Agent |
| 2026-05-13 | 新增 F002SM Sales Manager 旗標顯示於 Top Bar（25 場景）：涵蓋後端 Login API `isSalesManager` 欄位補充（6 場景）、前端 TopBar Badge 元件（7 場景）、前端整合（4 場景）、edge case（4 場景）、Manual E2E（4 場景）；補充 4 項風險（RISK-F002SM-001~004）；總場景數由 812 增至 837 | Test Designer Agent |
| 2026-05-18 | 新增 E07 M06 篩選欄位管理（F075 v1.4）測試設計（48 場景）：BE service 單元（18 場景，含 `getAvailableColumns` happy path / empty / 排序 / camelCase mapping + `_inferSuggestedFieldType` 15 dataType 逐型別驗證）；BE E2E SQLite（8 場景，Guard 矩陣 / Feature Flag / 路由排序回歸）；BE Integration PostgreSQL TC（2 場景，BR-13 含停用過濾 + 空陣列）；前端 Component（16 場景，dropdown / hint data-state / RISK-003 user-overridden 決議 / empty / toast / Edit regression）；跨模組整合（2 場景，F050 contract + F076 categorical）；命名漂移 regression（2 場景）。新增 `regression/M06-regression-guards.md`（TC-GUARD-M06-NAMING-001/002）。版本升至 v2.12；總場景數由 961 增至 1009 | Test Designer Agent |
