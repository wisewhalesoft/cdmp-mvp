# E07 ETL Seed Script 所需環境變數
# 用法：複製為 .env.local 後填入實際值（.env.local 已被 gitignore）

# CDMP API 連線
API_BASE_URL=http://localhost:3000
ADMIN_EMAIL=admin@cdmp.test
ADMIN_PASSWORD=P@ssw0rd123

# 注意：APYHFC16.OB datasource 已手動建立於 CDMP 中，此 script 不會建立 datasource
# 若要建立其他環境的 datasource，可擴充 seed-e07-etl.mjs 並補入下列變數：
# OB_DB_HOST=
# OB_DB_PORT=1433
# OB_DB_NAME=OB
# OB_DB_USER=
# OB_DB_PASSWORD=
