/**
 * F050 v2.1 Phase 5d 波 2：FieldBasePage stub
 *
 * 對應 prototype: /prototypes/37-base-code.html
 *
 * 此為波 2 之最小 stub，僅輸出 testid 以通過 App.tsx 路由 mount 驗證。
 * 完整 2-Tab 容器（含 ?tab=fields|options URL sync、v2.1 廢除 banner、
 * Tab bar + FieldsTab / OptionsTab）由波 3 / 4 / 5 落地。
 */
export function FieldBasePage() {
  return <div data-testid="field-base-page">FieldBase (stub — 波 3 落地)</div>;
}
