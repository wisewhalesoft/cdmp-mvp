# MSSQL 測試連線範本 — 複製成 apps/api/.env.test.mssql 並填入本機/ dev MSSQL 連線（.env.test.mssql 已 gitignored，密碼不入版控）。
# 由 src/database/__tests__/mssql-env-preload.ts 自動載入（dotenv，非覆寫：inline / CI env 優先）。
# *.mssql.spec.ts 會據此連真 MSSQL 驗證；未提供則 fallback 至 preload 內建預設（localhost:1433 / CDMP_TEST）。
NODE_ENV=test
DB_TYPE=mssql
DB_HOST=localhost
DB_PORT=1433
DB_USERNAME=cdmp
DB_PASSWORD=請填入本機/dev MSSQL 密碼
DB_NAME=CDMP_TEST
DB_MSSQL_ENCRYPT=true
DB_MSSQL_TRUST_CERT=true
JWT_SECRET=cdmp-test-jwt-secret-key
PORT=3001
